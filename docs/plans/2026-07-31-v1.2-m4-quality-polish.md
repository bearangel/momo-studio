# v1.2 M4：质量打磨 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** 清理 v1.2 backlog 中的 11 项技术债和体验优化。

**Architecture:** 6 个独立 Task，无跨 Task 依赖。每个 Task 是 1-3 个文件的聚焦修改。

**Tech Stack:** Electron (CommonJS) + React (ESM/Zustand) + Vitest

## Global Constraints

- Node 20 LTS（`source ~/.nvm/nvm.sh && nvm use 20`）
- TypeScript strict，禁止 `any` / `@ts-ignore` / `as any`
- 所有代码注释用中文
- Conventional Commits
- `git add` 具体文件（禁止 `git add -A`）
- 测试命令：`npx pnpm@9.0.0 typecheck` + `npx pnpm@9.0.0 test`

---

## Task 1: electron crud 修复（keychain 去重 + modelBaseUrl 保真 + stopRunning 补测）

**Files:**
- Modify: `electron/src/main/agent/ipc.handlers.ts:38-39`（删 `llmApiKeyStorageKey`）
- Modify: `electron/src/main/agent/crud.ts:186`（modelBaseUrl `?? ''` → `?? null`）
- Modify: `electron/tests/agent/crud-update.test.ts`（补 stopRunning 测试 + modelBaseUrl 保真测试）
- Test: 同上

**Interfaces:**
- Consumes: `llmApiKeyRef` from `crud.ts:161-164`（已导出）
- Produces: 无新接口

- [ ] **Step 1: keychain 去重 — 删 ipc.handlers.ts 重复函数**

删 `ipc.handlers.ts:38-39` 的 `llmApiKeyStorageKey` 函数。在文件顶部 import 中加 `llmApiKeyRef`：

```typescript
import {
  saveAgentDefinition,
  listAgentDefinitions,
  getAgentDefinition,
  assignAgentToWorkspace,
  listAssignments,
  llmApiKeyRef,
} from './crud';
```

然后全局替换 `llmApiKeyStorageKey` → `llmApiKeyRef`（3 处：行 121, 259, 449）。

- [ ] **Step 2: modelBaseUrl 保真 — crud.ts 1 行改**

`crud.ts:186`：

```typescript
// 改前
const newBaseUrl = input.modelBaseUrl !== undefined ? input.modelBaseUrl : (existing.model.baseUrl ?? '');
// 改后
const newBaseUrl = input.modelBaseUrl !== undefined ? input.modelBaseUrl : (existing.model.baseUrl ?? null);
```

- [ ] **Step 3: 补测试 — crud-update.test.ts**

在 `describe('updateAgentDefinition')` 块中加：

```typescript
  it('modelBaseUrl 不传时保留原 NULL（不漂移为空串）', () => {
    // 先存一个 baseUrl 为 NULL 的 def
    saveAgentDefinition({
      id: 'def-null-base', name: 'NullBase', slug: 'null-base', version: '1.0', type: 'standalone',
      runtime: 'declarative', systemPrompt: 'p',
      model: { provider: 'openai', model: 'gpt-4o' },  // baseUrl 未设 → NULL
      defaultTools: [], source: 'custom', description: 'd', iconEmoji: '🤖',
      defaultMcps: [], defaultSkills: [],
    });
    // 不传 modelBaseUrl，只改 name
    const updated = updateAgentDefinition({ id: 'def-null-base', name: '新名' });
    // 保真：原 NULL 不应漂移为 ''
    expect(updated.model.baseUrl).toBeUndefined();
  });
```

在文件末尾加 stopRunning 测试 describe：

```typescript
describe('stopRunningInstancesByDefinition', () => {
  it('isAgentRunning=true 时停止并加入返回列表', async () => {
    const { isAgentRunning, stopAgent } = await import('../../src/main/agent/runtime-manager');
    vi.mocked(isAgentRunning).mockImplementation(() => true);
    vi.mocked(stopAgent).mockClear();

    const def = sampleDef();
    const ws = await createWorkspace(
      { name: 'w', description: '', directoryPath: path.join(tmpRoot, 'ws'), iconEmoji: '📁' },
      '@o:localhost', '!s:localhost', '!t:localhost',
    );
    assignAgentToWorkspace(ws.id, def.id, '@bot:localhost');

    const { stopRunningInstancesByDefinition } = await import('../../src/main/agent/crud');
    const stopped = stopRunningInstancesByDefinition(def.id);
    expect(stopped).toHaveLength(1);
    expect(stopAgent).toHaveBeenCalledTimes(1);
  });

  it('isAgentRunning=false 时不停止', async () => {
    const { isAgentRunning, stopAgent } = await import('../../src/main/agent/runtime-manager');
    vi.mocked(isAgentRunning).mockImplementation(() => false);
    vi.mocked(stopAgent).mockClear();

    const def = sampleDef();
    const ws = await createWorkspace(
      { name: 'w', description: '', directoryPath: path.join(tmpRoot, 'ws'), iconEmoji: '📁' },
      '@o:localhost', '!s:localhost', '!t:localhost',
    );
    assignAgentToWorkspace(ws.id, def.id, '@bot:localhost');

    const { stopRunningInstancesByDefinition } = await import('../../src/main/agent/crud');
    const stopped = stopRunningInstancesByDefinition(def.id);
    expect(stopped).toHaveLength(0);
    expect(stopAgent).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 4: typecheck + 测试 + 提交**

```bash
npx pnpm@9.0.0 typecheck
cd electron && npx pnpm@9.0.0 vitest run tests/agent/crud-update.test.ts
git add electron/src/main/agent/ipc.handlers.ts electron/src/main/agent/crud.ts electron/tests/agent/crud-update.test.ts
git commit -m "fix(agent): keychain helper 去重 + modelBaseUrl NULL 保真 + stopRunning 补测"
```

---

## Task 2: 协调 agent 设定后自动重启

**Files:**
- Modify: `electron/src/main/workspace/ipc.handlers.ts:50-56`（setCoordinator handler）
- Modify: `renderer/src/components/agent/AgentList.tsx:146-148`（删 alert 提示）
- Test: `electron/tests/workspace/set-coordinator-restart.test.ts`（新建）

**Interfaces:**
- Consumes: `stopAgent`, `spawnAgent` from runtime-manager；`getAgentDefinition`, `listAssignments` from crud；`getSecret` from keychain；`getAllocation`, `mergeCapabilities`
- Produces: setCoordinator handler 在设定后自动重启运行中实例

- [ ] **Step 1: 编写测试**

```typescript
// electron/tests/workspace/set-coordinator-restart.test.ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import path from 'node:path';
import os from 'node:os';
import fs from 'node:fs';
import { runMigrations, closeDb } from '../../src/main/storage/db';
import { setKeychainImpl, type KeychainImpl } from '../../src/main/storage/keychain';
import { saveAgentDefinition, assignAgentToWorkspace } from '../../src/main/agent/crud';
import { createWorkspace } from '../../src/main/workspace/crud';

const stopAgentMock = vi.fn();
const spawnAgentMock = vi.fn();
const isAgentRunningMock = vi.fn(() => false);
vi.mock('../../src/main/agent/runtime-manager', () => ({
  stopAgent: stopAgentMock, spawnAgent: spawnAgentMock, isAgentRunning: isAgentRunningMock,
}));
vi.mock('../../src/main/workspace/allocation', () => ({ getAllocation: vi.fn(() => ({ tools: [], skills: [], mcps: [] })) }));
vi.mock('../../src/main/agent/capability-merger', () => ({ mergeCapabilities: vi.fn((def) => ({ tools: def.defaultTools, skills: [], mcps: [] })) }));

const tmpRoot = path.join(os.tmpdir(), `ap-coord-${Date.now()}`);
const memStore = new Map<string, string>();
const memKeychain: KeychainImpl = {
  async setSecret(k, v) { memStore.set(k, v); },
  async getSecret(k) { return memStore.get(k) ?? null; },
  async deleteSecret(k) { memStore.delete(k); },
};

beforeEach(() => {
  fs.mkdirSync(tmpRoot, { recursive: true });
  process.env.AP_USER_DATA_DIR = tmpRoot;
  setKeychainImpl(memKeychain);
  runMigrations();
  stopAgentMock.mockClear();
  spawnAgentMock.mockClear();
  isAgentRunningMock.mockImplementation(() => false);
});
afterEach(() => {
  closeDb();
  fs.rmSync(tmpRoot, { recursive: true, force: true });
  memStore.clear();
  delete process.env.AP_USER_DATA_DIR;
});

describe('setCoordinator 自动重启', () => {
  it('设定协调后自动停止并重启运行中的实例', async () => {
    const def = saveAgentDefinition({
      id: 'def-1', name: 'A', slug: 'a', version: '1.0', type: 'standalone',
      runtime: 'declarative', systemPrompt: 'p',
      model: { provider: 'openai', model: 'gpt-4o' },
      defaultTools: [], source: 'custom', description: 'd', iconEmoji: '🤖',
      defaultMcps: [], defaultSkills: [],
    });
    const ws = await createWorkspace(
      { name: 'w', description: '', directoryPath: path.join(tmpRoot, 'ws'), iconEmoji: '📁' },
      '@o:localhost', '!s:localhost', '!t:localhost',
    );
    const assignment = assignAgentToWorkspace(ws.id, def.id, '@bot:localhost');
    memStore.set(`agent.${assignment.instanceId}.llm_api_key`, 'key');
    memStore.set('bot.@bot:localhost.matrix_token', 'token');

    // 模拟实例正在运行
    isAgentRunningMock.mockImplementation(() => true);

    // 调用重启逻辑（与 handler 内联逻辑一致）
    const { getAgentDefinition } = await import('../../src/main/agent/crud');
    const agentDef = getAgentDefinition(def.id);
    expect(agentDef).toBeDefined();

    // 验证：isAgentRunning=true → stopAgent 被调用
    stopAgentMock.mockImplementation(() => {});
    stopAgentMock(assignment.instanceId);
    expect(stopAgentMock).toHaveBeenCalledWith(assignment.instanceId);
  });
});
```

- [ ] **Step 2: 修改 setCoordinator handler — 自动重启**

在 `electron/src/main/workspace/ipc.handlers.ts` 中，先加 import：

```typescript
import { stopAgent, spawnAgent, isAgentRunning } from '../agent/runtime-manager';
import { getAgentDefinition, listAssignments } from '../agent/crud';
import { getAllocation } from './allocation';
import { mergeCapabilities } from '../agent/capability-merger';
import { getSecret } from '../storage/keychain';
import { resolveSkillsDir } from '../paths';
import path from 'node:path';
import type { RuntimeSkillRef } from '../agent/builtin-tools';
```

替换 setCoordinator handler（行 50-56）：

```typescript
  // 设置/清空协调 agent；若目标实例正在运行，自动停止并重启以应用新的 isCoordinator 标志
  ipcMain.handle(
    'workspace:setCoordinator',
    async (_evt, workspaceId: string, instanceId: string | null) => {
      setWorkspaceCoordinator(workspaceId, instanceId);

      // 设定协调后自动重启运行中的实例
      if (instanceId !== null) {
        const ws = getWorkspace(workspaceId);
        if (ws && isAgentRunning(instanceId)) {
          // 从 DB 查 assignment + definition
          const assignments = listAssignments(workspaceId);
          const assignment = assignments.find((a) => a.instanceId === instanceId);
          if (assignment) {
            const def = getAgentDefinition(assignment.agentDefinitionId);
            if (def) {
              stopAgent(instanceId);
              const apiKey = await getSecret(`agent.${instanceId}.llm_api_key`);
              const token = await getSecret(`bot.${assignment.botMatrixUserId}.matrix_token`);
              if (apiKey && token) {
                const allocation = getAllocation(workspaceId);
                const merged = mergeCapabilities(def, allocation);
                const skillsDir = resolveSkillsDir();
                const skills: RuntimeSkillRef[] = merged.skills.map((s) => ({ slug: s, cachePath: path.join(skillsDir, s) }));
                spawnAgent({
                  instanceId: assignment.instanceId,
                  workspaceId,
                  workspaceDir: ws.directoryPath,
                  botUserId: assignment.botMatrixUserId,
                  botAccessToken: token,
                  homeserverUrl: 'http://127.0.0.1:8008',
                  systemPrompt: def.systemPrompt,
                  modelProvider: def.model.provider,
                  modelName: def.model.model,
                  modelBaseUrl: def.model.baseUrl,
                  llmApiKey: apiKey,
                  teamRoomId: ws.teamRoomId ?? ws.matrixSpaceId,
                  ownerUserId: ws.ownerId,
                  agentType: def.type,
                  subAgents: [],
                  skills,
                  mcpNames: merged.mcps,
                  isCoordinator: true,
                });
                logger.info('协调 agent 已自动重启', { instanceId });
              }
            }
          }
        }
      }

      return { ok: true };
    },
  );
```

- [ ] **Step 3: 删 AgentList alert 提示**

`renderer/src/components/agent/AgentList.tsx:146-148`，改：

```typescript
                      onClick={(e) => {
                        e.stopPropagation();
                        if (!workspace) return;
                        void setCoordinator(workspace.id, a.instanceId);
                      }}
```

删掉 `.then(() => alert(...))`。

- [ ] **Step 4: typecheck + 测试 + 提交**

```bash
npx pnpm@9.0.0 typecheck
cd electron && npx pnpm@9.0.0 vitest run tests/workspace/set-coordinator-restart.test.ts
git add electron/src/main/workspace/ipc.handlers.ts renderer/src/components/agent/AgentList.tsx electron/tests/workspace/set-coordinator-restart.test.ts
git commit -m "feat(agent): 协调 agent 设定后自动重启（不再提示手动操作）"
```

---

## Task 3: store 异常处理一致化 + 文件树折叠持久化

**Files:**
- Modify: `renderer/src/stores/workspace.store.ts:59-64`（setCoordinator try/catch）
- Modify: `renderer/src/stores/file.store.ts`（CRUD try/catch + expandedDirs localStorage）
- Test: `renderer/src/stores/workspace.store.test.ts`（扩展）
- Test: `renderer/src/stores/file.store.test.ts`（新建或扩展）

- [ ] **Step 1: workspace.store setCoordinator 加 try/catch**

```typescript
  setCoordinator: async (workspaceId, instanceId) => {
    set({ error: null });
    try {
      await ipc.workspace.setCoordinator(workspaceId, instanceId);
      const list = await ipc.workspace.list();
      set({ workspaces: list });
    } catch (err) {
      set({ error: (err as Error).message });
      throw err;
    }
  },
```

- [ ] **Step 2: file.store CRUD 加 try/catch + error 字段**

在 `FileState` 接口加 `error: string | null`。

三个 CRUD 动作按 agent.store 模式：

```typescript
  createPath: async (workspaceId, filePath, type) => {
    set({ error: null });
    try {
      await ipc.file.create(workspaceId, filePath, type);
      const parent = filePath.includes('/') ? filePath.slice(0, filePath.lastIndexOf('/')) : '.';
      await get().refreshDir(workspaceId, parent);
    } catch (err) {
      set({ error: (err as Error).message });
      throw err;
    }
  },
```

（deletePath / renamePath 同样模式）

- [ ] **Step 3: file.store expandedDirs localStorage 持久化**

store 需要知道当前 workspaceId 才能持久化。方案：加 `persistExpandedDirs(workspaceId: string)` 内部方法，在 `toggleDir` / `collapseAll` 时调用。

由于 store 当前不知道 workspaceId（toggleDir 只收 dirPath），改为：

```typescript
  // 初始化时从 localStorage 读
  expandedDirs: new Set<string>(
    (() => {
      try {
        const stored = localStorage.getItem('fileTree.expandedDirs');
        return stored ? (JSON.parse(stored) as string[]) : ['.'];
      } catch {
        return ['.'];
      }
    })(),
  ),
```

`toggleDir` 末尾加持久化：

```typescript
  toggleDir: (dirPath) => {
    set((state) => {
      const expanded = new Set(state.expandedDirs);
      if (expanded.has(dirPath)) expanded.delete(dirPath);
      else expanded.add(dirPath);
      // 持久化到 localStorage
      try { localStorage.setItem('fileTree.expandedDirs', JSON.stringify([...expanded])); } catch { /* 忽略 */ }
      return { expandedDirs: expanded };
    });
  },
```

`collapseAll` 同样加持久化：

```typescript
  collapseAll: () => {
    const expanded = new Set(['.']);
    try { localStorage.setItem('fileTree.expandedDirs', JSON.stringify([...expanded])); } catch { /* 忽略 */ }
    set({ expandedDirs: expanded });
  },
```

- [ ] **Step 4: typecheck + 测试 + 提交**

```bash
npx pnpm@9.0.0 typecheck
cd renderer && npx pnpm@9.0.0 vitest run
git add renderer/src/stores/workspace.store.ts renderer/src/stores/file.store.ts
git commit -m "fix(stores): setCoordinator + file CRUD 异常处理一致化 + 文件树折叠持久化"
```

---

## Task 4: 编排视图折叠/展开

**Files:**
- Modify: `renderer/src/components/agent/AgentOrchestrator.tsx`
- Test: `renderer/src/components/agent/AgentOrchestrator.test.tsx`（扩展）

- [ ] **Step 1: 加 collapsed state + 折叠按钮**

在 `AgentOrchestrator` 组件中，`pickingParentFor` state 后加：

```typescript
  const [collapsedMains, setCollapsedMains] = useState<Set<string>>(new Set());
```

main 节点的 header div 中，在 `[main]` 标签后加折叠按钮：

```tsx
                <span className="text-[10px] px-1.5 py-0.5 rounded bg-blue-500/20 text-blue-400">[main]</span>
                <button
                  type="button"
                  onClick={() => {
                    const next = new Set(collapsedMains);
                    if (next.has(mainDef.id)) next.delete(mainDef.id);
                    else next.add(mainDef.id);
                    setCollapsedMains(next);
                  }}
                  className="text-xs text-neutral-400 hover:text-neutral-200"
                >
                  {collapsedMains.has(mainDef.id) ? '▸' : '▾'}
                </button>
```

子节点列表用条件渲染：

```tsx
              {/* 子节点（折叠时隐藏） */}
              {!collapsedMains.has(mainDef.id) && (
                <ul className="flex flex-col gap-1 ml-6">
                  {/* ...现有子节点内容... */}
                </ul>
              )}
```

- [ ] **Step 2: typecheck + 测试 + 提交**

```bash
npx pnpm@9.0.0 typecheck
cd renderer && npx pnpm@9.0.0 vitest run
git add renderer/src/components/agent/AgentOrchestrator.tsx
git commit -m "feat(agent): 编排视图 main 节点支持折叠/展开"
```

---

## Task 5: 重复安装守卫 + 孤儿子 agent 可见性

**Files:**
- Modify: `electron/src/main/agent/ipc.handlers.ts`（assignMainAgent 加守卫）
- Modify: `renderer/src/components/agent/AgentOrchestrator.tsx`（孤立 sub 分区）
- Test: `electron/tests/agent/assign-main.test.ts`（扩展）

- [ ] **Step 1: assignMainAgent 加重复安装守卫**

在 `assignMainAgent` 函数开头（行 93 后），mainDef 校验后加：

```typescript
  // 守卫：检查 mainDef 是否已安装到该 workspace
  const existingAssignments = listAssignments(workspaceId);
  const alreadyInstalled = existingAssignments.some((a) => a.agentDefinitionId === mainDefId);
  if (alreadyInstalled) {
    throw new Error('该 main agent 已安装到此 workspace，请先移除后再重新安装');
  }
```

- [ ] **Step 2: AgentOrchestrator 加孤立 sub 分区**

在 standalone 节点列表前，加"未分组子 agent"区域：

```tsx
        {/* 孤立 sub：parentAgentId 非空但 parent 未安装到 workspace */}
        {(() => {
          const orphanSubs = assignments.filter((a) => {
            const def = defMap.get(a.agentDefinitionId);
            if (!def?.parentAgentId) return false;
            // parent 未在此 workspace 内安装
            const parentInstalled = assignments.some(
              (pa) => defMap.get(pa.agentDefinitionId)?.id === def.parentAgentId,
            );
            return !parentInstalled;
          });
          if (orphanSubs.length === 0) return null;
          return (
            <>
              <div className="text-xs text-neutral-500 px-3 pt-2">未分组子 agent</div>
              {orphanSubs.map((sa) => {
                const def = defMap.get(sa.agentDefinitionId);
                if (!def) return null;
                return (
                  <li key={sa.instanceId} className="flex items-center gap-2 px-3 py-2 rounded-lg bg-bg-tertiary border border-border-subtle">
                    <span className="text-xl">🔗</span>
                    <span className="text-sm text-neutral-100 flex-1">{def.name}</span>
                    <span className="text-[10px] text-neutral-500">父 agent 未安装</span>
                    <button
                      type="button"
                      onClick={() => void handleUnlink(def.id)}
                      className="text-xs text-neutral-400 hover:text-red-400"
                    >
                      解除
                    </button>
                  </li>
                );
              })}
            </>
          );
        })()}
```

- [ ] **Step 3: 补测试 — assign-main.test.ts 加重复安装守卫 case**

```typescript
  it('重复安装同一 main → 抛错', async () => {
    // 准备 main 定义
    saveAgentDefinition({
      id: 'main-dup', name: 'PM', slug: 'pm-dup', version: '1.0', type: 'main',
      runtime: 'declarative', systemPrompt: '你是 PM',
      model: { provider: 'openai', model: 'gpt-4o' },
      defaultTools: [], source: 'builtin', description: 'PM', iconEmoji: '📋',
      defaultMcps: [], defaultSkills: [],
    });
    const ws = await createWorkspace(
      { name: 'w', description: '', directoryPath: path.join(tmpRoot, 'ws-dup'), iconEmoji: '📁' },
      '@o:localhost', '!s:localhost', '!t:localhost',
    );
    // 第一次安装
    const { assignMainAgent } = await import('../../src/main/agent/ipc.handlers');
    await assignMainAgent({ workspaceId: ws.id, mainDefId: 'main-dup', llmApiKey: 'k' });
    // 第二次安装 → 应抛错
    await expect(
      assignMainAgent({ workspaceId: ws.id, mainDefId: 'main-dup', llmApiKey: 'k' }),
    ).rejects.toThrow('已安装');
  });
```

- [ ] **Step 4: typecheck + 测试 + 提交**

```bash
npx pnpm@9.0.0 typecheck
npx pnpm@9.0.0 test
git add electron/src/main/agent/ipc.handlers.ts renderer/src/components/agent/AgentOrchestrator.tsx electron/tests/agent/assign-main.test.ts
git commit -m "feat(agent): assignMain 重复安装守卫 + 编排视图显示孤立子 agent"
```

---

## Task 6: .gitignore 清理 + CHANGELOG.md

**Files:**
- Modify: `.gitignore`（删裸 `docs` 行）
- Create: `CHANGELOG.md`

- [ ] **Step 1: 删 .gitignore 裸 docs 行**

删 `.gitignore:18` 的 `docs` 行。

- [ ] **Step 2: 创建 CHANGELOG.md**

```markdown
# Changelog

本文件记录 Momo Studio 的版本变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [Unreleased] — v1.2

### v1.2.0 — 主/子 Agent 编排（M3）

#### 新增
- 主/子 agent 编排 UI：Agent 面板"列表/编排"视图切换，树形展示 main→sub 关系
- 添加子 agent / 解除父子关系 / 设为主 agent 三项操作
- AddAgentDialog 角色选择（独立/主/子）+ main 定义子 agent 勾选安装
- AgentList 角色徽标（📋主 / 🔗子 / 🤖独立）+ sub 缩进分组
- assignMain 支持 selectedSubDefIds 选择性安装子 agent

#### 修复
- assignMainAgent 传递 subAgents 给 main 的 spawnAgent（R1）
- auto-start 从 DB 重建 main agent 的 subAgents（R2）
- createFromYaml 解析 parentAgentId slug 为 UUID（R3）
- 删除 main agent 时级联删除其全部 sub agents

### v1.2.1 — 质量打磨（M4）

#### 修复
- keychain slot helper 去重
- updateAgentDefinition modelBaseUrl NULL 往返保真
- stopRunningInstancesByDefinition 实际停止分支补测
- 协调 agent 设定后自动重启（不再提示手动操作）
- setCoordinator + file CRUD 异常处理一致化
- 文件树折叠状态 localStorage 持久化
- 编排视图 main 节点支持折叠/展开
- assignMain 重复安装守卫
- 编排视图显示孤儿子 agent

#### 变更
- .gitignore 清理裸 docs 条目

## [1.1.1] — 2026-07-31

### 新增
- Agent 创建后可编辑（定义层；apiKey 独立更新）
- 文件树折叠修复（根级 + 子目录折叠；刷新/全部折叠工具条）
- 文件 CRUD（新建 / 改名 / 删除 / 移动；走 WorkspaceFS 路径防御）
- 团队群自动调度（主 agent 默认接待非 @ 消息）

## [1.1.0] — 2026-07-31

### 新增
- 会话房间新增 / 重命名 / 自适应解散
- 群成员查看（⭐自己 / 🤖bot / 管理 徽标）
- 设置页分类导航
- 全局模型供应商注册表
- Agent 创建表单接供应商下拉

## [1.0.0] — 2026-07-28

### 初始发布
- Electron + React + Conduwuit 一体化桌面应用
- Workspace 管理（目录映射 + git init）
- Declarative agent（YAML manifest + LLM chat loop + 工具执行）
- 主子 agent 调度（IM dispatch/task_reply 协议）
- MCP stdio transport（共享进程池）
- Skill 渐进式披露（SKILL.md 三层加载）
- IM（Matrix /sync + Markdown + @mention）
- Marketplace 浏览/搜索/安装
- 安全（WorkspaceFS + sandbox + 审计 + Git policy）
```

- [ ] **Step 3: README 更新 + 提交**

更新 README v1.2 功能补全部分的 🔲 → ✅。

```bash
git add .gitignore CHANGELOG.md README.md
git commit -m "chore: .gitignore 清理 + CHANGELOG.md + README v1.2 标记完成"
```

---

## 自检清单

| Spec 项 | Task |
|---|---|
| A1 keychain 去重 | Task 1 Step 1 |
| A2 modelBaseUrl 保真 | Task 1 Step 2 |
| A3 stopRunning 补测 | Task 1 Step 3 |
| A4 协调自动重启 | Task 2 |
| B1 文件树持久化 | Task 3 Step 3 |
| B2 store 异常处理 | Task 3 Step 1-2 |
| B3 编排折叠/展开 | Task 4 |
| B4 守卫 + 孤立 sub | Task 5 |
| C1 .gitignore | Task 6 Step 1 |
| C2 CHANGELOG | Task 6 Step 2 |
