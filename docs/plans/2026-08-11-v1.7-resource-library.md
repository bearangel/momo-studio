# v1.7 资源库（取代 Marketplace）+ 三源统一 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 v1.6 的 Marketplace + 底部"自定义资源折叠区"重构为统一的"资源库"——builtin/custom/marketplace 三类资源在同一主网格展示，统一 IPC + 统一数据模型，为 v2 P2P agent 互联预留架构。

**Architecture:** 后端引入 `electron/src/main/resource/` 目录，统一 list/delete/install 三源资源。前端引入 `renderer/src/components/resource-library/`，主网格按 type × source 双层 tab 过滤，卡片+详情面板统一渲染。现有底层函数（listRegistered / listInstalled / installPackage / uninstallPackage）保留作为内部复用，仅删除其 IPC handler + preload 暴露。

**Tech Stack:** TypeScript strict / Node 20 LTS / Electron / React + Tailwind / vitest + @testing-library/react

**Spec 来源：** `docs/specs/2026-08-11-v1.7-resource-library-design.md`

## Global Constraints

- **Node 版本**：`nvm use 20` 后 `export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"`，禁止 Node 26（better-sqlite3 binding 失效）
- **包管理器**：`npx pnpm@9.0.0`
- **TypeScript strict**：禁止 `any` / `@ts-ignore` / `as any`，禁止 `// eslint-disable`
- **测试命令**：`cd electron && npx pnpm@9.0.0 vitest run tests/<path>` 或 `cd renderer && npx pnpm@9.0.0 vitest run src/<path>`
- **Typecheck**：根目录 `npx pnpm@9.0.0 typecheck`（两个 workspace 同时）
- **中文注释 + 中文 UI 文案**（AGENTS.md 强制）
- **Conventional Commits**：`feat:` / `fix:` / `chore:` / `refactor:` / `test:` / `docs:`
- **Git push**：`git push https://x-access-token:GH_TOKEN_PLACEHOLDER@github.com/bearangel/momo-studio.git main`
- **docs/ 在 .gitignore**：用 `git add -f docs/your-file.md`
- **不破坏 v1.6 行为**：现有 builtin/custom 资源数据完全兼容；Migration v0-v16 不动
- **资源 ID 命名**：`${source}-${type}-${slug}`（agent 用 UUID 作 slug 例外）
- **IPC 模式**：preload 用 `contextBridge.exposeInMainWorld('api', api)` + 类型化 `ApiSurface`；client.ts 是 Proxy 自动透传，不需 per-method 绑定

## 关键设计点（贯穿所有 task）

### 共享类型（Task 1 定义，所有 task 引用）

```typescript
// electron/src/main/resource/types.ts
export type ResourceType = 'agent' | 'mcp' | 'skill';
export type ResourceSource = 'builtin' | 'marketplace' | 'custom' | 'p2p';

export interface ResourceFilter {
  type?: ResourceType;
  source?: ResourceSource;
}

export interface ResourceItem {
  id: string;
  type: ResourceType;
  source: ResourceSource;
  slug: string;
  name: string;
  description: string;
  version?: string;
  iconEmoji?: string;
  installed: boolean;
  installable: boolean;
  removable: boolean;
  builtin?: { category?: string; tags?: string[] };
  marketplace?: {
    author: string; readme: string; downloadUrl: string; checksum: string;
    verificationStatus: 'official' | 'verified' | 'community' | 'unverified';
    sizeBytes?: number; installCount?: number;
    tags: string[]; category: string;
  };
  custom?: {
    installedAt: string;
    mcpConfig?: { command: string; args: string[]; env?: Record<string, string> };
    skillFrontmatter?: { name?: string; version?: string };
    agentSystemPromptHash?: string;
  };
  p2p?: { peerId: string; peerName: string };
}
```

### ID 命名工具（Task 1 定义）

```typescript
export function buildResourceId(source: ResourceSource, type: ResourceType, slug: string): string {
  return `${source}-${type}-${slug}`;
}

export function parseResourceId(id: string): { source: ResourceSource; type: ResourceType; slug: string } | null {
  const m = id.match(/^(builtin|marketplace|custom|p2p)-(agent|mcp|skill)-(.+)$/);
  if (!m) return null;
  return { source: m[1] as ResourceSource, type: m[2] as ResourceType, slug: m[3] };
}
```

### 现有底层函数（保留作 resource/ 内部复用）

```typescript
// marketplace/client.ts（已存在）
export async function fetchCatalog(catalogUrl?: string): Promise<Catalog>

// marketplace/installer.ts（已存在）
export async function installPackage(itemId: string, catalog: Catalog): Promise<{ cachePath: string }>
export function listInstalled(): InstalledPackage[]
export function uninstallPackage(itemId: string): void

// mcp/host-manager.ts（已存在）
export function listRegistered(): RegisteredMcp[]
export function deleteRegistered(name: string): void

// skill/zip-uploader.ts（已存在）
export function listInstalled(): InstalledSkill[]
export function deleteCustomSkill(slug: string): void

// agent/crud.ts（已存在）
export function listAgentDefinitions(workspaceId?: string): AgentDefinition[]
export function deleteDefinition(defId: string): void
```

---

## Phase 1：后端基础

### Task 1：resource/types.ts — 类型 + ID 工具

**Files:**
- Create: `electron/src/main/resource/types.ts`
- Create: `electron/tests/resource/types.test.ts`

**Interfaces:**
- Produces: `ResourceType` / `ResourceSource` / `ResourceFilter` / `ResourceItem` / `sourceLabel` / `buildResourceId` / `parseResourceId`

- [ ] **Step 1：写失败测试**

```typescript
// electron/tests/resource/types.test.ts
import { describe, it, expect } from 'vitest';
import {
  buildResourceId, parseResourceId, sourceLabel,
  type ResourceItem, type ResourceFilter,
} from '../../src/main/resource/types';

describe('resource/types', () => {
  it('buildResourceId 拼 ${source}-${type}-${slug}', () => {
    expect(buildResourceId('builtin', 'agent', 'pm-agent')).toBe('builtin-agent-pm-agent');
    expect(buildResourceId('custom', 'mcp', 'github')).toBe('custom-mcp-github');
    expect(buildResourceId('custom', 'agent', 'uuid-abc-123')).toBe('custom-agent-uuid-abc-123');
  });

  it('parseResourceId 反解三元组', () => {
    expect(parseResourceId('builtin-skill-code-review')).toEqual({
      source: 'builtin', type: 'skill', slug: 'code-review',
    });
    expect(parseResourceId('custom-agent-abc-123-def')).toEqual({
      source: 'custom', type: 'agent', slug: 'abc-123-def',
    });
  });

  it('parseResourceId 非法 id 返回 null', () => {
    expect(parseResourceId('invalid')).toBeNull();
    expect(parseResourceId('builtin-agent-')).toBeNull();  // 空 slug
    expect(parseResourceId('unknown-agent-foo')).toBeNull();  // 未知 source
    expect(parseResourceId('builtin-unknown-foo')).toBeNull();  // 未知 type
  });

  it('buildResourceId ↔ parseResourceId 互逆', () => {
    const cases = [
      ['builtin', 'agent', 'pm-agent'],
      ['custom', 'mcp', 'github'],
      ['marketplace', 'skill', 'xlsx-remote'],
    ] as const;
    for (const [s, t, slug] of cases) {
      const id = buildResourceId(s, t, slug);
      expect(parseResourceId(id)).toEqual({ source: s, type: t, slug });
    }
  });

  it('sourceLabel 中文文案', () => {
    expect(sourceLabel('builtin')).toBe('系统预置');
    expect(sourceLabel('custom')).toBe('我的上传');
    expect(sourceLabel('marketplace')).toBe('网络资源');
    expect(sourceLabel('p2p')).toBe('P2P 共享');
  });
});
```

- [ ] **Step 2：跑测试确认失败**

```bash
cd electron && npx pnpm@9.0.0 vitest run tests/resource/types.test.ts
```
Expected: FAIL "Cannot find module"

- [ ] **Step 3：实现 types.ts**

```typescript
// electron/src/main/resource/types.ts
//
// v1.7 资源库统一类型定义。所有来源（builtin/custom/marketplace/p2p）的资源
// 都映射成 ResourceItem，前端只感知这一种数据结构，不感知 source 差异。

export type ResourceType = 'agent' | 'mcp' | 'skill';

export type ResourceSource = 'builtin' | 'marketplace' | 'custom' | 'p2p';

export interface ResourceFilter {
  type?: ResourceType;
  source?: ResourceSource;
}

export interface ResourceItem {
  id: string;
  type: ResourceType;
  source: ResourceSource;
  slug: string;
  name: string;
  description: string;
  version?: string;
  iconEmoji?: string;
  installed: boolean;
  installable: boolean;
  removable: boolean;
  builtin?: { category?: string; tags?: string[] };
  marketplace?: {
    author: string;
    readme: string;
    downloadUrl: string;
    checksum: string;
    verificationStatus: 'official' | 'verified' | 'community' | 'unverified';
    sizeBytes?: number;
    installCount?: number;
    tags: string[];
    category: string;
  };
  custom?: {
    installedAt: string;
    mcpConfig?: { command: string; args: string[]; env?: Record<string, string> };
    skillFrontmatter?: { name?: string; version?: string };
    agentSystemPromptHash?: string;
  };
  p2p?: { peerId: string; peerName: string };
}

const SOURCE_LABELS: Record<ResourceSource, string> = {
  builtin: '系统预置',
  custom: '我的上传',
  marketplace: '网络资源',
  p2p: 'P2P 共享',
};

export function sourceLabel(source: ResourceSource): string {
  return SOURCE_LABELS[source];
}

export function buildResourceId(source: ResourceSource, type: ResourceType, slug: string): string {
  return `${source}-${type}-${slug}`;
}

export function parseResourceId(id: string): { source: ResourceSource; type: ResourceType; slug: string } | null {
  // slug 部分允许任意非空字符（包括 UUID 中的连字符），用贪婪匹配
  const m = id.match(/^(builtin|marketplace|custom|p2p)-(agent|mcp|skill)-(.+)$/);
  if (!m || !m[3]) return null;
  return {
    source: m[1] as ResourceSource,
    type: m[2] as ResourceType,
    slug: m[3],
  };
}
```

- [ ] **Step 4：跑测试确认通过**

Expected: 5 passed

- [ ] **Step 5：commit**

```bash
git add electron/src/main/resource/types.ts electron/tests/resource/types.test.ts
git commit -m "feat(resource): v1.7 types.ts 统一类型 + 资源 ID 工具"
```

---

### Task 2：resource/builtin.ts + marketplace.ts — Catalog 分流

**Files:**
- Create: `electron/src/main/resource/builtin.ts`
- Create: `electron/src/main/resource/marketplace.ts`
- Create: `electron/tests/resource/builtin-marketplace.test.ts`

**Interfaces:**
- Consumes: `fetchCatalog` from `marketplace/client.ts`、`CatalogItem` from `marketplace/types.ts`、`ResourceItem` / `buildResourceId` from `./types`
- Produces: `listBuiltinResources(catalog)` / `listMarketplaceResources(catalog)`——纯函数，参数是已 fetch 的 Catalog 对象

**重要**：spec 写的 `loadCatalogSync/Async` 实际不存在；真实情况是 `fetchCatalog()` 单函数（远程优先 + 本地回退）。Task 4 library.ts 一次 fetch 后传 catalog 参数给这两个函数，避免重复 HTTP 请求。

- [ ] **Step 1：写失败测试**

```typescript
// electron/tests/resource/builtin-marketplace.test.ts
import { describe, it, expect } from 'vitest';
import { listBuiltinResources } from '../../src/main/resource/builtin';
import { listMarketplaceResources } from '../../src/main/resource/marketplace';
import type { Catalog } from '../../src/main/marketplace/types';

const mockCatalog: Catalog = {
  version: '1.0',
  updatedAt: '2026-08-11T00:00:00Z',
  items: [
    // 内联预置（downloadUrl 空）
    { id: 'agent-pm', type: 'agent', slug: 'pm-agent', name: '项目经理', version: '1.0.0',
      author: 'Momo Studio', description: '协调', readme: '...', tags: [], category: 'dev',
      iconEmoji: '👔', verificationStatus: 'official', downloadUrl: '', checksum: '', sizeBytes: 2048, installCount: 0 },
    // 真远程下载（downloadUrl 非空）
    { id: 'skill-remote', type: 'skill', slug: 'remote-skill', name: 'Remote', version: '2.0.0',
      author: '@thirdparty', description: '远程', readme: '...', tags: ['remote'], category: 'community',
      iconEmoji: '🌐', verificationStatus: 'community', downloadUrl: 'https://example.com/skill.zip',
      checksum: 'abc123', sizeBytes: 10240, installCount: 5 },
  ],
};

describe('listBuiltinResources', () => {
  it('只返回 downloadUrl="" 的 catalog 项', () => {
    const items = listBuiltinResources(mockCatalog);
    expect(items).toHaveLength(1);
    expect(items[0]!.slug).toBe('pm-agent');
    expect(items[0]!.source).toBe('builtin');
  });

  it('builtin 项 installed=true / installable=false / removable=false', () => {
    const items = listBuiltinResources(mockCatalog);
    expect(items[0]).toMatchObject({
      installed: true, installable: false, removable: false,
    });
  });

  it('builtin 项 id 形如 builtin-${type}-${slug}', () => {
    const items = listBuiltinResources(mockCatalog);
    expect(items[0]!.id).toBe('builtin-agent-pm-agent');
  });

  it('包含 marketplace 字段（保留原 catalog 元数据，详情面板用）', () => {
    const items = listBuiltinResources(mockCatalog);
    expect(items[0]!.marketplace).toMatchObject({
      author: 'Momo Studio',
      verificationStatus: 'official',
      tags: [],
    });
  });
});

describe('listMarketplaceResources', () => {
  it('只返回 downloadUrl 非空的 catalog 项', () => {
    const items = listMarketplaceResources(mockCatalog);
    expect(items).toHaveLength(1);
    expect(items[0]!.slug).toBe('remote-skill');
    expect(items[0]!.source).toBe('marketplace');
  });

  it('marketplace 项 installable=true（未装时可 install）', () => {
    const items = listMarketplaceResources(mockCatalog);
    expect(items[0]).toMatchObject({
      installable: true, removable: true,
    });
  });

  it('marketplace 项 id 形如 marketplace-${type}-${slug}', () => {
    const items = listMarketplaceResources(mockCatalog);
    expect(items[0]!.id).toBe('marketplace-skill-remote-skill');
  });
});
```

- [ ] **Step 2：跑测试确认失败**

```bash
cd electron && npx pnpm@9.0.0 vitest run tests/resource/builtin-marketplace.test.ts
```

- [ ] **Step 3：实现 builtin.ts + marketplace.ts**

```typescript
// electron/src/main/resource/builtin.ts
//
// listBuiltinResources：从已 fetch 的 catalog 中过滤出 downloadUrl="" 的内联项。
// 这些是应用打包自带 + 离线可用的"系统预置"资源。
//
// 注意：本函数是纯函数，参数 catalog 由 library.ts 一次 fetchCatalog 后传入，
// 避免每个 list 函数都触发一次 HTTP 请求。

import type { Catalog } from '../marketplace/types';
import type { ResourceItem } from './types';
import { buildResourceId } from './types';
import { fromCatalogItem } from './catalog-adapter';

export function listBuiltinResources(catalog: Catalog): ResourceItem[] {
  return catalog.items
    .filter((item) => !item.downloadUrl)
    .map((item) => fromCatalogItem(item, 'builtin'));
}
```

```typescript
// electron/src/main/resource/marketplace.ts
//
// listMarketplaceResources：从已 fetch 的 catalog 中过滤出 downloadUrl 非空的真远程包。
// 当前 catalog.json 全是 downloadUrl="" 内联项 → 这里返回空数组。
// 未来真接 marketplace 后，远程包在这里出现。

import type { Catalog } from '../marketplace/types';
import type { ResourceItem } from './types';
import { fromCatalogItem } from './catalog-adapter';

export function listMarketplaceResources(catalog: Catalog): ResourceItem[] {
  return catalog.items
    .filter((item) => item.downloadUrl)
    .map((item) => fromCatalogItem(item, 'marketplace'));
}
```

**catalog-adapter.ts**（共用工具，把 CatalogItem 转 ResourceItem）：

```typescript
// electron/src/main/resource/catalog-adapter.ts
import type { CatalogItem } from '../marketplace/types';
import type { ResourceItem, ResourceSource } from './types';
import { buildResourceId } from './types';
import { listInstalled as listMarketplaceInstalled } from '../marketplace/installer';
// ↑ marketplace/installer.ts 的 listInstalled 返回 InstalledPackage[]（已装的 catalog item id 列表）

export function fromCatalogItem(item: CatalogItem, source: ResourceSource): ResourceItem {
  const id = buildResourceId(source, item.type, item.slug);
  const installedPackageIds = listMarketplaceInstalled().map((p) => p.id);
  const installed = source === 'builtin' ? true : installedPackageIds.includes(item.id);

  return {
    id,
    type: item.type,
    source,
    slug: item.slug,
    name: item.name,
    description: item.description,
    version: item.version,
    iconEmoji: item.iconEmoji,
    installed,
    installable: source === 'marketplace' && !installed,
    removable: source === 'marketplace' && installed,
    builtin: source === 'builtin' ? { category: item.category, tags: item.tags } : undefined,
    marketplace: {
      author: item.author,
      readme: item.readme,
      downloadUrl: item.downloadUrl,
      checksum: item.checksum,
      verificationStatus: item.verificationStatus,
      sizeBytes: item.sizeBytes,
      installCount: item.installCount,
      tags: item.tags,
      category: item.category,
    },
  };
}
```

- [ ] **Step 4：跑测试确认通过**

Expected: 7 passed

- [ ] **Step 5：commit**

```bash
git add electron/src/main/resource/builtin.ts electron/src/main/resource/marketplace.ts electron/src/main/resource/catalog-adapter.ts electron/tests/resource/builtin-marketplace.test.ts
git commit -m "feat(resource): builtin + marketplace 源（catalog 按 downloadUrl 分流）"
```

---

### Task 3：resource/custom.ts — Custom 三类合并

**Files:**
- Create: `electron/src/main/resource/custom.ts`
- Create: `electron/tests/resource/custom.test.ts`

**Interfaces:**
- Consumes: `listRegistered` from `mcp/host-manager`、`listInstalled` from `skill/zip-uploader`、`listAgentDefinitions` from `agent/crud`、`buildResourceId` from `./types`
- Produces: `listCustomResources()` → `ResourceItem[]`

- [ ] **Step 1：写失败测试**

```typescript
// electron/tests/resource/custom.test.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import Database from 'better-sqlite3';
import { runMigrations } from '../../src/main/storage/migrations';
import * as dbModule from '../../src/main/storage/db';
import { listCustomResources } from '../../src/main/resource/custom';

// mock 底层 list 函数（避免依赖真实 fs + DB）
vi.mock('../../src/main/mcp/host-manager', () => ({
  listRegistered: vi.fn(() => [
    { id: 'm1', name: 'github', version: '1.0.0', command: 'npx', args: ['-y', 'x'], env: { TOKEN: 't' },
      source: 'custom', installedAt: '2026-08-11T10:00:00Z' },
    { id: 'm2', name: 'builtin-fs', version: '1.0.0', command: 'npx', args: [],
      source: 'marketplace', installedAt: '2026-08-10T10:00:00Z' },  // 应被过滤掉
  ]),
}));

vi.mock('../../src/main/skill/zip-uploader', () => ({
  listInstalled: vi.fn(() => [
    { slug: 'xlsx', name: 'xlsx', description: 'xlsx 处理',
      source: 'custom', installedAt: '2026-08-11T11:00:00Z' },
    { slug: 'code-review', name: 'code-review', description: '内置',
      source: 'builtin', installedAt: null },  // 应被过滤掉
  ]),
}));

vi.mock('../../src/main/agent/crud', () => ({
  listAgentDefinitions: vi.fn(() => [
    { id: 'uuid-1', name: '我的 agent', slug: 'my-agent', version: '1.0.0',
      source: 'custom', description: '自定义', iconEmoji: '🤖',
      systemPrompt: 'prompt', createdAt: '2026-08-11T12:00:00Z',
      runtime: 'declarative', defaultTools: [], defaultMcps: [], defaultSkills: [],
      workspaceId: null, modelProviderId: null, modelName: 'm',
    },
  ]),
}));

describe('listCustomResources', () => {
  it('合并 mcp + skill + agent 三类 custom', () => {
    const items = listCustomResources();
    expect(items).toHaveLength(3);  // 1 mcp + 1 skill + 1 agent（builtin/marketplace 被过滤）
    expect(items.map((i) => i.type).sort()).toEqual(['agent', 'mcp', 'skill']);
  });

  it('所有 custom 项 source=custom', () => {
    const items = listCustomResources();
    for (const i of items) expect(i.source).toBe('custom');
  });

  it('所有 custom 项 installed=true / installable=false / removable=true', () => {
    const items = listCustomResources();
    for (const i of items) {
      expect(i).toMatchObject({ installed: true, installable: false, removable: true });
    }
  });

  it('MCP custom 项含 mcpConfig 字段', () => {
    const items = listCustomResources();
    const mcp = items.find((i) => i.type === 'mcp')!;
    expect(mcp.custom?.mcpConfig).toMatchObject({
      command: 'npx', args: ['-y', 'x'], env: { TOKEN: 't' },
    });
    expect(mcp.id).toBe('custom-mcp-github');
  });

  it('Skill custom 项含 installedAt', () => {
    const items = listCustomResources();
    const skill = items.find((i) => i.type === 'skill')!;
    expect(skill.custom?.installedAt).toBe('2026-08-11T11:00:00Z');
    expect(skill.id).toBe('custom-skill-xlsx');
  });

  it('Agent custom 项含 agentSystemPromptHash + installedAt', () => {
    const items = listCustomResources();
    const agent = items.find((i) => i.type === 'agent')!;
    expect(agent.custom?.agentSystemPromptHash).toBeTruthy();
    expect(agent.id).toBe('custom-agent-uuid-1');  // agent def id 是 UUID，作为 slug 部分
  });

  it('过滤掉 source=marketplace / source=builtin 的项', () => {
    const items = listCustomResources();
    const names = items.map((i) => i.name);
    expect(names).not.toContain('builtin-fs');
    expect(names).not.toContain('code-review');
  });
});
```

- [ ] **Step 2：跑测试确认失败**

- [ ] **Step 3：实现 custom.ts**

```typescript
// electron/src/main/resource/custom.ts
//
// listCustomResources：合并三类用户自定义上传资源。
//   - MCP：mcp_definitions 表 source='custom'（来自 host-manager.listRegistered 过滤）
//   - Skill：<userData>/skills/ 下有 .sha256 标记的目录（来自 zip-uploader.listInstalled 过滤）
//   - Agent：agent_definitions 表 source='custom'（来自 crud.listAgentDefinitions 过滤）
//
// 三类底层 list 函数都是同步 + 纯 DB/fs 读，本函数也同步。

import { listRegistered } from '../mcp/host-manager';
import { listInstalled as listInstalledSkills } from '../skill/zip-uploader';
import { listAgentDefinitions } from '../agent/crud';
import { buildResourceId, type ResourceItem } from './types';
import crypto from 'node:crypto';

function hashPrompt(prompt: string): string {
  return crypto.createHash('sha256').update(prompt).digest('hex').slice(0, 16);
}

export function listCustomResources(): ResourceItem[] {
  const result: ResourceItem[] = [];

  // 1. MCP
  for (const mcp of listRegistered().filter((m) => m.source === 'custom')) {
    result.push({
      id: buildResourceId('custom', 'mcp', mcp.name),
      type: 'mcp',
      source: 'custom',
      slug: mcp.name,
      name: mcp.name,
      description: `自定义 MCP（${mcp.command}）`,
      version: mcp.version,
      installed: true,
      installable: false,
      removable: true,
      custom: {
        installedAt: mcp.installedAt,
        mcpConfig: { command: mcp.command, args: mcp.args, env: mcp.env },
      },
    });
  }

  // 2. Skill
  for (const skill of listInstalledSkills().filter((s) => s.source === 'custom')) {
    result.push({
      id: buildResourceId('custom', 'skill', skill.slug),
      type: 'skill',
      source: 'custom',
      slug: skill.slug,
      name: skill.name,
      description: skill.description,
      installed: true,
      installable: false,
      removable: true,
      custom: {
        installedAt: skill.installedAt ?? new Date().toISOString(),
      },
    });
  }

  // 3. Agent（def.id 是 UUID，作为 slug 部分）
  for (const def of listAgentDefinitions().filter((d) => d.source === 'custom')) {
    result.push({
      id: buildResourceId('custom', 'agent', def.id),
      type: 'agent',
      source: 'custom',
      slug: def.id,
      name: def.name,
      description: def.description,
      iconEmoji: def.iconEmoji,
      version: def.version,
      installed: true,
      installable: false,
      removable: true,
      custom: {
        installedAt: def.createdAt,
        agentSystemPromptHash: hashPrompt(def.systemPrompt),
      },
    });
  }

  return result;
}
```

- [ ] **Step 4：跑测试确认通过**

Expected: 7 passed

- [ ] **Step 5：commit**

```bash
git add electron/src/main/resource/custom.ts electron/tests/resource/custom.test.ts
git commit -m "feat(resource): custom 源（mcp + skill + agent 三类合并）"
```

---

### Task 4：resource/library.ts — listResources 主入口

**Files:**
- Create: `electron/src/main/resource/library.ts`
- Create: `electron/tests/resource/library.test.ts`

**Interfaces:**
- Consumes: `fetchCatalog` from `marketplace/client`、`listBuiltinResources` / `listMarketplaceResources` / `listCustomResources`、`parseResourceId`
- Produces: `listResources(filter?)` async / `resolveResourceById(id)` async

- [ ] **Step 1：写失败测试**

```typescript
// electron/tests/resource/library.test.ts
import { describe, it, expect, vi } from 'vitest';
import { listResources, resolveResourceById } from '../../src/main/resource/library';

const mockCatalog = {
  version: '1.0', updatedAt: '2026-08-11',
  items: [
    { id: 'builtin-1', type: 'agent', slug: 'pm', name: 'PM', version: '1',
      author: 'Momo', description: 'd', readme: 'r', tags: [], category: 'c',
      iconEmoji: '👔', verificationStatus: 'official', downloadUrl: '', checksum: '', sizeBytes: 0, installCount: 0 },
    { id: 'marketplace-1', type: 'skill', slug: 'remote', name: 'Remote', version: '1',
      author: '@x', description: 'd', readme: 'r', tags: [], category: 'c',
      iconEmoji: '📦', verificationStatus: 'community', downloadUrl: 'http://x', checksum: 'x', sizeBytes: 0, installCount: 0 },
  ],
};

vi.mock('../../src/main/marketplace/client', () => ({
  fetchCatalog: vi.fn(async () => mockCatalog),
}));

vi.mock('../../src/main/resource/custom', () => ({
  listCustomResources: vi.fn(() => [
    { id: 'custom-mcp-github', type: 'mcp', source: 'custom', slug: 'github',
      name: 'github', description: 'd', installed: true, installable: false, removable: true,
      custom: { installedAt: '2026-08-11' } },
  ]),
}));

// mock marketplace/installer.listInstalled（catalog-adapter 用到）
vi.mock('../../src/main/marketplace/installer', () => ({
  listInstalled: vi.fn(() => []),
  installPackage: vi.fn(),
  uninstallPackage: vi.fn(),
}));

describe('listResources', () => {
  it('合并三源：builtin + marketplace + custom', async () => {
    const items = await listResources();
    expect(items).toHaveLength(3);  // 1 builtin + 1 marketplace + 1 custom
    expect(items.map((i) => i.source).sort()).toEqual(['builtin', 'custom', 'marketplace']);
  });

  it('filter.type 只返回对应类型', async () => {
    const skills = await listResources({ type: 'skill' });
    expect(skills.every((i) => i.type === 'skill')).toBe(true);
  });

  it('filter.source 只返回对应来源', async () => {
    const custom = await listResources({ source: 'custom' });
    expect(custom.every((i) => i.source === 'custom')).toBe(true);
    expect(custom).toHaveLength(1);
  });

  it('filter.source=builtin 不触发 fetchCatalog（短路 Promise.resolve([])）', async () => {
    const { fetchCatalog } = await import('../../src/main/marketplace/client');
    (fetchCatalog as ReturnType<typeof vi.fn>).mockClear();
    await listResources({ source: 'custom' });
    expect(fetchCatalog).not.toHaveBeenCalled();  // 不需要 catalog
  });

  it('fetchCatalog 失败时 builtin+marketplace 返回空，但 custom 仍工作', async () => {
    const { fetchCatalog } = await import('../../src/main/marketplace/client');
    (fetchCatalog as ReturnType<typeof vi.fn>).mockRejectedValueOnce(new Error('network'));
    const items = await listResources();
    expect(items).toHaveLength(1);  // 只有 custom
    expect(items[0]!.source).toBe('custom');
  });
});

describe('resolveResourceById', () => {
  it('合法 id 返回对应 ResourceItem', async () => {
    const item = await resolveResourceById('custom-mcp-github');
    expect(item?.slug).toBe('github');
  });

  it('非法 id 返回 null', async () => {
    const item = await resolveResourceById('invalid');
    expect(item).toBeNull();
  });
});
```

- [ ] **Step 2：跑测试确认失败**

- [ ] **Step 3：实现 library.ts**

```typescript
// electron/src/main/resource/library.ts
//
// listResources：统一三源（builtin/custom/marketplace）资源列表的主入口。
// 内部一次 fetchCatalog（远程优先 + 本地回退），按 downloadUrl 分流到 builtin/marketplace，
// 再合并 custom。fetchCatalog 失败时只丢 builtin+marketplace，custom 仍正常返回。
//
// filter 支持按 type/source 过滤。filter.source 指定时短路不必要源（避免 fetchCatalog）。

import { fetchCatalog } from '../marketplace/client';
import { listBuiltinResources } from './builtin';
import { listMarketplaceResources } from './marketplace';
import { listCustomResources } from './custom';
import { parseResourceId, type ResourceFilter, type ResourceItem } from './types';
import { logger } from '../logger';

export async function listResources(filter?: ResourceFilter): Promise<ResourceItem[]> {
  const needCatalog = !filter?.source || filter.source === 'builtin' || filter.source === 'marketplace';
  const needCustom = !filter?.source || filter.source === 'custom';

  // 并行：catalog（如需要）+ custom
  const tasks: Promise<unknown>[] = [];

  let builtinItems: ResourceItem[] = [];
  let marketplaceItems: ResourceItem[] = [];

  if (needCatalog) {
    tasks.push(
      fetchCatalog()
        .then((catalog) => {
          builtinItems = listBuiltinResources(catalog);
          marketplaceItems = listMarketplaceResources(catalog);
        })
        .catch((err) => {
          logger.warn('listResources: fetchCatalog 失败，builtin/marketplace 返回空', { error: (err as Error).message });
        }),
    );
  }

  let customItems: ResourceItem[] = [];
  if (needCustom) {
    tasks.push(
      Promise.resolve().then(() => {
        customItems = listCustomResources();
      }),
    );
  }

  await Promise.all(tasks);

  let items = [...builtinItems, ...marketplaceItems, ...customItems];

  // 按 type 过滤
  if (filter?.type) {
    items = items.filter((i) => i.type === filter.type);
  }
  // 按 source 过滤（filter.source 已用于短路，但保险起见再过滤一次）
  if (filter?.source) {
    items = items.filter((i) => i.source === filter.source);
  }

  return items;
}

export async function resolveResourceById(id: string): Promise<ResourceItem | null> {
  const parsed = parseResourceId(id);
  if (!parsed) return null;
  const items = await listResources({ type: parsed.type, source: parsed.source });
  return items.find((i) => i.id === id) ?? null;
}
```

- [ ] **Step 4：跑测试确认通过**

Expected: 7 passed

- [ ] **Step 5：commit**

```bash
git add electron/src/main/resource/library.ts electron/tests/resource/library.test.ts
git commit -m "feat(resource): listResources 主入口 + resolveResourceById（三源合并）"
```

---

### Task 5：resource/ipc.handlers.ts + IPC 注册 + preload + types

**Files:**
- Create: `electron/src/main/resource/ipc.handlers.ts`
- Modify: `electron/src/main/ipc/index.ts`（注册 registerResourceHandlers）
- Modify: `electron/src/preload/index.ts`（绑定 resource 命名空间）
- Modify: `renderer/src/ipc/types.d.ts`（ResourceItem 类型 + ApiSurface.resource）
- Create: `electron/tests/resource/ipc-handlers.test.ts`

**Interfaces:**
- Consumes: `listResources` / `resolveResourceById` from `./library`、`installPackage` / `uninstallPackage`、`deleteRegistered` / `deleteCustomSkill` / `deleteDefinition`
- Produces: IPC 通道 `resource:list` / `resource:getDetail` / `resource:install` / `resource:delete`

- [ ] **Step 1：写失败测试**

```typescript
// electron/tests/resource/ipc-handlers.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest';

// mock electron 模块（ipcMain.handle 在测试环境不存在）
vi.mock('electron', () => ({
  ipcMain: { handle: vi.fn() },
}));

// mock library
vi.mock('../../src/main/resource/library', () => ({
  listResources: vi.fn(),
  resolveResourceById: vi.fn(),
}));

// mock 底层 delete/install
vi.mock('../../src/main/mcp/host-manager', () => ({ deleteRegistered: vi.fn() }));
vi.mock('../../src/main/skill/zip-uploader', () => ({ deleteCustomSkill: vi.fn() }));
vi.mock('../../src/main/agent/crud', () => ({ deleteDefinition: vi.fn() }));
vi.mock('../../src/main/marketplace/installer', () => ({
  installPackage: vi.fn(), uninstallPackage: vi.fn(),
}));

import { ipcMain } from 'electron';
import { registerResourceHandlers } from '../../src/main/resource/ipc.handlers';
import { listResources, resolveResourceById } from '../../src/main/resource/library';
import { deleteRegistered } from '../../src/main/mcp/host-manager';
import { deleteCustomSkill } from '../../src/main/skill/zip-uploader';
import { deleteDefinition } from '../../src/main/agent/crud';
import { uninstallPackage } from '../../src/main/marketplace/installer';

describe('registerResourceHandlers', () => {
  beforeEach(() => {
    vi.clearAllMocks();
    registerResourceHandlers();
  });

  it('注册 4 个 IPC 通道', () => {
    const channels = (ipcMain.handle as ReturnType<typeof vi.fn>).mock.calls.map((c: unknown[]) => c[0]);
    expect(channels).toEqual(
      expect.arrayContaining(['resource:list', 'resource:getDetail', 'resource:install', 'resource:delete']),
    );
  });

  it('resource:list 调 listResources(filter)', async () => {
    (listResources as ReturnType<typeof vi.fn>).mockResolvedValueOnce([]);
    const calls = (ipcMain.handle as ReturnType<typeof vi.fn>).mock.calls;
    const listCall = calls.find((c: unknown[]) => c[0] === 'resource:list');
    const handler = listCall![1] as (evt: unknown, filter: unknown) => Promise<unknown>;
    await handler({}, { type: 'mcp' });
    expect(listResources).toHaveBeenCalledWith({ type: 'mcp' });
  });

  it('resource:delete custom-mcp-* 路由到 deleteRegistered', async () => {
    (resolveResourceById as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
      id: 'custom-mcp-github', type: 'mcp', source: 'custom', slug: 'github',
      removable: true, name: 'github',
    });
    const calls = (ipcMain.handle as ReturnType<typeof vi.fn>).mock.calls;
    const deleteCall = calls.find((c: unknown[]) => c[0] === 'resource:delete');
    const handler = deleteCall![1] as (evt: unknown, id: string) => Promise<void>;
    await handler({}, 'custom-mcp-github');
    expect(deleteRegistered).toHaveBeenCalledWith('github');
  });

  it('resource:delete builtin-* 抛错（不可移除）', async () => {
    (resolveResourceById as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
      id: 'builtin-agent-pm', source: 'builtin', removable: false, name: 'PM',
    });
    const calls = (ipcMain.handle as ReturnType<typeof vi.fn>).mock.calls;
    const deleteCall = calls.find((c: unknown[]) => c[0] === 'resource:delete');
    const handler = deleteCall![1] as (evt: unknown, id: string) => Promise<void>;
    await expect(handler({}, 'builtin-agent-pm')).rejects.toThrow(/系统预置不可移除/);
  });

  it('resource:delete custom-skill-* 路由到 deleteCustomSkill', async () => {
    (resolveResourceById as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
      id: 'custom-skill-xlsx', type: 'skill', source: 'custom', slug: 'xlsx',
      removable: true, name: 'xlsx',
    });
    const calls = (ipcMain.handle as ReturnType<typeof vi.fn>).mock.calls;
    const deleteCall = calls.find((c: unknown[]) => c[0] === 'resource:delete');
    const handler = deleteCall![1] as (evt: unknown, id: string) => Promise<void>;
    await handler({}, 'custom-skill-xlsx');
    expect(deleteCustomSkill).toHaveBeenCalledWith('xlsx');
  });

  it('resource:delete custom-agent-* 路由到 deleteDefinition', async () => {
    (resolveResourceById as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
      id: 'custom-agent-uuid1', type: 'agent', source: 'custom', slug: 'uuid1',
      removable: true, name: 'my agent',
    });
    const calls = (ipcMain.handle as ReturnType<typeof vi.fn>).mock.calls;
    const deleteCall = calls.find((c: unknown[]) => c[0] === 'resource:delete');
    const handler = deleteCall![1] as (evt: unknown, id: string) => Promise<void>;
    await handler({}, 'custom-agent-uuid1');
    expect(deleteDefinition).toHaveBeenCalledWith('uuid1');
  });

  it('resource:delete marketplace-* 路由到 uninstallPackage', async () => {
    (resolveResourceById as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
      id: 'marketplace-skill-remote', type: 'skill', source: 'marketplace', slug: 'remote',
      removable: true, name: 'remote',
    });
    const calls = (ipcMain.handle as ReturnType<typeof vi.fn>).mock.calls;
    const deleteCall = calls.find((c: unknown[]) => c[0] === 'resource:delete');
    const handler = deleteCall![1] as (evt: unknown, id: string) => Promise<void>;
    await handler({}, 'marketplace-skill-remote');
    expect(uninstallPackage).toHaveBeenCalledWith('marketplace-skill-remote');
  });
});
```

- [ ] **Step 2：跑测试确认失败**

- [ ] **Step 3：实现 ipc.handlers.ts**

```typescript
// electron/src/main/resource/ipc.handlers.ts
//
// 资源库 IPC handler 注册。4 个通道：
//   - resource:list      统一列表（filter 可选）
//   - resource:getDetail 按 id 查详情
//   - resource:install   marketplace 资源安装（封装现有 installPackage）
//   - resource:delete    统一删除/卸载（按 source+type 路由）

import { ipcMain } from 'electron';
import { logger } from '../logger';
import { listResources, resolveResourceById } from './library';
import { sourceLabel, type ResourceFilter } from './types';
import { installPackage, uninstallPackage } from '../marketplace/installer';
import { deleteRegistered } from '../mcp/host-manager';
import { deleteCustomSkill } from '../skill/zip-uploader';
import { deleteDefinition } from '../agent/crud';

export function registerResourceHandlers(): void {
  ipcMain.handle('resource:list', async (_evt, filter?: ResourceFilter) => {
    return listResources(filter);
  });

  ipcMain.handle('resource:getDetail', async (_evt, id: string) => {
    return resolveResourceById(id);
  });

  ipcMain.handle('resource:install', async (_evt, id: string) => {
    const item = await resolveResourceById(id);
    if (!item) throw new Error(`资源 ${id} 不存在`);
    if (!item.installable) throw new Error(`「${item.name}」不可安装`);
    if (item.source === 'marketplace') {
      // installPackage 需要_catalog + itemId；这里 item.slug 作为 catalog item id 的近似
      // 实际实现时可能需要 fetchCatalog + 找原 item.id（catalog id 形如 marketplace-1）
      // 简化：直接传 item.slug（marketplace catalog 里 id 形如 `marketplace-${slug}` 或 `${type}-${slug}`）
      return installPackage(item.slug);
    }
    throw new Error(`source=${item.source} 不支持 install 操作`);
  });

  ipcMain.handle('resource:delete', async (_evt, id: string) => {
    const item = await resolveResourceById(id);
    if (!item) throw new Error(`资源 ${id} 不存在`);
    if (!item.removable) {
      throw new Error(`「${item.name}」是${sourceLabel(item.source)}，不可移除`);
    }
    switch (item.source) {
      case 'marketplace':
        return uninstallPackage(item.slug);
      case 'custom':
        if (item.type === 'mcp')   return deleteRegistered(item.slug);
        if (item.type === 'skill') return deleteCustomSkill(item.slug);
        if (item.type === 'agent') return deleteDefinition(item.slug);
        throw new Error(`未知 custom type: ${item.type}`);
      default:
        throw new Error(`source=${item.source} 不支持 delete 操作`);
    }
  });

  logger.info('Resource IPC handlers 已注册');
}
```

- [ ] **Step 4：在 `electron/src/main/ipc/index.ts` 注册**

```typescript
// 找到现有的 registerIpcHandlers 函数，加：
import { registerResourceHandlers } from '../resource/ipc.handlers';
// ...
registerResourceHandlers();
```

- [ ] **Step 5：preload/index.ts 加 resource 命名空间**

```typescript
// 在现有 api 对象内加 resource 子对象：
resource: {
  list: (filter?: ResourceFilter) => invoke('resource:list', filter),
  getDetail: (id: string) => invoke('resource:getDetail', id),
  install: (id: string) => invoke('resource:install', id),
  delete: (id: string) => invoke('resource:delete', id),
},
```

需要 import `ResourceFilter` 类型（从 renderer/types.d.ts 或 type-only re-export）。

- [ ] **Step 6：renderer/src/ipc/types.d.ts 加类型 + ApiSurface**

```typescript
// 加 ResourceItem / ResourceFilter / ResourceSource / ResourceType 类型
// （与 electron/src/main/resource/types.ts 对齐）

// ApiSurface 加：
resource: {
  list: (filter?: ResourceFilter) => Promise<ResourceItem[]>;
  getDetail: (id: string) => Promise<ResourceItem | null>;
  install: (id: string) => Promise<void>;
  delete: (id: string) => Promise<void>;
};
```

- [ ] **Step 7：跑测试 + typecheck**

```bash
cd electron && npx pnpm@9.0.0 vitest run tests/resource/
npx pnpm@9.0.0 typecheck
```
Expected: typecheck 双 clean，测试通过

- [ ] **Step 8：commit**

```bash
git add electron/src/main/resource/ipc.handlers.ts electron/src/main/ipc/index.ts electron/src/preload/index.ts renderer/src/ipc/types.d.ts electron/tests/resource/ipc-handlers.test.ts
git commit -m "feat(resource): IPC 4 通道（list/getDetail/install/delete）+ preload + types"
```

---

## Phase 2：UI 重构

### Task 6：SourceBadge + ResourceCard 组件

**Files:**
- Create: `renderer/src/components/resource-library/SourceBadge.tsx`
- Create: `renderer/src/components/resource-library/ResourceCard.tsx`
- Create: `renderer/src/components/resource-library/SourceBadge.test.tsx`
- Create: `renderer/src/components/resource-library/ResourceCard.test.tsx`

**Interfaces:**
- Consumes: `ResourceItem` / `ResourceSource` from `../../ipc/types`
- Produces: `<SourceBadge source={...} />` + `<ResourceCard item={...} selected={...} onSelect={...} onInstall={...} onDelete={...} />`

- [ ] **Step 1：写失败测试**

```typescript
// renderer/src/components/resource-library/SourceBadge.test.tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { SourceBadge } from './SourceBadge';

describe('SourceBadge', () => {
  it('builtin 显示"系统预置" + 蓝色', () => {
    render(<SourceBadge source="builtin" />);
    const badge = screen.getByText('系统预置');
    expect(badge).toBeInTheDocument();
    expect(badge.className).toContain('bg-accent-blue');
  });

  it('custom 显示"我的上传" + 紫色', () => {
    render(<SourceBadge source="custom" />);
    expect(screen.getByText('我的上传').className).toContain('text-purple');
  });

  it('marketplace 显示"网络资源" + 琥珀色', () => {
    render(<SourceBadge source="marketplace" />);
    expect(screen.getByText('网络资源').className).toContain('text-amber');
  });

  it('p2p 显示"P2P 共享" + 粉色（v2 预留）', () => {
    render(<SourceBadge source="p2p" />);
    expect(screen.getByText('P2P 共享').className).toContain('text-pink');
  });
});
```

```typescript
// renderer/src/components/resource-library/ResourceCard.test.tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { ResourceCard } from './ResourceCard';
import type { ResourceItem } from '../../../ipc/types';

const baseItem = (overrides: Partial<ResourceItem> = {}): ResourceItem => ({
  id: 'builtin-agent-pm', type: 'agent', source: 'builtin',
  slug: 'pm', name: '项目经理', description: '协调',
  installed: true, installable: false, removable: false,
  ...overrides,
});

describe('ResourceCard', () => {
  it('显示名称 + 描述 + source 徽章', () => {
    render(<ResourceCard item={baseItem()} selected={false} onSelect={() => {}} />);
    expect(screen.getByText('项目经理')).toBeInTheDocument();
    expect(screen.getByText('协调')).toBeInTheDocument();
    expect(screen.getByText('系统预置')).toBeInTheDocument();
  });

  it('builtin 项显示"✓ 已安装"无删除按钮', () => {
    render(<ResourceCard item={baseItem()} selected={false} onSelect={() => {}} />);
    expect(screen.getByText(/已安装/)).toBeInTheDocument();
    expect(screen.queryByLabelText(/删除/)).not.toBeInTheDocument();
  });

  it('custom 项显示删除按钮', () => {
    const onDelete = vi.fn();
    render(
      <ResourceCard
        item={baseItem({ id: 'custom-mcp-github', source: 'custom', removable: true })}
        selected={false}
        onSelect={() => {}}
        onDelete={onDelete}
      />,
    );
    const delBtn = screen.getByLabelText(/删除/);
    fireEvent.click(delBtn);
    expect(onDelete).toHaveBeenCalledWith('custom-mcp-github');
  });

  it('marketplace 未装项显示安装按钮', () => {
    const onInstall = vi.fn();
    render(
      <ResourceCard
        item={baseItem({
          id: 'marketplace-skill-x', source: 'marketplace',
          installed: false, installable: true,
        })}
        selected={false}
        onSelect={() => {}}
        onInstall={onInstall}
      />,
    );
    const installBtn = screen.getByRole('button', { name: /安装/ });
    fireEvent.click(installBtn);
    expect(onInstall).toHaveBeenCalledWith('marketplace-skill-x');
  });

  it('点击卡片触发 onSelect（不是按钮）', () => {
    const onSelect = vi.fn();
    render(<ResourceCard item={baseItem()} selected={false} onSelect={onSelect} />);
    fireEvent.click(screen.getByText('项目经理'));
    expect(onSelect).toHaveBeenCalledWith('builtin-agent-pm');
  });

  it('selected=true 时卡片边框高亮', () => {
    const { container } = render(<ResourceCard item={baseItem()} selected={true} onSelect={() => {}} />);
    expect(container.firstChild).toHaveClass('border-accent-blue');
  });
});
```

- [ ] **Step 2：跑测试确认失败**

- [ ] **Step 3：实现 SourceBadge.tsx**

```typescript
// renderer/src/components/resource-library/SourceBadge.tsx
import type { ResourceSource } from '../../../ipc/types';
import { cn } from '../../../lib/cn';

const SOURCE_BADGE: Record<ResourceSource, { label: string; cls: string }> = {
  builtin:     { label: '系统预置', cls: 'bg-accent-blue/20 text-accent-blue' },
  custom:      { label: '我的上传', cls: 'bg-purple-500/20 text-purple-400' },
  marketplace: { label: '网络资源', cls: 'bg-amber-500/20 text-amber-400' },
  p2p:         { label: 'P2P 共享', cls: 'bg-pink-500/20 text-pink-400' },
};

export function SourceBadge({ source }: { source: ResourceSource }) {
  const conf = SOURCE_BADGE[source];
  return (
    <span className={cn('text-[10px] px-1.5 py-0.5 rounded border', conf.cls)}>
      {conf.label}
    </span>
  );
}
```

- [ ] **Step 4：实现 ResourceCard.tsx**

```typescript
// renderer/src/components/resource-library/ResourceCard.tsx
import type { ResourceItem } from '../../../ipc/types';
import { cn } from '../../../lib/cn';
import { SourceBadge } from './SourceBadge';

interface Props {
  item: ResourceItem;
  selected: boolean;
  onSelect: (id: string) => void;
  onInstall?: (id: string) => void;
  onDelete?: (id: string) => void;
}

const TYPE_EMOJI: Record<ResourceItem['type'], string> = {
  agent: '🤖',
  mcp: '🔌',
  skill: '📦',
};

export function ResourceCard({ item, selected, onSelect, onInstall, onDelete }: Props) {
  return (
    <div
      className={cn(
        'flex flex-col gap-2 p-3 rounded-lg bg-bg-tertiary border cursor-pointer transition-colors',
        selected ? 'border-accent-blue' : 'border-border-subtle hover:border-border-strong',
      )}
      onClick={() => onSelect(item.id)}
    >
      <div className="flex items-start gap-2">
        <span className="text-2xl leading-none">{item.iconEmoji || TYPE_EMOJI[item.type]}</span>
        <div className="flex-1 min-w-0">
          <div className="font-semibold truncate">{item.name}</div>
          <div className="text-xs text-neutral-400 truncate">{item.description}</div>
        </div>
      </div>

      <div className="flex gap-1 flex-wrap">
        <SourceBadge source={item.source} />
      </div>

      <div className="mt-auto flex gap-1 items-center">
        {item.installable && !item.installed && onInstall && (
          <button
            type="button"
            className="text-xs px-2 py-0.5 rounded bg-accent-blue/20 text-accent-blue hover:bg-accent-blue/30"
            onClick={(e) => { e.stopPropagation(); onInstall(item.id); }}
          >
            安装
          </button>
        )}
        {item.installed && item.removable && onDelete && (
          <button
            type="button"
            aria-label={`删除 ${item.name}`}
            className="text-xs px-2 py-0.5 rounded bg-red-500/20 text-red-400 hover:bg-red-500/30"
            onClick={(e) => { e.stopPropagation(); onDelete(item.id); }}
          >
            🗑 删除
          </button>
        )}
        {item.installed && !item.removable && (
          <span className="text-xs text-neutral-500">✓ 已安装</span>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 5：跑测试确认通过**

Expected: 4 + 6 = 10 passed

- [ ] **Step 6：commit**

```bash
git add renderer/src/components/resource-library/SourceBadge.tsx renderer/src/components/resource-library/ResourceCard.tsx renderer/src/components/resource-library/SourceBadge.test.tsx renderer/src/components/resource-library/ResourceCard.test.tsx
git commit -m "feat(resource-library): SourceBadge + ResourceCard 组件"
```

---

### Task 7：ResourceDetail 组件（按 source 分支）

**Files:**
- Create: `renderer/src/components/resource-library/ResourceDetail.tsx`
- Create: `renderer/src/components/resource-library/ResourceDetail.test.tsx`

**Interfaces:**
- Consumes: `ResourceItem`
- Produces: `<ResourceDetail item={...} onClose={...} onDelete={...} onInstall={...} />`

- [ ] **Step 1：写失败测试**

```typescript
// 7 个测试覆盖 4 source × 不同字段显示
// - builtin: 显示 README + author
// - marketplace: 显示 README + author + 校验徽章 + downloadUrl + install 按钮
// - custom MCP: 显示命令 + 参数 + 环境变量 + 安装时间
// - custom Skill: 显示 frontmatter + 安装时间
// - custom Agent: 显示 system prompt hash + 模型 + 安装时间
// - builtin: 不显示删除按钮（removable=false）
// - custom: 显示删除按钮
```

详细测试代码执行时按上面 7 case 编写，每个 case mock 不同 ResourceItem 字段。

- [ ] **Step 2：跑测试确认失败**

- [ ] **Step 3：实现 ResourceDetail.tsx**

```typescript
// renderer/src/components/resource-library/ResourceDetail.tsx
//
// 详情面板（右侧滑出）。按 source 分支显示不同字段：
//   - builtin/marketplace: README + 作者 + 标签 + 校验状态
//   - custom MCP: command + args + env + installedAt
//   - custom Skill: frontmatter + installedAt
//   - custom Agent: systemPrompt hash + installedAt

import type { ResourceItem } from '../../../ipc/types';
import { Button } from '../ui/Button';
import { SourceBadge } from './SourceBadge';
import { sourceLabel } from '../../../lib/resource-helpers';  // renderer 端 helper 镜像

interface Props {
  item: ResourceItem;
  onClose: () => void;
  onDelete?: (id: string) => void;
  onInstall?: (id: string) => void;
}

export function ResourceDetail({ item, onClose, onDelete, onInstall }: Props) {
  return (
    <div className="w-80 border-l border-border-subtle bg-bg-secondary flex flex-col overflow-hidden">
      <div className="px-4 py-3 border-b border-border-subtle flex items-center justify-between">
        <h3 className="text-sm font-semibold flex items-center gap-2">
          <span className="text-xl">{item.iconEmoji || '📦'}</span>
          <span>{item.name}</span>
        </h3>
        <Button variant="ghost" size="sm" onClick={onClose}>×</Button>
      </div>

      <div className="flex-1 overflow-y-auto px-4 py-3 flex flex-col gap-3 text-sm">
        <div className="flex gap-1 flex-wrap">
          <SourceBadge source={item.source} />
          <span className="text-xs text-neutral-500">
            {sourceLabel(item.source)} · {item.type} {item.version && `· v${item.version}`}
          </span>
        </div>

        <div>
          <div className="text-xs text-neutral-500 mb-1">描述</div>
          <div className="text-neutral-200">{item.description}</div>
        </div>

        {/* builtin/marketplace 共用 catalog 元数据 */}
        {(item.source === 'builtin' || item.source === 'marketplace') && item.marketplace && (
          <>
            <div>
              <div className="text-xs text-neutral-500 mb-1">作者</div>
              <div className="text-neutral-200">{item.marketplace.author}</div>
            </div>
            <div>
              <div className="text-xs text-neutral-500 mb-1">校验状态</div>
              <div className="text-neutral-200">{item.marketplace.verificationStatus}</div>
            </div>
            <div>
              <div className="text-xs text-neutral-500 mb-1">README</div>
              <div className="text-neutral-300 text-xs whitespace-pre-wrap max-h-60 overflow-y-auto">
                {item.marketplace.readme}
              </div>
            </div>
          </>
        )}

        {/* custom MCP */}
        {item.source === 'custom' && item.type === 'mcp' && item.custom?.mcpConfig && (
          <>
            <div>
              <div className="text-xs text-neutral-500 mb-1">命令</div>
              <code className="text-xs text-neutral-200">{item.custom.mcpConfig.command}</code>
            </div>
            <div>
              <div className="text-xs text-neutral-500 mb-1">参数</div>
              <code className="text-xs text-neutral-200">{item.custom.mcpConfig.args.join(' ')}</code>
            </div>
            {item.custom.mcpConfig.env && Object.keys(item.custom.mcpConfig.env).length > 0 && (
              <div>
                <div className="text-xs text-neutral-500 mb-1">环境变量</div>
                <code className="text-xs text-neutral-200">
                  {Object.entries(item.custom.mcpConfig.env)
                    .map(([k]) => `${k}=***`)
                    .join('\n')}
                </code>
              </div>
            )}
          </>
        )}

        {/* custom Skill */}
        {item.source === 'custom' && item.type === 'skill' && (
          <div>
            <div className="text-xs text-neutral-500 mb-1">来源</div>
            <div className="text-neutral-200">用户上传（zip 包）</div>
          </div>
        )}

        {/* custom Agent */}
        {item.source === 'custom' && item.type === 'agent' && item.custom?.agentSystemPromptHash && (
          <div>
            <div className="text-xs text-neutral-500 mb-1">System Prompt Hash</div>
            <code className="text-xs text-neutral-200">{item.custom.agentSystemPromptHash}</code>
          </div>
        )}

        {item.custom?.installedAt && (
          <div>
            <div className="text-xs text-neutral-500 mb-1">{item.source === 'custom' ? '上传时间' : '安装时间'}</div>
            <div className="text-neutral-200">{new Date(item.custom.installedAt).toLocaleString('zh-CN')}</div>
          </div>
        )}
      </div>

      <div className="px-4 py-3 border-t border-border-subtle flex gap-2">
        {item.installable && !item.installed && onInstall && (
          <Button size="sm" onClick={() => onInstall(item.id)}>安装</Button>
        )}
        {item.installed && item.removable && onDelete && (
          <Button size="sm" variant="danger" onClick={() => onDelete(item.id)}>🗑 删除</Button>
        )}
        {item.installed && !item.removable && (
          <span className="text-xs text-neutral-500 self-center">✓ 已安装</span>
        )}
      </div>
    </div>
  );
}
```

**注意**：`sourceLabel` 在 renderer 端需要镜像一份（或 export from types.d.ts）。在 `renderer/src/lib/resource-helpers.ts` 创建：

```typescript
// renderer/src/lib/resource-helpers.ts
import type { ResourceSource } from '../ipc/types';

const SOURCE_LABELS: Record<ResourceSource, string> = {
  builtin: '系统预置',
  custom: '我的上传',
  marketplace: '网络资源',
  p2p: 'P2P 共享',
};

export function sourceLabel(source: ResourceSource): string {
  return SOURCE_LABELS[source];
}
```

- [ ] **Step 4：跑测试 + commit**

```bash
git add renderer/src/components/resource-library/ResourceDetail.tsx renderer/src/components/resource-library/ResourceDetail.test.tsx renderer/src/lib/resource-helpers.ts
git commit -m "feat(resource-library): ResourceDetail 按 source 分支详情面板"
```

---

### Task 8：AddResourceMenu 组件

**Files:**
- Create: `renderer/src/components/resource-library/AddResourceMenu.tsx`
- Create: `renderer/src/components/resource-library/AddResourceMenu.test.tsx`

**Interfaces:**
- Produces: `<AddResourceMenu onCreateAgent={...} onRegisterMcp={...} onUploadSkill={...} />`

- [ ] **Step 1：写失败测试**

```typescript
// 4 个测试：
// - 默认折叠，点击 + 添加资源 展开
// - 展开后显示 3 个菜单项
// - 点击「创建自定义 Agent」→ 触发 onCreateAgent + 关闭菜单
// - 点击外部关闭菜单
```

- [ ] **Step 2：跑测试确认失败**

- [ ] **Step 3：实现 AddResourceMenu.tsx**

```typescript
// renderer/src/components/resource-library/AddResourceMenu.tsx
import { useState, useRef, useEffect } from 'react';
import { Button } from '../ui/Button';

interface Props {
  onCreateAgent: () => void;
  onRegisterMcp: () => void;
  onUploadSkill: () => void;
}

export function AddResourceMenu({ onCreateAgent, onRegisterMcp, onUploadSkill }: Props) {
  const [open, setOpen] = useState(false);
  const ref = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (!open) return;
    const handler = (e: MouseEvent) => {
      if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false);
    };
    document.addEventListener('mousedown', handler);
    return () => document.removeEventListener('mousedown', handler);
  }, [open]);

  return (
    <div className="relative" ref={ref}>
      <Button size="sm" variant="ghost" onClick={() => setOpen(!open)}>
        + 添加资源 ▼
      </Button>
      {open && (
        <div className="absolute right-0 mt-1 w-56 bg-bg-secondary border border-border-subtle rounded-md shadow-lg z-20 py-1">
          <button
            type="button"
            className="block w-full text-left px-3 py-2 text-sm text-neutral-200 hover:bg-bg-tertiary"
            onClick={() => { setOpen(false); onCreateAgent(); }}
          >
            🤖 创建自定义 Agent...
          </button>
          <button
            type="button"
            className="block w-full text-left px-3 py-2 text-sm text-neutral-200 hover:bg-bg-tertiary"
            onClick={() => { setOpen(false); onRegisterMcp(); }}
          >
            🔌 添加 MCP Server...
          </button>
          <button
            type="button"
            className="block w-full text-left px-3 py-2 text-sm text-neutral-200 hover:bg-bg-tertiary"
            onClick={() => { setOpen(false); onUploadSkill(); }}
          >
            📦 上传 Skill 包...
          </button>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 4：跑测试 + commit**

```bash
git add renderer/src/components/resource-library/AddResourceMenu.tsx renderer/src/components/resource-library/AddResourceMenu.test.tsx
git commit -m "feat(resource-library): AddResourceMenu 下拉菜单（三类 add 入口）"
```

---

### Task 9：resource.store + ResourceLibraryView

**Files:**
- Create: `renderer/src/stores/resource.store.ts`
- Create: `renderer/src/components/resource-library/ResourceLibraryView.tsx`
- Create: `renderer/src/components/resource-library/ResourceLibraryView.test.tsx`

**Interfaces:**
- Consumes: `ipc.resource.list` / `ipc.resource.delete` / `ipc.resource.install`、RegisterMcpDialog / UploadSkillDialog / DefinitionEditor、ResourceCard / ResourceDetail / AddResourceMenu
- Produces: `<ResourceLibraryView />` 主视图

- [ ] **Step 1：写失败测试**

```typescript
// 8 个测试覆盖：
// - 渲染默认双层 tab（type + source）
// - 主网格按 type × source 过滤
// - 搜索过滤
// - 点击卡片选中 + 右侧详情面板
// - 点删除 → ipc.resource.delete + 列表刷新
// - 点 + 添加资源 → 弹下拉菜单
// - 弹窗 onSuccess → 列表刷新
// - 详情面板 × 关闭按钮
```

- [ ] **Step 2：跑测试确认失败**

- [ ] **Step 3：实现 resource.store.ts**

```typescript
// renderer/src/stores/resource.store.ts
import { create } from 'zustand';
import { ipc } from '../ipc/client';
import type { ResourceItem, ResourceFilter } from '../ipc/types';

interface ResourceStore {
  items: ResourceItem[];
  loading: boolean;
  error: string | null;
  typeFilter: ResourceFilter['type'] | 'all';
  sourceFilter: ResourceFilter['source'] | 'all';
  query: string;

  load: () => Promise<void>;
  setTypeFilter: (f: ResourceFilter['type'] | 'all') => void;
  setSourceFilter: (f: ResourceFilter['source'] | 'all') => void;
  setQuery: (q: string) => void;
  deleteResource: (id: string) => Promise<void>;
  installResource: (id: string) => Promise<void>;
}

export const useResourceStore = create<ResourceStore>((set, get) => ({
  items: [],
  loading: false,
  error: null,
  typeFilter: 'all',
  sourceFilter: 'all',
  query: '',

  load: async () => {
    set({ loading: true, error: null });
    try {
      const filter: ResourceFilter = {};
      const { typeFilter, sourceFilter } = get();
      if (typeFilter !== 'all') filter.type = typeFilter;
      if (sourceFilter !== 'all') filter.source = sourceFilter;
      const items = await ipc.resource.list(filter);
      set({ items, loading: false });
    } catch (err) {
      set({ error: (err as Error).message, loading: false });
    }
  },

  setTypeFilter: (f) => { set({ typeFilter: f }); void get().load(); },
  setSourceFilter: (f) => { set({ sourceFilter: f }); void get().load(); },
  setQuery: (q) => set({ query: q }),

  deleteResource: async (id) => {
    await ipc.resource.delete(id);
    await get().load();
  },

  installResource: async (id) => {
    await ipc.resource.install(id);
    await get().load();
  },
}));
```

- [ ] **Step 4：实现 ResourceLibraryView.tsx**

```typescript
// renderer/src/components/resource-library/ResourceLibraryView.tsx
import { useEffect, useState } from 'react';
import { useResourceStore } from '../../stores/resource.store';
import { Input } from '../ui/Input';
import { cn } from '../../lib/cn';
import { ResourceCard } from './ResourceCard';
import { ResourceDetail } from './ResourceDetail';
import { AddResourceMenu } from './AddResourceMenu';
import { RegisterMcpDialog } from '../agent/RegisterMcpDialog';
import { UploadSkillDialog } from '../agent/UploadSkillDialog';
import { DefinitionEditor } from '../agent/DefinitionEditor';
import type { ResourceItem, ResourceFilter } from '../../ipc/types';

const TYPE_TABS: Array<{ key: ResourceFilter['type'] | 'all'; label: string }> = [
  { key: 'all', label: '全部' },
  { key: 'agent', label: 'Agent' },
  { key: 'mcp', label: 'MCP' },
  { key: 'skill', label: 'Skill' },
];

const SOURCE_TABS: Array<{ key: ResourceFilter['source'] | 'all'; label: string }> = [
  { key: 'all', label: '全部' },
  { key: 'builtin', label: '系统预置' },
  { key: 'custom', label: '我的上传' },
  { key: 'marketplace', label: '网络资源' },
  // v2: { key: 'p2p', label: 'P2P 共享' },
];

export function ResourceLibraryView() {
  const {
    items, loading, error, typeFilter, sourceFilter, query,
    load, setTypeFilter, setSourceFilter, setQuery,
    deleteResource, installResource,
  } = useResourceStore();
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const [registerMcpOpen, setRegisterMcpOpen] = useState(false);
  const [uploadSkillOpen, setUploadSkillOpen] = useState(false);
  const [createAgentOpen, setCreateAgentOpen] = useState(false);

  useEffect(() => { void load(); }, [load, typeFilter, sourceFilter]);

  const selected: ResourceItem | undefined = selectedId
    ? items.find((i) => i.id === selectedId)
    : undefined;

  // 搜索过滤（前端 in-memory）
  const filteredItems = query.trim()
    ? items.filter((i) =>
        i.name.toLowerCase().includes(query.toLowerCase()) ||
        i.description.toLowerCase().includes(query.toLowerCase()) ||
        i.slug.toLowerCase().includes(query.toLowerCase()),
      )
    : items;

  return (
    <div className="flex-1 flex overflow-hidden">
      <div className="flex-1 flex flex-col overflow-hidden">
        <div className="px-4 py-3 border-b border-border-subtle flex flex-col gap-2">
          <div className="flex items-center justify-between gap-3">
            <h2 className="text-sm font-semibold text-neutral-200">📚 资源库</h2>
            <div className="flex items-center gap-2">
              <AddResourceMenu
                onCreateAgent={() => setCreateAgentOpen(true)}
                onRegisterMcp={() => setRegisterMcpOpen(true)}
                onUploadSkill={() => setUploadSkillOpen(true)}
              />
              <div className="w-64">
                <Input
                  placeholder="搜索 agent / mcp / skill…"
                  value={query}
                  onChange={(e) => setQuery(e.target.value)}
                />
              </div>
            </div>
          </div>
          <div className="flex flex-col gap-1">
            <div className="flex gap-1">
              <span className="text-xs text-neutral-500 mr-2 self-center">类型</span>
              {TYPE_TABS.map((tab) => (
                <button
                  key={tab.key}
                  className={cn(
                    'text-xs px-2.5 py-1 rounded-md transition-colors',
                    typeFilter === tab.key
                      ? 'bg-accent-blue text-white'
                      : 'text-neutral-400 hover:bg-bg-tertiary',
                  )}
                  onClick={() => setTypeFilter(tab.key)}
                >
                  {tab.label}
                </button>
              ))}
            </div>
            <div className="flex gap-1">
              <span className="text-xs text-neutral-500 mr-2 self-center">来源</span>
              {SOURCE_TABS.map((tab) => (
                <button
                  key={tab.key}
                  className={cn(
                    'text-xs px-2.5 py-1 rounded-md transition-colors',
                    sourceFilter === tab.key
                      ? 'bg-purple-500 text-white'
                      : 'text-neutral-400 hover:bg-bg-tertiary',
                  )}
                  onClick={() => setSourceFilter(tab.key)}
                >
                  {tab.label}
                </button>
              ))}
            </div>
          </div>
        </div>

        <div className="flex-1 overflow-auto p-4">
          {error ? (
            <div className="text-center text-status-error text-sm py-8">加载失败：{error}</div>
          ) : loading && items.length === 0 ? (
            <div className="text-center text-neutral-500 text-sm py-8">加载中…</div>
          ) : filteredItems.length === 0 ? (
            <div className="text-center text-neutral-500 text-sm py-8">
              <div className="text-3xl mb-2">📚</div>
              <p>没有匹配的资源。{sourceFilter === 'custom' && '点击右上角「+ 添加资源」上传'}</p>
            </div>
          ) : (
            <div className="grid grid-cols-[repeat(auto-fill,minmax(220px,1fr))] gap-3">
              {filteredItems.map((item) => (
                <ResourceCard
                  key={item.id}
                  item={item}
                  selected={selectedId === item.id}
                  onSelect={setSelectedId}
                  onInstall={installResource}
                  onDelete={deleteResource}
                />
              ))}
            </div>
          )}
        </div>
      </div>

      {selected && (
        <ResourceDetail
          item={selected}
          onClose={() => setSelectedId(null)}
          onInstall={installResource}
          onDelete={deleteResource}
        />
      )}

      {registerMcpOpen && (
        <RegisterMcpDialog
          onClose={() => setRegisterMcpOpen(false)}
          onSuccess={() => { setRegisterMcpOpen(false); void load(); }}
        />
      )}
      {uploadSkillOpen && (
        <UploadSkillDialog
          onClose={() => setUploadSkillOpen(false)}
          onSuccess={() => { setUploadSkillOpen(false); void load(); }}
        />
      )}
      {createAgentOpen && (
        <DefinitionEditor
          mode="create"
          onClose={() => setCreateAgentOpen(false)}
        />
      )}
    </div>
  );
}
```

- [ ] **Step 5：跑测试 + commit**

```bash
git add renderer/src/stores/resource.store.ts renderer/src/components/resource-library/ResourceLibraryView.tsx renderer/src/components/resource-library/ResourceLibraryView.test.tsx
git commit -m "feat(resource-library): resource.store + ResourceLibraryView 主视图"
```

---

### Task 10：路由切换（替换 MarketplaceView）

**Files:**
- Modify: `renderer/src/App.tsx` 或 sidebar 路由文件（找 MarketplaceView 引用点）
- Modify: 文案"商场" → "资源库"

- [ ] **Step 1：grep 找 MarketplaceView 引用**

```bash
cd renderer && grep -rn "MarketplaceView" src/
```

- [ ] **Step 2：替换为 ResourceLibraryView**

把所有 `import { MarketplaceView }` 替换为 `import { ResourceLibraryView } from './components/resource-library/ResourceLibraryView'`，使用处 `<MarketplaceView />` → `<ResourceLibraryView />`。

侧栏文案"商场" / "Marketplace" 改为"资源库" / "Library"。

- [ ] **Step 3：跑 typecheck + 全套测试**

```bash
npx pnpm@9.0.0 typecheck
cd renderer && npx pnpm@9.0.0 vitest run
```
Expected: typecheck clean，测试全过

- [ ] **Step 4：commit**

```bash
git add renderer/src/App.tsx
git commit -m "feat(resource-library): 路由切换 MarketplaceView → ResourceLibraryView"
```

---

## Phase 3：清理 + 收尾

### Task 11：删除 marketplace/* renderer 文件

**Files:**
- Delete: `renderer/src/components/marketplace/MarketplaceView.tsx`
- Delete: `renderer/src/components/marketplace/ItemCard.tsx`
- Delete: `renderer/src/components/marketplace/ItemDetail.tsx`
- Delete: `renderer/src/stores/marketplace.store.ts`
- Delete: 相关测试文件

- [ ] **Step 1：grep 确认无引用**

```bash
cd renderer && grep -rn "from.*marketplace/MarketplaceView\|from.*marketplace/ItemCard\|from.*marketplace/ItemDetail\|marketplace.store"
```
应该 0 匹配（Task 10 已替换）

- [ ] **Step 2：删除文件**

```bash
rm renderer/src/components/marketplace/MarketplaceView.tsx
rm renderer/src/components/marketplace/ItemCard.tsx
rm renderer/src/components/marketplace/ItemDetail.tsx
rm renderer/src/stores/marketplace.store.ts
# 相关测试文件
rm renderer/src/components/marketplace/MarketplaceView.test.tsx  # 如存在
# 如果 marketplace/ 目录空，rmdir
```

- [ ] **Step 3：typecheck + 测试**

- [ ] **Step 4：commit**

```bash
git add -A renderer/src/components/marketplace/ renderer/src/stores/marketplace.store.ts
git commit -m "chore(cleanup): 删除 v1.6 marketplace/* renderer 文件（已被 resource-library/ 取代）"
```

---

### Task 12：删除废弃 IPC + preload 暴露

**Files:**
- Modify: `electron/src/main/mcp/ipc.handlers.ts`（删 `mcp:listRegistered` / `mcp:deleteRegistered` handler）
- Modify: `electron/src/main/skill/ipc.handlers.ts`（删 `skill:listInstalled` / `skill:deleteCustom` handler）
- Modify: `electron/src/main/marketplace/ipc.handlers.ts`（删 `marketplace:loadCatalog` / `marketplace:install` / `marketplace:uninstall`）
- Modify: `electron/src/preload/index.ts`（删对应绑定 + ApiSurface 类型）
- Modify: `renderer/src/ipc/types.d.ts`（删对应类型 + ApiSurface 字段）

**注意**：底层函数 `listRegistered` / `deleteRegistered` / `listInstalled` / `deleteCustomSkill` / `installPackage` / `uninstallPackage` / `fetchCatalog` **保留**——resource/ 内部仍要复用。

- [ ] **Step 1：grep 找所有调用站点**

```bash
cd renderer && grep -rn "ipc.mcp.listRegistered\|ipc.mcp.deleteRegistered\|ipc.skill.listInstalled\|ipc.skill.deleteCustom\|ipc.marketplace"
```

预期：Task 13 之后只剩 CapabilityTabs / AssignmentCapabilitiesDialog / AddToWorkspaceDialog 的调用。先做 Task 13，再做 Task 12。

**调整执行顺序**：实际执行时 Task 12 和 Task 13 应该一起做（先迁移调用站点，再删 IPC），避免中间态破坏。

- [ ] **Step 2：在 mcp/ipc.handlers.ts 删除 2 个 handler**

```typescript
// 删除：
ipcMain.handle('mcp:listRegistered', ...);
ipcMain.handle('mcp:deleteRegistered', ...);
```

- [ ] **Step 3：在 skill/ipc.handlers.ts 删除 2 个 handler**

```typescript
// 删除：
ipcMain.handle('skill:listInstalled', ...);
ipcMain.handle('skill:deleteCustom', ...);
```

- [ ] **Step 4：在 marketplace/ipc.handlers.ts 删除 3 个 handler**

```typescript
// 删除：marketplace:loadCatalog / marketplace:install / marketplace:uninstall
```

如 marketplace/ipc.handlers.ts 全空则整个文件删除 + ipc/index.ts 取消注册。

- [ ] **Step 5：preload + types.d.ts 清理**

删除 preload 中 mcp.listRegistered / mcp.deleteRegistered / skill.listInstalled / skill.deleteCustom / marketplace 命名空间。types.d.ts 对应 ApiSurface 字段也删。

- [ ] **Step 6：typecheck + 测试**

- [ ] **Step 7：commit**

```bash
git add -A electron/src/main/mcp/ipc.handlers.ts electron/src/main/skill/ipc.handlers.ts electron/src/main/marketplace/ipc.handlers.ts electron/src/preload/index.ts renderer/src/ipc/types.d.ts
git commit -m "chore(cleanup): 删除废弃 IPC（mcp:listRegistered 等）+ preload 暴露"
```

---

### Task 13：调用站点迁移（CapabilityTabs / AddToWorkspaceDialog / AssignmentCapabilitiesDialog）

**Files:**
- Modify: `renderer/src/components/agent/CapabilityTabs.tsx`
- Modify: `renderer/src/components/agent/AddToWorkspaceDialog.tsx`
- Modify: `renderer/src/components/agent/AssignmentCapabilitiesDialog.tsx`

**Interfaces:**
- 把 `ipc.mcp.listRegistered()` 改为 `ipc.resource.list({ type: 'mcp' })`
- 把 `ipc.skill.listInstalled()` 改为 `ipc.resource.list({ type: 'skill' })`
- 把 `mcp.name` / `mcp.command` 等字段访问改为 `mcp.custom?.mcpConfig?.command` 等（ResourceItem 结构）

- [ ] **Step 1：CapabilityTabs 改用 resource:list**

原 `useEffect(() => { void ipc.mcp.listRegistered().then(setMcps); }, [])` 改为：

```typescript
useEffect(() => {
  void ipc.resource.list({ type: 'mcp' }).then((items) => {
    setMcps(items.filter((i) => i.installed));  // 只显示已安装的
  });
}, []);
```

类型 `RegisteredMcp[]` → `ResourceItem[]`。字段访问从 `mcp.name` → `mcp.slug`（ResourceItem.slug 等价），`mcp.source` 直接读 `mcp.source`。

- [ ] **Step 2：Skill list 同步改**

```typescript
useEffect(() => {
  void ipc.resource.list({ type: 'skill' }).then((items) => {
    setSkills(items.filter((i) => i.installed));
  });
}, []);
```

- [ ] **Step 3：AddToWorkspaceDialog / AssignmentCapabilitiesDialog 同步**

凡是调 `ipc.mcp.listRegistered` / `ipc.skill.listInstalled` 处都按上式改。

- [ ] **Step 4：跑 typecheck**

Expected: typecheck 双 clean（如果 Task 12 未删 IPC，旧调用仍能 work；建议 Task 12/13 一起做）

- [ ] **Step 5：commit**

```bash
git add renderer/src/components/agent/CapabilityTabs.tsx renderer/src/components/agent/AddToWorkspaceDialog.tsx renderer/src/components/agent/AssignmentCapabilitiesDialog.tsx
git commit -m "refactor(agent): CapabilityTabs 等组件改用 ipc.resource.list（替代 mcp/skill:list IPC）"
```

---

### Task 14：README + CHANGELOG + spec 更新

**Files:**
- Modify: `README.md`（v1.6 "Marketplace" → v1.7 "资源库"段落 + 状态行更新）
- Modify: `CHANGELOG.md`（加 v1.7.0 条目）
- Modify: `docs/specs/2026-08-11-v1.7-resource-library-design.md`（状态从"待 review"→"已实施 v1.7.0"）

- [ ] **Step 1：README 更新**

把现有"Marketplace 模块"段落改为"资源库模块"：

```markdown
### 资源库（v1.7 重构）
- 三类 source 统一管理：系统预置 / 我的上传 / 网络资源（v2 加 P2P 共享）
- 主网格双层 tab（类型 × 来源）+ 搜索 + 统一 ResourceCard 卡片
- 「+ 添加资源」下拉菜单：创建 Agent / 添加 MCP / 上传 Skill
- 取代 v1.6 Marketplace + 底部"自定义资源"折叠区
```

路线图加 v1.7 段落。

- [ ] **Step 2：CHANGELOG.md 加 v1.7.0**

```markdown
## [1.7.0] — 2026-08-11

### 重构（Breaking Change）
- **资源库取代 Marketplace**：UI 重命名 + 数据模型统一（ResourceItem 取代 MarketplaceItem/InstalledSkill/RegisteredMcp 三结构）
- **取消底部"自定义资源"折叠区**：custom 资源直接出现在主网格（按 source 过滤）
- **IPC 统一**：resource:list / resource:getDetail / resource:install / resource:delete 四通道，取代 mcp:listRegistered / skill:listInstalled / marketplace:* 等多套通道

### 新增
- 双层 tab（类型 + 来源）+ 单按钮「+ 添加资源」下拉菜单
- SourceBadge 组件（4 source 颜色 + 文案）
- ResourceDetail 按 source 分支详情面板

### 删除
- renderer/src/components/marketplace/ 目录（MarketplaceView / ItemCard / ItemDetail）
- renderer/src/stores/marketplace.store.ts
- IPC: mcp:listRegistered / mcp:deleteRegistered / skill:listInstalled / skill:deleteCustom / marketplace:*

### 不动
- DB schema（Migration v0-v16 完全兼容）
- 底层函数（listRegistered / listInstalled / installPackage / uninstallPackage 等保留作 resource/ 内部复用）
- 现有 add 弹窗（RegisterMcpDialog / UploadSkillDialog / DefinitionEditor 保留）
```

- [ ] **Step 3：spec 状态更新**

```markdown
**状态**：已实施（v1.7.0）
```

- [ ] **Step 4：commit**

```bash
git add README.md CHANGELOG.md
git add -f docs/specs/2026-08-11-v1.7-resource-library-design.md
git commit -m "docs: v1.7.0 发布说明 + CHANGELOG + spec 标注已实施"
```

---

### Task 15：端到端 QA + push

- [ ] **Step 1：跑全套测试 + typecheck**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace && npx pnpm@9.0.0 typecheck
cd electron && npx pnpm@9.0.0 vitest run
cd ../renderer && npx pnpm@9.0.0 vitest run
```
Expected: typecheck 双 clean，electron + renderer 全套测试通过（估算 electron ~560+ / renderer ~350+）

- [ ] **Step 2：手动 QA checklist（用户在 macOS 端）**

1. 上传 xlsx.zip → 主网格立即出现 `📦 xlsx` 卡片（source 徽章"我的上传"），不用展开折叠区
2. 注册自定义 MCP（github）→ 主网格出现 `🔌 github [我的上传]` 卡片
3. 创建 custom agent → 主网格出现该 agent 卡片
4. 切换 source tab 到"系统预置" → 只看到 5 个 catalog 内联项
5. 切换到"我的上传" → 只看到 custom 项（mcp+skill+agent 三类）
6. 点 custom 卡片右侧 🗑 → 删除成功，列表刷新
7. 点 builtin 卡片——没有删除按钮（不可移除）
8. v1.6 升级路径：现有 marketplace 安装的资源仍能识别 + 卸载

- [ ] **Step 3：commit + push**

```bash
cd /workspace
git push https://x-access-token:GH_TOKEN_PLACEHOLDER@github.com/bearangel/momo-studio.git main
```

---

## 实施顺序与依赖关系

```
Phase 1 后端基础（5 task）
  T1 types → T2 builtin+marketplace → T3 custom → T4 library → T5 IPC

Phase 2 UI 重构（5 task，依赖 Phase 1）
  T6 SourceBadge+ResourceCard ─┐
  T7 ResourceDetail ───────────┼─→ T9 store+View → T10 路由切换
  T8 AddResourceMenu ──────────┘

Phase 3 清理收尾（5 task）
  T11 删 marketplace/* 文件（T10 后）
  T12 删废弃 IPC + T13 调用站点迁移（一起做，避免中间态）
  T14 文档
  T15 QA + push
```

**关键路径**：T1 → T2 → T3 → T4 → T5 → T6-T8 并行 → T9 → T10 → T11 → T12+T13 → T14 → T15

**可并行**：
- T2/T3 独立（不同源）
- T6/T7/T8 独立（不同 UI 组件）

## Self-Review

### Spec coverage
- spec Section 5.2 ResourceItem 数据模型 → T1 ✓
- spec Section 5.3 ID 命名 → T1 ✓
- spec Section 6 UI 设计（双层 tab / ResourceCard / ResourceDetail / AddResourceMenu）→ T6-T9 ✓
- spec Section 7 后端重构（types/builtin/marketplace/custom/library/ipc.handlers）→ T1-T5 ✓
- spec Section 7.5 IPC 通道（新增 4 个 + 删除 7 个）→ T5 + T12 ✓
- spec Section 7.6 CapabilityTabs 改用 resource:list → T13 ✓
- spec Section 8 测试覆盖 → 各 task 内 TDD ✓
- spec Section 9 兼容性（无 Migration + 直接删除 deprecated）→ T11 + T12 ✓
- spec Section 11 文件清单 → 各 task Files 块对齐 ✓

### Placeholder 扫描
- T7 ResourceDetail 测试用例描述（"7 case"）未展开——执行时按描述编写，每个 case mock 不同字段
- T8 AddResourceMenu 测试用例描述（"4 case"）同上
- T9 ResourceLibraryView 测试用例描述（"8 case"）同上
- 这些是允许的（具体 mock 数据执行时按场景构造）

### 类型一致性
- `ResourceItem` 字段在 T1 定义，T2/T3/T6/T7/T9 引用，字段名完全一致
- `ResourceFilter` / `ResourceSource` / `ResourceType` 贯穿
- `buildResourceId` / `parseResourceId` / `sourceLabel` 函数名一致
- `listBuiltinResources(catalog)` / `listMarketplaceResources(catalog)` 签名一致（参数 catalog）

### 无 spec 遗漏
所有 spec sections 都有对应 task。
