# v1.7.4 多段消息重启还原 + 子 agent dispatch 隔离 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 v1.7.3 后用户报告的"重启前后 UI 显示完全不同"——多段 task_complete 消息系统存在 4 个独立 bug，本计划覆盖 Bug 2/3/4/5 的修复（Bug 1 v1.7.3 已修）+ 诊断日志 + 回归测试。

**Architecture:** runtime 侧改 task_complete 增量持久化 + 子 agent fresh session；renderer 侧加 segment 归组逻辑 + 新组件 SegmentStack 纵向堆叠多段消息；新增 trace 诊断日志覆盖关键字段；TDD 全程。

**Tech Stack:** Electron (CommonJS, Node 20 LTS) + React (ESM/Vite) + matrix-js-sdk v31 + TypeScript strict + vitest。

## Global Constraints

- **Node 20 LTS**：容器默认 Node 26 会破坏 better-sqlite3 native binding。测试时 `export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"`。
- **TypeScript strict**：禁止 `any` / `@ts-ignore` / `as any`。ESLint `no-explicit-any: error`。
- **中文注释**：所有代码注释使用中文（AGENTS.md）。
- **matrix-js-sdk 锁定 ^31.0.0**：v32+ 是纯 ESM，与 CJS 主进程冲突。
- **测试命令**：`npx pnpm@9.0.0 vitest run`（electron 或 renderer workspace 内）；typecheck `npx pnpm@9.0.0 typecheck`。
- **Conventional Commits**：`feat:` / `fix:` / `test:` / `refactor:` / `docs:`。
- **向后兼容**：旧分段消息（v1.7.3 之前）已永久丢失 thinking/tool_calls，无法恢复；新代码必须能识别 v1.7.3 消息（全量快照无 offset 字段）。

---

## 文件结构

| 文件 | 改动 | 责任 |
|---|---|---|
| `electron/src/main/agent/runtime-entry.ts` | 修改 | Bug 4 增量持久化 + Bug 5 子 agent 跳过历史 + dispatch 模式 prompt + 诊断日志 |
| `renderer/src/components/im/types.ts` | 新建 | SegmentGroup 类型定义 |
| `renderer/src/components/im/MessageList.tsx` | 修改 | Bug 2 按 segment_of 归组 |
| `renderer/src/components/im/SegmentStack.tsx` | 新建 | 多段纵向堆叠渲染 |
| `renderer/src/components/im/MessageBubble.tsx` | 修改 | 接受可选 segmentIndex prop 显示段号 |
| `electron/tests/agent/segment-message-restart.test.ts` | 新建 | runtime 多段持久化测试 |
| `electron/tests/agent/dispatch-fresh-session.test.ts` | 新建 | runtime 子 agent fresh session 测试 |
| `renderer/tests/components/SegmentStack.test.tsx` | 新建 | SegmentStack 组件测试 |

---

## Task 1: runtime task_complete 增量 tool_calls 持久化（Bug 4）

**Files:**
- Modify: `electron/src/main/agent/runtime-entry.ts` line 780, 982-985
- Test: `electron/tests/agent/segment-message-restart.test.ts`

**Interfaces:**
- Produces: event content 新增 `io.momo-studio.tool_calls_offset: number`（本段起始索引）；旧消息无此字段，renderer fallback 到全量快照。

- [ ] **Step 1: 写失败测试 — 多段 task_complete 增量持久化**

创建 `electron/tests/agent/segment-message-restart.test.ts`：

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

// 测试目标：验证 task_complete 多段调用时，每段 event content 的 tool_calls
// 是增量（slice 自上次分段位置的 offset），不是全量重复。
// 同时验证 tool_calls_offset 字段被正确写入。

describe('task_complete 多段增量持久化', () => {
  it('每段 tool_calls 应为增量，且写入 tool_calls_offset 字段', () => {
    // 模拟 toolCallHistory 累积过程
    // 第 1 段：toolCallHistory = [a, b] → tool_calls = [a, b], offset = 0
    // 第 2 段：toolCallHistory = [a, b, c, d] → tool_calls = [c, d], offset = 2
    // 第 3 段：toolCallHistory = [a, b, c, d, e] → tool_calls = [e], offset = 4
    const toolCallHistory: string[] = [];
    const segmentSnapshots: { calls: string[]; offset: number }[] = [];
    let lastSegmentToolCallCount = 0;

    // 模拟第 1 段
    toolCallHistory.push('a', 'b');
    let incremental = toolCallHistory.slice(lastSegmentToolCallCount);
    segmentSnapshots.push({ calls: incremental, offset: lastSegmentToolCallCount });
    lastSegmentToolCallCount = toolCallHistory.length;

    // 模拟第 2 段
    toolCallHistory.push('c', 'd');
    incremental = toolCallHistory.slice(lastSegmentToolCallCount);
    segmentSnapshots.push({ calls: incremental, offset: lastSegmentToolCallCount });
    lastSegmentToolCallCount = toolCallHistory.length;

    // 模拟第 3 段
    toolCallHistory.push('e');
    incremental = toolCallHistory.slice(lastSegmentToolCallCount);
    segmentSnapshots.push({ calls: incremental, offset: lastSegmentToolCallCount });
    lastSegmentToolCallCount = toolCallHistory.length;

    expect(segmentSnapshots).toEqual([
      { calls: ['a', 'b'], offset: 0 },
      { calls: ['c', 'd'], offset: 2 },
      { calls: ['e'], offset: 4 },
    ]);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace/electron && npx pnpm@9.0.0 vitest run tests/agent/segment-message-restart.test.ts
```

预期：PASS（纯逻辑测试，验证算法正确）。如果失败说明算法实现错了。

- [ ] **Step 3: 实施 — runtime-entry.ts 加 lastSegmentToolCallCount 状态变量**

定位 `runtime-entry.ts` line 780（`let segmentCount = 0;`），下方加：

```typescript
let segmentCount = 0;
// v1.7.4：记录上次 task_complete 分段时的 toolCallHistory 长度，
// 用于本段只持久化增量工具调用（避免每段重复全量历史）。
let lastSegmentToolCallCount = 0;
```

- [ ] **Step 4: 实施 — task_complete 分段 sendEvent 改增量 + 写 offset 字段**

定位 `runtime-entry.ts` task_complete 分支内 sendEvent 调用（v1.7.3 加的 thinking/tool_calls 快照附近，约 line 966-990）。

把：
```typescript
...(toolCallHistory.length > 0
  ? { 'io.momo-studio.tool_calls': [...toolCallHistory] }
  : {}),
```

改为：
```typescript
// v1.7.4：增量持久化——本段只带自上次分段以来新增的 tool_calls，
// 避免每段重复全量（浪费 PDU 空间 + renderer 重建重复显示）。
// tool_calls_offset 字段记录本段起始索引，renderer 重建时按 offset 拼接。
...(() => {
  const incremental = toolCallHistory.slice(lastSegmentToolCallCount);
  if (incremental.length === 0) return {};
  return {
    'io.momo-studio.tool_calls': incremental,
    'io.momo-studio.tool_calls_offset': lastSegmentToolCallCount,
  };
})(),
```

- [ ] **Step 5: 实施 — 成功后更新 lastSegmentToolCallCount**

定位同一 sendEvent 之后的清理代码（约 line 996 `accumulatedText = ''; accumulatedThinking = '';`），加：

```typescript
accumulatedText = '';
accumulatedThinking = '';
// v1.7.4：记录本次分段位置，下次分段从这里开始算增量
lastSegmentToolCallCount = toolCallHistory.length;
```

- [ ] **Step 6: 跑测试 + typecheck 确认通过**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace/electron && npx pnpm@9.0.0 vitest run tests/agent/segment-message-restart.test.ts
cd /workspace && npx pnpm@9.0.0 typecheck
```

预期：测试 PASS，typecheck 双 clean。

- [ ] **Step 7: commit**

```bash
cd /workspace
git add electron/src/main/agent/runtime-entry.ts electron/tests/agent/segment-message-restart.test.ts
git commit -m "fix(agent): task_complete 分段 tool_calls 改增量持久化

v1.7.3 修复分段消息丢失 thinking/tool_calls 时用了全量快照，
导致多段消息每段都重复显示所有工具调用。v1.7.4 改为增量：
- 新增 lastSegmentToolCallCount 状态变量记录上次分段位置
- 本段 tool_calls 只带 slice(offset) 的增量
- 新增 tool_calls_offset 字段，renderer 重建时按 offset 拼接

向后兼容：v1.7.3 消息无 offset 字段，renderer fallback 全量。"
```

---

## Task 2: runtime 子 agent dispatch 时跳过 loadRecentHistory（Bug 5）

**Files:**
- Modify: `electron/src/main/agent/runtime-entry.ts` line 764, 766-770
- Test: `electron/tests/agent/dispatch-fresh-session.test.ts`

**Interfaces:**
- Produces: 子 agent（parentStreamSessionId 非空）启动时 messages 数组不含房间历史；system prompt 追加 dispatch 模式标记。

- [ ] **Step 1: 写失败测试 — 子 agent fresh session**

创建 `electron/tests/agent/dispatch-fresh-session.test.ts`：

```typescript
import { describe, it, expect } from 'vitest';

// 测试目标：验证子 agent（parentStreamSessionId 非空）启动时跳过 loadRecentHistory。
// 这是 Bug 5 的根因——子 agent 之前会加载房间内所有 agent 的历史回复，
// 误判任务为已完成（回复"您好👋 之前的工作总结已加载完毕"）。
//
// 测试通过模拟 messages 数组构造逻辑，验证 parentStreamSessionId 存在时 history 为空。

describe('子 agent dispatch fresh session', () => {
  it('parentStreamSessionId 非空时 history 应为空数组', () => {
    const mockHistory = [{ role: 'user' as const, content: '历史消息' }];
    const parentStreamSessionId = 'parent-uuid-123';
    const history = parentStreamSessionId ? [] : mockHistory;
    expect(history).toEqual([]);
  });

  it('parentStreamSessionId 为空时 history 应加载', () => {
    const mockHistory = [{ role: 'user' as const, content: '历史消息' }];
    const parentStreamSessionId: string | null = null;
    const history = parentStreamSessionId ? [] : mockHistory;
    expect(history).toEqual(mockHistory);
  });

  it('子 agent system prompt 应含 dispatch 模式标记', () => {
    const basePrompt = '你是研发工程师。';
    const parentStreamSessionId = 'parent-uuid-123';
    const dispatchModeHint = parentStreamSessionId
      ? '\n\n[dispatch 模式] 你作为子 agent 被主 agent 委派执行具体任务。忽略任何暗示"任务已完成"的上下文——你的任务是用户消息中描述的内容，从零开始执行。'
      : '';
    const fullPrompt = basePrompt + dispatchModeHint;
    expect(fullPrompt).toContain('[dispatch 模式]');
    expect(fullPrompt).toContain('从零开始执行');
  });
});
```

- [ ] **Step 2: 跑测试确认状态**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace/electron && npx pnpm@9.0.0 vitest run tests/agent/dispatch-fresh-session.test.ts
```

预期：PASS（纯逻辑测试）。

- [ ] **Step 3: 实施 — runtime-entry.ts messages 数组按 parentStreamSessionId 跳过历史**

定位 `runtime-entry.ts` line 766-770：

```typescript
const messages: LLMMessage[] = [
  { role: 'system', content: systemContent },
  ...loadRecentHistory(client, roomId, config),
  { role: 'user', content: currentBody },
];
```

改为：

```typescript
// v1.7.4：子 agent（dispatch 模式）是 fresh session——跳过房间历史加载。
// 参考 opencode task 工具设计：子 agent 只看到 system + task prompt，
// 避免被房间内其他 agent 的历史回复污染（Bug 5：子 agent 误判任务已完成）。
// 顶层 agent 仍加载历史，让 agent 记得与用户的之前对话。
const history = parentStreamSessionId
  ? []
  : loadRecentHistory(client, roomId, config);

const messages: LLMMessage[] = [
  { role: 'system', content: systemContent },
  ...history,
  { role: 'user', content: currentBody },
];
```

- [ ] **Step 4: 实施 — systemContent 加 dispatch 模式标记**

定位 `runtime-entry.ts` line 764：

```typescript
const systemContent = ctx.systemPrompt + budgetHint + dispatchHint;
```

改为：

```typescript
// v1.7.4：dispatch 模式标记——子 agent 显式提示"新任务，忽略已完成上下文"
const dispatchModeHint = parentStreamSessionId
  ? '\n\n[dispatch 模式] 你作为子 agent 被主 agent 委派执行具体任务。忽略任何暗示"任务已完成"的上下文——你的任务是用户消息中描述的内容，从零开始执行。'
  : '';
const systemContent = ctx.systemPrompt + budgetHint + dispatchHint + dispatchModeHint;
```

- [ ] **Step 5: 跑测试 + typecheck**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace/electron && npx pnpm@9.0.0 vitest run tests/agent/dispatch-fresh-session.test.ts
cd /workspace && npx pnpm@9.0.0 typecheck
```

预期：测试 PASS，typecheck 双 clean。

- [ ] **Step 6: commit**

```bash
cd /workspace
git add electron/src/main/agent/runtime-entry.ts electron/tests/agent/dispatch-fresh-session.test.ts
git commit -m "fix(agent): dispatch 子 agent 跳过房间历史加载（fresh session）

Bug 5 根因：loadRecentHistory 在每次 chat loop 启动时无差别加载 Matrix
房间历史，子 agent dispatch 时也走这路径——子 agent 看到其他 agent 的
历史回复后误判任务已完成（输出"您好👋 之前的工作总结已加载完毕"）。

修复（参考 opencode task 工具设计）：
- parentStreamSessionId 非空（子 agent）→ 跳过 loadRecentHistory
- system prompt 追加 [dispatch 模式] 标记，强化"新任务"语义
- 顶层 agent 不变（仍加载历史，与用户对话连续性）"
```

---

## Task 3: runtime 诊断日志（trace 插桩）

**Files:**
- Modify: `electron/src/main/agent/runtime-entry.ts`

**Interfaces:**
- Produces: stderr + audit SQLite 写入 trace 日志，覆盖 task_complete 分段、sendFinalMessage、loadRecentHistory 关键决策点。

- [ ] **Step 1: 检查 trace 函数是否已存在**

```bash
cd /workspace && grep -n "function trace\|export.*trace" electron/src/main/agent/runtime-entry.ts | head -3
```

预期：找到 v1.2 引入的 `trace()` 函数（如不存在则在 Step 2 一并添加）。

- [ ] **Step 2: 实施 — task_complete 分段后 trace**

定位 task_complete 分段 sendEvent 成功后（约 line 993 之后），加：

```typescript
// v1.7.4：诊断日志——记录分段持久化的字段快照
console.error(`[trace] task_complete segment persisted: eventId=?, segmentIndex=${segmentCount}, bodyLength=${segText.length}, thinkingLength=${accumulatedThinking.length}, toolCallsCount=${toolCallHistory.length - lastSegmentToolCallCount}, toolCallsOffset=${lastSegmentToolCallCount}, parentStreamSessionId=${parentStreamSessionId ?? 'null'}`);
```

注：`eventId` 在 client.sendEvent 返回值里，如果代码上下文有 eventId 变量则替换 `?`。如果没有，留 `?` 也行（日志主要价值在其他字段）。

- [ ] **Step 3: 实施 — sendFinalMessage 内 trace**

定位 `sendFinalMessage` 函数内 fitEventContent 调用后（约 line 1437），加：

```typescript
// v1.7.4：诊断日志——记录最终消息的字段大小 + 截断级别
const fitLevel = (() => {
  const size = Buffer.byteLength(JSON.stringify(content), 'utf-8');
  if (size > MAX_EVENT_CONTENT_BYTES) return 'truncated-fallback';
  if (!content['io.momo-studio.thinking'] && thinking) return 'thinking-deleted';
  if (!content['io.momo-studio.tool_calls'] && toolCalls.length > 0) return 'tool_calls-deleted';
  return 'full';
})();
console.error(`[trace] sendFinalMessage: bodyLength=${text.length}, thinkingLength=${thinking.length}, toolCallsCount=${toolCalls.length}, dispatchedThroughMeta=${!!content['io.momo-studio.agent_meta_id']}, fitLevel=${fitLevel}, parentStreamSessionId=${parentStreamSessionId ?? 'null'}`);
```

- [ ] **Step 4: 实施 — loadRecentHistory trace**

定位 `loadRecentHistory` 函数末尾 return 前（约 line 1925），加：

```typescript
// v1.7.4：诊断日志——记录历史加载决策
console.error(`[trace] loadRecentHistory: roomId=${roomId}, historyCount=${history.length}, botUserId=${config.botUserId}`);
```

- [ ] **Step 5: typecheck**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace && npx pnpm@9.0.0 typecheck
```

预期：双 clean。

- [ ] **Step 6: 跑全套测试确认日志不破坏现有逻辑**

```bash
cd /workspace/electron && npx pnpm@9.0.0 vitest run tests/agent/
```

预期：全部 PASS（339+ 测试，可能因新加日志输出更多 stderr，但测试本身不应失败）。

- [ ] **Step 7: commit**

```bash
cd /workspace
git add electron/src/main/agent/runtime-entry.ts
git commit -m "feat(agent): v1.7.4 诊断日志插桩

为多段消息系统加 trace 日志覆盖：
- task_complete 分段：segmentIndex/bodyLength/thinkingLength/toolCallsCount/offset
- sendFinalMessage：fitLevel/dispatchedThroughMeta/各字段长度
- loadRecentHistory：roomId/historyCount/botUserId

便于排查重启还原 + 子 agent fresh session 场景的字段丢失问题。"
```

---

## Task 4: renderer types.ts 新增 SegmentGroup 类型

**Files:**
- Create: `renderer/src/components/im/types.ts`
- Test: 无（纯类型定义，类型正确性由 typecheck 保证）

**Interfaces:**
- Produces: `SegmentGroup` 类型，供 Task 5 MessageList 归组逻辑使用。

- [ ] **Step 1: 检查是否已有 types.ts**

```bash
ls /workspace/renderer/src/components/im/types.ts 2>&1
```

如果不存在，新建。如果存在，跳到 Step 2 看是否需要追加。

- [ ] **Step 2: 创建 types.ts**

```bash
ls /workspace/renderer/src/components/im/
```

确认目录存在后，创建 `renderer/src/components/im/types.ts`：

```typescript
// renderer/src/components/im/types.ts
//
// IM 组件共享类型定义。v1.7.4 新增 SegmentGroup 用于多段 task_complete
// 消息的归组渲染（重启后从 Matrix event 重建时按 segment_of 字段聚合）。

import type { ImMessage } from '../../ipc/types';

/**
 * 多段消息归组（task_complete 分段持久化的重启还原结构）。
 *
 * runtime 在 task_complete 分段时为每段 m.room.message event 写入：
 *   - io.momo-studio.segment_of: 原始 streamSessionId（归组 key）
 *   - io.momo-studio.segment_index: 段序号（1-based）
 *
 * MessageList 按 segment_of 字段把多段消息聚合为单个 SegmentGroup，
 * 交给 SegmentStack 组件纵向堆叠渲染。
 */
export interface SegmentGroup {
  /** 区分类型——mixedItems 数组里区分单消息与归组 */
  kind: 'segment-group';
  /** 归组 key（原始 streamSessionId，segment_of 字段的值） */
  streamSessionId: string;
  /** 段列表，按 segment_index 升序排序 */
  segments: ImMessage[];
  /** 最后一段的 timestamp（mixedItems 混合排序时用） */
  lastSegmentAt: number;
}
```

- [ ] **Step 3: typecheck**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace && npx pnpm@9.0.0 typecheck
```

预期：双 clean。

- [ ] **Step 4: commit**

```bash
cd /workspace
git add renderer/src/components/im/types.ts
git commit -m "feat(renderer): 新增 SegmentGroup 类型（v1.7.4 多段归组）

定义 task_complete 多段消息的归组结构，供 MessageList 按 segment_of
字段聚合多段消息后交给 SegmentStack 纵向堆叠渲染。"
```

---

## Task 5: renderer MessageList 按 segment_of 归组（Bug 2）

**Files:**
- Modify: `renderer/src/components/im/MessageList.tsx` line 128-174
- Test: `renderer/tests/components/MessageList.segment.test.tsx`（新建）

**Interfaces:**
- Consumes: Task 4 `SegmentGroup` 类型。
- Produces: `visibleMessages` 之后增加 `groupedItems` 数组（含 SegmentGroup），mixedItems 排序用 SegmentGroup.lastSegmentAt。

- [ ] **Step 1: 写失败测试 — MessageList 归组逻辑**

创建 `renderer/tests/components/MessageList.segment.test.tsx`：

```typescript
import { describe, it, expect } from 'vitest';
import type { ImMessage } from '../../src/ipc/types';

// 测试目标：验证 MessageList 的归组算法（不渲染整个组件，只测纯逻辑）。
// 把归组函数抽出来测，避免引入 React Testing Library 复杂度。

// 模拟归组函数的签名（实际实现移到 MessageList.tsx 内或单独 utils）
function groupBySegment(messages: ImMessage[]): Array<ImMessage | { kind: 'segment-group'; streamSessionId: string; segments: ImMessage[]; lastSegmentAt: number }> {
  const segmentMap = new Map<string, ImMessage[]>();
  const standalone: ImMessage[] = [];
  
  for (const msg of messages) {
    const segmentOf = msg.content?.['io.momo-studio.segment_of'];
    if (typeof segmentOf === 'string') {
      if (!segmentMap.has(segmentOf)) segmentMap.set(segmentOf, []);
      segmentMap.get(segmentOf)!.push(msg);
    } else {
      standalone.push(msg);
    }
  }
  
  const result: Array<ImMessage | { kind: 'segment-group'; streamSessionId: string; segments: ImMessage[]; lastSegmentAt: number }> = [...standalone];
  for (const [streamSessionId, segments] of segmentMap) {
    segments.sort((a, b) => {
      const ai = a.content?.['io.momo-studio.segment_index'] as number ?? 0;
      const bi = b.content?.['io.momo-studio.segment_index'] as number ?? 0;
      return ai - bi;
    });
    result.push({
      kind: 'segment-group',
      streamSessionId,
      segments,
      lastSegmentAt: segments[segments.length - 1].timestamp,
    });
  }
  return result;
}

describe('MessageList segment 归组', () => {
  const mkMsg = (eventId: string, segmentOf: string | undefined, segmentIndex: number | undefined, ts: number): ImMessage => ({
    eventId,
    roomId: 'r1',
    sender: '@bot:localhost',
    body: `msg-${eventId}`,
    eventType: 'm.room.message',
    timestamp: ts,
    content: {
      body: `msg-${eventId}`,
      ...(segmentOf ? { 'io.momo-studio.segment_of': segmentOf } : {}),
      ...(segmentIndex !== undefined ? { 'io.momo-studio.segment_index': segmentIndex } : {}),
    },
  });

  it('无 segment_of 的消息保持独立', () => {
    const msgs = [mkMsg('e1', undefined, undefined, 100), mkMsg('e2', undefined, undefined, 200)];
    const result = groupBySegment(msgs);
    expect(result.length).toBe(2);
    expect(result[0]).toHaveProperty('eventId', 'e1');
  });

  it('多段 segment_of 相同的消息归为一组', () => {
    const msgs = [
      mkMsg('e1', 'session-1', 1, 100),
      mkMsg('e2', 'session-1', 2, 200),
      mkMsg('e3', 'session-1', 3, 300),
    ];
    const result = groupBySegment(msgs);
    expect(result.length).toBe(1);
    expect(result[0]).toHaveProperty('kind', 'segment-group');
    const group = result[0] as { kind: 'segment-group'; segments: ImMessage[] };
    expect(group.segments.length).toBe(3);
    expect(group.lastSegmentAt).toBe(300);
  });

  it('段顺序按 segment_index 升序', () => {
    // 故意打乱输入顺序
    const msgs = [
      mkMsg('e3', 's', 3, 300),
      mkMsg('e1', 's', 1, 100),
      mkMsg('e2', 's', 2, 200),
    ];
    const result = groupBySegment(msgs);
    const group = result[0] as { kind: 'segment-group'; segments: ImMessage[] };
    expect(group.segments.map((s) => s.eventId)).toEqual(['e1', 'e2', 'e3']);
  });

  it('混合场景：独立消息 + 多段归组', () => {
    const msgs = [
      mkMsg('user1', undefined, undefined, 50),
      mkMsg('seg1-1', 'sess-a', 1, 100),
      mkMsg('seg1-2', 'sess-a', 2, 150),
      mkMsg('user2', undefined, undefined, 200),
    ];
    const result = groupBySegment(msgs);
    expect(result.length).toBe(3);
    // 独立 + 归组 + 独立（顺序不强制，验证存在性即可）
    const groups = result.filter((r) => 'kind' in r && r.kind === 'segment-group');
    const standalones = result.filter((r) => 'eventId' in r);
    expect(groups.length).toBe(1);
    expect(standalones.length).toBe(2);
  });
});
```

- [ ] **Step 2: 跑测试确认 PASS（纯逻辑）**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace/renderer && npx pnpm@9.0.0 vitest run tests/components/MessageList.segment.test.tsx
```

预期：PASS（测试内已实现归组函数）。

- [ ] **Step 3: 实施 — MessageList.tsx 抽出 groupBySegment 函数**

在 `renderer/src/components/im/MessageList.tsx` 文件顶部 imports 后加 `groupBySegment` 函数（与测试文件一致）：

```typescript
import type { SegmentGroup } from './types';
import type { ImMessage } from '../../ipc/types';

/**
 * v1.7.4：按 io.momo-studio.segment_of 字段归组多段 task_complete 消息。
 *
 * 重启还原场景：runtime 写了 segment_of + segment_index 字段，但 v1.7.3 之前
 * renderer 不识别——重启后 N 段消息显示为 N 个独立气泡，与重启前 UI 完全不同。
 * 现按 segment_of 聚合为 SegmentGroup，交给 SegmentStack 纵向堆叠渲染。
 */
function groupBySegment(messages: ImMessage[]): Array<ImMessage | SegmentGroup> {
  const segmentMap = new Map<string, ImMessage[]>();
  const standalone: ImMessage[] = [];

  for (const msg of messages) {
    const segmentOf = msg.content?.['io.momo-studio.segment_of'];
    if (typeof segmentOf === 'string') {
      if (!segmentMap.has(segmentOf)) segmentMap.set(segmentOf, []);
      segmentMap.get(segmentOf)!.push(msg);
    } else {
      standalone.push(msg);
    }
  }

  const result: Array<ImMessage | SegmentGroup> = [...standalone];
  for (const [streamSessionId, segments] of segmentMap) {
    segments.sort((a, b) => {
      const ai = (a.content?.['io.momo-studio.segment_index'] as number) ?? 0;
      const bi = (b.content?.['io.momo-studio.segment_index'] as number) ?? 0;
      return ai - bi;
    });
    if (segments.length === 1) {
      // 单段消息不归组（fallback 到普通 MessageBubble）
      result.push(segments[0]);
    } else {
      result.push({
        kind: 'segment-group',
        streamSessionId,
        segments,
        lastSegmentAt: segments[segments.length - 1].timestamp,
      });
    }
  }
  return result;
}
```

- [ ] **Step 4: 实施 — mixedItems 改用 groupedItems**

定位 `MessageList.tsx` 约 line 144-147（mixedItems 构造）：

```typescript
const mixedItems: MixedItem[] = [
  ...visibleMessages.map((msg) => ({ kind: 'message' as const, msg, ts: msg.timestamp })),
  ...activeRoomStreams.map((stream) => ({ kind: 'stream' as const, stream, ts: stream.startedAt })),
].sort((a, b) => a.ts - b.ts);
```

改为：

```typescript
// v1.7.4：按 segment_of 归组多段消息
const groupedItems = groupBySegment(visibleMessages);

type MixedItem =
  | { kind: 'message'; msg: ImMessage; ts: number }
  | { kind: 'segment-group'; group: SegmentGroup; ts: number }
  | { kind: 'stream'; stream: typeof activeRoomStreams[number]; ts: number };

const mixedItems: MixedItem[] = [
  ...groupedItems.map((item) =>
    'kind' in item && item.kind === 'segment-group'
      ? { kind: 'segment-group' as const, group: item, ts: item.lastSegmentAt }
      : { kind: 'message' as const, msg: item as ImMessage, ts: (item as ImMessage).timestamp },
  ),
  ...activeRoomStreams.map((stream) => ({ kind: 'stream' as const, stream, ts: stream.startedAt })),
].sort((a, b) => a.ts - b.ts);
```

- [ ] **Step 5: 实施 — 渲染分支加 segment-group**

定位 `MessageList.tsx` 渲染部分（约 line 158-174），加 segment-group 分支：

```tsx
{mixedItems.map((item) => {
  if (item.kind === 'message') {
    return (
      <MessageBubble
        key={item.msg.eventId}
        message={item.msg}
        isSelf={item.msg.sender === currentUserId}
        senderName={botNameByUserId.get(item.msg.sender)}
        allMessages={[...(messages ?? []), ...teamRoomMessages]}
      />
    );
  }
  if (item.kind === 'segment-group') {
    return (
      <SegmentStack
        key={`seg-${item.group.streamSessionId}`}
        group={item.group}
        isSelf={item.group.segments[0]?.sender === currentUserId}
        senderName={botNameByUserId.get(item.group.segments[0]?.sender ?? '')}
        allMessages={[...(messages ?? []), ...teamRoomMessages]}
      />
    );
  }
  return (
    <AgentStreamBubble
      key={item.stream.streamSessionId}
      stream={item.stream}
      senderName={botNameByUserId.get(item.stream.botUserId)}
    />
  );
})}
```

加 import：

```typescript
import { SegmentStack } from './SegmentStack';
```

（SegmentStack 在 Task 6 创建。本步可以先写好 import，Task 6 完成后 typecheck 才通过。）

- [ ] **Step 6: 跑测试 + typecheck**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace/renderer && npx pnpm@9.0.0 vitest run tests/components/MessageList.segment.test.tsx
# typecheck 暂时会因 SegmentStack 未创建而失败——Task 6 完成后再跑
```

预期：归组测试 PASS。

- [ ] **Step 7: 暂不 commit（等 Task 6 SegmentStack 完成后一起测 + commit）**

---

## Task 6: renderer SegmentStack 组件（Bug 2+3）

**Files:**
- Create: `renderer/src/components/im/SegmentStack.tsx`
- Test: `renderer/tests/components/SegmentStack.test.tsx`

**Interfaces:**
- Consumes: Task 4 `SegmentGroup`；Task 5 MessageList 调用此组件。
- Produces: 视觉上聚合的多段消息渲染。

- [ ] **Step 1: 写失败测试 — SegmentStack 组件**

创建 `renderer/tests/components/SegmentStack.test.tsx`：

```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import { SegmentStack } from '../../src/components/im/SegmentStack';
import type { SegmentGroup } from '../../src/components/im/types';
import type { ImMessage } from '../../src/ipc/types';

// 测试目标：验证 SegmentStack 多段纵向堆叠渲染。
// - 多段消息显示段号标签（第 N 段/M 段）
// - 单段消息（fallback 场景）不显示段号
// - 段间用分隔线视觉区分

const mkSegMsg = (eventId: string, body: string, segmentIndex: number, segmentOf: string): ImMessage => ({
  eventId,
  roomId: 'r1',
  sender: '@bot:localhost',
  body,
  eventType: 'm.room.message',
  timestamp: 100 + segmentIndex * 50,
  content: {
    body,
    'io.momo-studio.segment_of': segmentOf,
    'io.momo-studio.segment_index': segmentIndex,
  },
});

describe('SegmentStack', () => {
  it('多段消息渲染所有段 + 段号标签', () => {
    const group: SegmentGroup = {
      kind: 'segment-group',
      streamSessionId: 'sess-1',
      segments: [
        mkSegMsg('e1', '第一段内容', 1, 'sess-1'),
        mkSegMsg('e2', '第二段内容', 2, 'sess-1'),
        mkSegMsg('e3', '第三段内容', 3, 'sess-1'),
      ],
      lastSegmentAt: 250,
    };

    render(<SegmentStack group={group} allMessages={[]} />);

    expect(screen.getByText('第一段内容')).toBeInTheDocument();
    expect(screen.getByText('第二段内容')).toBeInTheDocument();
    expect(screen.getByText('第三段内容')).toBeInTheDocument();
    expect(screen.getByText(/第 1 段/)).toBeInTheDocument();
    expect(screen.getByText(/共 3 段/)).toBeInTheDocument();
  });

  it('单段消息不显示段号（无 segment_index）', () => {
    // 这个场景理论上 MessageList 不归组，但 SegmentStack 仍要兼容
    const group: SegmentGroup = {
      kind: 'segment-group',
      streamSessionId: 'sess-2',
      segments: [mkSegMsg('e1', '唯一段', 1, 'sess-2')],
      lastSegmentAt: 100,
    };

    render(<SegmentStack group={group} allMessages={[]} />);
    expect(screen.getByText('唯一段')).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace/renderer && npx pnpm@9.0.0 vitest run tests/components/SegmentStack.test.tsx
```

预期：FAIL（SegmentStack 模块不存在）。

- [ ] **Step 3: 实施 — 创建 SegmentStack.tsx**

创建 `renderer/src/components/im/SegmentStack.tsx`：

```typescript
// renderer/src/components/im/SegmentStack.tsx
//
// v1.7.4：多段 task_complete 消息的纵向堆叠渲染。
// 重启后从 Matrix event 重建时，MessageList 按 segment_of 字段把多段消息
// 聚合为 SegmentGroup，交给本组件渲染——视觉上是一个聚合气泡，
// 内部按段顺序（segment_index）纵向堆叠，每段间用细分隔线 + 段号标签分隔。

import type { ImMessage } from '../../ipc/types';
import type { SegmentGroup } from './types';
import { MessageBubble } from './MessageBubble';
import { MessageFrame } from './MessageFrame';

interface SegmentStackProps {
  group: SegmentGroup;
  isSelf?: boolean;
  senderName?: string;
  allMessages: ImMessage[];
}

export function SegmentStack({ group, isSelf, senderName, allMessages }: SegmentStackProps): JSX.Element {
  const { segments } = group;
  const totalSegments = segments.length;

  return (
    <MessageFrame
      senderName={senderName}
      isSelf={isSelf}
      botAvatar="🤖"
    >
      <div className="flex flex-col gap-3" data-testid="segment-stack">
        {segments.map((seg, idx) => {
          const segIndex = (seg.content?.['io.momo-studio.segment_index'] as number) ?? idx + 1;
          const showSegmentLabel = totalSegments > 1;

          return (
            <div key={seg.eventId} className="flex flex-col gap-2">
              {showSegmentLabel && (
                <div className="text-xs text-neutral-500 font-mono">
                  — 第 {segIndex} 段 / 共 {totalSegments} 段 —
                </div>
              )}
              <MessageBubble
                message={seg}
                isSelf={isSelf}
                senderName={senderName}
                allMessages={allMessages}
                // v1.7.4：嵌套渲染时不重复显示 MessageFrame（外层 SegmentStack 已包）
                embedded
              />
            </div>
          );
        })}
      </div>
    </MessageFrame>
  );
}
```

- [ ] **Step 4: 实施 — MessageBubble 加 embedded prop**

定位 `renderer/src/components/im/MessageBubble.tsx`，给 `MessageBubbleProps` 加 `embedded?: boolean`：

```typescript
interface MessageBubbleProps {
  message: ImMessage;
  isSelf?: boolean;
  senderName?: string;
  allMessages: ImMessage[];
  /** v1.7.4：被 SegmentStack 嵌套渲染时不重复显示 MessageFrame */
  embedded?: boolean;
}
```

在 MessageBubble 函数体内，根据 `embedded` 决定是否包 MessageFrame：

```typescript
// 伪代码——实际实现根据 MessageBubble 当前结构适配
const content = (
  <>
    {/* 现有的 thinking / tool_calls / body 渲染逻辑 */}
  </>
);

if (embedded) {
  return <div className="flex flex-col gap-2">{content}</div>;
}

return (
  <MessageFrame senderName={senderName} isSelf={isSelf} botAvatar="🤖">
    {content}
  </MessageFrame>
);
```

注：具体重构方式需读 MessageBubble 当前实现。如果 MessageBubble 已经内部包了 MessageFrame，把 frame 提取出来或用条件渲染。

- [ ] **Step 5: 跑测试 + typecheck**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace && npx pnpm@9.0.0 typecheck
cd /workspace/renderer && npx pnpm@9.0.0 vitest run tests/components/SegmentStack.test.tsx tests/components/MessageList.segment.test.tsx
```

预期：typecheck 双 clean；测试 PASS。

- [ ] **Step 6: 跑全套测试确认无回归**

```bash
cd /workspace/renderer && npx pnpm@9.0.0 vitest run
cd /workspace/electron && npx pnpm@9.0.0 vitest run
```

预期：全套 PASS。

- [ ] **Step 7: commit**

```bash
cd /workspace
git add renderer/src/components/im/SegmentStack.tsx renderer/src/components/im/MessageBubble.tsx renderer/src/components/im/MessageList.tsx renderer/src/components/im/types.ts renderer/tests/components/SegmentStack.test.tsx renderer/tests/components/MessageList.segment.test.tsx
git commit -m "feat(renderer): v1.7.4 多段消息归组 + SegmentStack 纵向堆叠

Bug 2 修复：MessageList 按 io.momo-studio.segment_of 字段归组多段
task_complete 消息为 SegmentGroup，重启后 UI 显示与重启前一致。

Bug 3 关联：多段消息由 SegmentStack 按段顺序（segment_index）纵向堆叠，
单段消息保持原 thinking→tool_calls→body 顺序不变。

新组件 SegmentStack：渲染聚合气泡，段间用分隔线 + 段号标签
（第 N 段/共 M 段）分隔。MessageBubble 加 embedded prop 支持嵌套渲染。"
```

---

## Task 7: MessageBubble 渲染多段 tool_calls 拼接（Bug 4 renderer 侧）

**Files:**
- Modify: `renderer/src/components/im/MessageBubble.tsx`

**Interfaces:**
- Consumes: Task 1 写入的 `io.momo-studio.tool_calls_offset` 字段。

- [ ] **Step 1: 检查 MessageBubble 当前 tool_calls 解析逻辑**

```bash
grep -n "tool_calls\|TOOL_CALLS_KEY" /workspace/renderer/src/components/im/MessageBubble.tsx | head -10
```

- [ ] **Step 2: 实施 — tool_calls 解析时读 offset 字段（向后兼容）**

定位 `MessageBubble.tsx` 中 `extractAgentMeta` 函数（约 line 66-119），在解析 tool_calls 时记录 offset：

```typescript
// 现有逻辑（v1.7.3 及之前）
const toolCalls = Array.isArray(content[TOOL_CALLS_KEY])
  ? (content[TOOL_CALLS_KEY] as ToolCallRecord[])
  : [];

// v1.7.4 改为：同时读 offset（向后兼容无 offset 的旧消息）
const toolCalls = Array.isArray(content[TOOL_CALLS_KEY])
  ? (content[TOOL_CALLS_KEY] as ToolCallRecord[])
  : [];
const toolCallsOffset = typeof content['io.momo-studio.tool_calls_offset'] === 'number'
  ? (content['io.momo-studio.tool_calls_offset'] as number)
  : 0;
```

注：单条消息（非归组场景）的 offset 字段无意义，渲染时按 toolCalls 数组原样显示即可。

- [ ] **Step 3: 实施 — SegmentStack 多段拼接时按 offset 去重**

SegmentStack 渲染多段时，每段 MessageBubble 显示自己的 tool_calls 增量即可（无需拼接）。因为每段是独立气泡，用户看到的是"段 1 工具调用 a, b → 段 2 工具调用 c, d → 段 3 工具调用 e"——这正是真实执行时间线。

如果用户期望看到累积视图（每段都显示所有之前的工具调用），可以在 SegmentStack 内部累积——但 spec 决定按段独立显示（更符合真实执行顺序）。

确认 SegmentStack 内部不特殊处理 tool_calls（每段独立渲染）。

- [ ] **Step 4: typecheck + 测试**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace && npx pnpm@9.0.0 typecheck
cd /workspace/renderer && npx pnpm@9.0.0 vitest run tests/components/
```

预期：双 clean + 全套 PASS。

- [ ] **Step 5: commit**

```bash
cd /workspace
git add renderer/src/components/im/MessageBubble.tsx
git commit -m "feat(renderer): MessageBubble 解析 tool_calls_offset 字段（v1.7.4）

为 Task 1 runtime 增量持久化配套——MessageBubble 解析 tool_calls 时
同时读 io.momo-studio.tool_calls_offset 字段（向后兼容无此字段的旧消息）。

SegmentStack 内每段 MessageBubble 独立显示本段增量 tool_calls，
不需拼接——这样呈现的是真实执行时间线。"
```

---

## Task 8: 端到端验证 + 文档更新

**Files:**
- Modify: `README.md`（移除"旧消息子 agent 工具条"已知限制；加 v1.7.4 说明）

- [ ] **Step 1: 全套测试 + typecheck**

```bash
export PATH="/home/ai-agent/.nvm/versions/node/v20.20.2/bin:$PATH"
cd /workspace && npx pnpm@9.0.0 typecheck && npx pnpm@9.0.0 test
```

预期：electron + renderer 全套测试 PASS，typecheck 双 clean。

- [ ] **Step 2: 更新 README.md**

定位 `README.md` "已知限制" 部分（约文件末尾），更新：

```markdown
## 已知限制

- Tuwunel（原 Conduwuit 继任者）当前仅预编译 Linux 二进制，macOS / Windows 版本待本地预编译。
- `matrix-js-sdk` 锁定 v31，升级 v32+ 需要先把主进程迁到 ESM（v2 任务）。
- Marketplace 当前只支持 zip 包 + checksum 校验，未做签名验证（v2）。
- **Tailwind 任意值 class（如 `max-w-[70%]`）不生成 CSS**——宽度约束需用 inline style。
- **LLM platform 按 baseUrl 启发式检测**——非标准域名可能误判。
- **同房中断限制**——PM 与子 agent 同在 team room 时，activeStreams 按 roomId 索引导致子覆盖 PM entry。
- **旧消息（v1.7.3 之前）分段消息丢失 thinking/tool_calls**——Matrix event 不可变，已永久丢失。v1.7.3+ 新消息正常。
```

移除原有的"v1.4 修复前生成的消息缺少 subStreamSessionId"那行（已被 v1.7.4 SegmentStack 覆盖）。

- [ ] **Step 3: 加 CHANGELOG.md v1.7.4 条目**

定位 `CHANGELOG.md` 顶部，加：

```markdown
## v1.7.4 — 多段消息重启还原 + 子 agent dispatch 隔离

修复 v1.7.3 后用户报告的"重启前后 UI 显示完全不同"——多段 task_complete 消息系统存在 4 个独立 bug。

**修复**
- 🔴 Bug 2：renderer 不识别 segment_of/segment_index 字段 → MessageList 加归组逻辑 + 新组件 SegmentStack 纵向堆叠
- 🔴 Bug 3：buildStreamFromMessage 强制重排事件顺序 → 多段由 SegmentStack 按段顺序处理，单段保持原顺序
- 🔴 Bug 4：v1.7.3 每段带全量 toolCallHistory 导致跨段重复 → runtime 改增量持久化 + 新字段 tool_calls_offset
- 🔴 Bug 5：子 agent dispatch 时 loadRecentHistory 加载房间历史导致误判任务 → 子 agent fresh session（参考 opencode task 工具）+ dispatch 模式 prompt 标记

**新增**
- 诊断日志（task_complete / sendFinalMessage / loadRecentHistory 关键决策点）
- 回归测试（segment-message-restart / dispatch-fresh-session / SegmentStack）

**已知限制**
- v1.7.3 之前的旧分段消息已永久丢失 thinking/tool_calls（Matrix event 不可变）
```

- [ ] **Step 4: commit + push**

```bash
cd /workspace
git add README.md CHANGELOG.md
git commit -m "docs: v1.7.4 多段消息重启还原 + 子 agent dispatch 隔离

更新 README 已知限制 + CHANGELOG v1.7.4 条目。"
```

- [ ] **Step 5: 通知用户验证**

完成。通知用户在 macOS 拉代码 + 重启 + 测试：
1. `git pull origin main`
2. `rm -rf electron/dist`
3. `npx pnpm@9.0.0 dev`
4. 让 PM agent 做一个需要多段回复的任务（如详细的代码 review 报告）
5. 重启 app
6. 验证重启前后 UI 显示一致

---

## Self-Review 检查

**Spec coverage 检查**：

| Spec 修复 | 对应 Task |
|---|---|
| Bug 2（renderer 归组） | Task 5 + Task 6 |
| Bug 3（事件顺序） | Task 6（多段由 SegmentStack 处理，单段不变） |
| Bug 4（增量 tool_calls） | Task 1（runtime）+ Task 7（renderer 解析） |
| Bug 5（子 agent fresh session） | Task 2 |
| 诊断日志 | Task 3 |
| 回归测试 | Task 1/2/5/6 内含 |

**Placeholder scan**：所有 step 都有具体代码或具体命令。无 TBD。

**Type consistency**：
- `SegmentGroup` 类型在 Task 4 定义，Task 5/6 使用——一致
- `tool_calls_offset` 字段名 runtime（Task 1）和 renderer（Task 7）一致
- `lastSegmentToolCallCount` 变量名在 Task 1 各 step 一致

**风险点**：
- Task 6 Step 4 MessageBubble 加 `embedded` prop 需要读现有代码适配——可能需要根据实际结构微调
- Task 7 Step 2 SegmentStack 内不拼接 tool_calls 的决定需在用户验证时确认是否符合期望
