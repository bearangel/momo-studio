# 2.0.0 P1「会话内核」实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用「SessionService + SQLite 单一真相源 + 进程内事件分发」取代 Matrix 传输层，终结 v1/v2 双轨，为 P2-P5 清障。

**Architecture:** 三段推进——先立新（sessions 表 + SessionService + 内部事件桥，Matrix 并存不干扰）→ 切流量（IPC/renderer/workspace 流程全部切到 session 模型）→ 删旧物（matrix/*、conduit、bot 注册、v1 长存进程路径全删）。消息真相源已经是 SQLite（v2.0 A 子系统），本计划拆除的是"传输这条腿"。

**Tech Stack:** Electron 主进程（CommonJS）+ better-sqlite3 + React/Zustand renderer（ESM）。不新增任何依赖，只删依赖。

**上游 spec:** `docs/specs/2026-08-23-v2.0.0-platform-refactor-design.md` §4/§5/§11。

## Global Constraints

- Node 20（容器内先 `nvm use 20`，Node 26 会破坏 better-sqlite3 native binding）。
- pnpm 用 `npx pnpm@9.0.0`；两个 workspace：`@momo-studio/electron`、`@momo-studio/renderer`。
- TypeScript strict：禁止 `any` / `@ts-ignore` / `as any`；每任务收尾跑 `npx pnpm@9.0.0 typecheck` 双 clean。
- 所有新代码注释用中文；标识符英文。
- Conventional Commits：`feat:` / `refactor:` / `test:` / `chore:` / `fix:`。
- 数据决策 D5 = **完全重新开始**：migration 链是 append-only（v23 起），新库重放全链即可，不做旧数据兼容分支。
- 每任务 TDD：先写失败测试再实现；测试命令模板 `cd electron && npx pnpm@9.0.0 vitest run tests/<path>`。
- `messages.sender` 新语义：`'owner' | assignmentId`。全程使用该约定。

---

## 阶段一：立新（Matrix 并存，不改现网行为）

### Task 1: Migration v23——sessions 表 + 列语义重命名

**Files:**
- Modify: `electron/src/main/storage/migrations/index.ts`（数组末尾追加）
- Test: `electron/tests/migrations/023-sessions.test.ts`

**Interfaces:**
- Produces: 表 `sessions(id, workspace_id, title, kind, settings_json, created_at, updated_at, last_message_at)`、`session_members(session_id, assignment_id, added_at)`；列重命名 `messages.room_id→session_id`、`messages.matrix_event_id 删除`、`tasks.execution_room_id→execution_session_id`、`tasks.source_room_id→source_session_id`、`agent_assignments.bot_matrix_user_id→agent_user_id`、`workspaces.team_room_id→team_session_id`、`workspaces.matrix_space_id 删除`、`room_settings 表删除`。

- [ ] **Step 1: 写失败测试**

```ts
// electron/tests/migrations/023-sessions.test.ts
// 验证 v23 迁移后的 schema 形状（列名/表存在性/外键）。模式参照 012-agent-role-separation.test.ts。
import { describe, it, expect, beforeAll } from 'vitest';
import Database from 'better-sqlite3';

let db: Database.Database;

beforeAll(() => {
  db = new Database(':memory:');
  db.pragma('foreign_keys = ON');
  // 依序执行编译产物里的全部 migration SQL（与 012 测试同法）：
  // import { MIGRATIONS } from '../../src/main/storage/migrations';
  // 直接 exec 每个 m.sql
});

describe('migration v23 sessions', () => {
  it('sessions 表存在且列齐全', () => {
    const cols = db.prepare("PRAGMA table_info('sessions')").all() as Array<{ name: string }>;
    const names = cols.map((c) => c.name);
    expect(names).toEqual(expect.arrayContaining([
      'id', 'workspace_id', 'title', 'kind', 'settings_json',
      'created_at', 'updated_at', 'last_message_at',
    ]));
  });
  it('session_members 双主键', () => {
    const pk = db.prepare("PRAGMA table_info('session_members')").all() as Array<{ name: string; pk: number }>;
    const pkCols = pk.filter((c) => c.pk > 0).map((c) => c.name).sort();
    expect(pkCols).toEqual(['assignment_id', 'session_id']);
  });
  it('messages.room_id 已重命名为 session_id 且 matrix_event_id 已删', () => {
    const cols = db.prepare("PRAGMA table_info('messages')").all() as Array<{ name: string }>;
    const names = cols.map((c) => c.name);
    expect(names).toContain('session_id');
    expect(names).not.toContain('room_id');
    expect(names).not.toContain('matrix_event_id');
  });
  it('tasks 两列已重命名', () => {
    const cols = db.prepare("PRAGMA table_info('tasks')").all() as Array<{ name: string }>;
    const names = cols.map((c) => c.name);
    expect(names).toContain('execution_session_id');
    expect(names).toContain('source_session_id');
    expect(names).not.toContain('execution_room_id');
  });
  it('agent_assignments.bot_matrix_user_id → agent_user_id', () => {
    const cols = db.prepare("PRAGMA table_info('agent_assignments')").all() as Array<{ name: string }>;
    const names = cols.map((c) => c.name);
    expect(names).toContain('agent_user_id');
    expect(names).not.toContain('bot_matrix_user_id');
  });
  it('workspaces: team_session_id 存在、matrix_space_id 不存在；room_settings 表已删', () => {
    const cols = db.prepare("PRAGMA table_info('workspaces')").all() as Array<{ name: string }>;
    const names = cols.map((c) => c.name);
    expect(names).toContain('team_session_id');
    expect(names).not.toContain('matrix_space_id');
    const t = db.prepare(
      "SELECT name FROM sqlite_master WHERE type='table' AND name='room_settings'",
    ).get();
    expect(t).toBeUndefined();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd electron && npx pnpm@9.0.0 vitest run tests/migrations/023-sessions.test.ts`
Expected: FAIL（sessions 表不存在）

- [ ] **Step 3: 在 MIGRATIONS 数组末尾（v22 之后）追加 v23**

```ts
// ─── v23：2.0.0 P1 会话内核——sessions 取代 Matrix room ────────────────────
// 设计依据 docs/specs/2026-08-23-v2.0.0-platform-refactor-design.md §5。
// 全新开始（D5）：不做数据回填，仅 schema 变形。
{
  version: 23,
  sql: `
    CREATE TABLE IF NOT EXISTS sessions (
      id TEXT PRIMARY KEY,
      workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
      title TEXT NOT NULL,
      kind TEXT NOT NULL DEFAULT 'chat' CHECK (kind IN ('chat', 'task_execution')),
      settings_json TEXT,
      created_at INTEGER NOT NULL,
      updated_at INTEGER NOT NULL,
      last_message_at INTEGER
    );
    CREATE INDEX IF NOT EXISTS idx_sessions_workspace ON sessions(workspace_id, last_message_at DESC);

    CREATE TABLE IF NOT EXISTS session_members (
      session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
      assignment_id TEXT NOT NULL REFERENCES agent_assignments(instance_id) ON DELETE CASCADE,
      added_at INTEGER NOT NULL,
      PRIMARY KEY (session_id, assignment_id)
    );

    ALTER TABLE messages RENAME COLUMN room_id TO session_id;
    ALTER TABLE messages DROP COLUMN matrix_event_id;

    ALTER TABLE tasks RENAME COLUMN execution_room_id TO execution_session_id;
    ALTER TABLE tasks RENAME COLUMN source_room_id TO source_session_id;

    ALTER TABLE agent_assignments RENAME COLUMN bot_matrix_user_id TO agent_user_id;

    ALTER TABLE workspaces RENAME COLUMN team_room_id TO team_session_id;
    ALTER TABLE workspaces DROP COLUMN matrix_space_id;

    DROP TABLE IF EXISTS room_settings;
  `,
},
```

注意：此步之后**全仓 typecheck 必然红**（repo 层仍引用旧列名）——这是预期状态，Task 2-3 修复 repo 与调用方。本任务的测试门禁是 023 测试通过 + 其余 migration 测试不回归。

- [ ] **Step 4: 跑测试确认通过 + migration 套件不回归**

Run: `cd electron && npx pnpm@9.0.0 vitest run tests/migrations/`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add electron/src/main/storage/migrations/index.ts electron/tests/migrations/023-sessions.test.ts
git commit -m "feat(storage): v23 migration——sessions/session_members 表与列语义重命名"
```

---

### Task 2: sessions repo + settings 语义迁移

**Files:**
- Create: `electron/src/main/storage/sessions/repo.ts`
- Modify: `electron/src/main/storage/messages/repo.ts`（`roomId`→`sessionId` 全量重命名）
- Modify: `electron/src/main/settings/crud.ts`（RoomSettings → SessionSettings，读 sessions.settings_json）
- Test: `electron/tests/storage/sessions-repo.test.ts`

**Interfaces:**
- Produces:
  - `interface SessionRow { id; workspaceId; title; kind: 'chat'|'task_execution'; settingsJson: string | null; createdAt; updatedAt; lastMessageAt: number | null }`
  - `insertSession(input: { workspaceId; title; kind? }): SessionRow`
  - `getSession(id): SessionRow | null`；`listSessionsByWorkspace(workspaceId): SessionRow[]`（按 lastMessageAt DESC, createdAt DESC）
  - `renameSession(id, title)`；`deleteSession(id)`；`touchSessionLastMessage(id)`
  - `updateSessionSettings(id, patch: Partial<SessionSettings>)`；`getSessionSettings(id): SessionSettings`（缺省 `{ maxToolCalls: null, conflictStrategy: 'ask' }`）
  - `interface SessionSettings { maxToolCalls: number | null; conflictStrategy: 'ask'|'queue'|'preempt'|'fork'|'reject' }`
  - 成员：`addSessionMember(sessionId, assignmentId)`、`removeSessionMember(sessionId, assignmentId)`、`listSessionMembers(sessionId): Array<{ assignmentId; addedAt }>`

- [ ] **Step 1: 写失败测试**（覆盖：插入/列表排序/重命名/删除级联清成员/settings 合并语义/maxToolCalls 解析优先级 session→global/成员增删/重复添加幂等）

```ts
// electron/tests/storage/sessions-repo.test.ts 关键用例骨架（模式抄 tests/storage/messages-repo.test.ts 的内存 DB setup）
it('insertSession 默认 kind=chat，listSessionsByWorkspace 按 lastMessageAt 倒序');
it('updateSessionSettings 两次 patch 合并不丢字段');
it('resolveMaxToolCalls：session 覆盖 > global 默认 10');
it('deleteSession 级联删除 session_members 行');
it('addSessionMember 重复添加幂等（INSERT OR IGNORE 语义）');
```

- [ ] **Step 2: 跑测试确认失败**（模块不存在）

- [ ] **Step 3: 实现 repo.ts**

```ts
// electron/src/main/storage/sessions/repo.ts
//
// sessions / session_members 表 CRUD（2.0.0 P1 会话内核）。
// 取代 Matrix room：workspace 隔离 = 外键；会话设置存 settings_json（取代 room_settings 表）。
import { randomUUID } from 'node:crypto';
import { getDb } from '../db';

export interface SessionRow {
  id: string;
  workspaceId: string;
  title: string;
  kind: 'chat' | 'task_execution';
  settingsJson: string | null;
  createdAt: number;
  updatedAt: number;
  lastMessageAt: number | null;
}

export interface SessionSettings {
  /** NULL=继承全局 */
  maxToolCalls: number | null;
  conflictStrategy: 'ask' | 'queue' | 'preempt' | 'fork' | 'reject';
}

type SqlRow = {
  id: string; workspace_id: string; title: string; kind: string;
  settings_json: string | null; created_at: number; updated_at: number; last_message_at: number | null;
};

function rowToCamel(r: SqlRow): SessionRow {
  return {
    id: r.id, workspaceId: r.workspace_id, title: r.title,
    kind: r.kind as SessionRow['kind'], settingsJson: r.settings_json,
    createdAt: r.created_at, updatedAt: r.updated_at, lastMessageAt: r.last_message_at,
  };
}

export function insertSession(input: { workspaceId: string; title: string; kind?: SessionRow['kind'] }): SessionRow {
  const db = getDb();
  const id = randomUUID();
  const now = Date.now();
  db.prepare(
    `INSERT INTO sessions (id, workspace_id, title, kind, settings_json, created_at, updated_at)
     VALUES (?, ?, ?, ?, NULL, ?, ?)`,
  ).run(id, input.workspaceId, input.title, input.kind ?? 'chat', now, now);
  return getSession(id)!;
}

export function getSession(id: string): SessionRow | null {
  const row = getDb().prepare('SELECT * FROM sessions WHERE id = ?').get(id) as SqlRow | undefined;
  return row ? rowToCamel(row) : null;
}

export function listSessionsByWorkspace(workspaceId: string): SessionRow[] {
  const rows = getDb().prepare(
    'SELECT * FROM sessions WHERE workspace_id = ? ORDER BY last_message_at DESC, created_at DESC',
  ).all(workspaceId) as SqlRow[];
  return rows.map(rowToCamel);
}

export function renameSession(id: string, title: string): void {
  getDb().prepare('UPDATE sessions SET title = ?, updated_at = ? WHERE id = ?').run(title, Date.now(), id);
}

export function deleteSession(id: string): void {
  getDb().prepare('DELETE FROM sessions WHERE id = ?').run(id);
}

/** 消息写入路径调用：刷新 last_message_at（排序键） */
export function touchSessionLastMessage(id: string): void {
  getDb().prepare('UPDATE sessions SET last_message_at = ? WHERE id = ?').run(Date.now(), id);
}

export function updateSessionSettings(id: string, patch: Partial<SessionSettings>): void {
  const merged = { ...getSessionSettings(id), ...patch };
  getDb().prepare('UPDATE sessions SET settings_json = ?, updated_at = ? WHERE id = ?')
    .run(JSON.stringify(merged), Date.now(), id);
}

export function getSessionSettings(id: string): SessionSettings {
  const row = getDb().prepare('SELECT settings_json FROM sessions WHERE id = ?').get(id) as
    | { settings_json: string | null } | undefined;
  if (!row?.settings_json) return { maxToolCalls: null, conflictStrategy: 'ask' };
  const parsed = JSON.parse(row.settings_json) as Partial<SessionSettings>;
  return {
    maxToolCalls: parsed.maxToolCalls ?? null,
    conflictStrategy: parsed.conflictStrategy ?? 'ask',
  };
}

// ─── 成员 ──────────────────────────────────────────────────────────────────

export function addSessionMember(sessionId: string, assignmentId: string): void {
  getDb().prepare(
    'INSERT OR IGNORE INTO session_members (session_id, assignment_id, added_at) VALUES (?, ?, ?)',
  ).run(sessionId, assignmentId, Date.now());
}

export function removeSessionMember(sessionId: string, assignmentId: string): void {
  getDb().prepare('DELETE FROM session_members WHERE session_id = ? AND assignment_id = ?')
    .run(sessionId, assignmentId);
}

export function listSessionMembers(sessionId: string): Array<{ assignmentId: string; addedAt: number }> {
  const rows = getDb().prepare(
    'SELECT assignment_id, added_at FROM session_members WHERE session_id = ? ORDER BY added_at ASC',
  ).all(sessionId) as Array<{ assignment_id: string; added_at: number }>;
  return rows.map((r) => ({ assignmentId: r.assignment_id, addedAt: r.added_at }));
}
```

- [ ] **Step 4: settings/crud.ts 切换到 sessions.settings_json**

删除 `getRoomSettings` / `updateRoomSettings`，替换为对 repo 的转调；`resolveMaxToolCalls(roomId)` 改签名为 `resolveMaxToolCalls(sessionId: string): number`（实现体改为 `getSessionSettings(sessionId)`）。`GlobalSettings` 保持不变。

- [ ] **Step 5: messages/repo.ts 列重命名**

`MessageRow.roomId` → `sessionId`；SqlRow `room_id` → `session_id`；所有 SQL 中 `room_id` → `session_id`；删除 `matrixEventId` 字段与 `updateMessageMatrixEventId` / `getMessageByMatrixEventId` 两个函数（调用方在 Task 7/12 清理，此刻先在 sync-manager.ts 两处调用点注释 `// v23 过渡：Task 12 删除本文件` 并将函数调用改为 no-op 注释掉——保持 typecheck 可过的方式：把 sync-manager 中这两处调用直接删除）。

同批用 grep 全仓修正编译错误：`grep -rn "roomId\|room_id" electron/src/main/storage/`（只修 storage 层；runtime/im 层的引用在 Task 5-8 修，此任务为让 typecheck 通过，允许在调用方做 `sessionId: x.roomId` 形式的临时字段映射——**不允许**，改为直接把调用方字段一起重命名，Task 5-8 只处理协议层）。

> 实操指引：本步骤 typecheck 大面积红是预期。修复顺序：storage/messages → storage/tasks/repo.ts（`executionRoomId`→`executionSessionId` 等，机械替换）→ agent/crud.ts（`botMatrixUserId`→`agentUserId`，DB 列名同步改）→ 其余按编译错误逐个机械替换。Task 2 的完成门禁是 `typecheck` 双 clean（允许 sync-manager 等文件保留 Matrix 逻辑但字段名已换）。

- [ ] **Step 6: 跑测试 + typecheck**

Run: `cd electron && npx pnpm@9.0.0 vitest run tests/storage/ && npx pnpm@9.0.0 typecheck`
Expected: 全 PASS，双 clean

- [ ] **Step 7: Commit**

```bash
git add -A electron/src electron/tests
git commit -m "feat(storage): sessions repo + room_settings/settings 列语义迁移（v23 收尾）"
```

---

### Task 3: session-ops——会话生命周期操作（纯 SQLite）

**Files:**
- Create: `electron/src/main/im/session-ops.ts`
- Test: `electron/tests/im/session-ops.test.ts`

**Interfaces:**
- Consumes: Task 2 的 sessions repo、`listAssignments(workspaceId)`（agent/crud.ts）、`getWorkspace`
- Produces:
  - `interface SessionSummary { id; workspaceId; title; kind; lastMessageAt; members: SessionMemberInfo[] }`
  - `interface SessionMemberInfo { assignmentId; agentName; iconEmoji; role; lastRunning; isCoordinator }`
  - `createSession(input: { workspaceId; title; memberAssignmentIds; kind? }): SessionRow`
  - `renameSession` / `deleteSessionOp(id)`（团队会话保护：`workspaces.team_session_id === id` 时抛错）
  - `getSessionsForWorkspace(workspaceId?): SessionSummary[]`
  - `getSessionMembersInfo(sessionId): SessionMemberInfo[]`（JOIN agent_assignments + agent_definitions）

- [ ] **Step 1: 写失败测试**（创建带成员 / 团队会话禁删 / workspace 过滤 / 成员信息 JOIN 正确）
- [ ] **Step 2: 确认失败**
- [ ] **Step 3: 实现**（成员信息 JOIN SQL：

```sql
SELECT a.instance_id, a.role, a.last_running, d.name, d.icon_emoji
FROM session_members m
JOIN agent_assignments a ON m.assignment_id = a.instance_id
JOIN agent_definitions d ON a.agent_definition_id = d.id
WHERE m.session_id = ?
ORDER BY m.added_at ASC
```

`isCoordinator` 由 `workspaces.coordinator_instance_id === instance_id` 判定。团队会话保护读 `workspaces.team_session_id`。）

- [ ] **Step 4: 测试 + typecheck + Commit** `feat(im): session-ops 会话生命周期（纯 SQLite，无 Matrix）`

---

### Task 4: RouterService 输入源解耦——plain 参数入口 + agent_user_id 路由

**Files:**
- Modify: `electron/src/main/agent/router-service.ts`
- Modify: `electron/src/main/agent/runtime-registry.ts`（`findAssignmentByBotUserId` → `findAssignmentByAgentUserId`，SQL 列名 `agent_user_id`）
- Test: 修改 `electron/tests/agent/router-service.test.ts`

**Interfaces:**
- Produces:
  - `async routeUserChat(input: { sessionId: string; assignmentId: string; body: string; streamSessionId?: string }): Promise<void>`——公开方法，内部构造 TaskConfig 调 runner.executeTask（从原私有 routeUserMessage 平移，event.getContent().body 改为入参 body）
  - `RoutedEvent` 更名 `InternalEvent`（形状不变：getType/getContent/getSender/getRoomId——桥接层可低成本构造）
  - `routeMatrixEvent` 更名 `routeEvent`（签名不变）

- [ ] **Step 1: 测试先行**——新增用例：`routeUserChat 直接派发 ephemeral task 到目标 runner（不经过 event 形状）`；改造现有 routeMatrixEvent 用例为 routeEvent。
- [ ] **Step 2: 确认失败**
- [ ] **Step 3: 实现**——原 `routeUserMessage(event, assignmentId)` 的函数体搬入 `routeUserChat`（`body` 来自入参；`roomId`→`sessionId` 字段同步）；`routeEvent` 的 `m.room.message` 分支改调 `this.routeUserChat({ sessionId: event.getRoomId() ?? '', assignmentId: directTargetAssignmentId, body })`。dispatch 分支的 `dispatch_to` 反查换 `findAssignmentByAgentUserId`。
- [ ] **Step 4: 测试 + typecheck + Commit** `refactor(agent): RouterService plain 入口 + agent_user_id 反查`

---

### Task 5: 内部事件桥——runtime-entry 的 dispatch/task_reply 脱离 Matrix 传输

**Files:**
- Create: `electron/src/main/agent/internal-event.ts`（协议类型 + 子进程侧发送函数）
- Create: `electron/src/main/agent/internal-event-bridge.ts`（主进程侧：child → RouterService）
- Modify: `electron/src/main/agent/runtime-entry.ts`（Matrix sendEvent 调用点替换）
- Modify: `electron/src/main/agent/runtime-spawner.ts`（message handler 识别内部事件转给 bridge）
- Test: `electron/tests/agent/internal-event-bridge.test.ts`

**Interfaces:**
- Produces:
  - 子进程侧：`sendInternalEvent(evt: InternalEventMsg): void`，其中

```ts
// electron/src/main/agent/internal-event.ts
//
// 子进程 → 主进程的内部事件协议（2.0.0 P1：取代 Matrix 自定义 event 传输）。
// runtime-entry 不再持有 Matrix client：dispatch / task_reply / abort_dispatch
// 经 child IPC 发给主进程，由 internal-event-bridge 路由到 RouterService。
// 消息体复用 dispatch.ts 的 content 构造器（buildDispatchMessage 等），零转换成本。

import {
  DISPATCH_EVENT_TYPE, TASK_REPLY_EVENT_TYPE, ABORT_DISPATCH_EVENT_TYPE,
} from './dispatch';

export const INTERNAL_EVENT_MSG = 'momo-internal-event';

/** child → main 的内部事件信封 */
export interface InternalEventMsg {
  type: typeof INTERNAL_EVENT_MSG;
  eventType: string;
  /** 目标会话（原 Matrix roomId） */
  sessionId: string;
  /** 发送者（assignmentId 或 'owner'） */
  sender: string;
  content: Record<string, unknown>;
}

export function sendInternalEvent(evt: Omit<InternalEventMsg, 'type'>): void {
  process.send?.({ type: INTERNAL_EVENT_MSG, ...evt });
}

// 便捷构造器：runtime-entry 现有调用点一一对应替换
export function sendDispatchEvent(sessionId: string, from: string, content: Record<string, unknown>): void {
  sendInternalEvent({ eventType: DISPATCH_EVENT_TYPE, sessionId, sender: from, content });
}
export function sendTaskReplyEvent(sessionId: string, from: string, content: Record<string, unknown>): void {
  sendInternalEvent({ eventType: TASK_REPLY_EVENT_TYPE, sessionId, sender: from, content });
}
export function sendAbortDispatchEvent(sessionId: string, from: string, content: Record<string, unknown>): void {
  sendInternalEvent({ eventType: ABORT_DISPATCH_EVENT_TYPE, sessionId, sender: from, content });
}
```

  - 主进程侧 bridge：

```ts
// electron/src/main/agent/internal-event-bridge.ts
//
// 主进程内部事件桥：runtime 子进程经 child IPC 发来的内部事件
// （dispatch / task_reply / abort_dispatch）转发给 RouterService.routeEvent。
// 这些事件是纯路由信号（不 INSERT messages——用户可见的 dispatch 展示
// 已由 message_events 的 dispatch_start/dispatch_result 承载）。
import type { InternalEventMsg, } from './internal-event';
import { getLogger } from '../logger';

interface RouteTarget {
  routeEvent(
    event: { getType(): string; getContent(): Record<string, unknown>; getSender(): string | undefined; getRoomId(): string | undefined },
    ownerUserId: string,
    targetAssignmentId: string | null,
    directTargetAssignmentId?: string,
  ): Promise<void>;
}

let router: RouteTarget | null = null;

export function setBridgeRouter(svc: RouteTarget | null): void {
  router = svc;
}

/** child IPC message 分发入口（runtime-spawner 调用）。返回 true 表示已消费。 */
export function handleChildMessage(msg: unknown): boolean {
  if (typeof msg !== 'object' || msg === null) return false;
  const m = msg as Partial<InternalEventMsg>;
  if (m.type !== 'momo-internal-event' || typeof m.eventType !== 'string') return false;
  if (!router) {
    getLogger().warn('内部事件到达但 RouterService 未启动，丢弃', { eventType: m.eventType });
    return true;
  }
  void router.routeEvent(
    {
      getType: () => m.eventType as string,
      getContent: () => (m.content ?? {}) as Record<string, unknown>,
      getSender: () => m.sender,
      getRoomId: () => m.sessionId,
    },
    'owner',
    null,
  );
  return true;
}
```

  注意：logger 是 `import { logger } from '../logger'` 直接导入（上面 getLogger 写法按仓库实际调整，统一用 `import { logger } from '../logger'`）。

- [ ] **Step 1: 写失败测试**——bridge 收到 dispatch 内部事件 → routeEvent 被以正确形状调用（注入 mock router）；非内部事件返回 false。
- [ ] **Step 2: 确认失败**
- [ ] **Step 3: 实现** + 接线：
  - `runtime-spawner.ts` messageHandler 前置一行 `if (handleChildMessage(msg)) return;`
  - `router-bootstrap.ts` 的 `ensureRouterService` 末尾（`setRouterService` 旁）加 `setBridgeRouter(currentRouterService)`；`destroyRouterService` 加 `setBridgeRouter(null)`。同步删除对 `setRouterService`（sync-manager 注入）的调用——**保留 sync-manager 侧的 setRouterService 导出**（阶段三删除），router-bootstrap 不再 import 它。
- [ ] **Step 4: runtime-entry 调用点替换**

在 `runtime-entry.ts` 中 `grep -n "sendEvent\|matrixClient\|client\." 定位全部 Matrix 传输调用；映射替换：
  - dispatch 发送（buildDispatchMessage 后的 `client.sendEvent(roomId, DISPATCH_EVENT_TYPE, content)`）→ `sendDispatchEvent(roomId, config.botUserId, content)`（`config.botUserId` 在 Task 10 更名 `agentUserId`，此处先保持原变量名）
  - task_reply 发送 → `sendTaskReplyEvent(...)`
  - abort_dispatch 发送 → `sendAbortDispatchEvent(...)`
  - **agent 最终消息的 Matrix 发送（task-driven 分支内的 sendEvent('m.room.message')）→ 直接删除**（SQLite 已由 chunk 路径落盘，见 spec §4.1 ③）
  - v1 分支（`taskDriven === false` 路径）的 Matrix 调用**本任务不动**（Task 13 整体删除）
  - Matrix client 的创建：task-driven 分支不再需要 client——把 `createMatrixClient` 调用移入 v1 分支内部（懒创建）

- [ ] **Step 5: 测试 + typecheck + Commit** `feat(agent): 内部事件桥——dispatch/task_reply 脱离 Matrix 传输`

---

### Task 6: StreamChunk 字段迁移 + stream-relay 抽取（runtime-manager 拆解第一步）

**Files:**
- Modify: `electron/src/main/agent/stream-chunk.ts`（`start.roomId`→`sessionId`、`start.botUserId`→`senderAgentId`、`todo_update.roomId`→`sessionId`）
- Create: `electron/src/main/agent/stream-relay.ts`（从 runtime-manager 平移 `handleStreamChunk` / `routeChunkToBuffer` / `getEventBuffer`，改造为独立模块）
- Modify: `electron/src/main/agent/runtime-spawner.ts`（chunk handler 改调 stream-relay）
- Modify: `electron/src/main/agent/runtime-entry.ts`（发 chunk 处字段同步）
- Modify: `electron/src/main/agent/tools/todo-tools.ts`（todo_update chunk 字段）
- Test: 平移并改造 `electron/tests/agent/runtime-stream.test.ts` 中 routeChunkToBuffer 相关用例到 `electron/tests/agent/stream-relay.test.ts`

**Interfaces:**
- Produces: `stream-relay.ts` 导出 `handleStreamChunk(chunk: StreamChunk): void`、`getEventBuffer(): MessageEventBuffer`、`abortStreamBySessionId(streamSessionId): boolean`（遍历 `agentRunners`，逐 runner 调 `abortStream`——从 runtime-manager 的按 room 索引改为按 streamSessionId，修掉 README 记录的"同房中断限制"技术债）
- Consumes: Task 2 的 `insertMessage({ sessionId, ... })`

实现要点：`routeChunkToBuffer` 的 `case 'start'` 里 `insertMessage({ sessionId: chunk.sessionId, sender: chunk.senderAgentId, ... })`；`sender` 语义已是 assignmentId（Task 10 起发端传 assignmentId）。`abortStreamBySessionId` 需要 runtime-registry 的 `agentRunners`——为避免循环依赖，采用注册反转：`stream-relay` 暴露 `setAbortResolver(fn: (streamSessionId) => boolean)`，由 `runtime-registry` 模块初始化时注入（`runtime-registry.ts` 顶层 `setAbortResolver((id) => { for (const r of agentRunners.values()) { r.abortStream(id); } return agentRunners.size > 0; })`——聚合语义：广播到所有 runner，各 runner 内部按活跃表自然过滤）。

- [ ] **Step 1: 平移测试改造为失败状态** → **Step 2: 确认失败** → **Step 3: 实现** → **Step 4: 全套 agent 测试 + typecheck** → **Step 5: Commit** `refactor(agent): stream-relay 独立模块 + chunk 字段 session 化（按 streamSessionId 中断）`

---

### Task 7: SessionService——用户消息写入 + 目标解析 + 路由

**Files:**
- Create: `electron/src/main/im/session-service.ts`
- Test: `electron/tests/im/session-service.test.ts`

**Interfaces:**
- Consumes: Task 2 repo、Task 3 session-ops、Task 4 `routeUserChat`、`broadcastLocalMessage`（p2p/index.ts）、`detectConflict`（task/conflict-detector.ts）、`listTasks`/`getTask`
- Produces:
  - `setSessionRouter(svc: { routeUserChat(input): Promise<void> } | null): void`（由 router-bootstrap 注入）
  - `async sendUserMessage(input: { sessionId: string; body: string; mentionedAssignmentIds?: string[] }): Promise<void>`
  - `resolveTarget(sessionId: string, mentionedAssignmentIds: string[]): string | null`——decide-response 三场景的 session 版

```ts
// electron/src/main/im/session-service.ts 核心（完整文件按此骨架补全 imports 与注释）
import { insertMessage } from '../storage/messages/repo';
import { touchSessionLastMessage, getSession, listSessionMembers } from '../storage/sessions/repo';
import { getSessionMembersInfo } from './session-ops';
import { getWorkspace, listWorkspaces } from '../workspace/crud';
import { broadcastLocalMessage } from '../p2p';
import { detectConflict } from '../task/conflict-detector';
import { listTasks, getTask } from '../storage/tasks/repo';
import { BrowserWindow } from 'electron';
import { logger } from '../logger';

interface SessionRouter {
  routeUserChat(input: { sessionId: string; assignmentId: string; body: string }): Promise<void>;
}

let router: SessionRouter | null = null;
export function setSessionRouter(svc: SessionRouter | null): void { router = svc; }

/**
 * 目标解析（原 decide-response 三场景的 session 语义）：
 *   1. 显式 mention → 第一个被 @ 且是本会话成员的 assignment
 *   2. 会话仅 1 个成员 agent → 自动响应（原"单聊无需 @"）
 *   3. 本会话是 workspace 团队会话（team_session_id）且有协调 agent → 协调 agent 自动接待
 *   4. 其余 → null（不路由）
 */
export function resolveTarget(sessionId: string, mentionedAssignmentIds: string[]): string | null {
  const members = listSessionMembers(sessionId).map((m) => m.assignmentId);
  const mentioned = mentionedAssignmentIds.find((id) => members.includes(id));
  if (mentioned) return mentioned;
  if (members.length === 1) return members[0]!;  // 单成员会话：发言即应答
  const session = getSession(sessionId);
  if (!session) return null;
  const ws = listWorkspaces().find((w) => w.id === session.workspaceId);
  if (ws && ws.teamSessionId === sessionId && ws.coordinatorInstanceId) return ws.coordinatorInstanceId;
  return null;
}

/** 用户消息写入路径：INSERT → 推 renderer → P2P 广播 → 冲突检测 → 路由到目标 agent */
export async function sendUserMessage(input: { sessionId: string; body: string; mentionedAssignmentIds?: string[] }): Promise<void> {
  const session = getSession(input.sessionId);
  if (!session) throw new Error(`会话不存在: ${input.sessionId}`);

  const msg = insertMessage({
    sessionId: input.sessionId,
    sender: 'owner',
    eventType: 'm.room.message',   // 事件类型字符串保留（renderer 渲染分支依赖；P2 收敛命名）
    body: input.body,
    workspaceId: session.workspaceId,
  });
  touchSessionLastMessage(input.sessionId);
  pushMessageRow(msg);

  // P2P 广播（fire-and-forget；未初始化时静默）
  void broadcastLocalMessage({
    sessionId: input.sessionId, sender: 'owner', body: input.body, eventType: 'm.room.message',
  });

  // 冲突检测（沿用 im:send 的保护语义：失败不阻塞）
  try {
    const conflict = detectConflict(input.sessionId, input.body, {
      findInProgressTaskByRoom: (r) =>
        listTasks({ executionSessionId: r, status: 'in_progress', limit: 1 })[0] ?? null,
      getTask,
    });
    if (conflict) {
      const win = BrowserWindow.getAllWindows()[0];
      if (win && !win.isDestroyed()) win.webContents.send('im:conflict', conflict);
    }
  } catch (err) {
    logger.warn('冲突检测失败（不阻塞消息发送）', { error: err instanceof Error ? err.message : String(err) });
  }

  const target = resolveTarget(input.sessionId, input.mentionedAssignmentIds ?? []);
  if (target && router) {
    await router.routeUserChat({ sessionId: input.sessionId, assignmentId: target, body: input.body });
  }
}

（实现说明：`pushMessageRow` 为模块内私有函数——`mainWindow.webContents.send('session:message', msg)`，窗口引用经模块级 `setSessionMainWindow(win)` 注入，由 main/index.ts 创建窗口后调用，与原 sync-manager 的 setMainWindow 同法。文件顶部的多余 import 按编译器提示收敛。）
```

（以上伪代码标记处按真实 imports 收敛——`pushMessageRow` 实现：模块级 `let mainWindow`，`setSessionMainWindow(win)` 由 main/index.ts 注入。）

`detectConflict` 的入参名 `executionRoomId` 需同步改 `executionSessionId`（grep `findInProgressTaskByRoom` 签名跟随改）。

- [ ] **Step 1: 失败测试**——用例覆盖 resolveTarget 四分支（mention 命中/单成员/团队会话协调/无目标）+ sendUserMessage 全链（INSERT 调用、push、路由调用、无 router 时不炸）
- [ ] **Step 2: 确认失败** → **Step 3: 实现** → **Step 4: 测试 + typecheck** → **Step 5: Commit** `feat(im): SessionService——消息写入/目标解析/进程内路由`

---

## 阶段二：切流量

### Task 8: session:* IPC 通道 + preload + 类型

**Files:**
- Create: `electron/src/main/im/session.ipc.handlers.ts`
- Modify: `electron/src/main/ipc/index.ts`（注册新 handler）
- Modify: `electron/src/preload/index.ts`（新增 `session` 命名空间；旧 `im` 命名空间暂留）
- Modify: `renderer/src/ipc/types.d.ts`（`SessionApiSurface` + `SessionSummary` / `SessionMemberInfo` / `ImMessage.roomId→sessionId`）

**Interfaces:**
- Produces IPC 契约（invoke 通道 8 个 + 推送 2 个沿用改名）：

| 通道 | 签名 |
|---|---|
| `session:list` | `(workspaceId?: string) => SessionSummary[]` |
| `session:get` | `(sessionId) => { session: SessionRow; members: SessionMemberInfo[] }` |
| `session:create` | `(input: { workspaceId; title; memberAssignmentIds; kind? }) => SessionRow` |
| `session:rename` | `(sessionId, title) => { ok: true }` |
| `session:delete` | `(sessionId) => { ok: true }`（团队会话抛错） |
| `session:send` | `(sessionId, body, mentionedAssignmentIds?) => void` |
| `session:getMessages` | `(sessionId) => { messages: ImMessage[]; eventsByMessage: Record<string, MessageEventRow[]> }`（实现在 im/ipc.handlers.ts 现逻辑上换 repo 调用） |
| `session:loadOlder` | `(sessionId, beforeTs, count?) => { messages; eventsByMessage; hasMore }` |
| 推送 `session:message` | 原 `im:message` 改名（SessionService pushMessageRow 发送名同步） |
| 推送 `session:message_event_batch` | 原 `im:message_event_batch` 改名（stream-relay onFlush 发送名同步） |

- [ ] **Step 1-5**：handler 实现（转调 Task 3/7，模式抄 im/ipc.handlers.ts）→ preload 暴露 `window.momo.session.*` → types.d.ts 定义 → `cd electron && npx pnpm@9.0.0 vitest run tests/im/`（补 session.ipc.handlers 冒烟测试：mock ipcMain）→ typecheck → Commit `feat(ipc): session:* 通道 + preload session 命名空间`

---

### Task 9: renderer 切换 im.store → session.store

**Files:**
- Create: `renderer/src/stores/session.store.ts`（im.store.ts 全量改造）
- Delete: `renderer/src/stores/im.store.ts`
- Modify: 所有 import im.store 的组件（`grep -rln "im.store" renderer/src`——约 10 个组件：MessageList/MiddlePanel/RoomList/MembersPanel/MessageInput/ConflictDialog 等）

**Interfaces:**
- Produces: `useSessionStore`——字段映射表（组件做机械替换）：

| 旧 | 新 |
|---|---|
| `rooms` | `sessions: SessionSummary[]` |
| `activeRoomId` | `activeSessionId` |
| `messagesByRoom` | `messagesBySession` |
| `loadRooms(workspaceId)` | `loadSessions(workspaceId)` |
| `selectRoom(id)` | `selectSession(id)` |
| `sendMessage(body)` | `sendMessage(body, mentionedAssignmentIds?)`（调 `ipc.session.send`） |
| `loadOlder(roomId)` | `loadOlder(sessionId)` |
| `receiveMessage` / `onIncomingEventBatch` | 同名保留（订阅 `session:message` / `session:message_event_batch`） |
| ImMessage.roomId | ImMessage.sessionId |
| mention 菜单数据源 `botMatrixUserId` | `assignmentId`（AgentAssignment.agentUserId） |

store 完整实现 = im.store.ts 逐行对照替换（IPC 调用换 `ipc.session.*`、去 `im:startSync` 调用、App 启动时不再触发）。组件内仅改 import 与字段名，**不改组件文件名与 UI 结构**（P2 UI 骨架任务统一重建外壳）。App.tsx / auth.store 的 Onboarding 分支处理放 Task 11。

- [ ] **Step 1: 以 im.store.test.ts 为底改写 session.store.test.ts（先失败）**
- [ ] **Step 2: 确认失败** → **Step 3: 实现 store + 组件机械替换** → **Step 4: `cd renderer && npx pnpm@9.0.0 vitest run` 全绿 + typecheck** → **Step 5: Commit** `refactor(renderer): im.store → session.store 全量切换`

---

### Task 10: workspace / agent 流程去 Matrix

**Files:**
- Modify: `electron/src/main/workspace/crud.ts`（createWorkspace：删 Space/团队群创建，改为 `insertSession({workspaceId, title: '团队会话', kind:'chat'})` + `UPDATE workspaces SET team_session_id = ?`）
- Modify: `electron/src/main/agent/crud.ts` / `spawn-helpers.ts`（agent 分配不再调 bot-registrar；生成 `agent_user_id = 'agent-' + slug + '-' + randomSuffix(6)`；`buildSpawnOpts` 产出新 `AgentRuntimeOpts`——删除 `botUserId`/`botAccessToken`/`homeserverUrl`/`ownerUserId`/`teamRoomId` 字段，新增 `agentAssignmentId`、`agentUserId`（展示名映射用）、`teamSessionId`）
- Modify: `electron/src/main/agent/init-runtime.ts`（删 `resolveBotToken` 依赖；`teamSessionId: ws.teamSessionId ?? undefined`）
- Modify: `electron/src/main/agent/runtime-entry.ts`（RuntimeConfig 字段跟随；`botUserId` 引用点改 `agentUserId`）
- Modify: `electron/src/main/agent/agent-runner.ts`（`AgentRunnerOpts.agentBotUserId`→`agentUserId`；`TaskConfig.dispatchContext.fromBotUserId`→`fromAssignmentId`）
- Modify: `electron/src/main/agent/router-service.ts`（dispatch content 的 `dispatch_from` 语义改 assignmentId）
- Test: 更新 `electron/tests/workspace/*`、`electron/tests/agent/*` 相应用例

**要点**：新建会话时成员写入 `session_members`（workspace 创建 → 默认团队会话；agent 加入 workspace → 自动加入团队会话，替代原"自动入群"逻辑——定位 `bot-registrar` 调用点旁的 invite 逻辑一并替换）。

- [ ] **Step 1-5**：失败测试（workspace 创建产出 team_session_id + 默认会话；agent 分配生成 agent_user_id 且入成员表）→ 实现 → 测试 + typecheck → Commit `refactor(workspace,agent): 创建流程去 Matrix——本地会话/身份生成`

---

### Task 11: 启动链与认证简化

**Files:**
- Modify: `electron/src/main/index.ts`（删 `startConduit` 调用与 import、`im:startSync` 注册、auth 相关初始化；启动即 `initTaskDrivenRuntime` + `setSessionMainWindow`）
- Modify: `electron/src/main/im/ipc.handlers.ts`（删 `im:startSync` / `im:send` 系列——被 session:* 取代；文件仅保留 exportRoomMessages 直到 Task 12 迁移）
- Modify: `renderer/src/App.tsx`（删 Onboarding 分支：有 workspace 直接 MainShell，无 workspace 显示首启创建对话框——复用现有 CreateWorkspaceDialog）
- Delete: `renderer/src/stores/auth.store.ts`、`renderer/src/components/onboarding/*`
- Test: renderer App 相关测试更新

**注意**：本任务删 im:startSync 后，Matrix /sync 不再启动——**这是切换点**，完成后系统实际运行已无 Matrix 流量（文件还在，属于阶段三清理）。

- [ ] **Step 1-5**：测试 → 实现 → `npx pnpm@9.0.0 typecheck` + renderer 全测 → Commit `refactor(main,renderer): 启动链去 Matrix + 首启流程简化`

---

## 阶段三：删旧物

### Task 12: 删除 Matrix 全家

**Files（删除清单）:**
- `rm -r electron/src/main/matrix/`（sync-manager / session / client / room-info）
- `rm -r electron/src/main/conduit/`
- `rm electron/src/main/agent/bot-registrar.ts`
- `rm electron/src/main/im/room-ops.ts`
- `rm electron/src/main/ipc/authFlows.ts`、`electron/src/main/ipc/auth.handlers.ts`（若路径不同按 `grep -rln "im:startSync\|authFlows\|registerAccount" electron/src/main` 定位）
- `rm electron/tests/conduit/ -r`、`rm electron/tests/matrix/ -r`、auth 相关测试文件
- `rm -r resources/tuwunel*`（二进制 + 下载脚本；`grep -n "postinstall\|download" electron/package.json` 清理脚本行）
- `cd electron && npx pnpm@9.0.0 remove matrix-js-sdk`
- preload/types.d.ts 清理：`ImRoomInfo` / `RoomMember` / `AuthResult` / `ConduitStatus` / `auth` / `im` 旧命名空间（im 导出中仍被 renderer 引用的先迁移到 session 命名空间）

**执行方式**：删除 → `npx pnpm@9.0.0 typecheck` → 按编译错误逐个清理 import（预期错误集中在 ipc/index.ts 注册表、sync 相关、exportRoomMessages 的 rooms 依赖——导出功能改读 sessions）。`im:exportRoomMessages` 迁到 `session:exportMessages`（读 sessions + messages，botNameMap 按 assignmentId JOIN）。

- [ ] **Step 1: 删除 + typecheck 清零** → **Step 2: `cd electron && npx pnpm@9.0.0 vitest run`（Matrix/conduit 测试已随文件删除，其余全绿）** → **Step 3: Commit** `chore!: 移除 Matrix/Tuwunel 全家（BREAKING：传输层内迁）`

### Task 13: 删除 v1 双轨 + runtime-entry 瘦身收尾

**Files:**
- Delete: `electron/src/main/agent/runtime-manager.ts`（活代码已在 Task 6 平移到 stream-relay；`AgentRuntimeOpts` 类型定义迁到新文件 `electron/src/main/agent/runtime-config.ts`，全仓 import 改指向）
- Delete: `electron/src/main/agent/auto-start.ts`、`electron/src/main/agent/decide-response.ts`、`electron/src/main/agent/message-target-resolver.ts`、`electron/tests/agent/decide-response.test.ts` 等对应测试
- Modify: `electron/src/main/agent/runtime-entry.ts`——删除 `taskDriven === false` 分支（startClient / ClientEvent.Event 监听 / handleEvent / decideResponse 调用 / Matrix import 全删）；`RuntimeConfig.taskDriven` 字段删除；目标行数 <900
- Modify: `electron/src/main/agent/runtime-registry.ts`（`startAgentRuntime` 双轨分流改单轨——删 `taskDriven` 参数与 v1 spawnAgent 分支）
- Modify: `agent/ipc.handlers.ts`（stop/start 单轨化：删 `isV1SubprocessAlive` 等双轨残留）

- [ ] **Step 1: 类型迁移 + 删除** → **Step 2: typecheck + electron 全测** → **Step 3: Commit** `refactor(agent)!: 删除 v1 长存进程双轨（runtime-manager/auto-start/decide-response）`

### Task 14: P1 验收

- [ ] **Step 1: 残留扫描（全部必须 0 命中）**

```bash
grep -rn "matrix-js-sdk\|startConduit\|bot-registrar\|botMatrixUserId\|matrix_space_id\|matrixSpaceId\|matrixEventId\|team_room_id\|room_settings\|execution_room_id" electron/src renderer/src --include="*.ts" --include="*.tsx" | grep -v "\.test\." || echo "CLEAN"
grep -rn "momo-studio\/matrix\|from '..\/matrix" electron/src --include="*.ts" || echo "CLEAN"
cd electron && grep -c "matrix-js-sdk" package.json || echo "CLEAN"
```

- [ ] **Step 2: 全量验证**

```bash
nvm use 20 && npx pnpm@9.0.0 typecheck   # 双 clean
npx pnpm@9.0.0 test                       # electron + renderer 全绿
```

- [ ] **Step 3: 手工冒烟（xvfb）**

```bash
xvfb-run -a --server-args="-screen 0 1280x800x24" npx pnpm@9.0.0 dev
```

验收点：首启建 workspace → 建会话拉 agent → 发消息收到流式回复（thinking/工具卡片/委派 chip）→ 中途杀进程重启 → 会话历史与中断前一致 → dev 模式无 Tuwunel 进程（`ps aux | grep -i tuwunel` 空）。

- [ ] **Step 4: Commit（如有收尾修复）+ 更新 README 状态段** `docs: P1 会话内核完成——README 2.0 重构状态更新`

---

## 任务依赖图

```
T1 → T2 → T3 → T7
         T4 ↗   ↘ T8 → T9 → T11
   T5（依赖 T4）      T10 ↗
   T6（依赖 T2）→ T7
T11 → T12 → T13 → T14
```

## 风险与回滚

- 阶段一纯增量（Matrix 并存），任意任务可独立回滚（revert 单 commit）。
- 切换点在 T11（删除 im:startSync 调用）——此前系统行为不变，此后 Matrix 成死代码；T11 出问题 revert 即可恢复旧行为。
- runtime-entry 的 T5/T13 动大文件：先 grep 定位、小步替换、每步跑 `tests/agent/` 既有套件（runtime-stream/dispatch-fresh-session/segment 等覆盖充分）。
