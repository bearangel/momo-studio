# v2.1 UI 设计系统重构 · P1 骨架+设置域 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** layout/ settings/ upgrade/ common/ 四域完成设计系统迁移——零裸色号、零 emoji/字形图标、手写弹窗收敛到 Dialog 原子件——使 P0 基座在真实界面上生效。

**Architecture:** 沿用 P0 的语义 token 体系与 12 个原子件；本计划是**存量迁移**：结构任务（图标 lucide 化、弹窗收敛、原子件键盘加固）给全代码，纯样式任务给逐行替换规格（旧值→新值，行号以迁移前文件为准）。机械完成度由 ESLint 逐文件零警告证明。

**Tech Stack:** React 18 + TypeScript strict + Tailwind 3.4 语义 token + lucide-react + zustand + Vitest（jsdom）

**Spec:** `docs/specs/2026-09-01-v2.1-ui-design-system-refactor-design.md`（§8 P1 行 + 终审 triage 移交项）
**P0 产出依赖:** 12 个 ui/ 原子件、`lib/task-status.ts`、ESLint 三规则（全局 warn + ui/ error）、`docs/dev/design-system.md`

## Global Constraints

- Node 20：`export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && nvm use 20`（每个 shell 前置；Node 26 会炸 better-sqlite3）
- pnpm 经 `npx pnpm@9.0.0`
- TypeScript strict：禁止 `any` / `@ts-ignore` / `as any`
- 注释中文；标识符英文；Conventional Commits
- 新代码只用语义 token / 原子件（`docs/dev/design-system.md`）
- **每任务必须跑 typecheck**（vitest 不查类型）：`cd renderer && npx pnpm@9.0.0 exec tsc --noEmit` exit 0
- **逐文件 lint 零警告是迁移完成度的机械证明**：`cd renderer && npx pnpm@9.0.0 exec eslint src/components/<domain>/<File>.tsx src/components/<domain>/<File>.test.tsx` → 0 warning 0 error
- 测试断言更新原则：引用旧 class/emoji/字形文本的断言改为**语义查询**（getByRole/getByLabelText/结构断言），不得直接削弱断言
- 迁移期混合外观是 spec §12 接受项：p2p/（NodeDiscoveryPanel）与 workspace/（CreateWorkspaceDialog）虽渲染在设置页/标题栏附近，仍属 P3——本计划不得触碰
- `ws.iconEmoji`（workspace tab 里的 emoji）是**用户数据**（DB 存储、创建时选择），不是 UI 图标——保留渲染，不改数据流

## 颜色映射裁定表（纯样式任务的唯一依据）

**表面：**
| 旧 | 新 | 语境 |
|---|---|---|
| `bg-bg-primary` | `bg-canvas` | 主区容器（MiddlePanel/ActivityBar 最外层） |
| `bg-bg-secondary` | `bg-surface-1` | 标题栏/侧栏/卡片外框/浮层 |
| `bg-bg-tertiary` | `bg-surface-2` | 输入框/卡内嵌套/徽标底 |
| `hover:bg-bg-tertiary` / `hover:bg-bg-tertiary/60` | `hover:bg-surface-3` | hover 态一律 surface-3 |
| `bg-black/50` | `bg-backdrop` | 弹窗遮罩（Dialog 已内置，删手写遮罩） |

**文字（层级裁定原则：标题类〔h* 标签或 font-medium/semibold 近旁〕→ primary；正文/可操作 → secondary；说明/占位/时间戳 → tertiary）：**
| 旧 | 新 |
|---|---|
| `text-neutral-100` | `text-primary` |
| `text-neutral-200` | `text-primary`（标题近旁）或 `text-secondary`（正文） |
| `text-neutral-300` | `text-secondary` |
| `text-neutral-400` | `text-secondary`（可操作）或 `text-tertiary`（说明/占位） |
| `text-neutral-500` | `text-tertiary` |
| `text-neutral-600` | `text-disabled` |
| `text-white` | `text-inverse`（accent 底上）或 `text-primary`（普通 hover 提亮） |
| `text-red-400` / `hover:text-red-300` / `hover:text-red-400` | `text-status-error` / `hover:text-status-error` |
| `text-green-400` | `text-status-success` |
| `bg-green-500/15` | `bg-status-success-tint` |
| `bg-red-500/15` | `bg-status-error-tint` |
| `bg-yellow-500/10` + `text-yellow-300` | `bg-status-warning-tint` + `text-status-warning` |
| `text-accent-blue` | `text-accent-600 dark:text-accent-300` |
| `bg-accent-blue`（含 `/90` hover） | `bg-accent-500`（hover `bg-accent-500/90`） |
| `border-accent-blue` / `focus:border-accent-blue` | `border-accent-500` / `focus:border-focus` |
| `bg-accent-blue/15`（选中底） | `bg-surface-active` |
| `accent-blue-500` / `accent-accent-blue`（表单 accent-color） | 换 Checkbox 原子件 |

**边框：** `border-border-subtle`→`border-subtle`；`border-border-strong`→`border-strong`。**阴影** `shadow-xl`/`shadow-2xl` 保留。

---

### Task 1: 原子件加固——Button type 回归锁 + Segmented 键盘 roving

**Files:**
- Modify: `renderer/src/components/ui/Button.test.tsx`（+1 断言）
- Modify: `renderer/src/components/ui/Segmented.tsx`（键盘漫游）
- Modify: `renderer/src/components/ui/Segmented.test.tsx`（+2 用例）

**Interfaces:**
- Produces: Segmented 支持方向键漫游（ArrowLeft/Right/Up/Down 移动焦点并选中、roving tabindex 仅激活项可 Tab）——AppearanceSettings 自动受益，API 不变

- [ ] **Step 1: Button.test.tsx 加 type 默认锁**（在「disabled 透传」用例后追加）

```tsx
  it('type 默认 button（表单内不触发隐式 submit）', () => {
    render(<Button>保存</Button>);
    expect(screen.getByRole('button', { name: '保存' }).getAttribute('type')).toBe('button');
  });
```

- [ ] **Step 2: Segmented.test.tsx 追加键盘用例**

```tsx
  it('方向键右移：焦点与选中同步移动', () => {
    const onChange = vi.fn();
    render(<Segmented options={OPTIONS} value="light" onChange={onChange} aria-label="主题模式" />);
    fireEvent.keyDown(screen.getByRole('radiogroup', { name: '主题模式' }), { key: 'ArrowRight' });
    expect(onChange).toHaveBeenCalledWith('dark');
  });

  it('roving tabindex：仅激活项可 Tab', () => {
    render(<Segmented options={OPTIONS} value="dark" onChange={() => {}} aria-label="主题模式" />);
    expect(screen.getByRole('radio', { name: '深色' }).tabIndex).toBe(0);
    expect(screen.getByRole('radio', { name: '浅色' }).tabIndex).toBe(-1);
    expect(screen.getByRole('radio', { name: '跟随系统' }).tabIndex).toBe(-1);
  });
```

- [ ] **Step 3: 跑测试确认新用例失败**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/ui/Segmented.test.tsx src/components/ui/Button.test.tsx
```

Expected: Segmented 新 2 例 FAIL（无键盘处理/tabIndex 全 0）；Button 6/6 PASS。

- [ ] **Step 4: Segmented.tsx 加 roving（组件全量替换）**

```tsx
// renderer/src/components/ui/Segmented.tsx
// 分段单选控件（v2.1 设计系统原子件）：主题切换器等三态/二态选择。
// v2.1 P1：补 WAI-ARIA radiogroup 键盘语义——方向键漫游 + roving tabindex。
import { useRef } from 'react';
import { cn } from '../../lib/cn';

export interface SegmentedOption<T extends string> {
  value: T;
  label: string;
}

interface Props<T extends string> {
  options: readonly SegmentedOption<T>[];
  value: T;
  onChange: (value: T) => void;
  'aria-label'?: string;
  className?: string;
}

export function Segmented<T extends string>({
  options,
  value,
  onChange,
  className,
  ...rest
}: Props<T>) {
  const refs = useRef<Array<HTMLButtonElement | null>>([]);

  /** 方向键漫游：焦点与选中同步移到相邻（循环）选项 */
  const moveFocus = (dir: 1 | -1): void => {
    const idx = options.findIndex((o) => o.value === value);
    const next = (idx + dir + options.length) % options.length;
    // noUncheckedIndexedAccess：索引访问需收窄（next 恒存在，guard 仅满足类型层）
    const nextOpt = options[next];
    if (!nextOpt) return;
    refs.current[next]?.focus();
    onChange(nextOpt.value);
  };

  return (
    <div
      role="radiogroup"
      onKeyDown={(e) => {
        if (e.key === 'ArrowRight' || e.key === 'ArrowUp') {
          e.preventDefault();
          moveFocus(1);
        } else if (e.key === 'ArrowLeft' || e.key === 'ArrowDown') {
          e.preventDefault();
          moveFocus(-1);
        }
      }}
      className={cn(
        'inline-flex rounded-md border border-subtle bg-surface-2 p-0.5',
        className,
      )}
      {...rest}
    >
      {options.map((o, i) => {
        const active = o.value === value;
        return (
          <button
            key={o.value}
            ref={(el) => {
              refs.current[i] = el;
            }}
            type="button"
            role="radio"
            aria-checked={active}
            tabIndex={active ? 0 : -1}
            onClick={() => onChange(o.value)}
            className={cn(
              'h-7 rounded px-3 text-[13px] font-medium transition-colors',
              active
                ? 'bg-surface-active text-accent-600 dark:text-accent-300'
                : 'text-secondary hover:text-primary',
            )}
          >
            {o.label}
          </button>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 5: 跑测试 + typecheck + lint**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/ui/Segmented.test.tsx src/components/ui/Button.test.tsx && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/ui/Segmented.tsx src/components/ui/Segmented.test.tsx src/components/ui/Button.test.tsx
```

Expected: 全 PASS；typecheck 0；eslint 0 warning 0 error。

- [ ] **Step 6: Commit**

```bash
git add renderer/src/components/ui/Segmented.tsx renderer/src/components/ui/Segmented.test.tsx renderer/src/components/ui/Button.test.tsx
git commit -m "feat(renderer): Segmented 键盘 roving（方向键漫游）+ Button type 默认回归锁"
```

---

### Task 2: PromptDialog → Dialog 收敛 + Dialog 焦点修正

**Files:**
- Rewrite: `renderer/src/components/common/PromptDialog.tsx`
- Modify: `renderer/src/components/ui/Dialog.tsx`（焦点语义 + p-0 清理）
- Modify: `renderer/src/components/ui/Dialog.test.tsx`（+1 用例）

**Interfaces:**
- Produces: PromptDialog 外部 API 不变（title/label/defaultValue/placeholder/password/onSubmit/onClose），消费方（WorkspaceTabs 等）零改动
- Dialog 修正：挂载时若内部已有元素持有焦点（如 autoFocus 输入框）则不抢焦

- [ ] **Step 1: Dialog.test.tsx 追加焦点保持用例**

```tsx
  it('内部 autoFocus 元素已持焦时不抢焦', () => {
    render(
      <Dialog open onClose={() => {}} title="T">
        <input data-testid="inner" autoFocus />
      </Dialog>,
    );
    expect(document.activeElement).toBe(screen.getByTestId('inner'));
  });
```

- [ ] **Step 2: 跑测试确认失败**（现实现无条件 focus 容器）

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/ui/Dialog.test.tsx
```

- [ ] **Step 3: Dialog.tsx 修正两处**

effect 内（原 `dialogRef.current?.focus();` 替换）：

```tsx
    // 仅当内部无元素持焦时聚焦容器（autoFocus 输入框优先）；tabIndex=-1 使容器可编程聚焦
    if (dialogRef.current && !dialogRef.current.contains(document.activeElement)) {
      dialogRef.current.focus();
    }
```

className 删除死类 ` p-0`（原 `'fixed left-1/2 ... flex-col rounded-lg border border-strong bg-surface-1 p-0 shadow-2xl outline-none'` → 去掉 ` p-0`）。

- [ ] **Step 4: PromptDialog.tsx 全量替换**

```tsx
// renderer/src/components/common/PromptDialog.tsx
// Electron window.prompt 替代件（Electron 中 prompt 恒返回 null）。
// v2.1 P1：外壳收敛到 Dialog 原子件——遮罩/Esc/焦点语义统一。
import { useState, type FormEvent } from 'react';
import { Dialog } from '../ui/Dialog';
import { Button } from '../ui/Button';
import { Input } from '../ui/Input';

interface Props {
  title: string;
  label?: string;
  defaultValue?: string;
  placeholder?: string;
  password?: boolean;
  onSubmit: (value: string) => void;
  onClose: () => void;
}

export function PromptDialog({ title, label, defaultValue = '', placeholder, password, onSubmit, onClose }: Props) {
  const [value, setValue] = useState(defaultValue);

  const handleSubmit = (e: FormEvent) => {
    e.preventDefault();
    onSubmit(value);
  };

  return (
    <Dialog open onClose={onClose} title={title} width={320}>
      <form onSubmit={handleSubmit} className="flex flex-col gap-3">
        {label && <p className="text-xs text-secondary">{label}</p>}
        <Input
          value={value}
          onChange={(e) => setValue(e.target.value)}
          autoFocus
          placeholder={placeholder}
          type={password ? 'password' : 'text'}
        />
        <div className="flex justify-end gap-2">
          <Button type="button" variant="ghost" onClick={onClose}>
            取消
          </Button>
          <Button type="submit">确定</Button>
        </div>
      </form>
    </Dialog>
  );
}
```

- [ ] **Step 5: 验证链**（消费方 WorkspaceTabs 测试是关键回归面）

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/ui/Dialog.test.tsx src/components/layout/WorkspaceTabs.test.tsx && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/common/PromptDialog.tsx src/components/ui/Dialog.tsx src/components/ui/Dialog.test.tsx
```

Expected: 全 PASS（WorkspaceTabs 删除确认流走 PromptDialog→Dialog，role=dialog/placeholder/按钮文案不变）；lint 0/0。若 WorkspaceTabs.test 有断言因遮罩点击关闭行为差异失败：PromptDialog 旧行为是点遮罩关、Dialog 同语义，仅 DOM 层级变化——按语义查询修正断言并记录。

- [ ] **Step 6: Commit**

```bash
git add renderer/src/components/common/PromptDialog.tsx renderer/src/components/ui/Dialog.tsx renderer/src/components/ui/Dialog.test.tsx
git commit -m "refactor(renderer): PromptDialog 收敛到 Dialog 原子件 + Dialog 焦点保持修正"
```

---

### Task 3: ActivityBar lucide 化 + token 化（含 Sidebar/ViewSidebar 图标契约）

**Files:**
- Rewrite: `renderer/src/components/layout/ActivityBar.tsx`
- Modify: `renderer/src/components/layout/Sidebar.tsx`（icon: string → ReactNode）
- Modify: `renderer/src/components/layout/ViewSidebar.tsx`（VIEW_META 图标 lucide 化）

**Interfaces:**
- Produces: `SidebarProps.icon: ReactNode`；ActivityItem.icon 为 LucideIcon
- Consumes: lucide 图标名若 `SquareKanban` 不存在（TS 报错）则用 `KanbanSquare`（同一图标的别名导出），记录选择

- [ ] **Step 1: ActivityBar.tsx 全量替换**

```tsx
// renderer/src/components/layout/ActivityBar.tsx
//
// 活动栏：主区左侧固定图标列。5 个主项（会话/文件/看板/Agent/资源库）+ 底部设置项；
// 激活项左侧 3px accent 指示条 + surface-active 选中底。宽度用 inline style。
// v2.1 P1：emoji 图标全部 lucide 化，样式全 token 化。
import type { LucideIcon } from 'lucide-react';
import { MessageSquare, Folder, SquareKanban, Bot, Library, Settings } from 'lucide-react';
import { cn } from '../../lib/cn';
import { useUiStore, type ViewKey } from '../../stores/ui.store';

interface ActivityItem {
  key: ViewKey;
  icon: LucideIcon;
  label: string;
}

const MAIN_ITEMS: ActivityItem[] = [
  { key: 'im', icon: MessageSquare, label: '会话' },
  { key: 'files', icon: Folder, label: '文件' },
  { key: 'tasks', icon: SquareKanban, label: '看板' },
  { key: 'agents', icon: Bot, label: 'Agent' },
  { key: 'marketplace', icon: Library, label: '资源库' },
];

const SETTINGS_ITEM: ActivityItem = { key: 'settings', icon: Settings, label: '设置' };

export function ActivityBar() {
  const activeView = useUiStore((s) => s.activeView);
  const setActiveView = useUiStore((s) => s.setActiveView);

  return (
    <nav
      className="shrink-0 border-r border-subtle bg-surface-1 flex flex-col items-center py-2.5 gap-1"
      style={{ width: 48 }}
      aria-label="活动栏"
    >
      {MAIN_ITEMS.map((item) => (
        <ActivityButton
          key={item.key}
          item={item}
          active={activeView === item.key}
          onSelect={setActiveView}
        />
      ))}
      <div className="flex-1" />
      <ActivityButton
        item={SETTINGS_ITEM}
        active={activeView === SETTINGS_ITEM.key}
        onSelect={setActiveView}
      />
    </nav>
  );
}

interface ActivityButtonProps {
  item: ActivityItem;
  active: boolean;
  onSelect: (view: ViewKey) => void;
}

function ActivityButton({ item, active, onSelect }: ActivityButtonProps) {
  const Icon = item.icon;
  return (
    <button
      type="button"
      aria-label={item.label}
      aria-current={active ? 'page' : undefined}
      title={item.label}
      onClick={() => onSelect(item.key)}
      className={cn(
        'relative flex w-full flex-col items-center rounded-lg transition-colors',
        active
          ? 'bg-surface-active text-accent-600 dark:text-accent-300'
          : 'text-tertiary hover:bg-surface-3 hover:text-primary',
      )}
      style={{ padding: '6px 0 5px', gap: 3 }}
    >
      {active && (
        <span
          data-testid="activity-indicator"
          className="absolute bg-accent-500"
          style={{ left: 0, top: 8, bottom: 8, width: 3, borderRadius: 2 }}
        />
      )}
      <Icon size={17} strokeWidth={1.75} aria-hidden />
      <span style={{ fontSize: 9.5, lineHeight: 1 }}>{item.label}</span>
    </button>
  );
}
```

- [ ] **Step 2: Sidebar.tsx 图标契约改 ReactNode**

L6 import 区加 `import type { ReactNode } from 'react';`（合并进现有 type import 行）；L11 `icon: string;` → `icon: ReactNode;`；L32 `<span style={{ fontSize: 17, lineHeight: 1 }}>{icon}</span>` → `<span style={{ lineHeight: 1 }}>{icon}</span>`；容器/按钮 class 按映射表：L22 `bg-bg-secondary border-r border-border-subtle` → `border-r border-subtle bg-surface-1`；L30 `text-neutral-400 hover:text-neutral-100 hover:bg-bg-tertiary/60` → `text-tertiary hover:bg-surface-3 hover:text-primary`；L41 同容器替换。

- [ ] **Step 3: ViewSidebar.tsx VIEW_META lucide 化**

L7 后加 `import { MessageSquare, Folder, SquareKanban } from 'lucide-react';`；ViewMeta 接口 `icon: string` → `icon: ReactNode`（`import type { ReactNode } from 'react'`）；L25-27 三项 → `im: { icon: <MessageSquare size={17} strokeWidth={1.75} aria-hidden />, label: '会话' }` 等三行同构（Folder / SquareKanban，与 ActivityBar 同名图标保持一致）。

- [ ] **Step 4: 验证链**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/layout/ && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/layout/ActivityBar.tsx src/components/layout/Sidebar.tsx src/components/layout/ViewSidebar.tsx
```

Expected: layout 测试全 PASS（若有断言引用 emoji 文本，按语义查询修正）；lint 0/0。

- [ ] **Step 5: Commit**

```bash
git add renderer/src/components/layout/ActivityBar.tsx renderer/src/components/layout/Sidebar.tsx renderer/src/components/layout/ViewSidebar.tsx
git commit -m "refactor(renderer): ActivityBar 图标 lucide 化 + 侧栏图标契约 ReactNode + token 化"
```

---

### Task 4: TitleBar lucide 化 + token 化

**Files:**
- Rewrite: `renderer/src/components/layout/TitleBar.tsx`

- [ ] **Step 1: 全量替换**

```tsx
// renderer/src/components/layout/TitleBar.tsx
//
// 顶部标题栏：Logo + workspace tabs + 拖拽区 + 窗口控件。
// win/linux 自绘窗口控件（lucide 图标）；mac 留给原生红绿灯（不渲染 Logo 与控件）。
// v2.1 P1：字形图标（◉▢❐─✕）全部 lucide 化，样式 token 化（品牌渐变改 accent 实底）。
import { useEffect, useState } from 'react';
import { Minus, Square, Copy, X, Sparkles } from 'lucide-react';
import { isMac, dragStyle, noDragStyle } from '../../lib/platform';
import { WorkspaceTabs } from './WorkspaceTabs';
import { ipc } from '../../ipc/client';
import { cn } from '../../lib/cn';

export function TitleBar() {
  const mac = isMac();
  const [maximized, setMaximized] = useState(false);

  useEffect(() => {
    // 初始查询一次 + 订阅主进程 maximize/unmaximize 推送（切换 还原/最大化 图标）
    void ipc.window.isMaximized().then(setMaximized);
    return ipc.window.onMaximizedChanged(setMaximized);
  }, []);

  return (
    <header
      className="h-10 shrink-0 flex items-center gap-1 border-b border-subtle bg-surface-1 px-2 select-none"
      style={dragStyle}
    >
      {!mac && (
        <span className="flex items-center gap-1.5 pl-1 pr-2 text-xs text-secondary">
          <span
            className="flex items-center justify-center rounded-md bg-accent-500 text-inverse"
            style={{ width: 18, height: 18 }}
          >
            <Sparkles size={11} strokeWidth={2} aria-hidden />
          </span>
          Momo Studio
        </span>
      )}
      {/* mac 原生红绿灯占位 ~78x28，把第一个 tab 让出来避免被遮 */}
      {mac && <div style={{ width: 78 }} className="shrink-0" />}
      <WorkspaceTabs />
      {/* tabs 与窗口控件之间的空白拖拽区 */}
      <div className="flex-1 h-full" />
      {!mac && (
        <div className="flex h-full" style={noDragStyle}>
          <WinCtlBtn ariaLabel="最小化" onClick={() => ipc.window.minimize()}>
            <Minus size={12} strokeWidth={1.75} aria-hidden />
          </WinCtlBtn>
          <WinCtlBtn
            ariaLabel={maximized ? '还原' : '最大化'}
            onClick={() => ipc.window.toggleMaximize()}
          >
            {maximized ? (
              <Copy size={12} strokeWidth={1.75} aria-hidden />
            ) : (
              <Square size={12} strokeWidth={1.75} aria-hidden />
            )}
          </WinCtlBtn>
          <WinCtlBtn ariaLabel="关闭" danger onClick={() => ipc.window.close()}>
            <X size={12} strokeWidth={1.75} aria-hidden />
          </WinCtlBtn>
        </div>
      )}
    </header>
  );
}

interface WinCtlBtnProps {
  ariaLabel: string;
  onClick: () => void;
  danger?: boolean;
  children: React.ReactNode;
}

/** 自绘窗口控制按钮（44px 宽，hover 高亮；关闭按钮 hover 红） */
function WinCtlBtn({ ariaLabel, onClick, danger, children }: WinCtlBtnProps) {
  return (
    <button
      type="button"
      aria-label={ariaLabel}
      title={ariaLabel}
      onClick={onClick}
      className={cn(
        'flex h-full w-11 items-center justify-center text-secondary hover:text-primary',
        danger ? 'hover:bg-status-error hover:text-inverse' : 'hover:bg-surface-3',
      )}
    >
      {children}
    </button>
  );
}
```

- [ ] **Step 2: 验证链**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/layout/TitleBar.test.tsx && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/layout/TitleBar.tsx
```

Expected: PASS；若 TitleBar.test 断言旧字形文本（─/▢/❐/✕/◉），改为 `getByRole('button', { name: '最小化' })` 等语义查询；lint 0/0。

- [ ] **Step 3: Commit**

```bash
git add renderer/src/components/layout/TitleBar.tsx
git commit -m "refactor(renderer): TitleBar 窗口控件与 Logo lucide 化 + token 化"
```

---

### Task 5: WorkspaceTabs token 化

**Files:**
- Modify: `renderer/src/components/layout/WorkspaceTabs.tsx`

- [ ] **Step 1: 逐处替换**（行号以当前文件为准）

1. L7 import 区加：`import { X, Plus } from 'lucide-react';`
2. L118-123 tab class：active `'bg-bg-tertiary text-neutral-100'` → `'bg-surface-2 text-primary'`；inactive `'text-neutral-400 hover:text-neutral-200'` → `'text-secondary hover:text-primary'`
3. L148 重命名 input：`bg-bg-primary border border-accent-blue rounded px-1 text-xs text-neutral-100 outline-none` → `rounded border border-accent-500 bg-surface-2 px-1 text-xs text-primary outline-none`
4. L157-167 关闭按钮：`×` 文本 → `<X size={12} strokeWidth={1.75} aria-hidden />`；class `opacity-0 group-hover:opacity-100 px-0.5 text-neutral-400 hover:text-neutral-200` → `opacity-0 group-hover:opacity-100 px-0.5 text-tertiary hover:text-primary`
5. L169 underline：`bg-accent-blue` → `bg-accent-500`
6. L178-186 新建按钮：`＋` 文本 → `<Plus size={14} strokeWidth={1.75} aria-hidden />`；class `text-neutral-400 hover:bg-bg-tertiary hover:text-white` → `text-secondary hover:bg-surface-3 hover:text-primary`
7. L193 右键菜单浮层：`bg-bg-secondary border border-border-subtle shadow-xl` → `border border-subtle bg-surface-1 shadow-xl`
8. L198/L208 菜单项：`text-neutral-300 hover:bg-bg-tertiary` → `text-secondary hover:bg-surface-3`（两处）
9. L218 删除项：`text-status-error hover:bg-bg-tertiary` → `text-status-error hover:bg-surface-3`

注意：`{ws.iconEmoji}`（L153）是用户数据——**保留不动**。

- [ ] **Step 2: 验证链**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/layout/WorkspaceTabs.test.tsx && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/layout/WorkspaceTabs.tsx
```

Expected: PASS（断言按语义查询修正：如 getByRole('button', { name: '新建工作空间' })）；lint 0/0。

- [ ] **Step 3: Commit**

```bash
git add renderer/src/components/layout/WorkspaceTabs.tsx
git commit -m "refactor(renderer): WorkspaceTabs token 化（×/＋ lucide 化、菜单浮层、下划线）"
```

---

### Task 6: MainLayout + MiddlePanel token 化（EmptyState 接入）

**Files:**
- Modify: `renderer/src/components/layout/MainLayout.tsx`
- Modify: `renderer/src/components/layout/MiddlePanel.tsx`

- [ ] **Step 1: MainLayout.tsx**：L68 `bg-bg-primary` → `bg-canvas`

- [ ] **Step 2: MiddlePanel.tsx 逐处替换**

1. L5 import 区加：`import { Folder } from 'lucide-react';` 与 `import { EmptyState } from '../ui/EmptyState';`
2. L41-47 无 workspace 引导 → EmptyState：

```tsx
  if (!workspace) {
    return (
      <div className="flex flex-1 items-center justify-center bg-canvas">
        <EmptyState
          icon={Folder}
          title="创建或选择一个工作空间开始"
          description="工作空间是 agent 协作与文件沙箱的根目录"
        />
      </div>
    );
  }
```

3. L66 会话头部：`border-b border-border-subtle bg-bg-secondary` → `border-b border-subtle bg-surface-1`
4. L67 会话名：`text-sm text-neutral-100` → `text-sm text-primary`
5. L118-121 未知视图兜底：外层 `text-neutral-500` → 容器加 `bg-canvas`、文字 `text-tertiary`

- [ ] **Step 3: 验证链**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/layout/ && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/layout/MainLayout.tsx src/components/layout/MiddlePanel.tsx
```

Expected: 全 PASS；lint 0/0。

- [ ] **Step 4: Commit**

```bash
git add renderer/src/components/layout/MainLayout.tsx renderer/src/components/layout/MiddlePanel.tsx
git commit -m "refactor(renderer): MainLayout/MiddlePanel token 化 + 空状态接入 EmptyState"
```

---

### Task 7: SettingsNav / SettingsView 外壳 token 化

**Files:**
- Modify: `renderer/src/components/settings/SettingsNav.tsx`
- Modify: `renderer/src/components/settings/SettingsView.tsx`

- [ ] **Step 1: SettingsNav.tsx**（P0 遗留旧 class + 头注释清理）

1. 头注释 L1 `// renderer/src/components/settings/SettingsNav.tsx 全文替换：` → `// renderer/src/components/settings/SettingsNav.tsx`（P0 遗留指令措辞清理）
2. L26 容器：`border-r border-border-subtle bg-bg-secondary` → `border-r border-subtle bg-surface-1`
3. L29 分组标签：`text-neutral-500` → `text-tertiary`
4. L37-39 按钮：active `'bg-bg-tertiary text-neutral-100'` → `'bg-surface-active text-primary'`；inactive `'text-neutral-300 hover:bg-bg-tertiary/60'` → `'text-secondary hover:bg-surface-3'`

- [ ] **Step 2: SettingsView.tsx**

1. L41 无 workspace 提示：`text-neutral-500` → `text-tertiary`（容器加 `bg-canvas`）
2. L50 顶栏：`border-b border-border-subtle bg-bg-secondary` → `border-b border-subtle bg-surface-1`
3. L51-58 返回按钮：`text-neutral-300 hover:text-neutral-100 ... hover:bg-bg-tertiary/60` → `text-secondary hover:bg-surface-3 hover:text-primary`
4. L59 标题：`text-neutral-100` → `text-primary`

- [ ] **Step 3: 验证链**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/settings/SettingsView.test.tsx src/components/settings/AppearanceSettings.test.tsx && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/settings/SettingsNav.tsx src/components/settings/SettingsView.tsx
```

Expected: PASS；lint 0/0。

- [ ] **Step 4: Commit**

```bash
git add renderer/src/components/settings/SettingsNav.tsx renderer/src/components/settings/SettingsView.tsx
git commit -m "refactor(renderer): SettingsNav/SettingsView 外壳 token 化（选中态 surface-active）"
```

---

### Task 8: ProviderDialog → Dialog 收敛

**Files:**
- Rewrite: `renderer/src/components/settings/ProviderDialog.tsx`

- [ ] **Step 1: 全量替换**（保留全部业务逻辑与文案；外壳换 Dialog；表单控件换原子件）

```tsx
// renderer/src/components/settings/ProviderDialog.tsx
// 添加供应商对话框（仅用于创建；编辑在 ProviderSettings 右列配置卡）。
// v2.1 P1：外壳收敛到 Dialog 原子件；表单控件换 Input/Select/Checkbox/Button。
import { useState, type FormEvent } from 'react';
import { ipc } from '../../ipc/client';
import type { ModelProvider, ProviderPlatform } from '../../ipc/types';
import { Dialog } from '../ui/Dialog';
import { Button } from '../ui/Button';
import { Input } from '../ui/Input';
import { Select } from '../ui/Select';
import { Checkbox } from '../ui/Checkbox';

interface Props {
  open: boolean;
  onClose: () => void;
  onSaved: (created: ModelProvider) => void;
}

export function ProviderDialog({ open, onClose, onSaved }: Props) {
  const [name, setName] = useState('');
  const [platform, setPlatform] = useState<ProviderPlatform>('openai');
  const [baseUrl, setBaseUrl] = useState('');
  const [apiKey, setApiKey] = useState('');
  const [isDefault, setIsDefault] = useState(false);
  const [testing, setTesting] = useState(false);
  const [testResult, setTestResult] = useState<string | null>(null);
  const [saving, setSaving] = useState(false);

  const handleTest = async (): Promise<void> => {
    setTesting(true);
    setTestResult(null);
    try {
      // model 留空——后端返回「请先填写模型名或在模型服务页拉取模型列表」
      const r = await ipc.provider.testConnection({ baseUrl, apiKey, model: '' });
      // r.error 是 string | undefined（IPC 类型），null 收窄需要兜底文案
      setTestResult(r.ok ? '连接成功' : r.error ?? '连接失败');
    } catch (e) {
      setTestResult(e instanceof Error ? e.message : String(e));
    } finally {
      setTesting(false);
    }
  };

  const handleSubmit = async (e: FormEvent): Promise<void> => {
    e.preventDefault();
    setSaving(true);
    try {
      const created = await ipc.provider.create({ name, baseUrl, apiKey, platform, isDefault });
      onSaved(created);
      onClose();
    } catch (err) {
      alert(`保存失败：${err instanceof Error ? err.message : String(err)}`);
    } finally {
      setSaving(false);
    }
  };

  if (!open) return null;

  return (
    <Dialog open onClose={onClose} title="添加供应商" width={420}>
      <form onSubmit={handleSubmit} className="flex flex-col gap-3">
        <Input
          label="名称"
          value={name}
          onChange={(e) => setName(e.target.value)}
          required
        />
        <Select
          label="平台"
          value={platform}
          onChange={(e) => setPlatform(e.target.value as ProviderPlatform)}
        >
          <option value="openai">OpenAI 兼容</option>
          <option value="anthropic">Anthropic</option>
        </Select>
        <Input
          label="Base URL"
          value={baseUrl}
          onChange={(e) => setBaseUrl(e.target.value)}
          required
          placeholder="https://open.bigmodel.cn/api/coding/paas/v4"
        />
        <Input
          label="API Key"
          type="password"
          value={apiKey}
          onChange={(e) => setApiKey(e.target.value)}
          required
        />
        <Checkbox
          label="设为默认供应商"
          checked={isDefault}
          onChange={(e) => setIsDefault(e.target.checked)}
        />
        <div className="flex items-center gap-2">
          <Button
            type="button"
            variant="secondary"
            size="sm"
            onClick={handleTest}
            disabled={testing || !apiKey || !baseUrl}
          >
            {testing ? '测试中…' : '测试连接'}
          </Button>
          {testResult && <span className="text-xs text-secondary">{testResult}</span>}
        </div>
        <div className="flex justify-end gap-2">
          <Button type="button" variant="ghost" onClick={onClose}>
            取消
          </Button>
          <Button type="submit" disabled={saving}>
            {saving ? '保存中…' : '保存'}
          </Button>
        </div>
      </form>
    </Dialog>
  );
}
```

注：测试结果文案去掉 ✅/❌ 前缀（改为由文字本身表达；如需状态色后续用 Badge）。原 `if (!open) return null` 保留在 hooks 之后（Dialog 自身也判 open，双保险无副作用）。

- [ ] **Step 2: 验证链**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/settings/ProviderDialog.test.tsx src/components/settings/ProviderSettings.test.tsx && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/settings/ProviderDialog.tsx
```

Expected: PASS（ProviderDialog.test 若断言 `✅ 连接成功`/`❌` 文本前缀或 role=dialog 的 DOM 层级，按新文案/语义查询修正——断言意图不变：成功/失败结果可见、对话框可提交）；lint 0/0。

- [ ] **Step 3: Commit**

```bash
git add renderer/src/components/settings/ProviderDialog.tsx
git commit -m "refactor(renderer): ProviderDialog 收敛到 Dialog + 表单原子件化"
```

---

### Task 9: ProviderSettings token 化 + Star 图标

**Files:**
- Modify: `renderer/src/components/settings/ProviderSettings.tsx`

- [ ] **Step 1: 逐处替换**

1. L5 import 区加：`import { Star, Plus } from 'lucide-react';`
2. L61 aside：`border border-border-subtle rounded-lg bg-bg-secondary` → `rounded-lg border border-subtle bg-surface-1`
3. L63 列表头计数：`text-neutral-400` → `text-secondary`
4. L64-65 添加按钮：`＋` 文本 → `<Plus size={12} strokeWidth={1.75} aria-hidden />`；class `border border-border-subtle text-neutral-300 hover:bg-bg-tertiary` → `border border-subtle text-secondary hover:bg-surface-3`
5. L69 空态文案：`text-neutral-500` → `text-tertiary`
6. L73-75 供应商行：selected `'bg-accent-blue/15 text-neutral-100'` → `'bg-surface-active text-primary'`；inactive `'text-neutral-300 hover:bg-bg-tertiary'` → `'text-secondary hover:bg-surface-3'`；模板字符串改 cn() 组合
7. L76 默认标记：`<span title="默认供应商" className="shrink-0">⭐</span>` → `<Star size={12} strokeWidth={1.75} aria-hidden fill="currentColor" className="shrink-0 text-accent-500" />`（title 语义改到行按钮上已有的 name；保留 title 属性加到 Star 外层 span：`<span title="默认供应商" className="shrink-0 text-accent-500"><Star … /></span>`）
8. L78 模型数徽标：`bg-bg-tertiary text-neutral-400` → `bg-surface-2 text-secondary`
9. L99 右列空态：`text-neutral-500` → `text-tertiary`（边框 `border-border-subtle` → `border-subtle`）
10. L139/L141 testResult：`'✅ 连接成功'` → `'连接成功'`；`` `❌ ${r.error}` `` → `r.error ?? '连接失败'`（IPC 类型 error?: string 同 Task 8 根因）；展示 span 加 `className={cn('text-xs', r.ok ? 'text-status-success' : 'text-status-error')}`——需把 r 存 state（改为 `setTestResult(r.ok ? { ok: true, text: '连接成功' } : { ok: false, text: r.error ?? '连接失败' })`，类型 `useState<{ ok: boolean; text: string } | null>`；catch 分支 `{ ok: false, text: e instanceof Error ? e.message : String(e) }`）
11. L171 配置卡标题：`text-neutral-100` → `text-primary`
12. L173-174 设为默认：`⭐ 设为默认` → `<Star size={12} strokeWidth={1.75} aria-hidden /> 设为默认`；class `text-neutral-400 hover:text-neutral-200 disabled:opacity-40` → `inline-flex items-center gap-1 text-secondary hover:text-primary disabled:opacity-40`
13. L175-176 删除：`text-red-400 hover:text-red-300` → `text-status-error hover:text-status-error`
14. L169 配置卡容器：`border border-border-subtle rounded-lg bg-bg-secondary` → `rounded-lg border border-subtle bg-surface-1`
15. L180-197 四个 label/输入/下拉（名称/平台/BaseURL/APIKey）：label `text-neutral-400` → `text-secondary`；input `bg-bg-tertiary border border-border-subtle rounded ... text-neutral-100` → `rounded border border-subtle bg-surface-2 ... text-primary`（四处同构）
16. L200-203 检查连接：`border border-border-subtle text-neutral-300 hover:bg-bg-tertiary` → `border border-subtle text-secondary hover:bg-surface-3`
17. L206-209 保存：`bg-accent-blue text-white` → `bg-accent-500 text-inverse`

- [ ] **Step 2: 验证链**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/settings/ProviderSettings.test.tsx && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/settings/ProviderSettings.tsx
```

Expected: PASS（测试中 `⭐` 断言按 P0 Task 6 模式改语义查询/svg 断言：如 nav/列表行 `getByTitle('默认供应商')` 或行内 svg 数量）；lint 0/0。

- [ ] **Step 3: Commit**

```bash
git add renderer/src/components/settings/ProviderSettings.tsx
git commit -m "refactor(renderer): ProviderSettings token 化 + 默认标记 Star 化 + 测试结果状态色"
```

---

### Task 10: ProviderModelList token 化 + 图标

**Files:**
- Modify: `renderer/src/components/settings/ProviderModelList.tsx`

- [ ] **Step 1: 逐处替换**

1. L8 import 区加：`import { RefreshCw, Plus } from 'lucide-react';` 与 `import { Checkbox } from '../ui/Checkbox';`、`import { Button } from '../ui/Button';`
2. L89 容器：`border border-border-subtle rounded-lg bg-bg-secondary` → `rounded-lg border border-subtle bg-surface-1`
3. L91 标题：`text-neutral-100` → `text-primary`
4. L93-96 拉取按钮：`↻ 获取模型列表` → `<RefreshCw size={12} strokeWidth={1.75} aria-hidden /> 获取模型列表`（按钮加 `inline-flex items-center gap-1`）；class `border border-border-subtle text-neutral-300 hover:bg-bg-tertiary` → `border border-subtle text-secondary hover:bg-surface-3`
5. L97-100 手动添加：`＋ 手动添加` → `<Plus … /> 手动添加` 同上处理
6. L106-112 内联输入：`bg-bg-tertiary border border-border-subtle ... text-neutral-100` → `rounded border border-subtle bg-surface-2 ... text-primary`；L113 添加提交 → `<Button type="submit" size="sm">添加</Button>`
7. L114-115 取消 → `<Button type="button" variant="ghost" size="sm">取消</Button>`
8. L119 error：`text-red-400` → `text-status-error`
9. L120/L122/L215 说明：`text-neutral-500` → `text-tertiary`（文案中「↻」「＋」引用改为「获取模型列表」「手动添加」）
10. L128 行分割：`border-border-subtle` → `border-subtle`；L129-135 启用 checkbox（accent-blue-500 原生）→ `<Checkbox checked={m.enabled} onChange={() => void handleToggle(m)} aria-label={`启用 ${m.modelId}`} />`（Checkbox 无 label 时输出裸盒，aria-label 经 rest 透传）
11. L136 code 颜色：`text-neutral-200 : text-neutral-500 line-through` → `text-primary : text-disabled line-through`
12. L139-140 删除：`text-neutral-400 hover:text-red-400` → `text-tertiary hover:text-status-error`

- [ ] **Step 2: 验证链**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/settings/ProviderModelList.test.tsx && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/settings/ProviderModelList.tsx
```

Expected: PASS；lint 0/0。

- [ ] **Step 3: Commit**

```bash
git add renderer/src/components/settings/ProviderModelList.tsx
git commit -m "refactor(renderer): ProviderModelList token 化 + 图标化 + Checkbox 原子件接入"
```

---

### Task 11: DefaultModelSettings 图标 lucide 化 + 原子件接入

**Files:**
- Modify: `renderer/src/components/settings/DefaultModelSettings.tsx`

- [ ] **Step 1: 逐处替换**

1. L10 import 区加：`import type { LucideIcon } from 'lucide-react';`、`import { MessageSquare, Eye, Dna, ArrowLeftRight } from 'lucide-react';`、`import { Select } from '../ui/Select';`、`import { Button } from '../ui/Button';`
2. CardSpec L16 `icon: string` → `icon: LucideIcon`；CARDS 四项：💬→MessageSquare / 👁→Eye / 🧬→Dna / 🔀→ArrowLeftRight
3. L149 标题 `text-neutral-100` → `text-primary`；L150 说明 `text-neutral-400` → `text-secondary`
4. L152 error `text-red-400` → `text-status-error`
5. L155 无供应商空态：`border-border-subtle ... text-neutral-500` → `border-subtle ... text-tertiary`（改用 EmptyState 原子件：icon=Library、title=暂无供应商…，import Library）
6. L168 卡容器：`border border-border-subtle rounded-lg bg-bg-secondary` → `rounded-lg border border-subtle bg-surface-1`
7. L171 `<span className="text-base">{card.icon}</span>` → `<card.icon size={16} strokeWidth={1.75} aria-hidden className="shrink-0" />`
8. L172 卡标题 `text-neutral-100` → `text-primary`
9. L174 未来徽标：`bg-yellow-500/10 text-yellow-300` → `bg-status-warning-tint text-status-warning`
10. L176/L180/L195/L214 说明文字 `text-neutral-500`/`text-neutral-400` → `text-tertiary`
11. L182-210 两个 select：label `text-[11px] text-neutral-400` → `text-[11px] text-secondary`；select 元素换 `<Select aria-label="供应商" … className="mt-1">`（Select 自带 w-full；disabled 透传；options children 原样）
12. L219-220 清除按钮：`text-neutral-400 hover:text-neutral-200 disabled:opacity-40` → `<Button type="button" variant="ghost" size="sm" disabled={!pick}>清除</Button>`
13. L221-225 保存：`bg-accent-blue text-white disabled:opacity-50` → `<Button type="button" size="sm" disabled={...} onClick={...}>`（保留 savingField 文案逻辑）
14. L226 已保存：`text-green-400` → `text-status-success`

- [ ] **Step 2: 验证链**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/settings/ && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/settings/DefaultModelSettings.tsx
```

Expected: settings 域测试全 PASS；lint 0/0。

- [ ] **Step 3: Commit**

```bash
git add renderer/src/components/settings/DefaultModelSettings.tsx
git commit -m "refactor(renderer): DefaultModelSettings 四卡图标 lucide 化 + Select/Button/EmptyState 接入"
```

---

### Task 12: ConversationSettings + GitPolicySettings token 化

**Files:**
- Modify: `renderer/src/components/settings/ConversationSettings.tsx`
- Modify: `renderer/src/components/settings/GitPolicySettings.tsx`

- [ ] **Step 1: ConversationSettings**

1. import 区加 Button/Input：`import { Button } from '../ui/Button';`、`import { Input } from '../ui/Input';`
2. L37 加载中：`text-neutral-400` → `text-secondary`
3. L42 标题 `text-neutral-100` → `text-primary`
4. L44 label `text-neutral-300` → `text-secondary`；L46-53 number input → `<Input type="number" … style 移除，className 用原子件默认 + style={{ width: 128 }} 保留>`（Input 透传 min/onChange/value）
5. L55 说明 `text-neutral-500` → `text-tertiary`
6. L63-69 保存按钮 → `<Button type="submit" disabled={saving}>`；L70 已保存 `text-green-400` → `text-status-success`

- [ ] **Step 2: GitPolicySettings**

1. import 区加：`import { Select } from '../ui/Select';`、`import { Checkbox } from '../ui/Checkbox';`、`import { X, CircleCheck, CircleX } from 'lucide-react';`
2. L114 加载中 `text-neutral-500` → `text-tertiary`
3. L123/L126 标题与说明：`text-neutral-200` → `text-primary`；`text-neutral-500` → `text-tertiary`
4. L136 error `text-red-400` → `text-status-error`
5. L139-147 允许 commit 开关：原生 checkbox（accent-accent-blue）→ `<Checkbox label="允许 agent 自动 commit" checked={draft.allowAgentCommits} onChange={...} />`
6. L165 校验级别 label `text-neutral-300` → `text-secondary`；L166-178 select → `<Select label="Commit 校验级别" …>`（children options 原样）
7. L189 预览 label 同上；L190-196 textarea：`bg-bg-tertiary border border-border-subtle text-neutral-100 focus:border-accent-blue` → `border border-subtle bg-surface-2 text-primary focus:border-focus`
8. L198-207 预览结果：`bg-green-500/15 text-green-400` → `bg-status-success-tint text-status-success`；`bg-red-500/15 text-red-400` → `bg-status-error-tint text-status-error`；`✓ 合规` → `<CircleCheck size={12} strokeWidth={1.75} aria-hidden /> 合规`；`✗ ${error}` → `<CircleX size={12} strokeWidth={1.75} aria-hidden /> ${error}`（容器已有 inline-flex items-center gap-1）
9. L241 无 pattern 说明 `text-neutral-600` → `text-disabled`
10. L248 pattern 项容器：`bg-bg-tertiary border border-border-subtle` → `border border-subtle bg-surface-2`
11. L250 code 色：`text-accent-blue` → `text-accent-600 dark:text-accent-300`
12. L252-254 行内文字：`text-neutral-200` → `text-primary`；`text-neutral-500`（两处）→ `text-tertiary`
13. L256-262 删除按钮：`✕` 文本 → `<X size={12} strokeWidth={1.75} aria-hidden />`；class `text-neutral-500 hover:text-red-400` → `text-tertiary hover:text-status-error`
14. L268 添加表单容器：`bg-bg-tertiary border border-border-subtle` → `border border-subtle bg-surface-2`

- [ ] **Step 3: 验证链**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/settings/ && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/settings/ConversationSettings.tsx src/components/settings/GitPolicySettings.tsx
```

（注：GitPolicySettings 无专属测试文件——以 settings 域套件 + 全量回归为门禁。）Expected: PASS；lint 0/0。

- [ ] **Step 4: Commit**

```bash
git add renderer/src/components/settings/ConversationSettings.tsx renderer/src/components/settings/GitPolicySettings.tsx
git commit -m "refactor(renderer): Conversation/GitPolicy 设置 token 化 + 原子件接入"
```

---

### Task 13: AuditLog + About token 化（配额条 class 化）

**Files:**
- Modify: `renderer/src/components/settings/AuditLog.tsx`
- Modify: `renderer/src/components/settings/About.tsx`

- [ ] **Step 1: AuditLog 逐处替换**

1. L137 标题 `text-neutral-200` → `text-primary`；L138 说明 `text-neutral-500` → `text-tertiary`
2. L142 配额卡容器：`border border-border-subtle` → `border border-subtle`
3. L145/L188/L200 筛选 label `text-neutral-400` → `text-secondary`
4. L153/L196/L208 三个筛选输入：`bg-bg-tertiary border border-border-subtle ... text-neutral-100 focus:border-accent-blue` → `border border-subtle bg-surface-2 ... text-primary focus:border-focus`
5. L163 quotaError `text-red-400` → `text-status-error`；L164 cleanupMsg `text-green-400` → `text-status-success`
6. L167 进度条槽：`bg-bg-tertiary` → `bg-surface-2`
7. **L169-176 配额条（inline hex 消除）**：

```tsx
              <div
                data-testid="audit-quota-bar"
                className={cn(
                  'h-2 rounded transition-width',
                  usedPct >= 95 ? 'bg-status-error' : 'bg-accent-500',
                )}
                style={{ width: `${usedPct}%` }}
              />
```

   （import 区加 cn：`import { cn } from '../../lib/cn';`；删除原注释「宽度/颜色用 inline style」）
8. L178 配额说明 `text-neutral-500` → `text-tertiary`
9. L216 error `text-red-400` → `text-status-error`
10. L219 表格容器 `border-border-subtle` → `border-subtle`；L221 表头 `bg-bg-secondary text-neutral-400` → `bg-surface-1 text-secondary`
11. L233/L239 空态 `text-neutral-500` → `text-tertiary`
12. L245 行 hover `hover:bg-bg-tertiary` → `hover:bg-surface-3`；L246-250 单元格 `text-neutral-300`/`text-neutral-200` → `text-secondary`/`text-primary`
13. L252-258 成功/失败徽标：`bg-green-500/15 text-green-400` → `bg-status-success-tint text-status-success`；`bg-red-500/15 text-red-400` → `bg-status-error-tint text-status-error`（拼接字符串改 cn()）
14. L263 耗时 `text-neutral-400` → `text-secondary`；L274 分页 `text-neutral-500` → `text-tertiary`

- [ ] **Step 2: About 逐处替换**

1. L27 error `text-red-400` → `text-status-error`；L28 加载中 `text-neutral-400` → `text-secondary`
2. L43 标题 `text-neutral-100` → `text-primary`；L44 说明 `text-neutral-400` → `text-secondary`
3. L46 信息卡：`border border-border-subtle rounded-lg bg-bg-secondary` → `rounded-lg border border-subtle bg-surface-1`
4. L50/L51 行：`text-neutral-500` → `text-tertiary`；`text-neutral-100` → `text-primary`
5. L56 脚注 `text-neutral-500` → `text-tertiary`

- [ ] **Step 3: 验证链**

```bash
cd renderer && npx pnpm@9.0.0 vitest run src/components/settings/AuditLog.test.tsx && npx pnpm@9.0.0 vitest run src/components/settings/ && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/settings/AuditLog.tsx src/components/settings/About.tsx
```

Expected: PASS（AuditLog.test 若断言配额条 inline backgroundColor hex 值，改为断言语义 class：`toHaveClass('bg-status-error')`/`'bg-accent-500'`）；lint 0/0。

- [ ] **Step 4: Commit**

```bash
git add renderer/src/components/settings/AuditLog.tsx renderer/src/components/settings/About.tsx
git commit -m "refactor(renderer): AuditLog/About token 化——配额条 inline hex 收敛为语义 class"
```

---

### Task 14: UpgradeNotice token 化

**Files:**
- Modify: `renderer/src/components/upgrade/UpgradeNotice.tsx`

- [ ] **Step 1: 逐处替换**

1. import 区加：`import { X } from 'lucide-react';`、`import { Button } from '../ui/Button';`
2. L23 卡片：`border border-border-subtle bg-bg-secondary shadow-xl ... text-neutral-200` → `border border-subtle bg-surface-1 shadow-xl ... text-secondary`
3. L26 标题 `text-white` → `text-primary`
4. L27-34 关闭按钮：`×` 文本 → `<X size={14} strokeWidth={1.75} aria-hidden />`；class `text-neutral-500 hover:text-neutral-300` → `text-tertiary hover:text-primary`
5. L36 说明 `text-neutral-400` → `text-secondary`
6. L42 导出目录 code：`bg-bg-primary border border-border-subtle ... text-neutral-300` → `border border-subtle bg-canvas ... text-secondary`
7. L48-54 知道了 → `<Button onClick={onDismiss}>知道了</Button>`

- [ ] **Step 2: 验证链**

```bash
cd renderer && npx pnpm@9.0.0 test && npx pnpm@9.0.0 exec tsc --noEmit && npx pnpm@9.0.0 exec eslint src/components/upgrade/UpgradeNotice.tsx
```

Expected: 全量 PASS；lint 0/0。

- [ ] **Step 3: Commit**

```bash
git add renderer/src/components/upgrade/UpgradeNotice.tsx
git commit -m "refactor(renderer): UpgradeNotice token 化 + Button/图标原子件化"
```

---

### Task 15: 文档勘正——README 任意值表述 + design-system 补 Dialog 指引

**Files:**
- Modify: `/workspace/README.md`（已知限制 + 技术债务表）
- Modify: `/workspace/docs/dev/design-system.md`（追加 Dialog 消费方指引小节）

- [ ] **Step 1: README 两处勘正**

1. 「已知限制」第二条「**Tailwind 任意值 class（如 `max-w-[70%]`）不生成 CSS**——宽度约束需用 inline style…」整条替换为：

```markdown
- **Tailwind 任意值 class 仅静态书写时保证生成**——运行时动态拼接的 class（模板字符串插值）JIT 扫描器不可见、对应 CSS 不生成（v2.1 P0 探针实测结论）。静态任意值（`max-w-[70%]`、`text-[13px]`）可正常使用；规范见 `docs/dev/design-system.md` §5。
```

2. 「技术债务跟踪」表首行「Tailwind 任意值 class 不生成 CSS」行的「影响」列同步改为「已定位根因：动态拼接 class 不可见（静态书写正常）；规范已禁动态拼接」，状态列改为「已于 v2.1 P0 勘正」。

- [ ] **Step 2: design-system.md 追加 Dialog 指引**（文末追加）

```markdown
## 6. Dialog 消费方指引（P1 起生效）

- `onClose` 请传**稳定引用**（useCallback 或模块级函数）——内联箭头函数会因 Dialog 的 effect 依赖在父组件每次渲染时重挂监听并重新抢焦
- Dialog 的 Esc 关闭与 SettingsView 的全局 Esc 返回会**同时触发**：设置页内的弹窗消费方应在自身的 keydown 处理中 `stopPropagation` 或先关弹窗再由状态判定是否返回
- Dialog 不内置焦点陷阱（focus trap）与滚动锁定；多层弹窗叠加时一个 Esc 会关闭所有层（各层独立监听）——需要单层关闭语义时由消费方管理层级
```

- [ ] **Step 3: 全量测试零回归 + Commit**

```bash
cd renderer && npx pnpm@9.0.0 test
git add README.md docs/dev/design-system.md
git commit -m "docs: README 任意值 class 表述勘正（P0 探针结论）+ design-system 补 Dialog 指引"
```

---

### Task 16: P1 收官验证

**Files:**
- 无新文件（验证任务；若范围扫描发现 routes/App 外壳裸色号，作为本任务修复项）

- [ ] **Step 1: 四域 + 外壳范围 lint 零警告**

```bash
cd renderer && npx pnpm@9.0.0 exec eslint src/components/layout src/components/settings src/components/upgrade src/components/common --ext .ts,.tsx 2>&1 | tail -5
```

Expected: 0 warning 0 error。随后范围确认（P3 域不得提前动）：

```bash
cd renderer && npx pnpm@9.0.0 exec eslint src/routes src/App.tsx 2>&1 | tail -3
```

若 MainShell/App 存在裸色号警告（外壳归属 P1 精神）：按映射表就地修复（预计 <5 处），随收官 commit。

- [ ] **Step 2: 类型检查 + 全量测试 + 构建**

```bash
cd /workspace && npx pnpm@9.0.0 typecheck && npx pnpm@9.0.0 test && npx pnpm@9.0.0 build
```

Expected: 双 typecheck clean；electron + renderer 全绿；build exit 0。

- [ ] **Step 3: xvfb 冒烟（浅色默认态）**

```bash
ELECTRON_DISABLE_SANDBOX=1 timeout 120 xvfb-run -a --server-args="-screen 0 1280x800x24" npx pnpm@9.0.0 dev
```

Expected: vite ready + Window ready，exit 124；无 CSP 违规、无 Uncaught。遇到 AGENTS.md 陷阱表问题按表处置（僵尸进程清理 / electron-rebuild 后**须 rebuild 回 Node ABI** 再跑测试）。

- [ ] **Step 4: 计数与收尾**

```bash
cd renderer && npx pnpm@9.0.0 exec eslint src 2>&1 | grep -c warning || true
git status
```

Expected: 剩余 warning 全部位于 P2/P3 域（im/agent/resource-library/task-board/files/editor/p2p/workspace）——记录总数为 P2 预算基线；工作树净（.superpowers/ 遗留除外）。

---

## 计划自审记录

- **Spec 覆盖**（spec §8 P1 行）：layout/（T3-6：ActivityBar/TitleBar/WorkspaceTabs/MainLayout/MiddlePanel/Sidebar/ViewSidebar）✓；settings/（T7-13 十文件全覆盖，AppearanceSettings 已于 P0 完成）✓；upgrade/（T14）✓；common/（T2 PromptDialog→Dialog）✓；弹窗收敛（T2/T8：common+settings 域两个手写弹窗；workspace 域 CreateWorkspaceDialog 属 P3）✓；验收线「四域零裸色号、零 emoji」由 T16 Step1 机械证明 ✓。
- **终审 triage 移交项**：Segmented 键盘 roving（T1）✓；Button type 锁（T1）✓；README 勘正（T15）✓；Dialog 焦点语义/消费方指引（T2+T15）✓；p-0 死类（T2）✓；SettingsNav 注释残留（T7）✓；浅色默认走查（T16 Step3 浅色冒烟 + 主机人工项延续）✓。
- **占位符扫描**：无 TBD；结构任务全代码；样式任务为逐行旧→新规格 + 映射表，行号基于迁移前文件（若实现中发现行号漂移，以就近同名旧串定位，规格值不变）。
- **类型一致性**：`LucideIcon`（T3/T11）、`ReactNode` Sidebar icon（T3）、Select/Checkbox/Button/Input 原子件 API 均为 P0 已落地签名。
- **有意排除**：p2p/NodeDiscoveryPanel（渲染于设置页但目录属 P3）、workspace/CreateWorkspaceDialog（渲染于 TitleBar 附近但目录属 P3）、`ws.iconEmoji`（用户数据非 UI 图标）——均记录于 Global Constraints。
