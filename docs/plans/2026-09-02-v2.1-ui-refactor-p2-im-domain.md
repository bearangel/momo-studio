# v2.1 UI 设计系统重构 · P2 会话域 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** im/ 域（26 生产组件）完成设计系统迁移——状态色四份调色板收敛到 task-status、全部 inline-hex/emoji 消除、四个手写弹窗收敛 Dialog、markdown 挂 md-body、Monaco 双主题——使会话界面在明暗两模式下成立。

**Architecture:** P0/P1 产出（语义 token、12 原子件、Dialog capture-Esc、task-status.ts、md-body CSS）直接消费。本计划是**存量迁移**：im/ 的三 Chip/流式组件是 inline-style hex 重灾区 → 全量重写给完整代码；弹窗四件 → Dialog 收敛给完整代码；纯 class 清扫给行号规格。机械完成度 = 逐文件 ESLint 零警告。

**Tech Stack:** React 18 + TypeScript strict + Tailwind 语义 token + lucide-react + zustand + Vitest（jsdom）

**Spec:** `docs/specs/2026-09-01-v2.1-ui-design-system-refactor-design.md`（§8 P2 行 + P0/P1 终审移交项）
**依赖:** ui/ 12 原子件、`lib/task-status.ts`、`lib/cn.ts`、globals.css `.md-body` 规则、theme.store

## Global Constraints

- Node 20：`export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && nvm use 20`（每 shell 前置）
- pnpm 经 `npx pnpm@9.0.0`；**每任务必须 typecheck**：`cd renderer && npx pnpm@9.0.0 exec tsc --noEmit` exit 0
- **逐文件 lint 零警告**是完成度机械证明：`cd renderer && npx pnpm@9.0.0 exec eslint src/components/im/<File>.tsx [对应 .test.tsx]` → 0/0
- TypeScript strict + `noUncheckedIndexedAccess`（索引访问收窄）；禁 `any`/`@ts-ignore`/`as any`
- 注释中文；Conventional Commits；测试断言改语义查询不削弱
- **用户数据 emoji 豁免**：`iconEmoji`/`subAgentAvatar` 等来自 DB 的 emoji 是数据不是 UI 图标，保留渲染；UI 图标位的 emoji（⏳✅📌📑💭等）全部换 lucide
- 表单控件优先原子件（Input/Select/Checkbox/Button）；弹窗外壳一律 Dialog 原子件
- 零 inline hex（rgb/rgba 字面量随重写消灭；`md-body` 内样式由 globals.css 承担）
- P3 域（agent/task-board/files/p2p/workspace 资源）文件不得触碰；CodeEditor.tsx 仅做主题接线（tab 栏 token 化留 P3）

## 颜色映射裁定表（与 P1 一致，im/ 特定裁定加注）

沿用 P1 计划的映射表（`bg-bg-secondary→bg-surface-1`、`bg-bg-tertiary→bg-surface-2`、`hover:bg-bg-tertiary→hover:bg-surface-3`、neutral 层级裁定、`text-white→text-inverse/primary`、`text-accent-blue→text-accent-600 dark:text-accent-300`、`bg-accent-blue/20 选中→bg-surface-active`、green/red→status 对、`bg-black/50 遮罩→Dialog 内置 bg-backdrop`）。im/ 特定：

| 场景 | 裁定 |
|---|---|
| inline hex 状态色（#4ade80/#f87171/#fbbf24/#888 等） | 按 tone 收敛：成功=success、失败=error、等待/执行=warning、灰=neutral——一律走 `task-status.ts` / `dispatchStatusStyle` / Badge，禁止重造映射 |
| #60a5fa（流式光标/streaming） | `bg-accent-500` |
| #444/#333/#374151 边框 | `border-strong`（卡内分割）/ `border-subtle`（外框）按层级 |
| #1f2937/#111827 弹窗底 | Dialog 原子件（bg-surface-1） |
| rgba(0,0,0,0.2) 卡内底 | `bg-surface-2` 或 `bg-black/20`（气泡内嵌套块，暗色语义弱底——用 `bg-canvas/60`? **裁定：嵌套代码/详情块用 `bg-surface-2`**） |
| 💭 思考区 indigo rgba(99,102,241,0.1) | `bg-status-violet-tint` + `text-status-violet` |
| accent-purple（DispatchCard 调度） | `bg-status-violet-tint` + `text-status-violet`（border `border-status-violet/40`） |
| `bg-bg-quaternary`（TaskReplyCard，未定义 token） | `bg-surface-3` |
| 弹出层（mention 菜单/预算面板/成员浮层） | `border-subtle bg-surface-1 shadow-lg`（对齐 P1 WorkspaceTabs 菜单）+ 行 hover `hover:bg-surface-3` |

---

### Task 1: 原子件吸收与加固（P0/P1 终审移交项）

**Files:**
- Modify: `renderer/src/components/ui/EmptyState.tsx`（role 透传）
- Modify: `renderer/src/components/ui/EmptyState.test.tsx`（+1 用例）
- Modify: `renderer/src/components/settings/DefaultModelSettings.tsx`（空态恢复 role="status"）
- Modify: `renderer/src/components/ui/Segmented.test.tsx`（+2 断言/用例）
- Modify: `renderer/src/components/settings/ProviderDialog.test.tsx`（+1 失败路径用例）
- Modify: `docs/dev/design-system.md`（§6 第一条软化）

- [ ] **Step 1: EmptyState 加 role 透传**（Props 加 `role?: 'status'`，spread 到根 div——aria 属性透传即可：`<div role={role} ...>`；ts: role 限定 `'status' | undefined`）。测试 +1：

```tsx
  it('role 透传：role=status 时可被 role 查询命中', () => {
    render(<EmptyState icon={Inbox} title="空" role="status" />);
    expect(screen.getByRole('status')).toBeInTheDocument();
  });
```

- [ ] **Step 2: DefaultModelSettings 无供应商空态恢复语义**：`<EmptyState icon={Library} ...>` 加 `role="status"`（P0 迁移时丢失的原语义）。

- [ ] **Step 3: Segmented 焦点/单选项锁**：键盘用例补 `expect(screen.getByRole('radio', { name: '深色' })).toHaveFocus();`（方向键后焦点与选中同步）；+1 单选项用例：

```tsx
  it('单选项边界：Arrow 循环回自身且不崩溃', () => {
    const single = [{ value: 'only', label: '唯一' }] as const;
    const onChange = vi.fn();
    render(<Segmented options={single} value="only" onChange={onChange} aria-label="单选" />);
    fireEvent.keyDown(screen.getByRole('radiogroup', { name: '单选' }), { key: 'ArrowRight' });
    expect(onChange).toHaveBeenCalledWith('only');
  });
```

- [ ] **Step 4: ProviderDialog 失败路径锁**（`?? '连接失败'` 兜底 + ok:false 文案）：

```tsx
  it('测试连接失败：显示后端错误文案', async () => {
    window.api = { ...window.api, provider: { ...mockProvider, testConnection: vi.fn().mockResolvedValue({ ok: false, error: '连接超时' }) } };
    render(<ProviderDialog open onClose={() => {}} onSaved={() => {}} />);
    fireEvent.change(screen.getByLabelText('Base URL'), { target: { value: 'https://x' } });
    fireEvent.change(screen.getByLabelText(/API Key/), { target: { value: 'k' } });
    fireEvent.click(screen.getByRole('button', { name: '测试连接' }));
    expect(await screen.findByText('连接超时')).toHaveClass('text-status-error');
  });
```

（mock 形状对齐该测试文件既有 mock——实现者按文件现状适配 mock 结构，断言意图不变：error 文案 + 状态色。）

- [ ] **Step 5: design-system.md §6 第一条软化**：原文「`onClose` 请传稳定引用…重新抢焦」改为「P1 起 Dialog 已原子件级防抢焦（内部已持焦时不抢）+ Esc capture 阻断双触发；稳定引用（useCallback）仍是推荐写法但不再是硬性要求」。

- [ ] **Step 6: 验证链**：`vitest run src/components/ui/ src/components/settings/` 全绿 + tsc 0 + eslint 六文件 0/0。
- [ ] **Step 7: Commit** `feat(renderer): 原子件吸收项——EmptyState role 透传 + Segmented/ProviderDialog 回归锁补全`

---

### Task 2: task-status.ts 派生 TaskStatus + dispatchStatusStyle 扩展

**Files:**
- Modify: `renderer/src/lib/task-status.ts`
- Modify: `renderer/src/lib/task-status.test.ts`

**Interfaces:**
- Produces: `TaskStatusKey` 改为派生自 `types.d.ts` 的 `TaskStatus`（`import type { TaskStatus } from '../ipc/types'`，`export type TaskStatusKey = TaskStatus`——P0 终审「类型收窄」处方，消除双声明漂移）
- Produces: `type DispatchStatus = 'queued' | 'executing' | 'completed' | 'failed' | 'aborted'` + `dispatchStatusStyle(status: DispatchStatus): { label; tone: BadgeTone; className }`（Task 5 DispatchChip 消费）

- [ ] **Step 1: 测试先行**——task-status.test.ts 改 import 并 +2 用例：

```ts
import type { TaskStatus } from '../ipc/types';
// 类型层派生锁：TaskStatusKey 与 ipc TaskStatus 是同一类型
const _typeLock: TaskStatusKey = 'draft' as TaskStatus;
void _typeLock;

describe('dispatchStatusStyle', () => {
  it('五状态 tone 映射：queued=neutral/executing=warning/completed=success/failed=error/aborted=warning', () => {
    expect(dispatchStatusStyle('queued').tone).toBe('neutral');
    expect(dispatchStatusStyle('executing').tone).toBe('warning');
    expect(dispatchStatusStyle('completed').tone).toBe('success');
    expect(dispatchStatusStyle('failed').tone).toBe('error');
    expect(dispatchStatusStyle('aborted').tone).toBe('warning');
  });
  it('className 与 Badge tone 同源', () => {
    expect(dispatchStatusStyle('failed').className).toContain('bg-status-error-tint');
    expect(dispatchStatusStyle('executing').className).toContain('bg-status-warning-tint');
  });
});
```

- [ ] **Step 2: 确认 FAIL** → **Step 3: 实现**——task-status.ts 顶部改 import/派生；文件尾部追加：

```ts
export type DispatchStatus = 'queued' | 'executing' | 'completed' | 'failed' | 'aborted';

const DISPATCH_LABEL: Record<DispatchStatus, string> = {
  queued: '排队',
  executing: '执行中',
  completed: '完成',
  failed: '失败',
  aborted: '已中断',
};

/** dispatch 委派状态 tone（旧 DispatchChip STATUS_CONFIG 的 hex 收敛） */
const DISPATCH_TONE: Record<DispatchStatus, BadgeTone> = {
  queued: 'neutral',
  executing: 'warning',
  completed: 'success',
  failed: 'error',
  aborted: 'warning',
};

export function dispatchStatusStyle(status: DispatchStatus): TaskStatusStyle {
  const tone = DISPATCH_TONE[status];
  return {
    label: DISPATCH_LABEL[status],
    tone,
    className: `inline-flex h-5 items-center gap-1 rounded px-2 text-xs font-medium ${BADGE_TONE_CLASSES[tone]}`,
  };
}
```

（`TaskStatusStyle` 接口已有；返回类型复用。）注意：TaskChip 现以 `STATUS_COLOR[task.status] ?? '#888'` 消费——Task 3 重写后才切换；本任务不动消费方。

- [ ] **Step 4: 验证**：vitest task-status 全绿 + tsc 0 + eslint 0/0（含类型锁证明派生成功）。
- [ ] **Step 5: Commit** `feat(renderer): task-status 派生 ipc TaskStatus + dispatchStatusStyle 五态映射`

---

### Task 3: TaskChip 重写（task-status 接线）

**Files:**
- Rewrite: `renderer/src/components/im/TaskChip.tsx`
- Modify: `renderer/src/components/im/TaskChip.test.tsx`（如存在——按 Explore 无；以 RoomList/MessageList 测试覆盖回归）

- [ ] **Step 1: 全量替换**

```tsx
// renderer/src/components/im/TaskChip.tsx
//
// #T-XXX 任务 chip：任务编号 + 短标题，点击跳转。颜色走 task-status 统一映射
// （v2.1：原 inline hex STATUS_COLOR 调色板退役，与 TaskCard/DispatchChip 同源）。
import { Hash } from 'lucide-react';
import { cn } from '../../lib/cn';
import { taskStatusStyle } from '../../lib/task-status';
import type { TaskRow } from '../../ipc/types';

interface TaskChipProps {
  task: Pick<TaskRow, 'id' | 'title' | 'status'>;
  onClick?: (taskId: string) => void;
}

export function TaskChip({ task, onClick }: TaskChipProps) {
  const status = taskStatusStyle(task.status);
  const truncated = task.title.length > 12 ? task.title.slice(0, 12) + '...' : task.title;
  return (
    <button
      type="button"
      onClick={() => onClick?.(task.id)}
      title={task.title}
      className={cn('inline-flex items-center gap-1 text-xs', status.className)}
    >
      <Hash size={11} strokeWidth={2} aria-hidden />
      <span className="font-mono">{task.id}</span>
      <span className="max-w-[140px] truncate">{truncated}</span>
    </button>
  );
}
```

（📌→Hash；border/hex 全撤——tone className 自带 tint 底。）
- [ ] **Step 2: 验证**：`vitest run src/components/im/` 全绿（消费方测试断言旧 DOM 的按语义适配：chip 按钮可按 title 定位）+ tsc + eslint 0/0。
- [ ] **Step 3: Commit** `refactor(renderer): TaskChip 接线 task-status——inline hex 调色板退役`

---

### Task 4: ToolCallChip 重写（三态 tint + lucide）

**Files:**
- Rewrite: `renderer/src/components/im/ToolCallChip.tsx`

- [ ] **Step 1: 全量替换**

```tsx
// renderer/src/components/im/ToolCallChip.tsx
//
// 工具调用卡片：紧凑一行（工具名 + 参数摘要 + 状态图标 + 耗时），点击展开
// 参数 JSON 与结果。三态 tint（执行中 warning / 成功 success / 失败 error）
// 与 Badge tone 同源（v2.1：inline hex STATUS_COLOR/STATUS_BG 退役）。
import { useState } from 'react';
import { CircleCheck, CircleX, ChevronDown, ChevronRight, Loader2 } from 'lucide-react';
import { cn } from '../../lib/cn';

interface Props {
  toolName: string;
  args: Record<string, unknown>;
  result?: string;
  success: boolean;
  durationMs?: number;
  isExecuting?: boolean;
  defaultExpanded?: boolean;
}

/** 把参数对象格式化为单行摘要，最多 60 字符 */
function summarizeArgs(args: Record<string, unknown>): string {
  return Object.entries(args)
    .map(([k, v]) => `${k}: ${typeof v === 'string' ? v : JSON.stringify(v)}`)
    .join(', ')
    .slice(0, 60);
}

export function ToolCallChip({
  toolName,
  args,
  result,
  success,
  durationMs,
  isExecuting,
  defaultExpanded,
}: Props) {
  const [expanded, setExpanded] = useState(defaultExpanded ?? false);

  const toneCls = isExecuting
    ? 'bg-status-warning-tint text-status-warning'
    : success
      ? 'bg-status-success-tint text-status-success'
      : 'bg-status-error-tint text-status-error';
  const StatusIcon = isExecuting ? Loader2 : success ? CircleCheck : CircleX;

  return (
    <div className="mb-1">
      <button
        type="button"
        aria-expanded={expanded}
        onClick={() => setExpanded(!expanded)}
        className={cn(
          'flex w-full items-center gap-1.5 rounded px-2 py-1 text-left text-xs transition-colors',
          toneCls,
        )}
      >
        <StatusIcon size={12} strokeWidth={1.75} aria-hidden className={isExecuting ? 'animate-spin' : undefined} />
        <span className="font-mono text-primary">{toolName}</span>
        {summarizeArgs(args) && (
          <span className="truncate text-[11px] text-tertiary" title={summarizeArgs(args)}>
            {summarizeArgs(args)}
          </span>
        )}
        <span className="ml-auto shrink-0 text-[11px] text-tertiary">
          {isExecuting ? '执行中...' : durationMs !== undefined ? `${durationMs}ms` : ''}
        </span>
        {expanded ? (
          <ChevronDown size={12} strokeWidth={1.75} aria-hidden />
        ) : (
          <ChevronRight size={12} strokeWidth={1.75} aria-hidden />
        )}
      </button>
      {expanded && (
        <div className="md-body mt-1 max-h-[300px] overflow-auto whitespace-pre-wrap rounded border border-subtle bg-surface-2 p-2 font-mono text-[11px] text-secondary">
          <div>参数: {JSON.stringify(args, null, 2)}</div>
          {result !== undefined && <div className="mt-1">结果: {result}</div>}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 验证**：`vitest run src/components/im/`（ToolCallChip 消费方在 AgentStreamBubble/SubAgentSection 测试内——断言 `⏳/✓/✗` 文本的按 svg/aria-expanded 语义适配）+ tsc + eslint 0/0。
- [ ] **Step 3: Commit** `refactor(renderer): ToolCallChip 三态 tint 化——lucide 状态图标 + 展开详情 token`

---

### Task 5: DispatchChip 重写（dispatchStatusStyle + lucide 全量化）

**Files:**
- Rewrite: `renderer/src/components/im/DispatchChip.tsx`
- Modify: `renderer/src/components/im/DispatchChip.test.tsx`（6 处 emoji/颜色断言语义适配）

- [ ] **Step 1: 全量替换**

```tsx
// renderer/src/components/im/DispatchChip.tsx
//
// dispatch 委派 chip：紧凑头行展示子 agent 委派状态，点击展开 SubAgentSection。
// v2.1：状态走 dispatchStatusStyle（tone 同源 Badge）；图标全 lucide；
// 活动提示/计时器/展开箭头去字形。自动展开语义（userToggled）不变。
import { useEffect, useState } from 'react';
import type { LucideIcon } from 'lucide-react';
import { Brain, ChevronDown, ChevronRight, CircleCheck, CircleSlash, CircleX, Clock, Loader2, PenLine, Send, Timer, Wrench } from 'lucide-react';
import type { StreamState } from '../../stores/stream.store';
import { Avatar } from '../ui/Avatar';
import { dispatchStatusStyle, type DispatchStatus } from '../../lib/task-status';
import { SubAgentSection } from './SubAgentSection';

export interface DispatchChild {
  subStreamSessionId: string;
  subAgentName: string;
  subAgentAvatar?: string;
  status: DispatchStatus;
}

interface DispatchChipProps {
  child: DispatchChild;
  subStream?: StreamState;
}

/** 状态图标：executing 用旋转 Loader（活动指示） */
const STATUS_ICON: Record<DispatchStatus, LucideIcon> = {
  queued: Clock,
  executing: Loader2,
  completed: CircleCheck,
  failed: CircleX,
  aborted: CircleSlash,
};

/** 各状态的自动展开默认值（用户未手动 toggle 前） */
const AUTO_EXPANDED: Record<DispatchStatus, boolean> = {
  executing: true,
  failed: true,
  completed: false,
  queued: false,
  aborted: false,
};

/** 活动提示：最后内容段 → 小图标 + 文案 */
function deriveActivity(subStream: StreamState | undefined): { icon: LucideIcon; text: string } | null {
  if (!subStream) return { icon: Clock, text: '已派出，等待启动…' };
  const last = subStream.segments[subStream.segments.length - 1];
  if (!last) return { icon: Clock, text: '已启动…' };
  switch (last.kind) {
    case 'thinking':
      return { icon: Brain, text: '思考中…' };
    case 'tool_call':
      return { icon: Wrench, text: last.result === null ? last.toolName : `${last.toolName}（完成）` };
    case 'text':
      return { icon: PenLine, text: '输出中…' };
    case 'dispatch':
      return null;
  }
}

/** 每秒跳动的耗时显示（executing 期间） */
function ElapsedTimer({ since }: { since: number }) {
  const [now, setNow] = useState(() => Date.now());
  useEffect(() => {
    const timer = setInterval(() => setNow(Date.now()), 1000);
    return () => clearInterval(timer);
  }, []);
  const seconds = Math.max(0, Math.floor((now - since) / 1000));
  const text = seconds >= 60 ? `${Math.floor(seconds / 60)}m${String(seconds % 60).padStart(2, '0')}s` : `${seconds}s`;
  return (
    <span className="inline-flex items-center gap-0.5 text-tertiary">
      <Timer size={11} strokeWidth={1.75} aria-hidden />
      {text}
    </span>
  );
}

export function DispatchChip({ child, subStream }: DispatchChipProps) {
  const status = dispatchStatusStyle(child.status);
  const StatusIcon = STATUS_ICON[child.status];
  const isBusy = child.status === 'executing' || child.status === 'queued';
  const activity = isBusy ? deriveActivity(subStream) : null;
  const timerSince = isBusy && subStream ? subStream.startedAt : null;

  const [expanded, setExpanded] = useState(() => AUTO_EXPANDED[child.status]);
  const [userToggled, setUserToggled] = useState(false);

  useEffect(() => {
    if (!userToggled) {
      setExpanded(AUTO_EXPANDED[child.status]);
    }
  }, [child.status, userToggled]);

  const handleToggle = () => {
    setUserToggled(true);
    setExpanded((prev) => !prev);
  };

  return (
    <div className="mb-1">
      <button
        type="button"
        aria-expanded={expanded}
        onClick={handleToggle}
        className="flex w-full items-center gap-1.5 rounded border border-subtle bg-surface-2 px-2 py-1 text-left text-xs transition-colors hover:bg-surface-3"
      >
        <Send size={12} strokeWidth={1.75} aria-hidden className="shrink-0 text-accent-500" />
        {child.subAgentAvatar ? (
          <span aria-hidden className="shrink-0">{child.subAgentAvatar}</span>
        ) : (
          <Avatar name={child.subAgentName} bot size="sm" />
        )}
        <span className="shrink-0 text-primary">{child.subAgentName}</span>
        <span className={status.className}>
          <StatusIcon size={11} strokeWidth={1.75} aria-hidden className={child.status === 'executing' ? 'animate-spin' : undefined} />
          {status.label}
        </span>
        {activity && (
          <span className="inline-flex items-center gap-0.5 text-[11px] text-tertiary" data-testid="dispatch-activity">
            <activity.icon size={11} strokeWidth={1.75} aria-hidden />
            {activity.text}
          </span>
        )}
        {timerSince !== null && <ElapsedTimer since={timerSince} />}
        <span className="ml-auto shrink-0 text-tertiary" aria-hidden>
          {expanded ? <ChevronDown size={12} strokeWidth={1.75} /> : <ChevronRight size={12} strokeWidth={1.75} />}
        </span>
      </button>

      {expanded && subStream && <SubAgentSection stream={subStream} />}
    </div>
  );
}
```

（📤→Send；avatar 数据 emoji 豁免保留、缺省走 Avatar bot；「便于测试断言颜色」注释随 hex 撤销而删。）
- [ ] **Step 2: 测试适配**：DispatchChip.test 断言 `✅ 完成`/`⏳ 执行中` 文本或 toHaveStyle('#4ade80') 的 → 改 `getByTestId('dispatch-activity')` 仍在 + 状态行 `getByText('完成')` + `toHaveClass('bg-status-success-tint')`（六处逐一映射，断言意图不变：状态可见且着色正确）。
- [ ] **Step 3: 验证**：vitest im/ 全绿 + tsc + eslint 0/0。
- [ ] **Step 4: Commit** `refactor(renderer): DispatchChip 接线 dispatchStatusStyle——emoji/字形/inline hex 全清`

---

### Task 6: ThinkingSection + TodoSection 重写

**Files:**
- Rewrite: `renderer/src/components/im/ThinkingSection.tsx`
- Rewrite: `renderer/src/components/im/TodoSection.tsx`

- [ ] **Step 1: ThinkingSection 全量替换**

```tsx
// renderer/src/components/im/ThinkingSection.tsx
//
// AI 思考过程折叠区：默认折叠，展开渲染 Markdown。思考区用 violet tint
// （v2.1：原 indigo inline hex 退役）。空内容不渲染。
import { useState } from 'react';
import { Brain, ChevronDown, ChevronRight } from 'lucide-react';
import ReactMarkdown from 'react-markdown';
import remarkGfm from 'remark-gfm';

interface Props {
  content: string;
  isStreaming?: boolean;
}

export function ThinkingSection({ content, isStreaming }: Props) {
  const [expanded, setExpanded] = useState(false);
  void isStreaming; // 占位，后续流式动画预留

  if (!content) return null;

  return (
    <div className="mb-2">
      <button
        type="button"
        aria-expanded={expanded}
        onClick={() => setExpanded(!expanded)}
        className="flex w-full items-center gap-1.5 rounded bg-status-violet-tint px-2 py-1 text-xs text-status-violet"
      >
        <Brain size={12} strokeWidth={1.75} aria-hidden />
        <span>思考过程</span>
        <span className="ml-auto" aria-hidden>
          {expanded ? <ChevronDown size={12} strokeWidth={1.75} /> : <ChevronRight size={12} strokeWidth={1.75} />}
        </span>
      </button>
      {expanded && (
        <div className="md-body mt-1 max-h-[400px] overflow-auto rounded bg-surface-2 p-2 text-xs text-secondary">
          <ReactMarkdown remarkPlugins={[remarkGfm]}>{content}</ReactMarkdown>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: TodoSection 全量替换**

```tsx
// renderer/src/components/im/TodoSection.tsx
//
// 任务面板：TodoItem[] 可折叠列表（含进度）。流式默认展开、结束自动折叠。
// v2.1：inline hex/字形退役（📋→ListTodo；✓▶○→lucide；▼▶→Chevron）。
import { useEffect, useState } from 'react';
import { Check, ChevronDown, ChevronRight, Circle, ListTodo, Play } from 'lucide-react';
import { cn } from '../../lib/cn';
import type { TodoItem } from '../../ipc/types';

interface Props {
  todos: TodoItem[];
  isStreaming: boolean;
}

export function TodoSection({ todos, isStreaming }: Props) {
  const [expanded, setExpanded] = useState(isStreaming);

  useEffect(() => {
    if (!isStreaming) setExpanded(false);
  }, [isStreaming]);

  if (todos.length === 0) return null;

  const doneCount = todos.filter((t) => t.status === 'completed').length;
  const totalCount = todos.length;
  const progressPct = Math.round((doneCount / totalCount) * 100);

  return (
    <div className="my-2 overflow-hidden rounded border border-subtle text-[13px]">
      <button
        type="button"
        onClick={() => setExpanded(!expanded)}
        className="flex w-full items-center justify-between bg-surface-2 px-3 py-1.5"
      >
        <span className="inline-flex items-center gap-1.5 font-medium text-primary">
          <ListTodo size={13} strokeWidth={1.75} aria-hidden />
          任务
        </span>
        <span className="inline-flex items-center gap-1 text-xs text-tertiary">
          {doneCount}/{totalCount}（{progressPct}%）
          {expanded ? <ChevronDown size={12} strokeWidth={1.75} aria-hidden /> : <ChevronRight size={12} strokeWidth={1.75} aria-hidden />}
        </span>
      </button>
      {expanded && (
        <ul className="m-0 list-none px-3 py-2">
          {todos.map((t, i) => {
            const Icon = t.status === 'completed' ? Check : t.status === 'in_progress' ? Play : Circle;
            return (
              <li
                key={t.id}
                className={cn(
                  'flex gap-2',
                  t.status === 'completed' ? 'line-through opacity-60' : '',
                  t.status === 'in_progress' ? 'font-medium text-accent-600 dark:text-accent-300' : 'text-secondary',
                  i === todos.length - 1 ? '' : 'mb-1',
                )}
              >
                <Icon size={12} strokeWidth={1.75} aria-hidden className="mt-0.5 shrink-0" />
                <span>
                  {i + 1}. {t.subject}
                </span>
              </li>
            );
          })}
        </ul>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 验证**：vitest im/（TodoSection 测试断言 `text-blue-400` 的 → `text-accent-600`；`✓/▶/○` 文本的 → svg 语义）+ tsc + eslint 0/0。
- [ ] **Step 4: Commit** `refactor(renderer): Thinking/Todo 折叠区 token 化——violet tint 思考区 + lucide 状态图标`

---

### Task 7: SubAgentSection + SegmentStack token 化

**Files:**
- Modify: `renderer/src/components/im/SubAgentSection.tsx`
- Modify: `renderer/src/components/im/SegmentStack.tsx`

- [ ] **Step 1: SubAgentSection 逐处替换**

1. L28-35 容器：inline style（borderLeft #444 + padding/margin）→ `className="my-1 border-l-2 border-strong pl-2"`（style 全删）
2. L68 text 段包装：`[&_pre]:overflow-x-auto [&_pre]:max-w-full` 删、加 `md-body`（保留 `[&_p]` 三条 reset 与 overflow-hidden min-w-0）
3. L75-85 流式光标：`background: '#60a5fa'` → `className="... bg-accent-500"`（style 只留尺寸/对齐/animation；建议直接 `className="inline-block h-3.5 w-0.5 bg-accent-500 align-text-bottom"` + style 只留 `marginLeft: 2, animation: '...'`）

- [ ] **Step 2: SegmentStack 逐处替换**

1. L33-34：`text-neutral-500` → `text-tertiary`；`📑 ` 文本删除，前置 `<Layers size={12} strokeWidth={1.75} aria-hidden className="inline-block align-[-1px]" />`（import Layers）；行容器加 `inline-flex items-center gap-1`
2. L44：`text-neutral-600` → `text-disabled`

- [ ] **Step 3: 验证**：vitest im/ + tsc + eslint 两文件 0/0。
- [ ] **Step 4: Commit** `refactor(renderer): SubAgentSection/SegmentStack token 化——嵌套边框与光标去 hex`

---

### Task 8: AgentStreamBubble 重写（状态栏/停止/错误框/md-body）

**Files:**
- Rewrite: `renderer/src/components/im/AgentStreamBubble.tsx`
- Modify: `renderer/src/components/im/AgentStreamBubble.test.tsx`（如有颜色/emoji 断言按 tone class 适配）

- [ ] **Step 1: 全量替换**（保留全部业务逻辑/聚合/dispatch 反查——只改展示层）

在原文件基础上做以下替换（实现者按此清单逐处执行，逻辑行零改动）：

1. import 区加：`import { Hourglass, CircleCheck, CircleX, CircleSlash, Loader2, Square } from 'lucide-react';`、`import { Button } from '../ui/Button';`
2. 删除 `STATUS_COLOR`（hex map）与 `STATUS_DOT`（●✓⚠⏹）；新增：

```tsx
/** 状态展示：tone class + lucide 图标（streaming 用旋转 Loader） */
const STATUS_TONE: Record<StreamState['status'], string> = {
  streaming: 'bg-status-warning-tint text-status-warning',
  done: 'bg-status-success-tint text-status-success',
  failed: 'bg-status-error-tint text-status-error',
  aborted: 'bg-status-warning-tint text-status-warning',
};
const STATUS_ICON: Record<StreamState['status'], typeof Loader2> = {
  streaming: Loader2,
  done: CircleCheck,
  failed: CircleX,
  aborted: CircleSlash,
};
```

（streaming 原为 #60a5fa 蓝点——裁定归 warning tint 保持「活动=黄」与 executing 一致；STATUS_TEXT 保留。）

3. L123 bubbleClassName：`"bg-bg-tertiary text-neutral-100 border border-border-subtle"` → `"bg-surface-2 text-primary border border-subtle"`
4. L166 text 段包装：删 `[&_pre]:overflow-x-auto [&_pre]:max-w-full [&_code]:rounded [&_code]:px-1 [&_code]:py-0.5 [&_code]:bg-black/30`，加 `md-body`（保留 `[&_p]` reset 三条 + overflow-hidden min-w-0）
5. L170-183 流式光标：`background: statusColor` → class `bg-accent-500`（style 留尺寸/对齐/animation）
6. L189-193 等待子任务：`⏳ 等待…` → `<Hourglass size={11} strokeWidth={1.75} aria-hidden /> 等待 {…} 子任务完成`，容器 `className="my-1 flex items-center gap-1 text-[11px] text-tertiary"`
7. L195-204 底部状态栏容器：inline borderTop #333 → `className="mt-2 flex flex-col gap-1 border-t border-subtle pt-1.5 text-[11px]"`
8. L207-209 状态行：`<span style={{ color: statusColor }}>{statusDot} {statusText}</span>` → `<span className={cn('inline-flex h-5 items-center gap-1 rounded px-2 font-medium', STATUS_TONE[stream.status])}><StatusIcon size={11} strokeWidth={1.75} aria-hidden className={isStreaming ? 'animate-spin' : undefined} />{statusText}</span>`（组件体 `const StatusIcon = STATUS_ICON[stream.status];`）
9. L210-231 停止按钮：inline 样式全删 → `<Button variant="secondary" size="sm" onClick={…}><Square size={11} strokeWidth={1.75} aria-hidden /> 停止</Button>`（保留 abortStream 逻辑）
10. L233-249 错误框：inline 全删 → `className="whitespace-pre-wrap break-words rounded border border-status-error/40 bg-status-error-tint px-2 py-1.5 font-mono text-[11px] text-status-error"`

- [ ] **Step 2: 验证**：`vitest run src/components/im/` 全绿（断言 toHaveStyle/#60a5fa 的按 `bg-accent-500`/tone class 适配；⏹/⏳ 文本按语义）+ tsc + eslint 0/0。
- [ ] **Step 3: Commit** `refactor(renderer): AgentStreamBubble 状态栏 Badge 化——停止按钮/错误框/光标 token 化 + md-body`

---

### Task 9: MessageFrame + MessageBubble + DispatchCard + TaskReplyCard token 化

**Files:**
- Modify: `renderer/src/components/im/MessageFrame.tsx`（2 处）
- Modify: `renderer/src/components/im/MessageBubble.tsx`（2 处）
- Modify: `renderer/src/components/im/DispatchCard.tsx`（4 处 + md-body）
- Modify: `renderer/src/components/im/TaskReplyCard.tsx`（4 处 + md-body + quaternary 修正）

- [ ] **Step 1: 逐处替换**

**MessageFrame**：L53 头像底 `bg-bg-tertiary` → `bg-surface-2`；L66 名字 `text-neutral-400` → `text-secondary`。
**MessageBubble**：L93 `isSelf ? 'bg-accent-blue text-white' : 'bg-bg-tertiary text-neutral-100'` → `isSelf ? 'bg-accent-500 text-inverse' : 'bg-surface-2 text-primary'`；L96 包装删 `[&_code]:…bg-black/30`（含 rounded/px/py）与 `[&_pre]:…`，加 `md-body`（保留 `[&_p]` reset）。
**DispatchCard**：L28 bubble → `"border border-status-violet/40 bg-status-violet-tint"`；L31 调度徽标 → `bg-status-violet-tint text-status-violet`（原 accent-purple/20——同为 violet 系双层，徽标改 `bg-status-violet/20`? **裁定：徽标 `bg-status-violet/20 text-status-violet`**，与卡片 tint 区分层级）；L35 `text-neutral-500` → `text-tertiary`；L40 body `text-neutral-100` → `text-primary` + 包装同上 md-body 化。
**TaskReplyCard**：L28 bubble → `"bg-surface-2 text-primary border border-subtle"`；L31 徽标 `bg-bg-quaternary text-neutral-300`（**未定义 token**）→ `bg-status-violet/20 text-status-violet`（任务回执与调度同为系统事件徽标，violet 系）；L35 `text-tertiary`；L40 `text-primary` + md-body。

- [ ] **Step 2: 验证**：`vitest run src/components/im/` 全绿（MessageBubble/DispatchCard 测试断言 accent-blue/purple 的按新 token 适配）+ tsc + eslint 四文件 0/0。
- [ ] **Step 3: Commit** `refactor(renderer): 消息四件 token 化——自己消息 accent-500 + 调度/回执 violet + md-body 接线`

---

### Task 10: Monaco 双主题接线

**Files:**
- Modify: `renderer/src/components/editor/CodeEditor.tsx`（仅主题相关——tab 栏其余 token 化留 P3）

- [ ] **Step 1: 主题注册 + 订阅**

1. import 区：`import { useThemeStore } from '../../stores/theme.store';`、`import type { Monaco } from '@monaco-editor/react';`（该包导出 `Monaco` 类型；若名不符用 `typeof import('monaco-editor')` 的 editor 类型——实现者按 d.ts 适配并记录）
2. 组件体加：`const resolved = useThemeStore((s) => s.resolved);`
3. Editor 组件（L83-99）：`theme="vs-dark"` → `theme={resolved === 'dark' ? 'momo-dark' : 'momo-light'}`，并加 `beforeMount`：

```tsx
          beforeMount={(monaco) => {
            // v2.1 双主题：编辑器底色对齐 canvas token（bg-canvas 的 hex 值），
            // 注册失败回退内置 vs/vs-dark（catch 防御——spec §12 错误处理）
            try {
              monaco.editor.defineTheme('momo-dark', {
                base: 'vs-dark',
                inherit: true,
                rules: [],
                colors: {
                  'editor.background': '#08090A',
                  'editor.lineHighlightBackground': '#181A1F',
                  'editorLineNumber.foreground': '#7F828B',
                  'editor.selectionBackground': '#5E6AD255',
                },
              });
              monaco.editor.defineTheme('momo-light', {
                base: 'vs',
                inherit: true,
                rules: [],
                colors: {
                  'editor.background': '#FFFFFF',
                  'editor.lineHighlightBackground': '#F1F1F3',
                  'editorLineNumber.foreground': '#82858F',
                  'editor.selectionBackground': '#5E6AD230',
                },
              });
            } catch {
              // 注册失败回退：theme prop 兜底用内置主题
            }
          }}
```

（注册失败的 catch 兜底：若 defineTheme 抛错，theme 仍指向 momo-* 会静默失色——**修正**：catch 时置组件级 `themeFallback` state 为 true，theme prop 三元改为 fallback ? ('vs-dark'/'vs') : momo-*。实现按此做。）

- [ ] **Step 2: 验证**：vitest editor 相关（CodeEditor 无专属测试——跑全量 im/ + files/ 相关）+ tsc + eslint CodeEditor（注意：CodeEditor 的 tab 栏旧 token 本任务**不修**，eslint 该文件会有 P3 域警告——**门禁口径：本任务只验证 tsc + 全量测试**，lint 归 P3）。
- [ ] **Step 3: Commit** `feat(renderer): Monaco momo-light/dark 双主题注册与订阅`

---

### Task 11: RoomList + SessionSidebarHeader token 化

**Files:**
- Modify: `renderer/src/components/im/RoomList.tsx`（12 处）
- Modify: `renderer/src/components/im/SessionSidebarHeader.tsx`（2 处）
- Modify: `renderer/src/components/im/RoomList.test.tsx`（4 处 emoji 断言适配）

- [ ] **Step 1: RoomList 逐处替换**（行号为今日 grep 实测）

1. L44 溢出计数：`text-neutral-500` → `text-tertiary`
2. L86/L113 容器 + L94 空态容器：`bg-bg-secondary` → `bg-surface-1`
3. L87/L96 加载/空态：`text-neutral-500` → `text-tertiary`；空态若为纯文案可顺手接 EmptyState（icon=MessageSquare，title=原文案）——**裁定：接 EmptyState**
4. L104 新建按钮：`text-accent-blue hover:underline` → `text-accent-600 dark:text-accent-300 hover:underline`
5. L123/124 房间行：active `'bg-bg-tertiary border-accent-blue text-neutral-100'` → `'bg-surface-active border-transparent text-primary'`；inactive → `'border-transparent text-secondary hover:bg-surface-3'`
6. L130 hover 工具条：`bg-bg-secondary/90` → `bg-surface-1/90`
7. L138 重命名：`text-neutral-500 hover:text-neutral-200` → `text-tertiary hover:text-primary`
8. L139/149 删除：`text-neutral-500 hover:text-red-400` → `text-tertiary hover:text-status-error`

**SessionSidebarHeader**：L61 快速会话 `'bg-accent-blue/20 text-accent-blue hover:bg-accent-blue/30'` → `'bg-surface-active text-accent-600 dark:text-accent-300 hover:bg-surface-active/70'`；L70 协作 `'bg-bg-tertiary text-neutral-300 hover:text-neutral-100'` → `'bg-surface-2 text-secondary hover:text-primary'`。

- [ ] **Step 2: 测试适配**：RoomList.test 4 处 emoji（💬 等）→ svg.lucide-* 或语义查询。
- [ ] **Step 3: 验证**：vitest im/ + tsc + eslint 两文件 0/0。
- [ ] **Step 4: Commit** `refactor(renderer): RoomList/SessionSidebarHeader token 化——选中态 accent + EmptyState`

---

### Task 12: MentionInput + InputToolbar + RoomToolBudgetBadge token 化

**Files:**
- Modify: `renderer/src/components/im/MentionInput.tsx`（10 处 + 🔒）
- Modify: `renderer/src/components/im/InputToolbar.tsx`（3 处）
- Modify: `renderer/src/components/im/RoomToolBudgetBadge.tsx`（10 处）

- [ ] **Step 1: MentionInput 逐处替换**

1. L193 输入区容器：`border-border-subtle bg-bg-secondary` → `border-subtle bg-surface-1`
2. L195/L212 两个弹出菜单：`bg-bg-tertiary border border-border-subtle shadow-xl` → `border border-subtle bg-surface-1 shadow-lg`
3. L196/L213 菜单标题：`text-neutral-500` → `text-tertiary`
4. L202/L219 菜单行：`hover:bg-bg-primary` → `hover:bg-surface-3`
5. L239 mention chip：`bg-accent-blue/20 text-accent-blue hover:bg-red-500/20 hover:text-red-400` → `bg-surface-active text-accent-600 dark:text-accent-300 hover:bg-status-error-tint hover:text-status-error`
6. L248 只读提示：`🔒 ` 文本删除 → 前置 `<Lock size={12} strokeWidth={1.75} aria-hidden className="inline-block align-[-1px]" />`（import Lock），行容器 `inline-flex items-center gap-1`；`text-neutral-500` → `text-tertiary`
7. L265 textarea：`bg-bg-tertiary border border-border-subtle … text-neutral-100 placeholder-neutral-*` → `border border-subtle bg-surface-2 … text-primary placeholder:text-disabled focus:border-focus focus:outline-none`

**InputToolbar**：L29 容器 → `border-t border-subtle bg-surface-1`；L41/42 成员 toggle：active `'bg-accent-blue/20 text-accent-blue'` → `'bg-surface-active text-accent-600 dark:text-accent-300'`；inactive → `'text-secondary hover:bg-surface-3 hover:text-primary'`。

**RoomToolBudgetBadge**：L83 触发器 → `text-secondary hover:bg-surface-3 hover:text-primary`；L93 弹层 → `border border-subtle bg-surface-1 shadow-lg`；L95 标题 → `text-tertiary`；L97/102/107/112 选项行 → `text-secondary`（cursor 保留）；L123 数字输入 → `rounded border border-subtle bg-surface-2 px-1.5 py-0.5 text-primary focus:border-focus focus:outline-none disabled:opacity-40`；L128 取消 → `text-secondary hover:text-primary`；L130 保存 → `<Button size="sm" disabled={…}>`（import Button）。

- [ ] **Step 2: 验证**：vitest im/ + tsc + eslint 三文件 0/0。
- [ ] **Step 3: Commit** `refactor(renderer): MentionInput/InputToolbar/RoomToolBudgetBadge token 化——弹出层统一 surface-1`

---

### Task 13: MembersPanel + ExportChatButton(→Dialog) + MessageList token 化

**Files:**
- Modify: `renderer/src/components/im/MembersPanel.tsx`（5 处 + 👑）
- Rewrite: `renderer/src/components/im/ExportChatButton.tsx`（→ Dialog）
- Modify: `renderer/src/components/im/MessageList.tsx`（5 处）
- Modify: `renderer/src/components/im/MembersPanel.test.tsx`（如断言 👑）

- [ ] **Step 1: MembersPanel**：L11 浮层容器 → `border-l border-subtle bg-surface-1 shadow-lg`；L12 标题 → `border-b border-subtle text-tertiary`；L16 成员行 → `text-secondary`；L19 `<span … text-accent-blue">👑 Leader</span>` → `<span className="inline-flex items-center gap-0.5 text-[10px] text-accent-600 dark:text-accent-300"><Crown size={10} strokeWidth={1.75} aria-hidden /> Leader</span>`（import Crown）；L25 在线态 `'bg-bg-tertiary text-neutral-500'`（离线 dot）→ `'bg-surface-3 text-tertiary'`（在线侧保持原 success 类不动——按文件实况裁定，只换旧 token）。

- [ ] **Step 2: ExportChatButton 全量替换**（⤓→Download；手写 modal→Dialog）

```tsx
// renderer/src/components/im/ExportChatButton.tsx
//
// 会话导出：数量输入弹窗 + ipc.session.exportMessages + Blob 下载。
// v2.1：⤓→Download；手写 modal 收敛 Dialog 原子件。失败红字保留、导出中禁双击。
import { useState } from 'react';
import { Download } from 'lucide-react';
import { ipc } from '../../ipc/client';
import { Button } from '../ui/Button';
import { Dialog } from '../ui/Dialog';
import { Input } from '../ui/Input';

interface Props {
  sessionId: string;
}

export function ExportChatButton({ sessionId }: Props) {
  const [open, setOpen] = useState(false);
  const [limit, setLimit] = useState(100);
  const [exporting, setExporting] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleConfirm = async (): Promise<void> => {
    if (exporting) return;
    setExporting(true);
    setError(null);
    try {
      const { filename, content } = await ipc.session.exportMessages(sessionId, limit);
      const blob = new Blob([content], { type: 'text/markdown;charset=utf-8' });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = filename;
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
      setOpen(false);
    } catch (err) {
      setError((err as Error).message);
    } finally {
      setExporting(false);
    }
  };

  return (
    <>
      <button
        type="button"
        className="inline-flex items-center gap-1 rounded px-2 py-1 text-xs text-secondary hover:bg-surface-3 hover:text-primary"
        onClick={() => setOpen(true)}
        title="导出会话为 Markdown"
      >
        <Download size={12} strokeWidth={1.75} aria-hidden />
        导出
      </button>

      <Dialog
        open={open}
        onClose={exporting ? () => undefined : () => setOpen(false)}
        title="导出会话"
        width={384}
      >
        <div className="flex flex-col gap-3">
          <Input
            label="消息数量（最近 N 条）"
            type="number"
            min={1}
            max={1000}
            value={limit}
            onChange={(e) => setLimit(Math.max(1, Math.min(1000, Number(e.target.value) || 100)))}
            disabled={exporting}
          />
          {error && <div className="text-sm text-status-error">{error}</div>}
          <div className="flex justify-end gap-2">
            <Button variant="ghost" type="button" onClick={() => setOpen(false)} disabled={exporting}>
              取消
            </Button>
            <Button type="button" onClick={handleConfirm} disabled={exporting}>
              {exporting ? '导出中…' : '确定'}
            </Button>
          </div>
        </div>
      </Dialog>
    </>
  );
}
```

- [ ] **Step 3: MessageList**：L82/90/98 三个空/加载态接 EmptyState（loading 场景 icon=MessageSquare + 文案；「未选择房间」等纯文案态）——**裁定**：三处均换 EmptyState（icon 按语义：加载=MessageSquare、无会话=MessageSquare、未选择=MessageSquare），`text-neutral-500` 类随接换删除；L118/121 历史提示 → `text-tertiary`。
- [ ] **Step 4: 验证**：vitest im/（ExportChat 断言 modal 文本的走 role=dialog）+ tsc + eslint 三文件 0/0。
- [ ] **Step 5: Commit** `refactor(renderer): MembersPanel/ExportChat/MessageList token 化——ExportChat 收敛 Dialog`

---

### Task 14: CreateTaskDialog + ConflictDialog → Dialog + CreateTaskButton + InlineTaskSuggestion

**Files:**
- Rewrite: `renderer/src/components/im/CreateTaskDialog.tsx`（inline-style 全表单 → Dialog + 原子件）
- Rewrite: `renderer/src/components/im/ConflictDialog.tsx`（→ Dialog + Checkbox）
- Rewrite: `renderer/src/components/im/CreateTaskButton.tsx`（📌 → IconButton + ListPlus）
- Modify: `renderer/src/components/im/InlineTaskSuggestion.tsx`（💡📌 → lucide + token）

- [ ] **Step 1: CreateTaskDialog 全量替换**（表单结构/字段/提交逻辑全保留；labelStyle/inputStyle 等 6 个 style 常量全删）

外壳：`<Dialog open={open} onClose={onClose} title="创建任务" width={480}>`，内部 form onSubmit={handleSubmit（改 FormEvent 版）}，字段：标题/描述(textarea token 类)/指派(Select)/优先级(Select)/计划开始/截止(datetime 用 Input type="datetime-local")，footer 取消(ghost)+创建(Button submit disabled 逻辑保留)。字段标签用原子件 label；textarea 无原子件 → `className="mt-1 w-full rounded border border-subtle bg-surface-2 px-3 py-2 text-[13px] text-primary focus:border-focus focus:outline-none"`（min-h-[80px]）。`if (!open) return null` 保留在 hooks 后（Dialog 内部亦判 open，双保险）。Select option children 原样。注意 `aria-label`（无 label prop 的 datetime 场景用 Select/Input 自带 label）。

- [ ] **Step 2: ConflictDialog 全量替换**（⚠️→CircleAlert warning 图标行；overlay/dialog/optionButton 三 style 常量全删）

外壳：`<Dialog open={open} onClose={onClose} title="任务冲突" width={480}>`；标题旁状态图标在内容区头行 `<div className="flex items-center gap-2 text-status-warning"><CircleAlert size={16} strokeWidth={1.75} aria-hidden /><p className="text-sm text-secondary">…原文案（#id strong 保留）</p></div>`；四个选项按钮 → `className="w-full rounded border border-subtle bg-surface-2 px-3 py-2 text-left text-[13px] text-primary hover:bg-surface-3"`（① ② 序号文本保留）；记住勾选 → `<Checkbox label="本会话记住选择（以后本会话自动按所选策略处理，不再弹窗）" checked={remember} onChange={…} />`；footer 关闭(ghost)。`data-testid="conflict-overlay"` 在旧遮罩上——测试若依赖改为 role=dialog 定位（适配时保持断言意图）。

- [ ] **Step 3: CreateTaskButton 全量替换**

```tsx
import { useState } from 'react';
import { ListPlus } from 'lucide-react';
import { IconButton } from '../ui/IconButton';
import { CreateTaskDialog } from './CreateTaskDialog';
// …Props 不变
export function CreateTaskButton({ workspaceId, sourceSessionId }: CreateTaskButtonProps) {
  const [open, setOpen] = useState(false);
  return (
    <>
      <IconButton aria-label="创建任务" title="创建任务" onClick={() => setOpen(true)} disabled={!workspaceId || !sourceSessionId}>
        <ListPlus size={14} strokeWidth={1.75} />
      </IconButton>
      <CreateTaskDialog …同原 />
    </>
  );
}
```

（IconButton 无 title prop——用 aria-label 即可，title 行删除；buttonStyle 常量删。）

- [ ] **Step 4: InlineTaskSuggestion 逐处替换**：L22 `💡 要把这个转成任务吗？` → `<Lightbulb size={12} strokeWidth={1.75} aria-hidden /> 要把这个转成任务吗？`（span 改 inline-flex items-center gap-1 text-accent-600 dark:text-accent-300 text-xs）；L24 `📌 创建任务` → Button secondary sm + `<ListPlus size={12}…/> 创建任务`；wrapperStyle 全删 → `className="my-2 flex items-center gap-2 rounded border border-accent-500/40 bg-surface-active px-2 py-1.5"`。

- [ ] **Step 5: 验证**：vitest im/（CreateTask/Conflict 测试按 role=dialog 语义适配；📌 断言→svg）+ tsc + eslint 四文件 0/0。
- [ ] **Step 6: Commit** `refactor(renderer): 任务弹窗两件收敛 Dialog——inline-style 表单全原子件化`

---

### Task 15: CollabSessionDialog + DefaultAgentPickerDialog → Dialog

**Files:**
- Modify: `renderer/src/components/im/CollabSessionDialog.tsx`（→ Dialog + token；👑→Crown；🤖 fallback→Avatar bot）
- Modify: `renderer/src/components/im/DefaultAgentPickerDialog.tsx`（→ Dialog + EmptyState）
- Modify: 两者测试（👑/🤖 断言适配）

- [ ] **Step 1: CollabSessionDialog**：外壳（L78-83 fixed 手写 modal）→ `<Dialog open onClose={onClose} title="创建协作会话" width={448}>`，内部 form/handlers 原样；页签（L95-116）`border-accent-blue text-neutral-100` → `border-accent-500 text-primary`、inactive → `border-transparent text-secondary hover:text-primary`、容器 `border-b border-subtle`；成员/团队行（L129/164）`text-neutral-300 hover:bg-bg-tertiary/50` → `text-secondary hover:bg-surface-3`；L144/146 在线徽标 `bg-status-success/20 text-status-success` 保持（已是 token；离线 `bg-bg-tertiary text-neutral-500` → `bg-surface-2 text-tertiary`）；L138/173 iconEmoji 数据豁免保留（`?? '🤖'` 缺省改 `<Avatar name={memberLabel(m)} bot size="sm" />`——即有 emoji 显示 emoji、无则 Avatar bot）；L175-177 👑 → `<Crown size={11} strokeWidth={1.75} aria-hidden className="text-accent-500" />`（外层 span title 保留，text-accent-600 dark:text-accent-300）；L178/184 `text-neutral-500`→`text-tertiary`、`text-red-400`→`text-status-error`；空态文案 L122/157 → `text-tertiary`。
- [ ] **Step 2: DefaultAgentPickerDialog**：外壳 → `<Dialog open onClose title="选择默认会话 agent" width={448}>`；L64-76 空态接 EmptyState（icon=Bot、title=原文案、description=原文案、action=关闭 Button ghost——EmptyState action prop）；L83 行 → `text-secondary hover:bg-surface-3`；L92 `?? '🤖'` 同上 Avatar bot 豁免处理；L98 error → `text-status-error`。
- [ ] **Step 3: 测试适配**：两测试文件的 👑/🤖 文本断言 → getByTitle('leader：…') 内 svg / Avatar svg 语义（保持断言意图：leader 标记可见、成员行可见）。
- [ ] **Step 4: 验证**：vitest im/ 全绿 + tsc + eslint 两文件（含测试）0/0。
- [ ] **Step 5: Commit** `refactor(renderer): 协作会话/默认agent弹窗收敛 Dialog——Crown/Avatar 化`

---

### Task 16: P2 收官验证

**Files:** 无新文件（验证）

- [ ] **Step 1: im/ 域 lint 零警告**：`cd renderer && npx pnpm@9.0.0 exec eslint src/components/im --ext .ts,.tsx` → 0/0（125 警告 → 0；CodeEditor 属 editor/ 不在本域）
- [ ] **Step 2: typecheck + 全量 + build**：`cd /workspace && npx pnpm@9.0.0 typecheck && npx pnpm@9.0.0 test && npx pnpm@9.0.0 build` → 全过（测试计数相对 P1 终审 781 基线按新增测试数报告）
- [ ] **Step 3: xvfb 冒烟**：`ELECTRON_DISABLE_SANDBOX=1 timeout 120 xvfb-run -a --server-args="-screen 0 1280x800x24" npx pnpm@9.0.0 dev` → Window ready / 零 CSP / 零 Uncaught（ABI 陷阱按 AGENTS 表处置并回退 Node ABI）
- [ ] **Step 4: census 复点**：`eslint src` 总数从 321 降 ~196（im 125 清零 + im 域测试内警告一并清）；记录 P3 预算
- [ ] **Step 5: 树净检查**（.superpowers/ 遗留除外）

---

## 计划自审记录

- **Spec 覆盖**（spec §8 P2 行）：im/ 26 文件全覆盖（T3-T15 逐文件）✓；task-status 统一（T2 派生+dispatch 扩展、T3/T5 接线；TaskCard 属 task-board/ 留 P3 接线）✓；Markdown 双主题（md-body ×6 接线点：T6/T7/T8/T9）✓；Monaco momo 双主题（T10）✓；验收线「im/ 零裸色号、零 emoji」由 T16 机械证明 ✓。P0/P1 终审移交：EmptyState role（T1）、Segmented 焦点锁/单选项（T1）、?? 失败路径（T1）、Dialog onClose 指引软化（T1）、Loader2 loading 态（T4 执行中旋转 + T12 不强制——文字加载态保留，Spinner 接入按 T11 EmptyState 覆盖）、task-status 类型收窄（T2）✓。
- **占位符扫描**：无 TBD；结构重写给全代码（T3/T4/T5/T6/T13 Export/T14 CreateTaskButton+InlineTask 均完整；T14 两弹窗给结构化规格——因字段多而外壳同构，规格+既有 Dialog 模板可实现；T15 同）；清扫给行号规格（今日 grep 实测）。
- **类型一致性**：`DispatchStatus`（T2 定义→T5 消费）、`TaskStatus`（types.d.ts→T2 派生→T3 消费）、`LucideIcon`（T5 STATUS_ICON map）、Badge tone 类经 `dispatchStatusStyle`/`taskStatusStyle` 同源 ✓。
- **已知裁定**：streaming 状态 tone 归 warning（原 #60a5fa 蓝→黄，与 executing 一致性优先）；dispatch 头行 Send 图标 accent；iconEmoji/subAgentAvatar 数据豁免。
- **有意排除**：CodeEditor tab 栏 token（P3）、TaskCard 接线（P3 task-board）、agent/ 域 TeamDialog 等（P3）。
