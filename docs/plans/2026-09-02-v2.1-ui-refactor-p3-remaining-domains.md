# v2.1 UI 设计系统重构 · P3 其余域 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** agent/ resource-library/ task-board/ files/ editor/ p2p/ workspace/ 七域完成设计系统迁移——196 警告清零、35 处渲染 emoji 消除、9 个手写弹窗收敛 Dialog、task-board 状态色调色板接线 task-status——v2.1 迁移收官（P4 只剩机制收紧）。

**Architecture:** P0-P2 已建立全部模式先例。本计划大量复用：**映射表**（P1 计划，全文引用）、**Dialog 收敛**（P2 Task 14/15 先例 ×7 次）、**Bot 兜底**（`{iconEmoji ?? <Bot/>}` ×6 处先例）、**taskStatusStyle 接线**（P2 TaskChip 先例）。新结构性工作仅一处：TaskCard/TaskSidebarPanel 接线（含远端未知状态防御）。

**Tech Stack:** 同 P0-P2。

**Spec:** `docs/specs/2026-09-01-v2.1-ui-design-system-refactor-design.md`（§8 P3 行）
**依赖:** P0-P2 全部产出；**模式先例索引**（实现者遇不确定形态时按此查）：
- Dialog 收敛 + 表单原子件化 → P2 Task 14（CreateTaskDialog）/ Task 15（CollabSessionDialog）
- 弹窗内 Avatar/Bot 兜底 → P2 Task 15 + MembersPanel 修复（6754039）
- 状态 chip 接线 taskStatusStyle → P2 Task 3（TaskChip）
- emoji → lucide 尺寸规范 → 密集行内 12px / 独立 14-16px，strokeWidth 1.75，aria-hidden + 按钮文案兜底 aria-label
- 弹出层（菜单/浮层非 modal）→ `border border-subtle bg-surface-1 shadow-lg` + 行 hover surface-3（P2 Task 12 先例）——**上下文菜单不是 Dialog，不收敛**

## Global Constraints

- Node 20 / pnpm 9 / typecheck 每任务 / 逐文件 lint 零警告（同 P1/P2）
- `noUncheckedIndexedAccess` 收窄；禁 `any`；注释中文；Conventional Commits
- 测试断言语义适配不削弱；**用户数据 emoji 豁免**（iconEmoji/iconEmoji 字段值照渲染）
- P2 已定的裁定全沿用：选中态 accent 形态（surface-active + accent-600/300）、surface-active 禁 /nn（hover 用 opacity）、streaming/activity=warning tint、系统事件徽标 violet/20

## 颜色映射表

**全文引用 P1 计划的映射表 + P2 新增裁定**（不再复制——实现者读 `docs/plans/2026-09-02-v2.1-ui-refactor-p1-shell-settings.md` 的「颜色映射裁定表」节 + 上面先例索引）。

---

### Task 1: TaskCard + TaskSidebarPanel 接线 task-status（新结构性）

**Files:**
- Rewrite: `renderer/src/components/task-board/TaskCard.tsx`
- Modify: `renderer/src/components/task-board/TaskSidebarPanel.tsx`（L21 import + L70-82 分区行）
- Modify: `renderer/src/lib/task-status.ts`（+1 函数）
- Modify: `renderer/src/lib/task-status.test.ts`（+1 用例）
- Modify: `renderer/src/components/task-board/TaskCard.test.tsx`、`TaskSidebarPanel.test.tsx`（断言适配）

**Interfaces:**
- Produces: `remoteStatusStyle(status: string): { label: string; className: string }`——远端未知状态防御（跨版本对端可能送来 TaskStatus 之外的枚举，TaskSidebarPanel L79 `?? t.status` 语义保留）
- TaskCard 的 `STATUS_LABEL`/`STATUS_COLOR` 导出删除（TaskSidebarPanel 是唯一消费方，grep 已证）

- [ ] **Step 1: task-status.test.ts +1 用例**（先红）：

```ts
describe('remoteStatusStyle（远端未知状态防御）', () => {
  it('已知状态走 taskStatusStyle；未知枚举回退 neutral + 原样文案', () => {
    expect(remoteStatusStyle('failed').className).toContain('bg-status-error-tint');
    expect(remoteStatusStyle('failed').label).toBe('失败');
    const unknown = remoteStatusStyle('archived');
    expect(unknown.label).toBe('archived');
    expect(unknown.className).toContain('bg-surface-3');
  });
});
```

- [ ] **Step 2: task-status.ts 追加**：

```ts
/**
 * 远端任务状态的安全展示（跨版本对端可能送来 TaskStatus 之外的枚举）：
 * 已知状态走 taskStatusStyle；未知回退 neutral tone + 原样文案。
 * TaskSidebarPanel 远端只读分区专用（spec D7 只读镜像）。
 */
export function remoteStatusStyle(status: string): { label: string; className: string } {
  if (STATUS_LABEL[status as TaskStatusKey] !== undefined) {
    const s = taskStatusStyle(status as TaskStatusKey);
    return { label: s.label, className: s.className };
  }
  return {
    label: status,
    className: `inline-flex h-5 items-center rounded px-2 text-xs font-medium ${BADGE_TONE_CLASSES.neutral}`,
  };
}
```

- [ ] **Step 3: TaskCard 全量替换**：

```tsx
// renderer/src/components/task-board/TaskCard.tsx
//
// 任务卡片（D 子系统 D7）：优先级徽标 + #短ID · 标题 + 状态徽标 + 调度信息 + 进度。
// v2.1 P3：状态色/标签退役本地双 map，接线 taskStatusStyle（与 TaskChip 同源）；
// 📅⏰🤖 → Calendar/Clock/Bot lucide。
import { Bot, Calendar, Clock } from 'lucide-react';
import type { TaskRow } from '../../ipc/types';
import { taskStatusStyle } from '../../lib/task-status';

/** 优先级标签（0=无 / 1=低 / 5=中 / 10=高） */
const PRIORITY_LABEL: Record<number, string> = { 0: '', 1: '低', 5: '中', 10: '高' };

interface TaskCardProps {
  task: TaskRow;
  selected: boolean;
  onSelect: () => void;
}

export function TaskCard({ task, selected, onSelect }: TaskCardProps) {
  const status = taskStatusStyle(task.status);
  const priorityLabel = PRIORITY_LABEL[task.priority];
  return (
    <button
      type="button"
      onClick={onSelect}
      className={`w-full cursor-pointer border-b border-subtle px-3 py-2 text-left transition-colors hover:bg-surface-3 ${
        selected ? 'bg-surface-active' : ''
      }`}
    >
      <div className="flex items-center justify-between gap-2">
        <span className="truncate text-sm font-medium text-primary">
          {priorityLabel && (
            <span className="mr-1 text-status-warning">[{priorityLabel}]</span>
          )}
          #{task.id.slice(0, 6)} · {task.title}
        </span>
        <span className={status.className}>{status.label}</span>
      </div>
      <div className="mt-1 flex gap-3 text-xs text-tertiary">
        {task.scheduledAt && (
          <span className="inline-flex items-center gap-1">
            <Calendar size={11} strokeWidth={1.75} aria-hidden />
            {new Date(task.scheduledAt).toLocaleDateString()}
          </span>
        )}
        {task.deadlineAt && (
          <span className="inline-flex items-center gap-1">
            <Clock size={11} strokeWidth={1.75} aria-hidden />
            {new Date(task.deadlineAt).toLocaleDateString()}
          </span>
        )}
        {task.assigneeAgentId && (
          <span className="inline-flex items-center gap-1">
            <Bot size={11} strokeWidth={1.75} aria-hidden />
            {task.assigneeAgentId.slice(0, 12)}
          </span>
        )}
      </div>
      {task.status === 'in_progress' && task.startedAt && (
        <div className="mt-1 text-xs text-tertiary">
          已用 {Math.round((Date.now() - task.startedAt) / 60000)} min · {task.toolCallsUsed}{' '}
          工具调用
        </div>
      )}
    </button>
  );
}
```

- [ ] **Step 4: TaskSidebarPanel**：L21 import 改 `import { remoteStatusStyle } from '../../lib/task-status';`；L77-80 状态 span 改：

```tsx
                {/* 跨版本对端可能送来未知状态枚举——remoteStatusStyle 回退原样展示 */}
                {(() => {
                  const s = remoteStatusStyle(t.status);
                  return (
                    <span className={s.className}>{s.label}</span>
                  );
                })()}
```

（保留注释语义；外层 div 不动。）

- [ ] **Step 5: 测试适配**：TaskCard.test 断言 STATUS_LABEL 文本（'执行中' 等）仍有效（taskStatusStyle label 同文案——除 `pending: '待启动'` vs task-status 的 `'待分配'`、`assigned: '排队中'` vs `'已分配'`——**文案统一为 task-status 版**，测试同步）；TaskSidebarPanel.test 同理；如有 toHaveStyle(hex) 改 toHaveClass。
- [ ] **Step 6: 验证**：vitest task-board + task-status + tsc + eslint 四文件 0/0。
- [ ] **Step 7: Commit** `refactor(renderer): TaskCard/SidebarPanel 接线 task-status——本地双 map 退役 + 远端未知状态防御`

---

### Task 2: task-board 其余四件 sweep

**Files:** TaskBoardView(3) TaskDetailPanel(9+emoji×2) TaskFilters(1) TaskList(1) + 对应测试

- [ ] 按映射表 sweep 全部旧 token（行号由实现者 grep 定位，规格值=映射表）；TaskDetailPanel:93/95 emoji → lucide；远端节点分区标题如 Crown/Users 需求按 P2 RoomList 先例。验证链 + Commit `refactor(renderer): task-board 看板四件 token 化`

### Task 3: agent 视图三件 sweep + emoji

**Files:** AgentsView(5) MembersPanel(11+emoji×5) TeamsPanel(9+emoji×3) CapabilityTabs(15) + 测试

- [ ] sweep + emoji lucide 化（行号列表已在普查 JSON：MembersPanel 61/86/135/141/146、TeamsPanel 37/84/97）；iconEmoji 数据豁免；👑→Crown（MembersPanel P1 先例）。验证链 + Commit `refactor(renderer): agent 视图四件 token 化 + 图标 lucide 化`

### Task 4: TeamDialog → Dialog

**Files:** agent/TeamDialog.tsx + 测试
- [ ] P2 Task 15 CollabSessionDialog 模式（表单在 Dialog 内、成员行/团队行 token、Avatar/Bot 兜底、提交逻辑逐字）。验证链 + Commit `refactor(renderer): TeamDialog 收敛 Dialog`

### Task 5: CreateAgentDialog → Dialog

**Files:** agent/CreateAgentDialog.tsx(18+emoji:241) + 测试
- [ ] P2 Task 14 CreateTaskDialog 模式（表单原子件化、⚡ 说明文案去 emoji、提交逐字）。验证链 + Commit

### Task 6: MemberEditDialog + DefinitionEditor modal 面收敛

**Files:** agent/MemberEditDialog.tsx(11+emoji:167) agent/DefinitionEditor.tsx(11)
- [ ] MemberEditDialog → Dialog（同模式）；DefinitionEditor 内的 fixed inset-0 按实际形态裁定（若是内嵌确认弹窗→Dialog；若是全屏编辑器浮层→保留结构仅 token 化，**实现者读文件后选并记录**）。验证链 + Commit

### Task 7: RegisterMcpDialog + UploadSkillDialog → Dialog

**Files:** agent/RegisterMcpDialog.tsx(6) agent/UploadSkillDialog.tsx(7) + 测试
- [ ] 同模式收敛（表单原子件化、文件输入保留原生+token）。验证链 + Commit

### Task 8: resource-library 五件 sweep + emoji

**Files:** ResourceDetail(29+emoji:165) ResourceLibraryView(11+emoji:97/176) ResourceCard(6+emoji:66 已是✓文本) AddResourceMenu(4+emoji:45/52/59) SourceBadge(1) + 测试
- [ ] sweep；ResourceDetail ✓/已安装 → P2 ResourceCard P1 先例（Check lucide）；AddResourceMenu 🤖→Bot/🧩→Puzzle?（**裁定：Puzzle 图标**，与资源库语义配）；SourceBadge 四色 → Badge tone 映射（builtin=accent/custom=neutral/marketplace=violet/p2p=success——**裁定**）。验证链 + Commit

### Task 9: files 三件 sweep + FileContextMenu

**Files:** FileTreeView(13+emoji×4) FileTree(5+emoji×3) FileContextMenu(8, fixed inset-0 = 右键菜单非 modal) + 测试
- [ ] sweep；FileContextMenu 保留自绘浮层结构仅 token（弹出层裁定）；文件类型 emoji（📄📁 等如存在）→ File/Folder lucide。验证链 + Commit

### Task 10: editor CodeEditor tab 栏 sweep

**Files:** editor/CodeEditor.tsx(3 警告)
- [ ] 仅 tab 栏/空态 token + 📄→File lucide + ● dirty 标记（保留文本●或换小圆点 span——**裁定：保留文本**，非 emoji）；主题接线（P2 Task 10）不动。验证链（lint 本文件 0/0）+ Commit

### Task 11: p2p NodeDiscoveryPanel sweep

**Files:** p2p/NodeDiscoveryPanel.tsx(12+emoji×3) + 测试
- [ ] sweep + emoji（🔗📡 等）→ lucide（Link/Radio）。验证链 + Commit

### Task 12: workspace CreateWorkspaceDialog → Dialog

**Files:** workspace/CreateWorkspaceDialog.tsx(5) + 测试
- [ ] Dialog 收敛；iconEmoji 选择器如为 emoji grid → **保留**（用户数据输入 UI，豁免），仅外壳 token。验证链 + Commit

### Task 13: EOF ×7 + cosmetic 批量清扫

**Files:** ConflictDialog/CreateTaskDialog/CreateTaskButton/ExportChatButton/InlineTaskSuggestion/MessageList/task-status.ts（EOF）；SessionSidebarHeader 头注释 ⚡👥；CollabSessionDialog Crown 外层死类；TaskChip 双重截断
- [ ] 一个 chore commit。验证：全量测试零回归。

### Task 14: P3 收官验证

- [ ] 七域 lint 0/0（196→0）+ typecheck + 全量测试 + build + xvfb 冒烟 + census 复点（预期 0）+ 树净

---

## 计划自审记录

- **Spec 覆盖**（§8 P3 行）：七域 26 生产文件全覆盖（T1-T12 逐文件）✓；验收线「全域完成」由 T14 机械证明 ✓。P2 终审移交：TaskSidebarPanel:77 接线（T1）✓、EOF/cosmetic（T13）✓、macOS 人工验收（人类任务，不在计划内）。
- **占位符**：T2-T12 为「映射表 + 先例索引 + 普查行号」驱动的 sweep 规格——P2 已验证此深度可实现（T11/T12 同深度全部 verbatim 落地）；唯一新结构（T1）给全代码。
- **有意排除**：iconEmoji 选择器 UI（用户数据输入，T12 裁定）；CodeEditor 主题（P2 已做）。
