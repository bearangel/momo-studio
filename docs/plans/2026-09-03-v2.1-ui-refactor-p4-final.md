# v2.1 UI 设计系统重构 · P4 收官期 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** v2.1 机制收官——deprecated token 物理删除、裸色号编译层灭绝（Tailwind `theme.colors` 独占）、ESLint 全局 error 化、e2e 双主题基线、规范终稿。完成后 v2.1 UI 重构全链路交付。

**Spec:** §8 P4 行 + P3 终审移交清单（见 ledger）。

## Global Constraints

同 P1-P3（Node 20 / typecheck 每任务 / 逐文件 lint / 注释中文 / Conventional Commits）。

**P4 特有红线**：任务顺序不可调换——T1 的 token 迁移必须先于 T2 的 token 块删除（否则 app 根背景静默消失，P3 终审 Important #3）。

---

### Task 1: 前置迁移 + 杂项字形（P3 终审移交）

**Files:** App.tsx / routes/MainShell.tsx / im/MentionInput.tsx / resource-library/ResourceLibraryView.tsx / settings/ProviderSettings.tsx / files/FileTreeView.tsx + 相关测试

- [ ] App.tsx:68 与 MainShell.tsx:12：`bg-bg-primary` → `bg-canvas`
- [ ] MentionInput.tsx:244：mention chip 的 `×` 文本 → `<X size={11} strokeWidth={1.75} aria-hidden />`（P1 WorkspaceTabs 先例）
- [ ] ResourceLibraryView.tsx:173：安装横幅文本 `✓` → Check lucide 12px（同文件已装态先例）
- [ ] ProviderSettings.tsx:74/108：文案「＋」→「添加」措辞（按钮已是 Plus 图标，文案对齐实际 UI）
- [ ] FileTreeView.tsx:205-253 内联重命名 modal → **收敛 PromptDialog**（同文件 create-in-dir 已用 PromptDialog；重命名是单字段输入，语义完全匹配；`stopPropagation` 死代码随外壳退役）
- [ ] CodeEditor tab-close `<span onClick>` a11y：结构改为 `<button type="button" aria-label>`（嵌套 button 问题：外层是 tab `<button>`——**裁定：外层改 `<div role="tab">` + tabIndex 0 + onKeyDown Enter/Space**，保留视觉不变；若改动面过大则记录豁免归 P2.1，实现者读文件后裁定并记录）
- [ ] 验证：全量测试 + typecheck + 全域 eslint 0/0 + Commit `refactor(renderer): P4 前置迁移——deprecated token 末两处 + 字形杂项 + FileTreeView 收敛`

### Task 2: Tailwind token 物理删除 + 独占切换

**Files:** renderer/tailwind.config.js + renderer/src/styles/tokens.test.ts

- [ ] 删除 `@deprecated` 三块：`accent.blue/purple`、`bg: {...}`、`border: {...}`
- [ ] `theme.extend.colors` → `theme.colors`（完全替换默认色阶；保留 `transparent`/`currentColor` 由 Tailwind 内建覆盖；`inverse: '#ffffff'` 保留为字面 token）
- [ ] tokens.test.ts：删「旧 token 迁移期共存」it-block；+1 断言 config 无 `extend` 且无 deprecated 键（防回潮锁）
- [ ] 全量渲染验证：build exit 0 + 全量测试 792 绿（如有组件仍引用已删 token → 类编译失效即视觉缺失——全量走查靠 T3/T5 e2e 兜底）+ 全域 eslint 0/0
- [ ] Commit `feat(renderer): Tailwind theme.colors 独占——deprecated token 物理删除，裸色号编译层灭绝`

### Task 3: ESLint 全局 error 化

**Files:** /workspace/eslint.config.mjs

- [ ] 三条 `no-restricted-syntax` 规则 severity `warn`→`error`（删目录粒度覆盖块，全局 error；`ui/` 专属块随之冗余可删）
- [ ] 反向验证：stdin 探针（P0 Task 19 先例）在任意 src 路径触发裸色阶类 → exit 1
- [ ] 全域 `eslint src` 0/0（此刻应天然为 0——P3 已清零）
- [ ] Commit `feat(lint): 设计系统约束全局 error 化——P4 收官`

### Task 4: e2e 双主题基线 + design-system.md 终稿

**Files:** tests/e2e/smoke.spec.ts（扩展）/ docs/dev/design-system.md / AGENTS.md（如需）

- [ ] smoke.spec 扩展双主题场景：(a) 默认浅色启动断言 body 背景色为浅色 token 值；(b) `localStorage['momo.theme']='dark'` 预设注入后启动断言 `.dark` class + 深色背景；(c) 运行时切换（page.evaluate 调 store 或点击设置入口——实现者按可行路径选）断言 class 翻转。构建产物运行（AGENTS e2e 流程 + electron-rebuild ABI 注意事项）
- [ ] design-system.md 终稿：加「P4 生效」机制段（theme.colors 独占 + 全局 error + P2 终审 sweep 后的 Esc capture 语义已修）；校对全文与落地一致
- [ ] Commit `test(e2e): 双主题基线 + docs: design-system 终稿`

### Task 5: P4 收官验证（v2.1 终点）

- [ ] 全域 eslint 0/0（error 级）+ grep `bg-bg-\|border-border-\|accent-blue\|accent-purple` src 零命中（物理灭绝证明）
- [ ] typecheck 双 clean + electron 1308 + renderer 792 全绿 + build exit 0
- [ ] e2e 全量（smoke 含双主题）通过
- [ ] xvfb 冒烟双主题各一次
- [ ] 树净 + v2.1 收官报告

---

## 计划自审

- P3 终审移交全覆盖：①bg-bg-primary 前置（T1，顺序红线）②FileTreeView（T1）③字形杂项（T1）④theme.colors 独占（T2）⑤ESLint error（T3）⑥e2e 基线（T4）。
- 唯一开放裁定：CodeEditor tab-close 结构（T1 内裁定，可豁免）。
