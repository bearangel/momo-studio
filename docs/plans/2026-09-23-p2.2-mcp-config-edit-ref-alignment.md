# P2.2 已装远程 MCP 配置编辑 + 引用对齐 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 已装远程 MCP 可查看/编辑配置（url + API key，保存即生效），卸载级联清理 agent 引用，MCP 页展示悬空引用提示卡。

**Architecture:** migration 040 给 `mcp_definitions` 加 `config_schema` 列；安装链落 schema；编辑走三级降级（库存 schema → Smithery 实时拉取回写 → 裸表单）；`updateMcpConfig` 复用安装链抽取的 `composeRemoteConfig` 重组 url/headers 后专用 UPDATE（不换 id）+ name 级驱逐进程池；卸载断面级联清理 `agent_definitions.default_mcps`；dangling 扫描聚合悬空引用供 UI 提示卡。

**Tech Stack:** Electron 主进程（CommonJS + better-sqlite3）· React renderer（语义 token）· vitest 双 workspace

**Spec:** `docs/specs/2026-09-23-p2.2-mcp-config-edit-ref-alignment-design.md`（契约形状以 spec §4 为准）

## Global Constraints

- `source ~/.nvm/nvm.sh && nvm use 20` 验证 v20 再跑测试；**禁止重建 better-sqlite3**
- TypeScript strict：禁 `any`/`as any`/`@ts-ignore`；注释全中文；文件尾换行
- renderer 只用语义 token + 原子组件（`components/ui/`）；lucide 图标 16px stroke 1.75
- IPC 契约两端成对改：electron handler + preload + `renderer/src/ipc/types.d.ts` 镜像同步
- 不 push、不动版本号（Task 8 除外，统一 bump `2.1.0-alpha.4`）
- Conventional Commits 中文；单测位置：electron 集中 `electron/tests/`（镜像 src 结构），renderer 贴源同目录
- commit 前跑：改动面 `vitest run <相关测试>`；Task 8 全量 + 根 typecheck

---

### Task 0: 锚点核验（半小时内完成，产出简报修正后续任务锚点）

**Files:** 无代码改动；产出 `.superpowers/sdd/p22-task0-brief.md` 简报

**Interfaces:** 无（信息任务）

- [ ] 核验并记录到简报：
  1. migration 039（`electron/src/main/storage/migrations/039_p21_mcp_bundle_cwd.ts`）的 up/down 代码形状——Task 1 的 040 照此结构写（含 DROP COLUMN down 是否可用：内存库冒烟 `ALTER TABLE t ADD COLUMN c TEXT NOT NULL DEFAULT '{}'; ALTER TABLE t DROP COLUMN c;` 两向跑通）
  2. `electron/src/main/resource/custom.ts` 中 MCP → ResourceItem 的填充函数名与行号（搜 `listRegistered` 调用），及 `ResourceItem.custom` 在 `renderer/src/ipc/types.d.ts` 的现形状——Task 6 填充 `custom.transport` 的精确锚点
  3. `electron/src/main/resource/ipc.handlers.ts` 现有 `resource:installSmitheryRemote` handler 的注册风格 + `electron/src/preload/index.ts` 对应暴露行——Task 6 照抄
  4. `electron/src/main/mcp/bundle-import.ts` 与 `resource/ipc.handlers.ts` 中 `deleteRegistered` 的全部生产调用点行号——Task 5 级联挂点
  5. `McpServerConfig`/`RegisteredMcp` 在 `electron/src/main/mcp/types.ts` 的精确行号与相邻字段
- [ ] 简报回贴给控制器确认后再开 Task 1

### Task 1: migration 040 + config_schema 列读写 + 安装链落 schema

**Files:**
- Create: `electron/src/main/storage/migrations/040_p22_mcp_config_schema.ts` + 在 `migrations/index.ts` 注册
- Modify: `electron/src/main/mcp/types.ts`（新类型 + 两接口加字段）
- Modify: `electron/src/main/mcp/host-manager.ts`（`McpDefinitionRow` 加 `config_schema: string`；`rowToRegistered`/`getMcpConfig`/`listRegistered` 的 SELECT 加列并解析；`registerMcpDefinition` INSERT 加列）
- Modify: `electron/src/main/resource/hub-install.ts`（`installSmitheryRemote` 落 schema）
- Test: `electron/tests/storage/migrations.test.ts`（追加 040 用例）、`electron/tests/mcp/host-manager-config-schema.test.ts`（新）、`electron/tests/resource/hub-install.test.ts`（追加落库断言）

**Interfaces:**
- Produces: `mcp/types.ts` 导出 `McpConfigSchemaMeta`、`McpConfigSchema`；`McpServerConfig.configSchema?: McpConfigSchema`；`RegisteredMcp.configSchema?: McpConfigSchema`（后续任务直接消费）

- [ ] **Step 1: 红测**——migration 040 up 后 `PRAGMA table_info(mcp_definitions)` 含 `config_schema`；down 后不含。roundtrip：`registerMcpDefinition({..., transport:'streamable_http', url:'https://x', configSchema:{required:['k'],properties:{k:{}}}})` → `getMcpConfig()` 回读 `configSchema` 深等；schema 缺省注册回读 `undefined`（'{}' 视为无）。`installSmitheryRemote` 落库后 `getMcpConfig` 能读到 schema
- [ ] **Step 2: 跑测确认红**——`cd electron && npx pnpm@9.0.0 vitest run tests/storage/migrations.test.ts tests/mcp/host-manager-config-schema.test.ts tests/resource/hub-install.test.ts`
- [ ] **Step 3: 实现**——migration 040 的 down 写**真 DROP COLUMN**（Task 0 冒烟已证可行；039 的 down 是 no-op 不照抄；MIGRATIONS 数组只消费 .up）。mcp/types.ts（JsonSchemaLike 的单源化：结构照 `hub-install.ts` 现接口，hub-install 改为 `export type JsonSchemaLike = McpConfigSchema` 别名重导出，renderer types.d.ts 镜像不动）：

```ts
/** config_schema 列的单字段元数据（Smithery configSchema 消费面形状） */
export interface McpConfigSchemaMeta {
  title?: string;
  description?: string;
  /** 字段注入位置；缺省进 header */
  'x-from'?: 'header' | 'query';
}

/** 远程 MCP 配置表单元数据（mcp_definitions.config_schema 列，JSON 序列化） */
export interface McpConfigSchema {
  required?: string[];
  properties?: Record<string, McpConfigSchemaMeta>;
}
```

  host-manager：`rowToRegistered` 加容错解析（坏 JSON → warn + undefined，照 `parseHeadersJson` 模式新写 `parseConfigSchema`）；`registerMcpDefinition` INSERT 列加 `config_schema`，值 `JSON.stringify(config.configSchema ?? {})`
- [ ] **Step 4: 跑测绿 + commit**——`feat: mcp_definitions 加 config_schema 列与读写（P2.2 Task 1）`

### Task 2: composeRemoteConfig 抽取（安装/编辑单点）

**Files:**
- Modify: `electron/src/main/resource/hub-install.ts`（从 `installSmitheryRemote` 抽出分流段）
- Test: `electron/tests/resource/compose-remote-config.test.ts`（新）

**Interfaces:**
- Produces: `composeRemoteConfig(url: string, config: Record<string, string>, schema?: McpConfigSchema): { finalUrl: string; headers: Record<string, string> }`（导出；Task 4 消费）

- [ ] **Step 1: 红测**——x-from=query 拼 URL（含已带 `?` 的 `&` 分支）；缺省/x-from=header 进 headers；**空串值剔除**（搬入本函数：`Object.entries(config).filter(([, v]) => v !== '')`，安装链行为不变由既有测试守护）；非 https 抛中文错误
- [ ] **Step 2: 红**——`vitest run tests/resource/compose-remote-config.test.ts`
- [ ] **Step 3: 实现**——

```ts
/** 安装/编辑共用的 x-from 分流组装（spec §5.2）。空串值剔除；非 https 拒绝。 */
export function composeRemoteConfig(
  url: string,
  config: Record<string, string>,
  schema?: McpConfigSchema,
): { finalUrl: string; headers: Record<string, string> } {
  if (!url.startsWith('https://')) {
    throw new Error(`远程 MCP url 必须以 https:// 开头: ${url}`);
  }
  const headers: Record<string, string> = {};
  const queryParts: string[] = [];
  for (const [key, value] of Object.entries(config).filter(([, v]) => v !== '')) {
    if (schema?.properties?.[key]?.['x-from'] === 'query') {
      queryParts.push(`${encodeURIComponent(key)}=${encodeURIComponent(value)}`);
    } else {
      headers[key] = value;
    }
  }
  const finalUrl =
    queryParts.length > 0
      ? `${url}${url.includes('?') ? '&' : '?'}${queryParts.join('&')}`
      : url;
  return { finalUrl, headers };
}
```

  `installSmitheryRemote` 改调本函数（https 防线移入后删除原处重复检查，错误文案兼容既有测试——若既有用例断言旧文案则同步更新用例）
- [ ] **Step 4: 绿（含既有 hub-install 测试全过）+ commit**——`refactor: 抽取 composeRemoteConfig 安装/编辑共用（P2.2 Task 2）`

### Task 3: host-manager 编辑断面（UPDATE 专语句 + name 级池驱逐）

**Files:**
- Modify: `electron/src/main/mcp/host-manager.ts`（新增两导出函数）
- Test: `electron/tests/mcp/host-manager-edit.test.ts`（新）

**Interfaces:**
- Produces: `updateRemoteMcpDefinition(name: string, url: string, headers: Record<string, string>, configSchema?: McpConfigSchema): void`（不存在/非 streamable_http/非 https 抛中文错误；schema 缺省保留原列值；**不换 id、不重置 installed_at**）
- Produces: `evictMcpByName(name: string): Promise<void>`（驱逐全部 workspace 池连接，disconnect 失败/启动失败态吞掉）

- [ ] **Step 1: 红测**——① update 后 id/installed_at 不变、url/headers_json 更新、config_schema 未传时保留；② 名字不存在 → `未注册`；③ stdio 行 → 中文拒绝；④ 非 https → 拒绝；⑤ evict：`getOrStartMcp` 建立假远程连接（mock HttpMcpClient 或本地 https server 不可行——用注入手法：先 pool.set 假 promise，evict 后 pool 清空且 disconnect 被调；或对 `evictMcpByName` 提取可测的纯遍历逻辑）+ evict 后 `getOrStartMcp` 走重建路径
- [ ] **Step 2: 红**——`vitest run tests/mcp/host-manager-edit.test.ts`
- [ ] **Step 3: 实现**——

```ts
/** P2.2：编辑已装远程 MCP 定义（专用 UPDATE——保 id/installed_at，不走 REPLACE）。 */
export function updateRemoteMcpDefinition(
  name: string,
  url: string,
  headers: Record<string, string>,
  configSchema?: McpConfigSchema,
): void {
  if (!url.startsWith('https://')) {
    throw new Error(`远程 MCP ${name} 配置更新失败：url 必须以 https:// 开头`);
  }
  const db = getDb();
  const row = db
    .prepare('SELECT transport, config_schema FROM mcp_definitions WHERE name = ?')
    .get(name) as { transport: string; config_schema: string } | undefined;
  if (!row) throw new Error(`MCP ${name} 未注册`);
  if (normalizeTransport(row.transport) !== 'streamable_http') {
    throw new Error(`MCP ${name} 不是远程条目，不支持配置编辑`);
  }
  const schemaJson =
    configSchema !== undefined
      ? JSON.stringify(configSchema)
      : row.config_schema || '{}';
  db.prepare(
    'UPDATE mcp_definitions SET url = ?, headers_json = ?, config_schema = ? WHERE name = ?',
  ).run(url, JSON.stringify(headers), schemaJson, name);
  logger.info('MCP 定义配置已更新', { name }); // 不打 url/headers（含 key）
}

/** P2.2：按名字驱逐全部 workspace 的池连接（编辑生效用；下回合 getOrStartMcp 重建）。 */
export async function evictMcpByName(name: string): Promise<void> {
  const keys = [...pool.keys()].filter((k) => k.endsWith(`:${name}`));
  const promises = keys.map((k) => {
    const p = pool.get(k);
    pool.delete(k);
    return p;
  });
  await Promise.all(
    promises.map(async (p) => {
      if (!p) return;
      try {
        const client = await p;
        await client.disconnect();
      } catch {
        // 启动失败或进程已退出，无需 disconnect
      }
    }),
  );
}
```

- [ ] **Step 4: 绿 + commit**——`feat: host-manager 远程定义编辑与 name 级池驱逐（P2.2 Task 3）`

### Task 4: resource 层编辑服务（三级降级 getMcpConfigView + updateRemoteMcpConfig）

**Files:**
- Create: `electron/src/main/resource/mcp-config.ts`
- Test: `electron/tests/resource/mcp-config.test.ts`（新）

**Interfaces:**
- Consumes: `composeRemoteConfig`（Task 2）、`updateRemoteMcpDefinition`/`evictMcpByName`/`getMcpConfig`（Task 3/既有）、`fetchSmitheryDetail`（既有）
- Produces: `getMcpConfigView(name: string): Promise<McpConfigView>`；`updateRemoteMcpConfig(name: string, input: McpConfigUpdateInput): Promise<void>`（类型形状 = spec §4.1/§4.2，`McpConfigView`/`McpConfigUpdateInput` 在本文件定义并导出，Task 6 做 renderer 镜像）

- [ ] **Step 1: 红测**——① 库 schema 命中：`config_schema` 非空 → bare=false、values 回显（header 字段读 headers、query 字段从 url `?k=v` decodeURIComponent 反解）；② 旧 smithery 行空 schema → fetchSmitheryDetail 被调（mock）→ 回写 DB（第二次调用不再 fetch）→ bare=false；③ fetch 失败 → bare=true + url/headers 原样 + values 空；④ 非 smithery 远程（custom）→ 直接 bare=true 不 fetch；⑤ update：schema 模式走 compose 重组 + `updateRemoteMcpDefinition` + `evictMcpByName` 被调；⑥ update 裸模式：input.headers 整包 + url 直写；⑦ stdio 名字 → 中文错误上抛；⑧ input.schema 透传落库。错误路径专项：fetch 抛非 2xx
- [ ] **Step 2: 红**——`vitest run tests/resource/mcp-config.test.ts`
- [ ] **Step 3: 实现**——核心骨架：

```ts
export interface McpConfigView { /* spec §4.1 七字段 */ }
export interface McpConfigUpdateInput { /* spec §4.2 */ }

/** 三级降级（spec §5.1）：库存 → smithery 实时拉取+回写 → 裸模式 */
async function resolveSchema(def: RegisteredMcp): Promise<{ schema: McpConfigSchema; bare: false } | { bare: true }> {
  if (def.configSchema && Object.keys(def.configSchema).length > 0) {
    return { schema: def.configSchema, bare: false };
  }
  if (def.source === 'smithery') {
    try {
      const schema = (await fetchSmitheryDetail(def.name)).connections[0]?.configSchema;
      if (schema && Object.keys(schema).length > 0) {
        try {
          getDb().prepare('UPDATE mcp_definitions SET config_schema = ? WHERE name = ?')
            .run(JSON.stringify(schema), def.name);
        } catch {
          logger.warn('config_schema 回写失败（不影响编辑）', { name: def.name });
        }
        return { schema, bare: false };
      }
    } catch {
      logger.warn('Smithery schema 拉取失败，降级裸模式', { name: def.name });
    }
  }
  return { bare: true };
}

/** query 字段从 url 反解、header 字段读 headers（spec §4.1 values 语义） */
function extractValues(url: string, headers: Record<string, string>, schema: McpConfigSchema): Record<string, string> {
  const values: Record<string, string> = {};
  const params = new URLSearchParams(url.split('?')[1] ?? '');
  for (const key of Object.keys(schema.properties ?? {})) {
    if (schema.properties?.[key]?.['x-from'] === 'query') {
      const v = params.get(key);
      if (v !== null) values[key] = v;
    } else if (headers[key] !== undefined) {
      values[key] = headers[key];
    }
  }
  return values;
}

export async function getMcpConfigView(name: string): Promise<McpConfigView> { /* getMcpConfig → 不存在抛/stdio 抛 → resolveSchema → extractValues 组装 */ }

export async function updateRemoteMcpConfig(name: string, input: McpConfigUpdateInput): Promise<void> {
  // 校验存在+远程（先 getMcpConfig）→ 裸模式: {finalUrl:url, headers:input.headers??{}}；
  // schema 模式: composeRemoteConfig(input.url, input.config, schema) →
  // updateRemoteMcpDefinition(name, finalUrl, headers, input.schema) → await evictMcpByName(name)
}
```

  注意：update 的 schema 模式需先 resolveSchema 拿 schema（回写副作用幂等无害）；裸/schema 判定与 getMcpConfigView 一致
- [ ] **Step 4: 绿 + commit**——`feat: 远程 MCP 配置查看/编辑服务三级降级（P2.2 Task 4）`

### Task 5: 卸载级联清理 + dangling 扫描

**Files:**
- Modify: `electron/src/main/agent/crud.ts`（新增 `removeMcpRefsFromAgents`——agent 层职责，避免 mcp 低层反向依赖 agent）
- Modify: 三个 deleteRegistered 生产断面（Task 0 已定位：`bundle-import.ts:569`、`resource/ipc.handlers.ts:239`、`hub-install.ts:146`（deleteRegisteredHubSafe，uninstallHubMcp 链内））：删行成功后调 `removeMcpRefsFromAgents(name)`。**级联不放 deleteRegistered 函数体内**——mcp 层反向 import agent/crud 会造成循环依赖（agent 层已 import host-manager）
- Create: dangling 扫描并入 `electron/src/main/resource/mcp-config.ts`（`listDanglingMcpRefs`）
- Test: `electron/tests/agent/crud-remove-mcp-refs.test.ts`、`electron/tests/resource/mcp-config.test.ts`（追加 dangling 用例）、受累断面既有测试更新

**Interfaces:**
- Produces: `removeMcpRefsFromAgents(name: string): string[]`（返回被清理的 agent 名单）
- Produces: `listDanglingMcpRefs(): DanglingMcpRef[]`（`DanglingMcpRef = { refName: string; agents: Array<{ definitionId: string; name: string }> }`，同文件导出；异常 warn + 空数组）

- [ ] **Step 1: 红测**——① 两个 agent 引用同名 MCP，卸载后两行 `default_mcps` 均剔除该 ref、**其他 ref 保留**、返回两名单；② 无引用返回空数组（不写库）；③ dangling：custom def 引用 `filesystem` 未注册 → 聚合 `[{refName:'filesystem', agents:[...]}]`；已注册名不列；同 def 重复引用去重；扫描异常（getDb 抛）→ 空数组
- [ ] **Step 2: 红**——`vitest run tests/agent/crud-remove-mcp-refs.test.ts tests/resource/mcp-config.test.ts`
- [ ] **Step 3: 实现**——

```ts
/** P2.2 卸载级联：从全部 agent 定义移除指定 MCP 引用，返回被清理的 agent 名单。 */
export function removeMcpRefsFromAgents(name: string): string[] {
  const affected = listAgentDefinitions().filter((d) =>
    d.defaultMcps.some((r) => r.ref === name),
  );
  for (const d of affected) {
    updateAgentDefinition({
      id: d.id,
      defaultMcps: d.defaultMcps.filter((r) => r.ref !== name),
    });
  }
  return affected.map((d) => d.name);
}
```

  三个断面接线（uninstallHubMcp 内示例）：`const cleaned = removeMcpRefsFromAgents(slug); if (cleaned.length > 0) logger.info('卸载级联清理 MCP 引用', { name: slug, agents: cleaned });`——放在 deleteRegistered 成功**之后**（删行失败时不动引用）
- [ ] **Step 4: 绿（含受累断面既有测试）+ commit**——`feat: 卸载级联清理 agent MCP 引用 + 悬空引用扫描（P2.2 Task 5）`

### Task 6: IPC 契约 + ResourceItem custom.transport 填充

**Files:**
- Modify: `electron/src/main/resource/ipc.handlers.ts`（三 handler：`resource:getMcpConfig` / `resource:updateMcpConfig` / `resource:danglingMcpRefs`，入参 string 校验照既有 handler 风格）
- Modify: `electron/src/preload/index.ts`（resource api 三方法，照 installSmitheryRemote 暴露模式）
- Modify: `renderer/src/ipc/types.d.ts`（镜像 `McpConfigView`/`McpConfigUpdateInput`/`DanglingMcpRef` + ResourceApi 三方法 + `ResourceItem.custom` 补可选 `transport?: 'stdio' | 'streamable_http'`）
- Modify: 两个 MCP → ResourceItem 填充点（Task 0 定位：`custom.ts:54-57` + `hub-install.ts:132`）：`custom` 加 `transport: m.transport`。**ResourceItem.custom 是双镜像**——renderer `types.d.ts:723-728` 与 electron `resource/types.ts:83-88` 两处都加可选 `transport?: 'stdio' | 'streamable_http'`
- Test: `electron/tests/resource/ipc-handlers.test.ts`（追加三 handler 用例）、`renderer/src/ipc/types.test.ts` 若有契约锁则追加（无则跳过，Task 7 UI 测试覆盖形状）

**Interfaces:**
- Consumes: Task 4/5 全部导出
- Produces: renderer `window.api.resource.getMcpConfig/updateMcpConfig/danglingMcpRefs`（Task 7 消费）

- [ ] **Step 1: 红测**——三 handler 透传/异常中文上抛；types.d.ts 镜像与 electron 端结构一致（编译期保证 + 关键字段断言）
- [ ] **Step 2: 红**——`vitest run tests/resource/ipc-handlers.test.ts`
- [ ] **Step 3: 实现 + 两端 typecheck**——`npx pnpm@9.0.0 typecheck` 0 错误（renderer 镜像错误在此暴露）
- [ ] **Step 4: 绿 + commit**——`feat: MCP 配置编辑/悬空引用 IPC 契约与 transport 填充（P2.2 Task 6）`

### Task 7: renderer —— McpConfigDialog + 配置按钮 + DanglingRefsCard

**Files:**
- Create: `renderer/src/components/resource-library/McpConfigDialog.tsx`
- Create: `renderer/src/components/resource-library/DanglingRefsCard.tsx`
- Modify: `ResourceDetail.tsx`（新可选 prop `onEditMcpConfig?: (item: ResourceItem) => void`；条件渲染「配置」按钮：`item.type==='mcp' && item.installed && item.custom?.transport==='streamable_http'`，与卸载按钮同排）
- Modify: `TypePageShell.tsx`（透传 onEditMcpConfig 给 ResourceDetail/ResourceRow 不需要——仅 ResourceDetail；`type==='mcp' && mode==='installed'` 时工具栏下方渲染 `<DanglingRefsCard />`）
- Modify: `ResourceLibraryView.tsx`（`configTarget` state + McpConfigDialog 挂载接线，照 McpConnectDialog 先例）
- Test: `McpConfigDialog.test.tsx`、`DanglingRefsCard.test.tsx`、`ResourceDetail.test.tsx`（追加按钮条件）

**Interfaces:**
- Consumes: `window.api.resource.getMcpConfig/updateMcpConfig/danglingMcpRefs`（Task 6）、`McpConfigView` 等类型

- [ ] **Step 1: 红测**——① Dialog schema 模式：字段回显 values、required 缺填禁提交、提交调 updateMcpConfig(config) 成功自关；② 裸模式：url Input + headers 动态键值行（可增删）、提交 (url, headers)；③ 失败红字留弹窗 + 提交期锁定（Esc/遮罩 no-op）；④ ResourceDetail：remote+installed 显示按钮、stdio 不显示、回调透传；⑤ DanglingRefsCard：空/null 不渲染、非空渲染 refName + agent 名单 + 指引文案；mock window.api 属性安装（照 McpConnectDialog.test.tsx 手法）
- [ ] **Step 2: 红**——`cd renderer && npx pnpm@9.0.0 vitest run src/components/resource-library/`
- [ ] **Step 3: 实现**——McpConfigDialog 骨架（复用 McpConnectDialog 的锁定模式；打开时调 getMcpConfig 加载，加载中显示「加载中…」；schema 模式提交 `{ url: view.url, config }`、裸模式 `{ url, headers }`；指引/标签全中文；语义 token）；DanglingRefsCard：mount 拉一次（`useEffect`），卡片样式照 installNotice 横幅基调（`border-subtle bg-status-warning-tint` 若存在，否则最接近的语义 token——以 `docs/dev/design-system.md` 为准），文案：`{agents.join('、')} 引用了未安装的 MCP：{refName}`+ 副文案「去 Smithery 搜索安装（注册名需与引用名一致），或编辑 agent 引用」；lucide `AlertTriangle` 16px
- [ ] **Step 4: 绿 + renderer typecheck + commit**——`feat: MCP 配置编辑弹窗/配置按钮/悬空引用提示卡（P2.2 Task 7）`

### Task 8: 收官（控制器执行）

- [ ] 三处 package.json `2.1.0-alpha.3` → `2.1.0-alpha.4`
- [ ] 全量：`npx pnpm@9.0.0 typecheck` + 双 workspace `test`（Node 20 下）全绿
- [ ] SDD 台账更新（`.superpowers/sdd/progress.md`）
- [ ] 终审（unspecified-high 审查整波 diff → 修复 → 复审）→ push

---

## Self-Review 记录

- Spec 覆盖：§3→Task 1；§4 三契约→Task 6（IPC）+Task 4（服务）；§5.1→Task 4；§5.2→Task 2/3/4；§5.3→Task 5；§5.4→Task 5；§6.1→Task 6/7；§6.2→Task 7；§6.3→Task 7；§7 错误处理→各 Task 测试步；§8 测试计划→各 Task 红测 ✓
- 类型一致性：`McpConfigSchema` 单源（mcp/types.ts），hub-install JsonSchemaLike 收敛为别名；`McpConfigView`/`McpConfigUpdateInput`/`DanglingMcpRef` electron 定义 → renderer 镜像 ✓
- 无占位符；级联挂点与 custom.ts 填充点的精确行号委托 Task 0 核验（防计划写死行号漂移）
