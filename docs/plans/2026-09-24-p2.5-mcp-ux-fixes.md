# P2.5 MCP 测试反馈五项修复 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 五项修复——参数框高度 / 导入弹窗尺寸 / 导入完成横幅化 / MCP 全字段编辑（stdio+远程）/ 删除确认。

**Architecture:** ①②③⑤纯 renderer 小改；④跨端——electron 通用编辑服务（mcp-config.ts 追加 + host-manager UPDATE）+ IPC 双通道 + 快速创建表单复用为编辑模式。

**Tech Stack:** 同 P2.4（Electron CJS + React ESM + vitest + better-sqlite3 真库契约测试）。

**Spec:** `docs/specs/2026-09-24-p2.5-mcp-ux-fixes-design.md`（D1-D5）

## Global Constraints

- 跑测试前 `source ~/.nvm/nvm.sh && nvm use 20`；**禁止**容器内 rebuild better-sqlite3
- TS strict 禁 any/@ts-ignore；注释/文案中文；文件尾换行
- 语义 token / lucide 图标；Conventional Commits；main 直接提交，每 Task 一 commit
- IPC 契约双端镜像（types.d.ts ↔ electron 定义 + preload + ApiSurface）；触碰 IPC 的任务触发 momo-boundary-rules
- 测试仿真真实语义（electron 契约测试走真实 DB；renderer mock 收窄到 window.api 属性）；错误路径专项用例
- 基线 commit `c57837a3`（P2.5 spec）

---

### Task 1: 弹窗尺寸 + 导入完成横幅化（spec ①②③/D1-D3）

**Files:**
- Modify: `renderer/src/components/agent/RegisterMcpDialog.tsx`（参数 textarea `rows={3}`→`rows={6}`，一行）
- Modify: `renderer/src/components/resource-library/McpJsonPasteDialog.tsx`（width 520→640；textarea `rows={8}`→`rows={14}`；全成功横幅化）
- Test: `renderer/src/components/resource-library/McpJsonPasteDialog.test.tsx`（done 阶段用例更新）

**Interfaces:**
- Produces: 无新接口（纯体验）；installNotice 横幅机制复用（`useResourceStore.setState({ installNotice })`，TypePageShell 既有渲染）

- [ ] **Step 1: 更新测试（先红）**

`McpJsonPasteDialog.test.tsx` 既有 done 阶段用例改造 + 新增两用例（mock 桩补 `useResourceStore` 直操作前先 `useResourceStore.setState({ installNotice: null })` 重置）：

```typescript
// 追加 describe('P2.5：导入完成横幅化（D3）', …)
it('全部成功 → 自动关弹窗 + installNotice 横幅「导入成功 N 条 MCP」', async () => {
  const onClose = vi.fn();
  render(<McpJsonPasteDialog onClose={onClose} onSuccess={() => {}} />);
  fireEvent.change(screen.getByLabelText('导入 JSON'), {
    target: { value: '{"mcpServers":{"a":{"command":"npx"},"b":{"command":"node"}}}' },
  });
  fireEvent.click(screen.getByRole('button', { name: '解析' }));
  fireEvent.click(await screen.findByRole('button', { name: '确认导入' }));
  await waitFor(() => expect(onClose).toHaveBeenCalledTimes(1));
  expect(useResourceStore.getState().installNotice).toBe('导入成功 2 条 MCP');
});

it('部分失败 → 弹窗保持打开展示失败明细（不横幅化）', async () => {
  registerMcp.mockImplementationOnce(() => Promise.resolve({})).mockImplementationOnce(() => Promise.reject(new Error('boom')));
  const onClose = vi.fn();
  render(<McpJsonPasteDialog onClose={onClose} onSuccess={() => {}} />);
  fireEvent.change(screen.getByLabelText('导入 JSON'), {
    target: { value: '{"mcpServers":{"ok1":{"command":"a"},"bad":{"command":"b"}}}' },
  });
  fireEvent.click(screen.getByRole('button', { name: '解析' }));
  fireEvent.click(await screen.findByRole('button', { name: '确认导入' }));
  expect(await screen.findByText('失败 1 条：')).toBeInTheDocument();
  expect(onClose).not.toHaveBeenCalled();
});
```

（既有「成功 N 条 + 关闭按钮」用例改为部分失败场景的断言或保留为失败路径变体——以实现时文件内既有用例为准调整，不删断言只改语义归属。）

- [ ] **Step 2: 跑红** `cd renderer && npx pnpm@9.0.0 vitest run src/components/resource-library/McpJsonPasteDialog.test.tsx`——新用例 FAIL（现行为留弹窗）。

- [ ] **Step 3: 实现**

`McpJsonPasteDialog.tsx`：
1. `<Dialog … width={520}>` → `width={640}`；textarea `rows={8}` → `rows={14}`
2. 顶部 `import { useResourceStore } from '../../stores/resource.store';`
3. `handleImport` 循环后：

```tsx
    setPhase({ kind: 'done', ok, failures });
    if (ok > 0) onSuccess();
    // P2.5 D3：全部成功 → 横幅 + 自动关（弹窗内直写 store，与 View 层 handleMcpConfigSubmit 同模式）
    if (failures.length === 0) {
      useResourceStore.setState({ installNotice: `导入成功 ${ok} 条 MCP` });
      onClose();
    }
```

`RegisterMcpDialog.tsx`：参数 textarea `rows={3}` → `rows={6}`。

- [ ] **Step 4: 跑绿** 同 Step 2 命令 + `src/components/agent/RegisterMcpDialog.test.tsx` 回归。

- [ ] **Step 5: Commit** `git add -u && git commit -m "feat: 弹窗尺寸修正 + 导入完成横幅化（P2.5 Task 1）"`

---

### Task 2: electron 通用编辑服务 + IPC 双端（spec ④/3.1-3.3）

**Files:**
- Modify: `electron/src/main/mcp/host-manager.ts`（追加 `updateMcpEntryDefinition`）
- Modify: `electron/src/main/resource/mcp-config.ts`（追加 `McpEditView` / `getMcpEditView` / `updateMcpEntry` 服务）
- Modify: `electron/src/main/resource/ipc.handlers.ts`（两 handler）
- Modify: `electron/src/preload/index.ts`（resource 命名空间两通道）
- Modify: `renderer/src/ipc/types.d.ts`（`McpEditView`/`McpEntryUpdateInput` 镜像 + ApiSurface 两方法）
- Test: `electron/tests/resource/mcp-edit.test.ts`（新建）

**Interfaces:**
- Produces（Task 3 依赖）: `ipc.resource.getMcpEditView(name): Promise<McpEditView>` / `ipc.resource.updateMcpEntry(name, input): Promise<void>`；`McpEditView` 字段见 spec §3.1；`McpEntryUpdateInput` = RegisterMcpInput 去 name（transport/version/command/args/env/url/headers/cwd）

- [ ] **Step 1: 契约测试（先红）**

`electron/tests/resource/mcp-edit.test.ts`（模式照抄 `register-mcp-quick-create.test.ts`：mock electron + tmp DB + registerResourceHandlers 取 handler 直调）：

```typescript
// 覆盖用例（简述，实现时按文件内既有断言风格展开为完整代码）：
// 1. getMcpEditView：registerMcpDefinition 播种 stdio 条目（command/args/env/cwd）→ 读回全字段
// 2. getMcpEditView：播种远程条目 → 读回 transport/url/headers，env 空对象
// 3. getMcpEditView：未注册名 → reject 中文含「未注册」
// 4. updateMcpEntry：改 stdio command/args/env → 读回落新值，且 id/installed_at/source 不变（先记录原值再比对）
// 5. updateMcpEntry：stdio → 切远程（transport streamable_http + url + headers）→ url/headers 落库、command 空串占位、cwd 落 NULL
// 6. updateMcpEntry：远程 url 非 https → reject（校验先于任何 DB 写）
// 7. updateMcpEntry：未注册名 → reject「未注册」
```

- [ ] **Step 2: 跑红** `cd electron && npx pnpm@9.0.0 vitest run tests/resource/mcp-edit.test.ts`

- [ ] **Step 3: 实现**

`host-manager.ts` 追加（与 `registerMcpDefinition` 同构校验/组装，UPDATE 不触碰 id/source/installed_at/config_schema）：

```typescript
/**
 * P2.5：全字段编辑 UPDATE（保 id/source/installed_at/config_schema——与
 * registerMcpDefinition 的 INSERT OR REPLACE 整行覆盖语义不同）。字段组装/
 * https 校验与注册同构；池驱逐由服务层（mcp-config.updateMcpEntry）负责。
 */
export function updateMcpEntryDefinition(name: string, config: McpEntryUpdateInput): void {
  const db = getDb();
  const row = db.prepare('SELECT id FROM mcp_definitions WHERE name = ?').get(name) as { id: string } | undefined;
  if (!row) throw new Error(`MCP ${name} 未注册`);
  const transport = config.transport ?? 'stdio';
  if (transport === 'streamable_http' && !config.url?.startsWith('https://')) {
    throw new Error(`远程 MCP ${name} 更新失败：url 必须是 https 地址`);
  }
  db.prepare(
    `UPDATE mcp_definitions SET version = ?, transport = ?, command = ?, args = ?, env = ?, url = ?, headers_json = ?, cwd = ? WHERE name = ?`,
  ).run(
    config.version ?? '1.0.0',
    transport,
    transport === 'streamable_http' && !config.command ? '' : config.command,
    JSON.stringify(config.args ?? []),
    JSON.stringify(config.env ?? {}),
    transport === 'streamable_http' ? (config.url ?? null) : null,
    transport === 'streamable_http' ? JSON.stringify(config.headers ?? {}) : null,
    config.cwd ?? null,
    name,
  );
  logger.info('MCP 定义已编辑', { name, transport }); // 不打 url/headers（含 key）
}
```

（`McpEntryUpdateInput` 类型定义在 `mcp/types.ts` 追加并导出——与 `McpServerConfig` 的可编辑字段子集：`{ transport?: 'stdio'|'streamable_http'; version?: string; command: string; args?: string[]; env?: Record<string,string>; url?: string; headers?: Record<string,string>; cwd?: string }`。）

`mcp-config.ts` 追加：

```typescript
/** P2.5：全字段编辑视图（stdio + 远程通吃；renderer types.d.ts 镜像源） */
export interface McpEditView {
  name: string;
  transport: 'stdio' | 'streamable_http';
  version: string;
  command: string;
  args: string[];
  env: Record<string, string>;
  /** 仅远程；stdio 为 undefined */
  url?: string;
  /** 仅远程；stdio 为空对象 */
  headers: Record<string, string>;
  /** 仅 stdio */
  cwd?: string;
}

/** 读全字段编辑视图（未注册抛中文错） */
export function getMcpEditView(name: string): McpEditView {
  const def = getMcpConfig(name) as RegisteredMcp | null;
  if (!def) throw new Error(`MCP ${name} 未注册`);
  return {
    name,
    transport: def.transport,
    version: def.version,
    command: def.command,
    args: def.args ?? [],
    env: def.env ?? {},
    url: def.url,
    headers: def.transport === 'streamable_http' ? (def.headers ?? {}) : {},
    cwd: def.cwd,
  };
}

/** 全字段编辑：校验 → UPDATE（保 id/installed_at/source/schema）→ 驱逐池连接 → 广播目录 */
export async function updateMcpEntry(name: string, input: McpEntryUpdateInput): Promise<void> {
  updateMcpEntryDefinition(name, input);
  await evictMcpByName(name);
  void broadcastLocalResourceCatalog();
}
```

（`broadcastLocalResourceCatalog` 自 `../p2p/resource-share` import——ipc.handlers.ts 既有同源引用。）

`ipc.handlers.ts` 追加两 handler（紧邻既有 `resource:updateMcpConfig`）：

```typescript
  // P2.5：MCP 全字段编辑（stdio + 远程；custom 源专用入口）
  ipcMain.handle('resource:getMcpEditView', (_evt, name: string) => getMcpEditView(name));
  ipcMain.handle('resource:updateMcpEntry', async (_evt, name: string, input: McpEntryUpdateInput) => {
    await updateMcpEntry(name, input);
  });
```

（import 行补 `getMcpEditView, updateMcpEntry, type McpEntryUpdateInput` 自 `./mcp-config`——`McpEntryUpdateInput` 从 `../mcp/types` re-export 或直接改 import 源，以 mcp-config.ts 实际导出面为准。）

`preload/index.ts` resource 段追加：

```typescript
    getMcpEditView: (name: string) => invoke('resource:getMcpEditView', name),
    updateMcpEntry: (name: string, input: McpEntryUpdateInput) => invoke('resource:updateMcpEntry', name, input),
```

`renderer/src/ipc/types.d.ts`：镜像 `McpEditView` / `McpEntryUpdateInput`（注明与 electron 端对齐）+ ApiSurface `resource` 段加两方法签名。

- [ ] **Step 4: 跑绿** `cd electron && npx pnpm@9.0.0 vitest run tests/resource/mcp-edit.test.ts tests/resource/`（资源域回归）+ 根 `npx pnpm@9.0.0 typecheck` 双 0

- [ ] **Step 5: Commit** `git add -A electron renderer/src/ipc/types.d.ts && git commit -m "feat: MCP 全字段编辑 electron 服务与 IPC 双端（P2.5 Task 2）"`

---

### Task 3: renderer 编辑模式 + 详情「编辑」按钮（spec ④/3.4）

**Files:**
- Modify: `renderer/src/components/agent/RegisterMcpDialog.tsx`（edit 模式）
- Modify: `renderer/src/components/resource-library/ResourceDetail.tsx`（编辑按钮门控）
- Modify: `renderer/src/components/resource-library/TypePageShell.tsx`（onEditMcpEntry 透传）
- Modify: `renderer/src/components/resource-library/ResourceLibraryView.tsx`（editTarget 接线）
- Test: `RegisterMcpDialog.test.tsx` / `ResourceDetail.test.tsx` / `TypePageShell.test.tsx` 增改

**Interfaces:**
- Consumes: Task 2 `getMcpEditView`/`updateMcpEntry`/`McpEditView`
- Produces: `RegisterMcpDialog` Props 追加 `edit?: { name: string }`；`ResourceDetail` Props 追加 `onEditMcpEntry?: (item: ResourceItem) => void`

- [ ] **Step 1: 测试先行（红）**

`RegisterMcpDialog.test.tsx` 追加 describe（mock 桩补 `resource.getMcpEditView`/`resource.updateMcpEntry`）：

```typescript
// 覆盖用例：
// 1. edit 模式 mount → getMcpEditView(name) 调用 + 全字段预填（stdio：command/args 换行 join/env 行/version/cwd；传输态 stdio）
// 2. edit 远程条目预填（url/headers 行；传输态 http）
// 3. 名称 Input disabled（引用键不可改）
// 4. 提交 → updateMcpEntry(name, payload) 收编辑载荷（非 registerMcp）+ mcp.start + onSuccess/onClose
// 5. edit 模式无同名二段确认（list 预检 mock 返回同名也不出现警示条）
// 6. getMcpEditView reject → 红字「加载失败」类文案（实现定的具体文案断言）
```

`ResourceDetail.test.tsx` 追加：`mcp + custom + installed + onEditMcpEntry` → 显示「编辑」按钮且点击回调带 item；`mcp + smithery 源` → 无编辑按钮（走配置）。`TypePageShell.test.tsx`：透传断言（详情面板出现编辑按钮）。

- [ ] **Step 2: 跑红**（三文件相关用例）

- [ ] **Step 3: 实现**

`RegisterMcpDialog.tsx`：
1. Props 加 `edit?: { name: string }`
2. mount 预填 effect：

```tsx
  const [loadingEdit, setLoadingEdit] = useState<boolean>(Boolean(edit));
  useEffect(() => {
    if (!edit) return;
    let cancelled = false;
    ipc.resource
      .getMcpEditView(edit.name)
      .then((view) => {
        if (cancelled) return;
        setTransport(view.transport === 'streamable_http' ? 'http' : 'stdio');
        setName(view.name);
        setVersion(view.version === '1.0.0' ? '' : view.version); // 缺省版本不回显（提交仍会落 1.0.0）
        setCommand(view.command);
        setArgsText(view.args.join('\n'));
        setCwd(view.cwd ?? '');
        setUrl(view.url ?? '');
        setEnvRows(recordToRows(view.env));
        setHeaderRows(recordToRows(view.headers));
      })
      .catch((err) => { if (!cancelled) setError(`读取 MCP 配置失败：${(err as Error).message}`); })
      .finally(() => { if (!cancelled) setLoadingEdit(false); });
    return () => { cancelled = true; };
  }, [edit]);
```

（`recordToRows`：`Object.entries(rec).map(([key, value]) => ({ key, value }))`，空对象给一行空行——与初始态一致。）

3. 提交分流：`handleSubmit` 里编辑模式跳过同名预检；注册调用替换：

```tsx
      const payload = transport === 'http'
        ? { version: version.trim() || undefined, command: '', transport: 'streamable_http' as const, url: url.trim(), headers: rowsToRecord(headerRows) }
        : { version: version.trim() || undefined, command: command.trim(), args: parsedArgs, env: rowsToRecord(envRows), cwd: cwd.trim() || undefined };
      if (edit) {
        await ipc.resource.updateMcpEntry(edit.name, payload);
      } else {
        await ipc.resource.registerMcp({ name: trimmedName, ...payload });
      }
      await ipc.mcp.start(activeWorkspaceId, edit ? edit.name : trimmedName);
```

4. UI：标题 `edit ? '编辑 MCP' : '快速创建 MCP'`；名称 Input `disabled={Boolean(edit)}` + 一句注释（name 是 agent 引用键）；`loadingEdit` 时表单体渲染「加载中…」占位；提交按钮文案 `edit ? (submitting ? '保存中…' : '保存') : …既有`。

`ResourceDetail.tsx`：Props 加 `onEditMcpEntry?: (item: ResourceItem) => void`；按钮区（既有「配置」按钮之后、删除之前）：

```tsx
        {/* P2.5 D4：custom 源已装 MCP 全字段编辑（stdio+远程；smithery/marketplace 走上方「配置」） */}
        {item.type === 'mcp' && item.installed && item.source === 'custom' && onEditMcpEntry && (
          <Button size="sm" onClick={() => onEditMcpEntry(item)} className="inline-flex items-center gap-1">
            <Pencil size={12} strokeWidth={1.75} aria-hidden />
            编辑
          </Button>
        )}
```

`TypePageShell.tsx`：Props 加 `onEditMcpEntry?: (item: ResourceItem) => void`，透传给 `ResourceDetail`。

`ResourceLibraryView.tsx`：

```tsx
  // P2.5：MCP 全字段编辑目标（null = 弹窗关）
  const [mcpEditTarget, setMcpEditTarget] = useState<string | null>(null);
  // …
  <TypePageShell … onEditMcpConfig={handleEditMcpConfig} onEditMcpEntry={(item) => setMcpEditTarget(item.slug)} />
  // 弹窗组：
  {mcpEditTarget && (
    <RegisterMcpDialog
      edit={{ name: mcpEditTarget }}
      onClose={() => setMcpEditTarget(null)}
      onSuccess={() => { setMcpEditTarget(null); void load(); }}
    />
  )}
```

- [ ] **Step 4: 跑绿** `cd renderer && npx pnpm@9.0.0 vitest run src/components/agent/RegisterMcpDialog.test.tsx src/components/resource-library` + 根 typecheck 双 0

- [ ] **Step 5: Commit** `git add -A renderer && git commit -m "feat: MCP 全字段编辑——表单编辑模式与详情入口（P2.5 Task 3）"`

---

### Task 4: ConfirmDialog + 删除确认（spec ⑤/D5）

**Files:**
- Create: `renderer/src/components/ui/ConfirmDialog.tsx`
- Modify: `renderer/src/components/resource-library/TypePageShell.tsx`（pendingDelete 拦截）
- Test: `renderer/src/components/ui/ConfirmDialog.test.tsx`（新建）+ `TypePageShell.test.tsx` 增改

**Interfaces:**
- Produces: `ConfirmDialog` Props `{ title: string; message: string; confirmLabel?: string; onConfirm: () => void; onClose: () => void }`（确认钮 Button `variant="danger"`，缺省文案「删除」）

- [ ] **Step 1: 测试先行（红）**

`ConfirmDialog.test.tsx`：渲染 title/message/双钮；点确认 → onConfirm + 不 onClose（由消费方负责）；点取消/Esc → onClose 不 onConfirm。
`TypePageShell.test.tsx` 追加：行删除钮点击 → 确认弹窗出现（含资源名）；确认 → `deleteResource` 执行（window.api.resource.delete 桩断言）+ 弹窗消失；取消 → 不删除；MCP 类型 message 含「悬空」提示句。

- [ ] **Step 2: 跑红**

- [ ] **Step 3: 实现**

```tsx
// renderer/src/components/ui/ConfirmDialog.tsx
// 确认弹窗原子件（P2.5 D5）：危险操作二次确认——删除资源等。
// 确认钮 danger 变体；Esc/遮罩关闭走 Dialog 既有语义。
import { Dialog } from './Dialog';
import { Button } from './Button';

interface Props {
  title: string;
  message: string;
  /** 确认钮文案（缺省「删除」） */
  confirmLabel?: string;
  onConfirm: () => void;
  onClose: () => void;
}

export function ConfirmDialog({ title, message, confirmLabel = '删除', onConfirm, onClose }: Props) {
  return (
    <Dialog open onClose={onClose} title={title} width={400}>
      <div className="flex flex-col gap-4">
        <p className="text-sm text-secondary">{message}</p>
        <div className="flex gap-2 justify-end">
          <Button variant="ghost" type="button" onClick={onClose}>取消</Button>
          <Button variant="danger" type="button" onClick={() => { onConfirm(); onClose(); }}>{confirmLabel}</Button>
        </div>
      </div>
    </Dialog>
  );
}
```

`TypePageShell.tsx`：

```tsx
  const [pendingDelete, setPendingDelete] = useState<ResourceItem | null>(null);
  /** 拦截行/详情删除 → 二次确认（P2.5 D5；三类型统一） */
  const requestDelete = (id: string): void => {
    const item = items.find((i) => i.id === id);
    if (item) setPendingDelete(item);
    else void deleteResource(id); // 列表已无此行（竞态兜底）——直删
  };
  // ResourceRow onDelete={requestDelete}；ResourceDetail onDelete={requestDelete}
  // 弹窗（Shell JSX 尾部）：
  {pendingDelete && (
    <ConfirmDialog
      title={`删除 ${pendingDelete.name}？`}
      message={
        pendingDelete.type === 'mcp'
          ? '此操作不可撤销。引用它的 agent 将出现悬空提示，需手动移除引用。'
          : '此操作不可撤销。'
      }
      confirmLabel="确认删除"
      onConfirm={() => void deleteResource(pendingDelete.id)}
      onClose={() => setPendingDelete(null)}
    />
  )}
```

（行/详情的 `onDelete` 原直连 `deleteResource` 处改为 `requestDelete`；注意 ResourceRow 的 onDelete 签名是 `(id: string) => void`——`requestDelete` 同签名直接替换。）

- [ ] **Step 4: 跑绿** `cd renderer && npx pnpm@9.0.0 vitest run src/components/ui/ConfirmDialog.test.tsx src/components/resource-library` + typecheck 双 0

- [ ] **Step 5: Commit** `git add -A renderer && git commit -m "feat: 删除资源二次确认（ConfirmDialog + 三类型统一接线，P2.5 Task 4）"`

---

### Task 5: 收官（控制器执行）

- [ ] 全量门禁：typecheck 双 0 + electron/renderer 全量 vitest（Node 20）
- [ ] 三处 `package.json` `2.1.0-alpha.6` → `2.1.0-alpha.7`；`CHANGELOG.md` v2.17 账本条目
- [ ] commit `chore: P2.5 收官——版本 2.1.0-alpha.7 + CHANGELOG v2.17` → 全支终审（unspecified-high）→ push
- [ ] 主机验收清单：①参数框默认 6 行；②导入框大尺寸；③全成功自动关+横幅；④stdio MCP 编辑改 command/env 后保存即生效（池驱逐重连）；⑤删除弹确认框

---

## Self-Review 记录

- **Spec 覆盖**：①②③→Task 1；§3.1-3.3→Task 2；§3.4→Task 3；§4→Task 4；§5 错误表散落各任务测试用例；§6 测试计划逐层对应；D1-D5 全落
- **占位扫描**：无 TBD；Task 2/4 关键代码完整，Task 3 预填/分流/按钮代码完整
- **类型一致**：`McpEditView`/`McpEntryUpdateInput` Task 2 定义 = Task 3 消费；`edit?: { name: string }` / `onEditMcpEntry` 贯穿 Task 3 三文件；`ConfirmDialog` Props Task 4 内自洽
- 已知裁定：编辑模式版本缺省 '1.0.0' 不回显输入框（提交仍落 1.0.0——避免「明明有版本却显示空」的困惑采用「非缺省才回显」）；ResourceRow onDelete 签名不变（id 参数），Shell 内查表取 item
