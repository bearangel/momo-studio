# P2.6 Skill 从 Git 仓库导入 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** skill 页新增「从 Git 仓库导入」——HTTPS zip 归档下载（GitHub codeload / GitLab archive，零 git 依赖）→ 全深度扫 SKILL.md → 两阶段预览导入。

**Architecture:** electron 新 `skill/git-import.ts`（host 适配器 + scan/import 两通道 + tmp 生命周期）复用 zip-uploader 的 AdmZip/`.sha256`/路径防御语义；renderer 新 `GitImportDialog`（照 McpJsonPasteDialog 阶段机，全成功横幅化照 P2.5 D3）。

**Tech Stack:** Electron CJS 主进程 + React ESM + vitest（electron 真库/注入 mock 下载；renderer window.api 属性桩）。

**Spec:** `docs/specs/2026-09-24-p2.6-skill-git-import-design.md`（D1-D6）。**spec 微调**（Task 2 顺手改 spec 文件）：`importGitRepoSkills` 返回 `{ imported: UploadedSkill[]; failures: Array<{ slug: string; reason: string }> }`（逐 skill 收集，实现 spec §5「有失败留窗逐条明细」——原 `UploadedSkill[]` 草案无法承载失败明细）。

## Global Constraints

- 跑测试前 `source ~/.nvm/nvm.sh && nvm use 20`；**禁止** rebuild better-sqlite3
- TS strict 禁 any/@ts-ignore；注释/文案中文；文件尾换行
- 语义 token / lucide；Conventional Commits；main 直接提交，每 Task 一 commit
- IPC 双端镜像（electron 定义/preload/types.d.ts/ApiSurface）；触碰 IPC 触发 momo-boundary-rules
- 测试禁真实网络（下载函数注入 mock）；错误路径专项用例；回归锁不弱化
- 基线 commit `7fff182d`（P2.6 spec）

---

### Task 1: electron git-import 服务（spec §3/D1-D2/D4-D6）

**Files:**
- Modify: `electron/src/main/paths.ts`（追加 `resolveTmpDir`）
- Modify: `electron/src/main/skill/zip-uploader.ts`（`parseFrontmatter` / `isIgnoredEntry` 改 export——同域复用，不复制）
- Create: `electron/src/main/skill/git-import.ts`
- Test: `electron/tests/skill/git-import.test.ts`

**Interfaces:**
- Consumes: `UploadedSkill` / `nameToSlug` / `getSkillsDir`（zip-uploader）；`resolveUserDataDir`（paths）；AdmZip（既有依赖）
- Produces（Task 2/3 依赖）: `buildArchiveUrl(repoUrl: string): string`；`scanGitRepoSkills(repoUrl, deps?): Promise<{ importId: string; skills: ScannedSkill[] }>`；`importGitRepoSkills(importId, deps?): Promise<{ imported: UploadedSkill[]; failures: Array<{ slug: string; reason: string }> }>`；`ScannedSkill { slug; name; description }`

- [ ] **Step 1: 测试先行（红）**

`electron/tests/skill/git-import.test.ts` 骨架（fixture 用 AdmZip 现场打包，注入 `fetchZip`——零真实网络；`tmpDir`/`skillsDir` 注入 tmp 目录）：

```typescript
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import fs from 'node:fs';
import path from 'node:path';
import os from 'node:os';
import AdmZip from 'adm-zip';
import {
  buildArchiveUrl, scanGitRepoSkills, importGitRepoSkills,
} from '../../src/main/skill/git-import';

const tmpRoot = path.join(os.tmpdir(), `ap-git-import-${Date.now()}`);

/** 构造 superpowers 形态的归档 zip：顶层 <repo>-<branch>/ 包 skills/<名>/SKILL.md(+资产) */
function makeArchive(entries: Record<string, string>): Buffer {
  const zip = new AdmZip();
  for (const [name, content] of Object.entries(entries)) zip.addFile(name, Buffer.from(content, 'utf-8'));
  return zip.toBuffer();
}
const SKILL_MD = (name: string, desc: string) =>
  `---\nname: ${JSON.stringify(name)}\ndescription: ${JSON.stringify(desc)}\n---\n\n正文 ${name}\n`;

const SUPERPOWERS_LIKE = {
  'superpowers-main/skills/brainstorming/SKILL.md': SKILL_MD('Brainstorming', '想法变设计'),
  'superpowers-main/skills/brainstorming/references/guide.md': '# 指南',
  'superpowers-main/skills/tdd/SKILL.md': SKILL_MD('TDD', '测试驱动'),
  'superpowers-main/README.md': '# 非 skill 文件',
  'superpowers-main/__MACOSX/junk': '垃圾',       // 应被 isIgnoredEntry 过滤
  'superpowers-main/skills/.DS_Store': '垃圾',
};

beforeEach(() => { fs.mkdirSync(tmpRoot, { recursive: true }); });
afterEach(() => { fs.rmSync(tmpRoot, { recursive: true, force: true }); });
```

用例清单（展开为完整 it，风格照文件内既有断言）：
1. **buildArchiveUrl**：`https://github.com/obra/superpowers` → `https://codeload.github.com/obra/superpowers/zip/HEAD`；`https://github.com/obra/superpowers/`（尾斜杠）与 `https://github.com/obra/superpowers.git`（.git 后缀）同归一；`https://gitlab.com/gitlab-org/gitlab` → `https://gitlab.com/gitlab-org/gitlab/-/archive/HEAD/gitlab.zip`；`www.github.com` 同 github；`https://gitee.com/x/y` → throw /暂不支持/；`http://github.com/x/y` → throw /https/；`https://github.com/x`（缺 repo）→ throw /owner\/repo/
2. **scan**：注入 `fetchZip: async () => makeArchive(SUPERPOWERS_LIKE)` + tmpDir → 返回 2 个 skill（slug=brainstorming/tdd，name/description 来自 frontmatter）；README/垃圾文件不产生条目；tmp 下存在 `git-import-<importId>.zip`
3. **scan 根级 SKILL.md**（无包裹目录的 zip：`{'SKILL.md': …, 'assets/a.md': …}`）→ 1 个 skill，slug=`nameToSlug(frontmatter.name)`
4. **scan 未发现**（zip 只有 README）→ throw /未发现 SKILL.md/
5. **scan 残留清理**：预先在 tmpDir 放 `git-import-stale.zip` → scan 后该文件被删
6. **import 落盘**：scan 后立即 import（skillsDir 注入）→ `skillsDir/brainstorming/SKILL.md` + `references/guide.md` 落盘 + `.sha256` 存在；`skillsDir/tdd/…` 同理；README 不落盘；imported 长度 2；tmp zip 已清理
7. **幂等**：同 buffer 重复 scan+import → 第二次 `.sha256` 相同 → 结果仍 imported 2（内部跳过重写，hash 文件 mtime 不变或内容等价——断言 hash 内容一致）
8. **覆盖更新**：改一条 SKILL.md 内容重新 scan+import → 目标目录内容为新版
9. **importId 一次性**：同 importId 第二次 import → throw /失效/
10. **scan 阶段路径防御**：zip 内 entry 名 `skills/evil/../../../etc/passwd` 含 `..` → 该条目被跳过（不抛错不落盘越界）；`skills/<slug>/SKILL.md` 的 slug 为 `..` → scan 阶段 throw /非法 slug/

- [ ] **Step 2: 跑红** `cd electron && npx pnpm@9.0.0 vitest run tests/skill/git-import.test.ts`

- [ ] **Step 3: 实现**

`paths.ts` 追加：

```typescript
/** 临时文件目录（<userData>/tmp；git 导入等中转用途，AP_USER_DATA_DIR 注入可测） */
export function resolveTmpDir(): string {
  const dir = path.join(resolveUserDataDir(), 'tmp');
  if (!fs.existsSync(dir)) fs.mkdirSync(dir, { recursive: true });
  return dir;
}
```

`zip-uploader.ts`：`function parseFrontmatter` → `export function parseFrontmatter`；`function isIgnoredEntry` → `export function isIgnoredEntry`（各加一行导出注释「P2.6 git-import 复用」）。

`git-import.ts` 核心（完整实现，注释中文）：

```typescript
// electron/src/main/skill/git-import.ts
// P2.6：从 Git 仓库导入 skill（spec 2026-09-24 §3，D1-D6）。
// 机制：HTTPS zip 归档下载（零 git 依赖）→ AdmZip 解析 → 剥顶层归档目录 →
// 全深度扫 **/SKILL.md → 每个父目录一个 skill，落 <skillsDir>/<slug>/。
// 两通道经 importId 关联：scan 下载落 tmp，import 一次性消费后清理；
// 幂等/覆盖/路径防御与 zip 上传同语义，hash 为逐 skill 目录内容 hash（D4）。
import crypto from 'node:crypto';
import fs from 'node:fs';
import path from 'node:path';
import AdmZip from 'adm-zip';
import { logger } from '../logger';
import { resolveTmpDir } from '../paths';
import { getSkillsDir, isIgnoredEntry, nameToSlug, parseFrontmatter, type UploadedSkill } from './zip-uploader';

/** 下载超时（ms）与大小上限（字节） */
const FETCH_TIMEOUT_MS = 60_000;
const MAX_ARCHIVE_BYTES = 100 * 1024 * 1024;

export interface ScannedSkill { slug: string; name: string; description: string }

/** host 适配器（白名单，gitee 等后续可加——D1） */
const HOST_ADAPTERS: Array<{ hosts: string[]; build: (owner: string, repo: string) => string }> = [
  { hosts: ['github.com', 'www.github.com'], build: (o, r) => `https://codeload.github.com/${o}/${r}/zip/HEAD` },
  { hosts: ['gitlab.com'], build: (o, r) => `https://gitlab.com/${o}/${r}/-/archive/HEAD/${r}.zip` },
];

/** 解析仓库 URL → 归档 zip URL。非 https / 未知 host / 缺 owner-repo 抛中文错 */
export function buildArchiveUrl(repoUrl: string): string {
  let u: URL;
  try { u = new URL(repoUrl); } catch { throw new Error('仓库地址不是合法 URL'); }
  if (u.protocol !== 'https:') throw new Error('仓库地址必须以 https:// 开头');
  const segs = u.pathname.split('/').filter(Boolean).map((s) => s.replace(/\.git$/, ''));
  if (segs.length < 2 || !segs[0] || !segs[1]) {
    throw new Error('仓库地址需含 <owner>/<repo>（如 https://github.com/obra/superpowers）');
  }
  // 多余路径段（/tree/main 等）忽略——MVP 恒用默认分支 HEAD（spec §8 非目标）
  const adapter = HOST_ADAPTERS.find((a) => a.hosts.includes(u.host));
  if (!adapter) throw new Error(`暂不支持 ${u.host}（当前支持 github.com / gitlab.com）`);
  return adapter.build(segs[0]!, segs[1]!);
}

/** 默认下载实现：超时 + 流式大小上限（D6）。404/401 归一为「私有/不存在」中文报错 */
async function defaultFetchZip(url: string): Promise<Buffer> {
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), FETCH_TIMEOUT_MS);
  try {
    const res = await fetch(url, { signal: controller.signal });
    if (res.status === 404 || res.status === 401) {
      throw new Error('仓库不存在或为私有，当前仅支持公开仓库');
    }
    if (!res.ok) throw new Error(`仓库压缩包下载失败：HTTP ${res.status}`);
    const lenHeader = Number(res.headers.get('content-length') ?? 0);
    if (lenHeader > MAX_ARCHIVE_BYTES) throw new Error(`仓库压缩包超过大小上限（100MB）`);
    if (!res.body) throw new Error('仓库压缩包下载失败：无响应体');
    const chunks: Buffer[] = [];
    let total = 0;
    const reader = res.body.getReader();
    for (;;) {
      const { done, value } = await reader.read();
      if (done) break;
      total += value.byteLength;
      if (total > MAX_ARCHIVE_BYTES) {
        controller.abort();
        throw new Error('仓库压缩包超过大小上限（100MB）');
      }
      chunks.push(Buffer.from(value));
    }
    return Buffer.concat(chunks);
  } catch (err) {
    if (err instanceof Error && err.name === 'AbortError') throw new Error('仓库压缩包下载超时（60s）');
    throw err;
  } finally {
    clearTimeout(timer);
  }
}

/** 解析后的归档视图：剥顶层目录后的全部文件条目（含原始 entryName 与剥后相对路径） */
interface ArchiveFile { entryName: string; rel: string; data: Buffer }

/** zip 条目清洗 + 顶层剥除（全条目共享同一首段才剥——D2/顶层剥除判定） */
function collectFiles(zip: AdmZip): ArchiveFile[] {
  const entries = zip.getEntries().filter(
    (e) => !e.isDirectory && !isIgnoredEntry(e.entryName) && !e.entryName.split('/').includes('.git'),
  );
  if (entries.length === 0) throw new Error('仓库压缩包为空');
  const firstSegs = new Set(entries.map((e) => e.entryName.split('/')[0]));
  const strip = firstSegs.size === 1 ? `${entries[0]!.entryName.split('/')[0]}/` : '';
  return entries.map((e) => ({
    entryName: e.entryName,
    rel: strip && e.entryName.startsWith(strip) ? e.entryName.slice(strip.length) : e.entryName,
    data: e.getData(),
  }));
}

/** skill 根识别：rel === 'SKILL.md' → 根级（skill 覆盖全部文件）；否则取父目录前缀 */
interface SkillRoot { slug: string; dirPrefix: string } // dirPrefix '' = 根级

function collectSkillRoots(files: ArchiveFile[], repoName: string): SkillRoot[] {
  const skillFiles = files.filter((f) => f.rel === 'SKILL.md' || f.rel.endsWith('/SKILL.md'));
  if (skillFiles.length === 0) throw new Error('未发现 SKILL.md（要求仓库内含 SKILL.md 文件）');
  const roots: SkillRoot[] = [];
  for (const f of skillFiles) {
    let slug: string;
    let dirPrefix: string;
    if (f.rel === 'SKILL.md') {
      slug = nameToSlug(parseFrontmatter(f.data.toString('utf-8')).name ?? '') || repoName;
      dirPrefix = '';
    } else {
      dirPrefix = `${f.rel.slice(0, f.rel.length - 'SKILL.md'.length)}`; // 含尾斜杠
      slug = dirPrefix.slice(0, -1).split('/').pop() ?? '';
    }
    if (!slug || slug.includes('..') || slug.includes('/') || slug.includes('\\')) {
      throw new Error(`非法 slug：${slug || '(空)'}`);
    }
    roots.push({ slug, dirPrefix });
  }
  return roots;
}

/** 逐 skill 目录内容 hash（相对路径排序 + 内容串接——D4，跨仓库重导入同内容才跳过） */
function hashSkillFiles(items: Array<{ rel: string; data: Buffer }>): string {
  const h = crypto.createHash('sha256');
  for (const it of [...items].sort((a, b) => (a.rel < b.rel ? -1 : 1))) {
    h.update(it.rel); h.update('\0'); h.update(it.data); h.update('\0');
  }
  return h.digest('hex');
}

/** scan→import 会话表：importId → tmp zip 路径（一次性，取出即删——D5） */
const IMPORT_SESSIONS = new Map<string, string>();

/** URL 里的仓库名（根级 SKILL.md slug 兜底用） */
function repoNameFromUrl(repoUrl: string): string {
  try {
    const segs = new URL(repoUrl).pathname.split('/').filter(Boolean);
    return (segs[1] ?? '').replace(/\.git$/, '');
  } catch { return ''; }
}

export async function scanGitRepoSkills(
  repoUrl: string,
  deps: { fetchZip?: (url: string) => Promise<Buffer>; tmpDir?: string } = {},
): Promise<{ importId: string; skills: ScannedSkill[] }> {
  const archiveUrl = buildArchiveUrl(repoUrl);
  const tmpDir = deps.tmpDir ?? resolveTmpDir();
  // 残留清理（进程崩溃兜底——D5）
  for (const f of fs.existsSync(tmpDir) ? fs.readdirSync(tmpDir) : []) {
    if (f.startsWith('git-import-')) {
      try { fs.rmSync(path.join(tmpDir, f), { force: true }); } catch { /* 尽力 */ }
    }
  }
  const buffer = await (deps.fetchZip ?? defaultFetchZip)(archiveUrl);
  const files = collectFiles(new AdmZip(buffer));
  const roots = collectSkillRoots(files, repoNameFromUrl(repoUrl));
  const skills: ScannedSkill[] = roots.map((r) => {
    const md = files.find((f) => f.rel === `${r.dirPrefix}SKILL.md`)!.data.toString('utf-8');
    const front = parseFrontmatter(md);
    return { slug: r.slug, name: front.name || r.slug, description: front.description ?? '' };
  });
  const importId = crypto.randomUUID();
  const tmpPath = path.join(tmpDir, `git-import-${importId}.zip`);
  fs.writeFileSync(tmpPath, buffer);
  IMPORT_SESSIONS.set(importId, tmpPath);
  logger.info('Git 仓库 skill 扫描完成', { url: repoUrl, count: skills.length });
  return { importId, skills };
}

export async function importGitRepoSkills(
  importId: string,
  deps: { skillsDir?: string } = {},
): Promise<{ imported: UploadedSkill[]; failures: Array<{ slug: string; reason: string }> }> {
  const tmpPath = IMPORT_SESSIONS.get(importId);
  if (!tmpPath) throw new Error('导入会话已失效，请重新扫描');
  IMPORT_SESSIONS.delete(importId);
  const skillsDir = deps.skillsDir ?? getSkillsDir();
  const imported: UploadedSkill[] = [];
  const failures: Array<{ slug: string; reason: string }> = [];
  try {
    const files = collectFiles(new AdmZip(fs.readFileSync(tmpPath)));
    const roots = collectSkillRoots(files, ''); // slug 已在 scan 定型，此处 repoName 不参与
    for (const root of roots) {
      try {
        // 该 skill 的文件集（根级 = 全部；包裹级 = dirPrefix 之下）+ 三层路径防御
        const items: Array<{ rel: string; data: Buffer }> = [];
        for (const f of files) {
          const rel = root.dirPrefix === '' ? f.rel
            : f.rel.startsWith(root.dirPrefix) ? f.rel.slice(root.dirPrefix.length) : null;
          if (rel === null || !rel || rel.includes('..')) continue; // 防御 1/2
          items.push({ rel, data: f.data });
        }
        const targetDir = path.join(skillsDir, root.slug);
        const resolvedTarget = path.resolve(targetDir);
        const hash = hashSkillFiles(items);
        const hashFile = path.join(targetDir, '.sha256');
        // 幂等：同 hash 跳过（D4）
        if (fs.existsSync(hashFile) && fs.readFileSync(hashFile, 'utf-8').trim() === hash) {
          const md = files.find((f) => f.rel === `${root.dirPrefix}SKILL.md`)!.data.toString('utf-8');
          const front = parseFrontmatter(md);
          imported.push({ slug: root.slug, name: front.name || root.slug, description: front.description ?? '' });
          continue;
        }
        // 覆盖 = 全量替换（清旧目录防资产残留）
        fs.rmSync(targetDir, { recursive: true, force: true });
        fs.mkdirSync(targetDir, { recursive: true });
        for (const it of items) {
          const dest = path.join(targetDir, it.rel);
          const resolvedDest = path.resolve(dest);
          // 防御 3：解析后必须在 targetDir 内
          if (resolvedDest !== resolvedTarget && !resolvedDest.startsWith(resolvedTarget + path.sep)) continue;
          fs.mkdirSync(path.dirname(dest), { recursive: true });
          fs.writeFileSync(dest, it.data);
        }
        fs.writeFileSync(hashFile, hash);
        const front = parseFrontmatter(fs.readFileSync(path.join(targetDir, 'SKILL.md'), 'utf-8'));
        imported.push({ slug: root.slug, name: front.name || root.slug, description: front.description ?? '' });
        logger.info('Git 仓库 skill 导入成功', { slug: root.slug });
      } catch (err) {
        failures.push({ slug: root.slug, reason: err instanceof Error ? err.message : String(err) });
      }
    }
    return { imported, failures };
  } finally {
    try { fs.rmSync(tmpPath, { force: true }); } catch { /* 尽力 */ }
  }
}
```

- [ ] **Step 4: 跑绿** `cd electron && npx pnpm@9.0.0 vitest run tests/skill/`（含 zip-uploader/registry 回归）+ 根 typecheck
- [ ] **Step 5: Commit** `git add -A electron && git commit -m "feat: skill 从 Git 仓库导入 electron 服务（P2.6 Task 1）"`

---

### Task 2: IPC 双端 + spec 微调（spec §4）

**Files:**
- Modify: `electron/src/main/resource/ipc.handlers.ts`（两 handler + import）
- Modify: `electron/src/preload/index.ts`（两通道）
- Modify: `renderer/src/ipc/types.d.ts`（`ScannedSkill` / `GitImportResult` 镜像 + ApiSurface 两方法）
- Modify: `docs/specs/2026-09-24-p2.6-skill-git-import-design.md`（§4 返回类型微调）
- Test: `electron/tests/resource/git-import-ipc.test.ts`（新建）

**Interfaces:**
- Consumes: Task 1 三函数
- Produces（Task 3 依赖）: `ipc.resource.scanGitRepoSkills(url)` / `ipc.resource.importGitRepoSkills(importId)`；renderer 类型 `ScannedSkill { slug; name; description }`、`GitImportResult { imported: UploadedSkill[]; failures: Array<{ slug: string; reason: string }> }`

- [ ] **Step 1: 契约测试（红）**——照 `register-mcp-quick-create.test.ts` 模式（mock electron + tmp AP_USER_DATA_DIR + registerResourceHandlers 取 handler 直调）。**下载注入难点**：handler 内调用真实 `scanGitRepoSkills`，其 `deps.fetchZip` 默认真实 fetch——契约测试用 `vi.mock('../../src/main/skill/git-import', …)` 保留真实实现但 spy `scanGitRepoSkills` 包一层注入 fetchZip？更简单：`vi.spyOn` 不可改内部默认参。**采用方案**：mock 模块中 `scanGitRepoSkills` 为可控桩（返回固定 importId + skills）+ `importGitRepoSkills` 桩——契约测试只锁「通道名/参数序/返回透传/成功后广播调用」，真实解析链已在 Task 1 单测覆盖（分层职责，符合 momo-test-rules）。用例：①scan 通道透传 url 返回桩结果；②import 通道透传 importId 返回 `{imported, failures}`；③import 抛错 → handler reject（中文错误透传）。

- [ ] **Step 2: 跑红** → **Step 3: 实现**——ipc.handlers.ts：

```typescript
import { scanGitRepoSkills, importGitRepoSkills } from '../skill/git-import';
  // …（resource 命名空间，uploadSkill 通道旁）
  // P2.6：Git 仓库 skill 导入两通道（scan 下载+解析落 tmp；import 一次性消费）
  ipcMain.handle('resource:scanGitRepoSkills', async (_evt, url: string) => scanGitRepoSkills(url));
  ipcMain.handle('resource:importGitRepoSkills', async (_evt, importId: string) => {
    const result = await importGitRepoSkills(importId);
    // 自定义资源变更 → 广播目录（与 registerMcp/uploadSkillZip 同语义）
    void broadcastLocalResourceCatalog();
    return result;
  });
```

preload（resource 段）：`scanGitRepoSkills: (url: string) => invoke('resource:scanGitRepoSkills', url)` / `importGitRepoSkills: (importId: string) => invoke('resource:importGitRepoSkills', importId)`。

types.d.ts：`ScannedSkill` / `GitImportResult` 镜像（注明与 electron 端对齐）+ ApiSurface 两方法签名。spec §4 改返回类型（imported/failures）。

- [ ] **Step 4: 跑绿** `cd electron && npx pnpm@9.0.0 vitest run tests/resource/git-import-ipc.test.ts tests/resource/` + 根 typecheck 双 0
- [ ] **Step 5: Commit** `git add -A electron renderer/src/ipc/types.d.ts docs/specs && git commit -m "feat: Git 仓库 skill 导入 IPC 双端（P2.6 Task 2）"`

---

### Task 3: renderer GitImportDialog + 入口接线（spec §5）

**Files:**
- Create: `renderer/src/components/resource-library/GitImportDialog.tsx`
- Modify: `renderer/src/components/resource-library/ResourceLibraryView.tsx`（AddMenu skill 分支 + 挂载）
- Test: `GitImportDialog.test.tsx`（新建）+ `ResourceLibraryView.test.tsx`（AddMenu 项断言）

**Interfaces:**
- Consumes: Task 2 `ipc.resource.scanGitRepoSkills` / `importGitRepoSkills` / `ScannedSkill` / `GitImportResult`
- Produces: `GitImportDialog` Props `{ onClose: () => void; onSuccess: () => void }`

- [ ] **Step 1: 测试先行（红）**——mock window.api 属性（`resource.scanGitRepoSkills` / `resource.importGitRepoSkills` / `resource.list`），beforeEach 重置 `useResourceStore.setState({ installNotice: null })`。用例：
1. URL 校验：空/非 https/github·gitlab 外 host → 「扫描」disabled + 提示
2. 扫描成功 → review 列表渲染 N 条（名称/描述/slug）+ 同名警示（list mock 返回含同 slug → 「将覆盖 1 个同名技能：…」）
3. 未发现 SKILL.md（scan reject）→ 红字 + 可返回修改
4. 确认导入 → importGitRepoSkills(importId) 入参正确；全成功（failures 空）→ installNotice「导入成功 N 条技能」+ onClose
5. 部分失败（failures 非空）→ 留窗展示失败明细 + onSuccess 已触发（imported>0 时）
6. 导入 reject → 红字留窗
7. ResourceLibraryView：skill 页 AddMenu 含「从 Git 仓库导入…」（View 既有测试追加断言）

- [ ] **Step 2: 跑红** → **Step 3: 实现**

`GitImportDialog.tsx`（阶段机照 McpJsonPasteDialog；宽度 560）：

```tsx
// 阶段：input → review → importing → done
type Phase =
  | { kind: 'input' }
  | { kind: 'review'; importId: string; skills: ScannedSkill[]; conflicts: string[] }
  | { kind: 'importing' }
  | { kind: 'done'; ok: number; failures: Array<{ slug: string; reason: string }> };

// input：Input URL（placeholder 'https://github.com/obra/superpowers'）+ 前端校验
//   urlOk = /^https:\/\/(www\.)?(github|gitlab)\.com\/[\w.-]+\/[\w.-]+/测试
// 「扫描」→ scanning 态文案「扫描中…」（下载耗时可观，按钮 disabled）
//   ipc.resource.scanGitRepoSkills(url) → list({type:'skill'}) 查 slug 交集 → review
// review：列表（名称 + 描述 + <code>slug</code>）+ conflicts 警示 + 「返回修改」/「确认导入 N 条」
// importing → ipc.resource.importGitRepoSkills(importId) →
//   failures 空 → setState({ installNotice: `导入成功 ${imported.length} 条技能` }) + onSuccess() + onClose()
//   否则 → done 阶段留窗（成功 X 条 / 失败明细逐条 + 关闭按钮；imported>0 时 onSuccess()）
// 失败红字 role=alert；语义 token 全覆盖；无 emoji
```

ResourceLibraryView：skill 分支 addItemsFor 第三项 `{ key: 'git-import', title: '从 Git 仓库导入…', hint: 'GitHub / GitLab 地址，自动发现全部 SKILL.md', onSelect: () => setGitImportOpen(true) }`；弹窗组挂载（onSuccess = load + 刷新）。

- [ ] **Step 4: 跑绿** `cd renderer && npx pnpm@9.0.0 vitest run src/components/resource-library` + 根 typecheck 双 0
- [ ] **Step 5: Commit** `git add -A renderer && git commit -m "feat: Git 仓库导入 skill 弹窗与入口（P2.6 Task 3）"`

---

### Task 4: 收官（控制器执行）

- [ ] 全量门禁：typecheck 双 0 + 两 workspace 全量 vitest（Node 20）
- [ ] 三处 `package.json` alpha.7 → alpha.8；`CHANGELOG.md` v2.18 账本
- [ ] commit → 全支终审（unspecified-high）→ push
- [ ] 主机验收：真实导入 `https://github.com/obra/superpowers`（预览条数/同名警示/全量落盘/横幅）；重复导入=更新；坏地址/私有仓库报错文案；重导入后 agent 会话 skill 可用（SkillRegistry 生效路径同 zip 上传）

---

## Self-Review 记录

- **Spec 覆盖**：§3→Task 1；§4→Task 2（含 spec 微调）；§5→Task 3；§6 安全表散落 Task 1 测试（防御负例/超限报错文案在单测 1/10 与 defaultFetchZip）+ Task 2 契约；§7 测试计划逐层对应；D1-D6 全落
- **占位扫描**：无 TBD；Task 1 服务全文完整；Task 3 弹窗给出完整阶段机骨架 + 行为规格（照既有 McpJsonPasteDialog 模式，测试用例逐条具体）
- **类型一致**：`ScannedSkill`/`GitImportResult` Task 2 定义 = Task 3 消费；importId 字符串贯穿；`UploadedSkill` 复用
- 已知裁定：契约测试 mock skill/git-import 模块（分层职责——真实链路 Task 1 单测覆盖）；`/tree/<branch>` 路径段忽略（恒 HEAD，spec §8）
